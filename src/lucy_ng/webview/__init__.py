"""lucy-ng webview package — FastAPI-based dashboard server for CASE runs.

Import ``lucy_ng.webview.app.create_app`` or ``lucy_ng.webview.server``
directly; this package init intentionally keeps no top-level imports so
that the core ``lucy`` CLI stays fastapi-free (WV-08).
"""
