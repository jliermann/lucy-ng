---
phase: 87-final-identity-verification-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_case_solution.py
  - tests/test_verify_case_identity.py
autonomous: true
requirements: [IDENT-01, IDENT-02, IDENT-03]

must_haves:
  truths:
    - "Given a solution SMILES, the tool returns a deterministic InChIKey and canonical SMILES (never a recalled name)."
    - "A structure present in the NMRShiftDB partition is found via the InChIKey path (e.g. indigo)."
    - "A structure present in the COCONUT partition is found via the canonical-SMILES fallback path."
    - "A structure absent from the DB returns a 'novel' verdict, not a guessed name."
    - "When a reported name disagrees with the tool-derived identity, the tool emits verdict 'tentative' plus a prominent warning (does not hard-fail)."
    - "The CASE4 (chamazulene wrong-isomer) and CASE5 (indigo↔isoindigo) hallucination patterns are caught by the gate on those structures."
    - "Existing aromatic-ring + formula verification behavior is unchanged."
  artifacts:
    - path: "scripts/verify_case_solution.py"
      provides: "derive_identity() function + check-identity subcommand + identity JSON block"
      contains: "def derive_identity"
    - path: "tests/test_verify_case_identity.py"
      provides: "Unit + regression tests for two-path lookup, mismatch/novel verdicts, CASE4/CASE5 fixtures"
      contains: "FVZVDQVUOAAMCG"
  key_links:
    - from: "scripts/verify_case_solution.py:derive_identity"
      to: "data/reference/lucy-ng-derep.db compounds table"
      via: "DatabaseFinder.find_derep_database() + parameterized sqlite3 query"
      pattern: "find_derep_database|inchi_key = \\?"
    - from: "scripts/verify_case_solution.py:derive_identity"
      to: "RDKit"
      via: "Chem.MolToInchiKey + Chem.MolToSmiles (default flags)"
      pattern: "MolToInchiKey|MolToSmiles"
---

<objective>
Extend `scripts/verify_case_solution.py` with the deterministic identity-derivation core and the binding name↔structure gate. This is the single shared deterministic path (D-05) that both the analyst (derivation, IDENT-01) and the gate (verification, IDENT-02) use. It also produces the structural-identity output (InChIKey + canonical SMILES) the analyst renders for tentative names (IDENT-03).

Purpose: Eliminate the parametric naming-hallucination failure class (CASE4 chamazulene, CASE5 indigo↔isoindigo) by deriving identity from the solved structure with a tool and flagging any reported name that does not match.

Output: `derive_identity()` importable function, a `check-identity` argparse subcommand extending the existing JSON+exit-code contract, and a regression-pinned test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-final-identity-verification-gate/87-CONTEXT.md
@.planning/phases/87-final-identity-verification-gate/87-RESEARCH.md

<interfaces>
<!-- Existing contract in scripts/verify_case_solution.py — preserve unchanged. -->
Existing default mode (MUST keep working, tests must stay green):
  CLI: verify_case_solution.py <merged_smi> <formula>
  JSON keys: pass, formula, top_n_checked, checks[] (smiles, aromatic_atoms,
             has_aromatic_ring, computed_formula, formula_match, passes)
  exit 0 if any candidate passes else 1.
Imports already present: argparse, json, sys, Path, Chem, rdMolDescriptors.

DB discovery helper (src/lucy_ng/database/finder.py:26):
  DatabaseFinder.find_derep_database() -> Path | None
  (env LUCY_DATABASE → data/reference/ → ~/.lucy → mdfind → Dropbox dev path)

DB compounds table (confirmed by research):
  columns: name, smiles, formula, inchi, inchi_key
  coconut rows (895,099): smiles populated, inchi_key = ''
  nmrshiftdb rows (33,344): inchi_key populated, smiles = ''
  No index on smiles/inchi_key; full-scan equality ~0.06s. DO NOT add an index.

Regression-fixture ground truth (from research, VERIFIED against live DB):
  Chamazulene  CCc1ccc2ccc(C)c-2c(C)c1            InChIKey FVZVDQVUOAAMCG-UHFFFAOYSA-N  → NO DB HIT
  Indigo       O=C1/C(=C2\Nc3ccccc3C2=O)Nc2ccccc21 InChIKey COHYTHOBJLSHDF-BUHFOSPRSA-N  → HIT via inchi_key (nmrshiftdb)
                 DB name string: "2,2'-Bis(2,3-dihydro-3-oxoindolyliden); Indigo"  (note: ';'-delimited synonyms)
  Isoindigo    O=C1Nc2ccccc2/C1=C1/C(=O)Nc2ccccc21 InChIKey MLCPSWPIYHDOKG-YPKPFQOOSA-N  → NO HIT
  Ibuprofen    CC(Cc1ccc(C(C)C)cc1)C(=O)O          InChIKey FLMVHZOJMRGXRO-UHFFFAOYSA-N  → NO HIT
</interfaces>

<name_match_design>
<!-- Load-bearing signal clarification (plan-checker WARNING 1). -->
The hallucination signal this gate must catch is NOT exact string equality. The two
load-bearing conditions are:
  (a) NO-HIT but the analyst asserted a trivial name  → tentative (CASE4 chamazulene,
      indirubin) — the structure isn't in the DB so the name cannot be confirmed.
  (b) DB-HIT whose derived name DISAGREES with the asserted name → tentative
      (CASE5 indigo reported as "isoindigo").
Exact name equality is deliberately NOT the gate. `name_match` is a tolerant,
token-normalised comparison used only to decide (b): a confirmed DB name must not
false-FAIL on punctuation/word-order/synonym-list differences (DB stores
"2,2'-Bis(...); Indigo"), and an unrelated short reported name must not false-PASS
via naive substring containment.
</name_match_design>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Test scaffold for identity derivation + two-path lookup + regression fixtures (Wave 0)</name>
  <files>tests/test_verify_case_identity.py</files>
  <read_first>
    - scripts/verify_case_solution.py (existing script: import surface, JSON style, argparse pattern)
    - tests/test_verify_case_solution.py (existing black-box subprocess test pattern to mirror)
    - src/lucy_ng/database/finder.py (DatabaseFinder.find_derep_database signature, for the DB-present skip guard)
    - .planning/phases/87-final-identity-verification-gate/87-RESEARCH.md (§"Phase Requirements → Test Map", §"Critical Finding 3" fixtures)
  </read_first>
  <behavior>
    - test_inchikey_and_canonical_smiles_derived: derive_identity("CCO") returns dict with non-empty "inchi_key" and "canonical_smiles" (LFQSCWFLJHTTHZ-UHFFFAOYSA-N) and never asserts a name field when matched is False.
    - test_invalid_smiles_returns_novel: derive_identity("not-a-smiles") returns {"matched": False, "confidence": "novel"} with an "error" key, no crash.
    - test_indigo_found_via_inchikey (DB-guarded): indigo SMILES → matched True, source "nmrshiftdb", derived name contains the token "indigo" (case-insensitive). Proves InChIKey path reaches the nmrshiftdb partition.
    - test_coconut_found_via_smiles_fallback (DB-guarded): a known COCONUT canonical SMILES (executor picks one live row via the SELECT in the action) → matched True, source "coconut", confidence "confirmed-structure".
    - test_chamazulene_no_hit: chamazulene SMILES → matched False, confidence "novel", inchi_key == "FVZVDQVUOAAMCG-UHFFFAOYSA-N".
    - test_ibuprofen_no_hit: ibuprofen SMILES → matched False (proves no-hit is common, even for a drug).
    - test_indigo_mislabel_mismatch (DB-guarded): check-identity with reported_name "isoindigo" on the indigo structure → name_match False, verdict "tentative", non-empty warning. (CASE5 regression — the test name carries the `indigo_mislabel` substring so the VALIDATION `-k` key is live.)
    - test_name_match_tolerant_to_punctuation_and_word_order (DB-guarded): check-identity with reported_name "Indigo" (or "indigo dye") on the indigo structure whose DB name is the ';'-delimited "2,2'-Bis(2,3-dihydro-3-oxoindolyliden); Indigo" → name_match True, verdict "confirmed" (no false-fail on punctuation / synonym-list / word order).
    - test_no_false_substring_match (DB-guarded OR unit via monkeypatch): a reported name that is a bare substring of an unrelated derived name (e.g. reported "in" against derived "Indigo") → name_match False (no naive containment false-pass).
    - test_chamazulene_asserted_name_is_tentative: check-identity with reported_name "chamazulene" on the chamazulene structure (no DB hit) → verdict "tentative", non-null warning, name_match null-or-false (CASE4 — asserted name on a no-hit cannot be confirmed).
    - test_novel_no_asserted_name_is_clean: check-identity with reported_name None on chamazulene → verdict "novel", warning None, name_match None.
    - DB-dependent tests guarded with skipif(DatabaseFinder.find_derep_database() is None).
  </behavior>
  <action>
    Create the test module mirroring the black-box style of tests/test_verify_case_solution.py. Tests may import `derive_identity` (and, where convenient, the name-normalisation helper) directly from scripts.verify_case_solution (add the scripts dir to sys.path as the existing test does, or import via importlib) AND include at least one test that subprocesses `python scripts/verify_case_solution.py check-identity ...` to lock the CLI contract. Pin every fixture SMILES and InChIKey from the <interfaces> table verbatim. For the coconut-fallback test, query the live DB inside the test for one row WHERE smiles != '' AND source = 'coconut' LIMIT 1, then feed that exact stored SMILES back through derive_identity and assert matched True — this proves the round-trip byte-equality match. Name the CASE5 mismatch test with an `indigo_mislabel` substring (e.g. `test_indigo_mislabel_mismatch`) so the VALIDATION `-k "... or indigo_mislabel"` key resolves. Use pytest.mark.skipif on DB-dependent tests keyed on DatabaseFinder.find_derep_database() being None. These tests will FAIL until Task 1 lands derive_identity + the subcommand + the hardened name_match — that is the RED state.
  </action>
  <verify>
    <automated>python -m pytest tests/test_verify_case_identity.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_verify_case_identity.py` exists and is collectable (`pytest --co` exits 0, lists the test functions above).
    - File contains the literal string `FVZVDQVUOAAMCG` (chamazulene fixture) and `COHYTHOBJLSHDF` (indigo fixture).
    - File contains a test whose name includes the substring `indigo_mislabel` (so `pytest -k "indigo_mislabel"` selects it).
    - File contains a tolerant-match test (`-k "tolerant or word_order or punctuation"`) and a no-false-substring test.
    - DB-dependent tests carry a `skipif`/`skip` guard referencing `find_derep_database`.
    - Running the tests (not just collecting) at this point FAILS or errors on the missing `derive_identity`/subcommand (RED).
  </acceptance_criteria>
  <done>Test module collectable; regression fixtures pinned; CASE4/CASE5 + tolerant-match + no-false-substring cases present; tests red against the not-yet-implemented function.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement derive_identity() + check-identity subcommand + identity JSON contract</name>
  <files>scripts/verify_case_solution.py, tests/test_verify_case_identity.py</files>
  <read_first>
    - scripts/verify_case_solution.py (extend in place; keep `main()` default mode and `_check_smiles`/`_parse_top3` untouched)
    - src/lucy_ng/database/finder.py (DatabaseFinder.find_derep_database — import it; fallback to inline LUCY_DATABASE + data/reference/ + Dropbox probe if lucy_ng not importable, per RESEARCH Pitfall 5)
    - src/lucy_ng/cli/dereplicate.py (precedent for surfacing entry.name / entry.inchi_key from the compounds table)
    - .planning/phases/87-final-identity-verification-gate/87-RESEARCH.md (§"Pattern 1" reference, §"Open Questions" #2 COCONUT semantics, §"Anti-Patterns")
    - tests/test_verify_case_identity.py (the contract these tests assert — implement to satisfy them)
  </read_first>
  <action>
    Add `derive_identity(smiles, db_path=None) -> dict` per D-01/D-02/D-03 (RESEARCH Pattern 1). Steps: Chem.MolFromSmiles → if None return {"matched": False, "error": "invalid smiles", "confidence": "novel"}; canonical = Chem.MolToSmiles(mol) with DEFAULT flags only (no isomericSmiles/canonical kwargs — RESEARCH Finding 2 byte-equality requirement); inchi_key = Chem.MolToInchiKey(mol). Resolve db_path via the passed arg or DatabaseFinder.find_derep_database(); if the DB is None, return the structural identity (inchi_key + canonical_smiles) with confidence "novel" and a "db_unavailable": true marker (RESEARCH Environment Availability fallback). Open the DB read-only (`sqlite3.connect(f"file:{db_path}?mode=ro", uri=True)`), row_factory = sqlite3.Row. Path 1 (InChIKey-first, reaches nmrshiftdb): parameterized `SELECT name, source FROM compounds WHERE inchi_key = ? AND inchi_key != '' LIMIT 1`. Path 2 only if Path 1 empty (canonical-SMILES fallback, reaches coconut): parameterized `SELECT name, source FROM compounds WHERE smiles = ? AND smiles != '' LIMIT 1`. Use `?` placeholders ONLY (no string formatting) — threat T-87-01. On no hit: {"matched": False, "inchi_key", "canonical_smiles", "confidence": "novel"}. On hit: resolve confidence per Open-Question #2 — if the matched `name` matches the COCONUT-accession pattern `^CNP\d+` (regex), set confidence "confirmed-structure" and add "trivial_name_confirmed": false (structure known in DB but accession is not a human name); otherwise confidence "confirmed". Always return name, source, inchi_key, canonical_smiles.

    Add a `_normalize_name(name) -> set[str]` token helper used ONLY for the name_match decision (plan-checker WARNING 1 — name_match must be tolerant, not exact/substring). It: drops any COCONUT-accession token matching `^CNP\d+`; splits the DB name on `;` synonym delimiters; lowercases; strips punctuation (commas, parentheses, apostrophes, hyphens) to spaces; splits into a token set; and discards generic filler tokens (e.g. "dye"). name_match is then computed by comparing the reported-name token set against the derived-name token set: True iff a significant token overlap exists (e.g. the reported set is a non-empty subset of, or shares its primary chemical token with, any `;`-split synonym's token set). Reject naive containment: a single short fragment like "in" must NOT match "indigo" — require whole-token equality after normalisation, not substring. This guarantees "Indigo" / "indigo dye" matches DB "2,2'-Bis(2,3-dihydro-3-oxoindolyliden); Indigo" (tolerant) while "isoindigo" does NOT match "indigo" (distinct tokens) → CASE5 caught.

    Add a `check-identity` subcommand (use argparse subparsers; the existing default merged_smi+formula invocation MUST remain a working positional path — keep backward compatibility, e.g. by detecting subcommand vs. legacy positional args). check-identity args: `--smiles` (required), `--reported-name` (optional), `--db` (optional path override). It calls derive_identity, computes name_match via _normalize_name (null when no reported-name OR no DB hit; else True/False by token-overlap rule above), and emits the identity JSON: {mode: "identity", reported_name, derived: {<derive_identity output>}, name_match: true|false|null, verdict, warning}. verdict logic (D-06): "confirmed"/"confirmed-structure" when matched and (name_match true or no reported name); "tentative" when matched and name_match false, OR when a reported-name is asserted but matched is False (no-hit + asserted name); "novel" when not matched and no reported-name. warning: a prominent non-null string ONLY on verdict "tentative" (e.g. "WARNING: reported name '<x>' does not match tool-derived identity '<derived or none>'; name downgraded to tentative."); null otherwise. EXIT CODE: 0 always for check-identity (D-06 — do NOT hard-fail; consumer reads verdict/warning). Leave the default aromatic+formula mode exit 0/1 behavior intact.

    Update tests/test_verify_case_identity.py only if a test stub needs the final agreed key names / helper import — do not weaken assertions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_verify_case_identity.py tests/test_verify_case_solution.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify_case_solution.py` contains `def derive_identity`, a `_normalize_name` token helper, and a `check-identity` argparse subcommand.
    - `python scripts/verify_case_solution.py check-identity --smiles 'O=C1/C(=C2\Nc3ccccc3C2=O)Nc2ccccc21'` prints JSON with `"matched": true`, `"source": "nmrshiftdb"`, `"name_match": null`, `"verdict": "confirmed"` (DB present) — exit 0.
    - `python scripts/verify_case_solution.py check-identity --smiles 'O=C1/C(=C2\Nc3ccccc3C2=O)Nc2ccccc21' --reported-name Indigo` prints `"name_match": true`, `"verdict": "confirmed"` (tolerant match against the ';'-delimited DB name) — exit 0.
    - `python scripts/verify_case_solution.py check-identity --smiles 'O=C1/C(=C2\Nc3ccccc3C2=O)Nc2ccccc21' --reported-name isoindigo` prints `"name_match": false`, `"verdict": "tentative"`, non-null `"warning"` — exit 0.
    - `python scripts/verify_case_solution.py check-identity --smiles 'CCc1ccc2ccc(C)c-2c(C)c1' --reported-name chamazulene` prints `"matched": false`, `"verdict": "tentative"`, non-null `"warning"` — exit 0 (CASE4: asserted name on no-hit).
    - `python scripts/verify_case_solution.py check-identity --smiles 'CCc1ccc2ccc(C)c-2c(C)c1'` (no reported name) prints `"matched": false`, `"verdict": "novel"`, `"warning": null` — exit 0.
    - SQL uses `?` placeholders (grep: `grep -v '^#' scripts/verify_case_solution.py | grep -c "WHERE inchi_key = ?"` ≥ 1; no f-string/`%`/`.format` interpolation into a SELECT).
    - Existing default mode still works: `tests/test_verify_case_solution.py` passes unchanged.
    - `pytest tests/test_verify_case_identity.py -q` exits 0 (GREEN, DB-dependent tests skip cleanly if DB absent), including the `indigo_mislabel`, tolerant-match, and no-false-substring tests.
  </acceptance_criteria>
  <done>derive_identity + _normalize_name + check-identity implemented; two-path lookup, COCONUT-accession semantics, tolerant token-based name_match, tentative/novel verdicts, parameterized SQL all verified; existing tests green; new regression suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solved SMILES → derive_identity | SMILES strings produced inside the CASE pipeline (already RDKit-parsed elsewhere) enter RDKit + are used to build a DB query. Only semi-trusted input in this phase. |
| derive_identity → bundled SQLite DB | Local read-only query against a bundled, read-only DB. No network, no auth, no writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Tampering/Injection | `derive_identity` SQL query | mitigate | Parameterized `?` placeholders only for both inchi_key and smiles lookups; no string interpolation of SMILES/InChIKey into SQL. Asserted in acceptance criteria. |
| T-87-02 | Denial of Service | malformed SMILES | mitigate | `Chem.MolFromSmiles` returns None → return verdict "novel"/"invalid smiles" gracefully; gate never crashes the report (degrades to "could not verify / tentative"). |
| T-87-03 | Information Disclosure | DB read | accept | Read-only (`mode=ro`) local bundled DB, no PII, no secrets; no exfil surface. |
| T-87-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RDKit + sqlite3 pre-existing, RESEARCH §Package Legitimacy Audit "not applicable"). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `pytest tests/test_verify_case_identity.py tests/test_verify_case_solution.py -q` exits 0.
- The five `check-identity` CLI invocations in the acceptance criteria produce the stated verdicts (confirmed / tolerant-confirmed / tentative-mismatch / tentative-no-hit-name / novel).
- No regression in the existing aromatic+formula default mode.
</verification>

<success_criteria>
- IDENT-01: `derive_identity` returns a tool-derived InChIKey + canonical SMILES from a SMILES; never a recalled name.
- IDENT-02 (binding layer): `check-identity` flags a reported-name vs derived-name mismatch as verdict "tentative" + warning; the load-bearing signals are (a) no-hit + asserted name and (b) derived≠reported, not exact string equality.
- IDENT-03: no-hit / mismatch → verdict carries tentative/novel with structural identity, not an asserted name.
- CASE4 (chamazulene no-hit, asserted name → tentative) and CASE5 (indigo↔isoindigo mismatch; tolerant match does not false-fail on the ';'-delimited DB name) caught — regression tests green.
</success_criteria>

<output>
Create `.planning/phases/87-final-identity-verification-gate/87-01-SUMMARY.md` when done.
</output>
