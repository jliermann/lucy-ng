---
phase: 92-orchestrator-integration
plan: 03
type: execute
wave: 3
depends_on: [92-02]
files_modified: []
autonomous: false
requirements: [WV-07]

must_haves:
  truths:
    - "In a FRESH Claude Code session, a real CASE run prints the dashboard URL + stop hint before the first [BEGIN] push"
    - "After the run ends and terminate_team fires, the webview server is still reachable"
    - "The orchestrator output tells the user to stop the server manually via `lucy webview stop <analysis_dir>`"
  artifacts: []
  key_links:
    - from: "fresh-session CASE run"
      to: "printed dashboard URL"
      via: "spawn_case_team Step 5 launch block"
      pattern: "Webview server running at"
---

<objective>
Final human verification of WV-07 live behavior. case.md is a skill prompt: edits made in Plan 92-02
are NOT loaded mid-session, so the only way to confirm the orchestrator actually launches, reports,
and leaves-running the dashboard is a smoke-test CASE run started in a FRESH Claude Code session.

This plan is a single blocking human-verify checkpoint. The automated grep/lifecycle tests (Plans
92-01/92-02) already prove the file contract; this checkpoint proves the live orchestration and
closes all three success criteria.

Purpose: SC #1 (URL + stop hint before work), SC #2 (server outlives the team), SC #3 (manual-stop note).
Output: human sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-orchestrator-integration/92-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Fresh-session smoke-test — dashboard launches, reports, survives</name>
  <read_first>
    - .planning/phases/92-orchestrator-integration/92-RESEARCH.md — "Manual (required once — session reload)" steps and Pitfall 5 (fresh-session requirement).
    - .claude/commands/lucy-ng/case.md — the committed Step 5 launch block (to confirm what the run should print).
  </read_first>
  <what-built>
    Plan 92-02 edited `case.md` so spawn_case_team Step 5 runs `lucy webview serve <compound_path>/analysis`
    (non-blocking) and prints the dashboard URL + `lucy webview stop` hint before the first `[BEGIN]`
    peak-picking push, and terminate_team deliberately leaves the server running. The four Wave 0 grep
    tests and the outlives-caller lifecycle test are already GREEN.
  </what-built>
  <how-to-verify>
    Prerequisite: the case.md edit from Plan 92-02 is committed, AND the webview extra is installed in
    this environment (`pip install -e '.[webview]'` if needed). Because case.md is a skill prompt, you
    MUST start a NEW Claude Code session before this test — edits do not reload mid-session.

    1. Open a FRESH Claude Code session in the lucy-ng project directory.
    2. Start a smoke-test CASE run on a small test dataset, e.g.:
         /lucy-ng:case data/<a small test compound dir> <formula> --smoke-test
       (use any CASE1-8 dataset under the test-data path; --smoke-test keeps it to ~1 iteration).
    3. Confirm the orchestrator prints, BEFORE the first `[BEGIN] peak-picking` push:
         - a line `Webview server running at http://127.0.0.1:NNNNN`
         - a line containing `lucy webview stop <...>/analysis`
    4. Open the printed URL in a browser and confirm the dashboard loads.
    5. Let the smoke test finish (it will call terminate_team).
    6. AFTER the run ends, reload the same URL (or run `lucy webview status <compound_path>/analysis`)
       and confirm the server is STILL running / the dashboard still loads (SC #2).
    7. Confirm the run output told you to stop it manually via `lucy webview stop <analysis_dir>` (SC #3).
    8. Clean up: `lucy webview stop <compound_path>/analysis`.

    If the environment lacks the `[webview]` extra, instead confirm the run prints the one-line
    "Webview dashboard unavailable — ... Install lucy-ng[webview] ..." warning and STILL completes the
    smoke test (OD-2 continue-on-failure). Note that survival (SC #2) then cannot be observed here.
  </how-to-verify>
  <acceptance_criteria>
    - PASS requires: URL + stop hint printed before the first `[BEGIN]` (SC #1); dashboard reachable
      after terminate_team (SC #2); manual-stop note present in output (SC #3).
    - OR, on a no-extra environment: the "Webview dashboard unavailable" warning is printed and the
      smoke run still completes (OD-2), with SC #2 explicitly recorded as "not observed — extra absent".
    - The verifier records the observed URL (or the warning text) and the pass/fail verdict in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the observed URL (or the unavailable-warning text), or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human verifier → live CASE run | Manual smoke test on a local dataset; loopback-only dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-06 | Denial of Service | orphan server after manual test | mitigate | verification step 8 stops the server via `lucy webview stop` |
| T-92-SC | Tampering | package installs | accept | phase installs NO packages; optional local `[webview]` extra is a pre-existing Phase 90 dependency |
</threat_model>

<verification>
Human sign-off on the three success criteria (or the documented no-extra fallback path).
</verification>

<success_criteria>
A fresh-session smoke-test CASE run demonstrates SC #1/#2/#3 live (URL + stop hint before work,
server survives terminate_team, manual-stop note in output); verdict + observed URL recorded.
</success_criteria>

<output>
Create `.planning/phases/92-orchestrator-integration/92-03-SUMMARY.md` when done, recording the
observed dashboard URL (or unavailable-warning text) and the human verdict.
</output>
