---
phase: 92-orchestrator-integration
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - .claude/commands/lucy-ng/case.md
  - .claude/commands/lucy-ng/references/progress-format.md
autonomous: true
requirements: [WV-07]

must_haves:
  truths:
    - "case.md launches `lucy webview serve <compound_path>/analysis` in spawn_case_team Step 5, after the run_start timing stamp and before the first [BEGIN] peak-picking push"
    - "The launch block prints the dashboard URL and the `lucy webview stop` hint to the user before team work begins"
    - "If `lucy webview serve` fails (e.g. [webview] extra absent), the CASE run continues with a one-line warning — never aborts"
    - "terminate_team carries an explicit comment that the server is intentionally left running and does NOT contain the literal `lucy webview stop` command"
    - "progress-format.md header has a `**Dashboard:**` field carrying the run-start URL"
  artifacts:
    - path: ".claude/commands/lucy-ng/case.md"
      provides: "WV-07 webview launch block in Step 5 + terminate_team no-stop comment"
      contains: "lucy webview serve"
    - path: ".claude/commands/lucy-ng/references/progress-format.md"
      provides: "OD-5 Dashboard header field"
      contains: "**Dashboard:**"
  key_links:
    - from: ".claude/commands/lucy-ng/case.md"
      to: "lucy webview serve <compound_path>/analysis"
      via: "foreground non-blocking Bash call in spawn_case_team Step 5"
      pattern: "lucy webview serve"
    - from: ".claude/commands/lucy-ng/case.md write_progress header"
      to: "progress-format.md Dashboard field"
      via: "orchestrator records the printed URL into the header"
      pattern: "\\*\\*Dashboard:\\*\\*"
---

<objective>
Wire the CASE orchestrator to auto-launch the (already-built, Phase 90/91) webview dashboard at
run start and report its URL + stop hint before any team work — the sole behavioral deliverable of
WV-07. Two edits, no Python source touched:

1. `case.md` spawn_case_team Step 5: insert a non-blocking `lucy webview serve <compound_path>/analysis`
   Bash block (guarded, continue-on-failure) between the run_start timing stamp and the first
   `[BEGIN] peak-picking` push; add an explicit "intentionally NOT stopped" comment to terminate_team.
2. `progress-format.md` header + case.md write_progress: add a `**Dashboard:**` field (OD-5) recording
   the run-start URL in the persistent CASE-PROGRESS.md header.

This turns the four Plan 92-01 grep tests GREEN. Live orchestration behavior is verified separately
in Plan 92-03 (fresh session required — case.md edits do not reload mid-session).

Purpose: observability. WV-07 is informative, not control — the dashboard must never gate the run.
Output: an edited skill file that launches, reports, and leaves-running the dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-orchestrator-integration/92-RESEARCH.md
@./CLAUDE.md
</context>

<design_decisions>
Adopted from RESEARCH Open Design Decisions (Claude's Discretion — no CONTEXT.md this phase):
- OD-1 Port: auto/ephemeral — do NOT pass `--port`. server.start() picks a free port.
- OD-2 Failure policy: continue-with-warning — a failed serve prints one warning line and the CASE run proceeds.
- OD-3 `--open`: never pass it — the orchestrator runs headless; the user opens the printed URL.
- OD-4 arg form: use `<compound_path>/analysis` verbatim (server.start() resolves it internally).
- OD-5 URL location: also record it in the CASE-PROGRESS.md header `**Dashboard:**` field.
- Smoke-test mode: the launch block precedes the smoke/normal branch, so it runs in both.
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Insert webview launch block + terminate_team no-stop comment in case.md</name>
  <files>.claude/commands/lucy-ng/case.md</files>
  <read_first>
    - .claude/commands/lucy-ng/case.md — spawn_case_team Step 5 (~lines 214-229): the "First, stamp timing" sentence at ~line 218 (run_start stamp, which does `mkdir -p <compound_path>/analysis`) and the `SendMessage(... content="[BEGIN] peak-picking ...")` block at ~lines 220-227. terminate_team at ~lines 876-908 (Step 0 run_end, Step 1 shutdown_request, Step 3 TeamDelete — no webview stop present).
    - src/lucy_ng/cli/webview.py — serve command: text output is exactly two lines (`Webview server running at <url>` / `Stop with: lucy webview stop <analysis_dir>`); non-zero exit + friendly ClickException when the extra is absent; no `--port`/`--open` needed.
    - src/lucy_ng/webview/server.py — start() ~lines 85-160: `Path(analysis_dir).resolve()`, `ValueError` if the dir is missing, `subprocess.Popen(..., start_new_session=True)` (why the server outlives terminate_team with no orchestrator cooperation), 0.5 s startup probe (why serve is non-blocking).
    - .planning/phases/92-orchestrator-integration/92-RESEARCH.md — Pattern 1 (non-blocking launch Bash), Pattern 2 (exact injection point), Pattern 3 (terminate_team no-stop), Anti-Patterns.
  </read_first>
  <action>
    Edit spawn_case_team Step 5. Immediately AFTER the "First, stamp timing ... both BEFORE the push below." sentence (~line 218) and BEFORE the `SendMessage(... [BEGIN] peak-picking ...)` block, insert a short subsection titled "Launch the webview dashboard (non-blocking)". It must instruct the orchestrator to run, as a FOREGROUND Bash command, `lucy webview serve "<compound_path>/analysis"` capturing combined output and exit code, then: on success echo the captured output (which already carries the URL line and the `Stop with: lucy webview stop <analysis_dir>` hint); on failure echo a single `Note: Webview dashboard unavailable — <error>` line plus an "Install lucy-ng[webview] to enable the live dashboard." line, then CONTINUE. State explicitly in prose that this call is non-blocking (serve returns in ~0.5 s after a startup probe) and therefore does NOT violate the headless "never background-and-wait" rule — the orchestrator must NOT use `&`, must NOT background-and-wait, must NOT pass `--port` or `--open`, and must NOT abort the CASE run if serve fails. Note that `<compound_path>/analysis` exists only because the run_start stamp created it, which is why the block sits after that stamp. Include the literal command strings `lucy webview serve` and `lucy webview stop` in this block (the stop hint documentation line) so the run-start region documents both. After the block, proceed to the phase_start stamp and the `[BEGIN] peak-picking` push regardless of webview outcome.
    Then edit terminate_team (~lines 876-908): add a one-line prose comment (Step 0 area, before the shutdown_request block) stating the webview server started at run_start is intentionally left running here — do NOT add a stop call — so the user can review the dashboard after the run (SC #2/#3, WV-07); the user stops it manually using the hint printed at run start. CRITICAL: this comment must NOT contain the literal strings `lucy webview serve` or `lucy webview stop` (the grep test test_terminate_team_no_stop asserts the terminate_team section is free of `lucy webview stop`). Do NOT change any existing terminate_team behavior. Do NOT place fenced code blocks that duplicate the stop command in terminate_team.
  </action>
  <verify>
    <automated>pytest tests/test_case_md_wv07.py::test_webview_launch_present tests/test_case_md_wv07.py::test_stop_hint_present tests/test_case_md_wv07.py::test_terminate_team_no_stop -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lucy webview serve" .claude/commands/lucy-ng/case.md` >= 1 and the first occurrence precedes the first `[BEGIN] peak-picking` (test_webview_launch_present PASS).
    - The run-start region (before `<step name="terminate_team">`) contains `lucy webview stop` (test_stop_hint_present PASS).
    - The terminate_team section (`<step name="terminate_team">` … `</step>`) contains ZERO occurrences of `lucy webview stop` (test_terminate_team_no_stop PASS): `sed -n '/<step name="terminate_team">/,/<\/step>/p' .claude/commands/lucy-ng/case.md | grep -c "lucy webview stop"` returns 0.
    - The launch block contains no `&` background operator and no `--port`/`--open` flag: `sed -n '/Launch the webview dashboard/,/BEGIN. peak-picking/p' .claude/commands/lucy-ng/case.md | grep -Ec -- '--port|--open|serve.*&' ` returns 0.
    - All three targeted grep tests exit 0 under pytest.
  </acceptance_criteria>
  <done>case.md launches the dashboard before the first push, prints URL + stop hint, continues on failure, and terminate_team documents the deliberate no-stop without the literal stop command.</done>
</task>

<task type="auto">
  <name>Task 2: Add OD-5 Dashboard header field to progress-format.md + case.md write_progress note</name>
  <files>.claude/commands/lucy-ng/references/progress-format.md, .claude/commands/lucy-ng/case.md</files>
  <read_first>
    - .claude/commands/lucy-ng/references/progress-format.md — the "File header" write trigger (~lines 30-40): the header block with `**Compound:**` / `**Formula:**` / `**Started (UTC):**` / `**Team:**` lines.
    - .claude/commands/lucy-ng/case.md — write_progress step (~lines 232-245): the orchestrator is the sole author of CASE-PROGRESS.md; header written after team spawns.
    - .planning/phases/92-orchestrator-integration/92-RESEARCH.md — OD-5 rationale (persistent record of the dashboard URL).
  </read_first>
  <action>
    In `progress-format.md`, add one line to the "File header" markdown block, directly after the `**Team:**` line: `**Dashboard:** <webview URL printed by `lucy webview serve` at run start, or "unavailable — [webview] extra not installed" if the launch failed>`. Keep it inside the same fenced header template so it is part of the header the orchestrator writes.
    In `case.md` write_progress step, add a short sentence instructing the orchestrator to record the dashboard URL captured from the run-start `lucy webview serve` output into the header's `**Dashboard:**` field (or "unavailable ..." when the launch warned). Do not restructure the step; one added sentence is sufficient. Do not touch any other progress sections.
  </action>
  <verify>
    <automated>pytest tests/test_case_md_wv07.py::test_progress_format_dashboard_field -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\*\*Dashboard:\*\*' .claude/commands/lucy-ng/references/progress-format.md` returns >= 1 (test_progress_format_dashboard_field PASS).
    - case.md write_progress step references recording the dashboard URL into the header: `grep -c -i 'Dashboard' .claude/commands/lucy-ng/case.md` returns >= 1.
    - No other header fields removed: `**Compound:**`, `**Formula:**`, `**Started (UTC):**`, `**Team:**` all still present in progress-format.md.
  </acceptance_criteria>
  <done>The Dashboard URL now has a persistent home in the CASE-PROGRESS.md header and case.md tells the orchestrator to fill it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator (case.md) → lucy CLI | case.md calls `lucy webview serve "<compound_path>/analysis"` as argv (no shell=True); compound_path was validated in Step 2 (`ls <compound_path>`) |
| webview server → 127.0.0.1 | Read-only dashboard bound to loopback only (Phase 90 control, unchanged) |
| CASE run availability | A failed/absent webview must not affect solver correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03 | Denial of Service | webview failure aborting a CASE run | mitigate | OD-2 continue-with-warning guard; serve failure prints one line and proceeds |
| T-92-04 | Information Disclosure | dashboard exposing analysis on the network | accept | server binds 127.0.0.1 only (Phase 90); this phase adds no bind change and passes no `--host` |
| T-92-05 | Elevation of Privilege | shell injection via compound_path | accept | passed as a single argv string, not via shell=True; path pre-validated in Step 2 — no new surface |
| T-92-SC | Tampering | package installs | accept | phase installs NO packages (case.md/markdown edits only) |
</threat_model>

<verification>
- `pytest tests/test_case_md_wv07.py -q` exits 0 (all four grep tests now GREEN).
- `pytest tests/test_cli_webview.py -q` still passes (no regression; no source touched).
- Static: launch block precedes first `[BEGIN] peak-picking`; terminate_team section free of `lucy webview stop`; no `&`/`--port`/`--open` in the launch block.
- Live orchestration behavior is NOT verifiable here (fresh-session reload required) — see Plan 92-03.
</verification>

<success_criteria>
case.md auto-launches the dashboard at run start, reports URL + stop hint before the first team
push, continues on webview failure, deliberately never stops the server in terminate_team, and the
URL is recorded in the CASE-PROGRESS.md header. All four Wave 0 grep tests are GREEN.
</success_criteria>

<output>
Create `.planning/phases/92-orchestrator-integration/92-02-SUMMARY.md` when done.
Note in the SUMMARY that a FRESH Claude Code session is required before Plan 92-03's live verification.
</output>
