---
phase: 33-documentation-and-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - ~/.claude/agents/lucy-diagnostic.md
autonomous: true

must_haves:
  truths:
    - "PROJECT.md 3 pending decisions updated to Good with rationale"
    - "PROJECT.md Current State section reflects actual v2.1 agent names and working orchestration, not paper-only language"
    - "PROJECT.md Active requirements show v2.1 features as validated or checked"
    - "lucy-diagnostic.md has ~400-600 lines of inlined LSD command reference and systematic procedures"
    - "lucy-diagnostic.md no longer depends on runtime file reads for critical LSD knowledge"
    - "supervisor.md confirmed deleted (CLNP-01)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Accurate project state reflecting v2.1 reality"
      contains: "v2.1"
    - path: "~/.claude/agents/lucy-diagnostic.md"
      provides: "Diagnostic agent with hybrid inlined LSD knowledge"
      min_lines: 800
  key_links:
    - from: ".planning/PROJECT.md"
      to: "~/.claude/agents/lucy-case-agent.md"
      via: "Current State section naming actual agent files"
    - from: "~/.claude/agents/lucy-diagnostic.md"
      to: "skill/diagnostic/SKILL.md"
      via: "Inlined critical sections + file path references for examples"
---

<objective>
Refresh PROJECT.md to reflect v2.1 reality (decisions, state, requirements) and bring the diagnostic agent up to the same hybrid inlining standard as the CASE agent.

Purpose: PROJECT.md is the authoritative project record. It must not contain "paper-only" or "pending" language for features that now work. The diagnostic agent is spawned for deep LSD failures and must have LSD command syntax knowledge guaranteed in context, not dependent on runtime file reads.

Output: Updated PROJECT.md with correct decisions/state/requirements, and lucy-diagnostic.md with ~400-600 lines of inlined LSD knowledge.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/33-documentation-and-cleanup/33-CONTEXT.md
@~/.claude/agents/lucy-diagnostic.md
@~/.claude/agents/lucy-case-agent.md (reference for hybrid inlining approach)
@skill/diagnostic/SKILL.md (source for inlining)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh PROJECT.md to reflect v2.1 reality</name>
  <files>.planning/PROJECT.md</files>
  <action>
  Make these specific changes to .planning/PROJECT.md:

  1. **Key Decisions table** — Update 3 pending decisions:

     Change `GSD-pattern sub-commands | v2.1: Skills as ~/.claude/commands/lucy-ng/*.md with Task() agent spawning | — Pending`
     to: `GSD-pattern sub-commands | v2.1: Skills as ~/.claude/commands/lucy-ng/*.md with Task() agent spawning | Good`

     Change `/lucy-ng:case NEVER dereplicates | v2.1: Absolute separation — dereplication is a separate sub-command | — Pending`
     to: `/lucy-ng:case NEVER dereplicates | v2.1: Absolute separation — dereplication is a separate sub-command | Good`

     Change `Sanitisation is AI-only | v2.1: No CLI for sanitise — requires AI reasoning to identify compound identifiers | — Pending`
     to: `Sanitisation is AI-only | v2.1: No CLI for sanitise — requires AI reasoning to identify compound identifiers | Good`

     Add 4 new v2.1 decisions to the table:
     - `Orchestration via Task() | v2.1: Orchestrator spawns agents using Task() with model: inherit | Good`
     - `Hybrid context inlining | v2.1: ~500-700 lines critical knowledge inlined in agents, detailed references via file paths | Good`
     - `Per-pattern intervention counters | v2.1: Track failures separately per loop pattern, 10-cycle escalation | Good`
     - `Diagnostic delegation threshold | v2.1: Specialist spawned after 2 failed basic interventions with same pattern | Good`

  2. **Current State section** — Full refresh:

     Change version from `v2.0 (shipped 2026-02-08)` to `v2.1 (shipped 2026-02-09)`.

     Replace the Capabilities bullet list to reflect actual state:
     ```
     **Capabilities:**
     - 9 CLI command groups, 22 commands (thin data-access wrappers)
     - SQLite database with 928K compounds (COCONUT + NMRShiftDB)
     - 7.9M HOSE statistics for database-backed 13C prediction
     - Full CASE pipeline: peak picking → LSD generation → solving → ranking
     - Skill documents: SKILL.md (1,079 lines), diagnostic SKILL.md (1,874 lines)
     - Sub-command skills: status, dereplicate, predict, sanitise, case (in ~/.claude/commands/lucy-ng/)
     - Agent definitions: lucy-case-agent.md (613 lines, hybrid inlined), lucy-diagnostic.md (hybrid inlined)
     - CASE orchestrator: spawns autonomous agent, monitors CASE-PROGRESS.md, detects 4 loop patterns, intervenes with advisory constraints, delegates to diagnostic specialist
     ```

     Replace the "What v2.0 delivered vs what's missing" block:
     ```
     **What v2.1 delivered:**
     - Working multi-agent orchestration: CASE agent spawned via Task(), progress monitored, loops detected and intervened
     - Sub-command skills replacing monolithic /lucy-ng skill
     - AI-driven sanitisation without CLI dependency
     - Diagnostic specialist integration with delegation threshold
     - v2.0's paper-only agent architecture replaced with working GSD-pattern orchestration
     ```

  3. **Architecture section** (line ~24-28):

     Replace:
     ```
     - **Multi-Agent**: CASE agent (does the work) + Supervisor agent (keeps it on track) + optional specialist agents
     ```
     with:
     ```
     - **Multi-Agent**: CASE agent (autonomous elucidation) + CASE orchestrator (loop detection, advisory intervention) + diagnostic specialist (deep LSD failure analysis)
     ```

  4. **Active Requirements** — Move all 5 v2.1 items from Active to Validated section, as they are all implemented and verified through Phase 32:
     - Sub-command skills following GSD pattern → Validated (Phase 27-31)
     - CASE orchestrator with real agent spawning → Validated (Phase 29)
     - Autonomous CASE agent definition → Validated (Phase 28)
     - AI-driven dataset sanitisation → Validated (Phase 31)
     - Diagnostic specialist agent reworked → Validated (Phase 30)

     Add them to the Validated list with version tag "v2.1". Remove them from Active.

  5. **Current Milestone section** (line ~13-21): Update to reflect v2.1 is complete:
     Change `## Current Milestone: v2.1 Working Multi-Agent CASE` to `## Current Milestone: v2.1 Working Multi-Agent CASE (Complete)`
     Keep the description for reference but note it's shipped.

  6. Remove the `supervisor SKILL.md (827 lines)` reference from Capabilities if present — supervisor logic dissolved into case.md orchestrator.
  </action>
  <verify>
  Run these checks:
  - `grep "Pending" .planning/PROJECT.md` should return 0 matches in the decisions table
  - `grep "paper-only" .planning/PROJECT.md` should return 0 matches
  - `grep "v2.1" .planning/PROJECT.md` should return multiple matches
  - `grep "lucy-case-agent" .planning/PROJECT.md` should return at least 1
  - `grep "lucy-diagnostic" .planning/PROJECT.md` should return at least 1
  - `grep "supervisor.md" .planning/PROJECT.md` should return 0
  </verify>
  <done>PROJECT.md decisions table has 0 pending items and 4 new v2.1 decisions; Current State reflects working orchestration with actual agent names; Active requirements moved to Validated; no paper-only language remains</done>
</task>

<task type="auto">
  <name>Task 2: Inline critical LSD knowledge into lucy-diagnostic.md (hybrid inlining)</name>
  <files>~/.claude/agents/lucy-diagnostic.md</files>
  <action>
  Bring lucy-diagnostic.md up to the same hybrid inlining standard as lucy-case-agent.md.

  **Benchmark:** lucy-case-agent.md has 613 lines total with ~528 lines of inlined critical knowledge in an `<inlined_critical_knowledge>` section.

  **What to inline from skill/diagnostic/SKILL.md (critical — agent MUST have this in context):**

  1. **Section 1: LSD Command Reference** (lines 19-300, ~280 lines) — Full command reference for MULT, HSQC/HMQC, HMBC, BOND, LIST, PROP, ELEM, SYME, DEFF, ELIM. This is THE core knowledge the diagnostic agent needs — what each command constrains, what happens when it's wrong, how to detect errors.

  2. **Section 2.1: Zero-Solution Failure Procedure** (lines 304-585, ~280 lines) — The complete 5-check systematic procedure. The diagnostic agent runs this procedure when spawned.

  3. **Section 2.2: Solution Explosion Procedure** (lines 587-853, ~265 lines) — The complete 5-check systematic procedure for the other failure type.

  **What to reference via file path (detailed examples, templates — agent can read at runtime):**
  - Section 3: Report template (agent already has this in the current definition at lines 197-309)
  - Section 4: Example reports (lines 1024-1718, ~694 lines — too large to inline, read when needed)
  - Section 5: Anti-patterns (agent already has these in current definition at lines 423-435)
  - Section 6: Cross-references (informational only)

  **Implementation approach:**

  1. Add an `<inlined_critical_knowledge>` section after the current frontmatter and role description (mirroring lucy-case-agent.md structure).

  2. Into this section, copy the full content of:
     - Section 1 (LSD Command Reference) — all commands with diagnostic-relevant details, edge cases, "what constrains", "what happens when wrong", "how to detect errors"
     - Section 2.1 (Zero-Solution Failure Procedure) — all 5 checks with decision trees
     - Section 2.2 (Solution Explosion Procedure) — all 5 checks with decision trees

  3. Keep the existing Domain Knowledge References section but update it to say: "The following sections from skill/diagnostic/SKILL.md are inlined below. For example diagnostic reports, see Section 4 in the full document."

  4. Keep the existing Diagnostic Workflow section (Steps 1-5) but update Step 2 to reference the inlined knowledge instead of the external file: "Follow the inlined systematic procedure below" instead of "Follow the appropriate systematic procedure from skill/diagnostic/SKILL.md Section 2."

  5. Keep the existing report template, important rules, and anti-patterns sections (they are already good and concise).

  6. Remove duplicate content — the current agent has a condensed version of checks in the Diagnostic Workflow section (lines 86-146). Once the full procedures are inlined, this condensed version becomes redundant. Replace it with a pointer to the inlined sections: "Run systematic checks from the Inlined LSD Knowledge section below."

  **Target size:** ~800-900 total lines (455 current + ~400-500 inlined content, minus ~60 lines of condensed duplicates removed).

  **Critical rule:** Do NOT inline the example diagnostic reports (Section 4, ~694 lines). Those are reference material the agent can read from skill/diagnostic/SKILL.md at runtime when writing reports.
  </action>
  <verify>
  Run these checks:
  - `wc -l ~/.claude/agents/lucy-diagnostic.md` should show 800-1000 lines
  - `grep -c "inlined_critical_knowledge" ~/.claude/agents/lucy-diagnostic.md` should return at least 1 (opening tag)
  - `grep -c "MULT - Atom Definitions" ~/.claude/agents/lucy-diagnostic.md` should return 1
  - `grep -c "Zero-Solution Failure Procedure" ~/.claude/agents/lucy-diagnostic.md` should return 1
  - `grep -c "Solution Explosion Procedure" ~/.claude/agents/lucy-diagnostic.md` should return 1
  - `grep -c "ELIM - Correlation Elimination" ~/.claude/agents/lucy-diagnostic.md` should return 1
  - `grep "skill/diagnostic/SKILL.md" ~/.claude/agents/lucy-diagnostic.md` should still reference it for examples
  </verify>
  <done>lucy-diagnostic.md has ~400-600 lines of inlined LSD command reference and systematic procedures; agent is guaranteed to have LSD knowledge in context when spawned; example reports still referenced via file path</done>
</task>

<task type="auto">
  <name>Task 3: Confirm supervisor.md deletion (CLNP-01)</name>
  <files>(no files modified — verification only)</files>
  <action>
  Verify that ~/.claude/agents/supervisor.md does NOT exist.

  Run: `ls ~/.claude/agents/supervisor.md 2>&1` — should return "No such file or directory"

  Also verify no other files reference supervisor.md as if it exists:
  - `grep -r "supervisor.md" ~/.claude/agents/` — should return 0 matches
  - `grep -r "supervisor.md" ~/.claude/commands/lucy-ng/` — should return 0 matches (the case.md orchestrator dissolved its logic, should not reference the old file)
  - `grep "supervisor.md" CLAUDE.md` — should return 0 matches

  If any stale references found, remove them.

  Document in the summary that CLNP-01 is satisfied — the file was already deleted in an earlier phase (likely Phase 30 when the diagnostic agent was renamed and supervisor logic was dissolved into case.md).
  </action>
  <verify>
  - `test ! -f ~/.claude/agents/supervisor.md && echo "CONFIRMED DELETED"` prints "CONFIRMED DELETED"
  - `grep -r "supervisor\.md" ~/.claude/agents/ ~/.claude/commands/lucy-ng/ CLAUDE.md 2>/dev/null | wc -l` returns 0
  </verify>
  <done>supervisor.md confirmed deleted, no stale references to it exist in agent files, sub-command files, or CLAUDE.md</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. PROJECT.md has 0 "Pending" decisions, reflects v2.1 working architecture with actual agent names
2. lucy-diagnostic.md has inlined LSD knowledge (~800-1000 total lines) and no longer depends solely on runtime file reads
3. supervisor.md confirmed deleted with no stale references anywhere
4. The diagnostic agent's inlined content covers all LSD commands and both systematic procedures
5. PROJECT.md Active requirements section is empty (all moved to Validated)
</verification>

<success_criteria>
- PROJECT.md accurately reflects v2.1 as shipped with working orchestration
- Diagnostic agent has LSD knowledge guaranteed in context (hybrid inlining standard matching CASE agent)
- CLNP-01 requirement satisfied (supervisor.md deleted, no stale references)
</success_criteria>

<output>
After completion, create `.planning/phases/33-documentation-and-cleanup/33-02-SUMMARY.md`
</output>
