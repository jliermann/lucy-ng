---
phase: 33-documentation-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/phases/33-documentation-and-cleanup/v2.1-RELEASE-NOTES.md
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md header references sub-command entry points, not skill/SKILL.md as primary workflow guide"
    - "CLAUDE.md has a Sub-Command Reference section listing all 5 /lucy-ng:* commands with descriptions"
    - "CLAUDE.md Architecture section names actual agent files (lucy-case-agent.md, lucy-diagnostic.md) not vague supervisor language"
    - "No references to supervisor.md remain in CLAUDE.md"
    - "v2.1 release notes exist summarizing working multi-agent orchestration vs v2.0 paper architecture"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated project-level instructions reflecting v2.1 architecture"
      contains: "Sub-Command Reference"
    - path: ".planning/phases/33-documentation-and-cleanup/v2.1-RELEASE-NOTES.md"
      provides: "v2.1 milestone release notes"
      contains: "Working Multi-Agent CASE"
  key_links:
    - from: "CLAUDE.md"
      to: "~/.claude/commands/lucy-ng/*.md"
      via: "Sub-command reference section listing all /lucy-ng:* commands"
    - from: "CLAUDE.md"
      to: "~/.claude/agents/lucy-case-agent.md"
      via: "Architecture section naming actual agent files"
---

<objective>
Update CLAUDE.md to reflect the v2.1 architecture (sub-command entry points, actual agent names, no supervisor references) and write v2.1 release notes.

Purpose: CLAUDE.md is the primary project instruction file read by every Claude session. It must accurately describe the current architecture so agents know how to interact with the system. Release notes capture what v2.1 delivered.

Output: Updated CLAUDE.md with sub-command reference section, corrected architecture section, and v2.1 release notes document.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/33-documentation-and-cleanup/33-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with sub-command reference and corrected architecture</name>
  <files>CLAUDE.md</files>
  <action>
  Make these specific changes to CLAUDE.md:

  1. **Header reference** (line 3): Change `**For CASE domain knowledge and workflow guidance, see skill/SKILL.md**` to reference the sub-command entry points:
     `**For CASE workflow, run /lucy-ng:case. For all commands, run /lucy-ng (routing page).**`

  2. **Add Sub-Command Reference section** after the header and before "End-User Setup". This section lists all 5 sub-commands:

     ```
     ## Sub-Command Reference

     Lucy-ng uses Claude Code sub-command skills for all workflows. Run `/lucy-ng` for the routing page.

     | Command | Description |
     |---------|-------------|
     | `/lucy-ng:status` | Check environment readiness (lucy-ng version, LSD solver, database) |
     | `/lucy-ng:dereplicate` | Match 13C NMR spectrum against compound database by molecular formula |
     | `/lucy-ng:predict` | Predict 13C chemical shifts for a SMILES structure using HOSE-code database |
     | `/lucy-ng:sanitise` | Remove compound identifiers from NMR dataset for blind CASE evaluation |
     | `/lucy-ng:case` | Full autonomous CASE workflow — spawns agent, monitors progress, detects loops, intervenes |

     **Agent files** (spawned by /lucy-ng:case orchestrator):
     - `~/.claude/agents/lucy-case-agent.md` — Autonomous CASE agent with inlined NMR/LSD knowledge
     - `~/.claude/agents/lucy-diagnostic.md` — LSD failure diagnostic specialist (spawned after 2 failed interventions)
     ```

  3. **Update CLI Output Reference intro** (around line 53): Change `The AI agent uses thin CLI commands via Bash, applying domain intelligence from skill/SKILL.md.` to `The AI agent uses thin CLI commands via Bash. Domain intelligence is encoded in sub-command skills and agent definitions.`

  4. **Update LSD file structure reference** (around line 109): Change `see skill/CASE/SKILL.md and skill/diagnostic/SKILL.md for LSD command reference` to `see skill/SKILL.md and skill/diagnostic/SKILL.md for LSD command reference` (skill/CASE/ path is stale — CASE knowledge is now in the agent definition)

  5. **Update Peak Picking API Reference note** (around line 154): Change `see skill/SKILL.md for the recommended workflow where the AI applies domain intelligence` to `see /lucy-ng:case sub-command for the recommended CASE workflow`

  6. **Remove or update any remaining references to "supervisor" or "supervisor.md"**: There should be none in CLAUDE.md (it was a v2.0 artifact), but verify. The Architecture section in CLAUDE.md doesn't exist in the current file — only PROJECT.md has an Architecture section. So no action needed here for CLAUDE.md specifically (PROJECT.md is handled in plan 02).

  Do NOT change the End-User Setup, Database Reference, Developer Reference, or Critical Architecture Decisions sections — those are accurate.
  </action>
  <verify>
  Run these checks:
  - `grep -c "skill/SKILL.md" CLAUDE.md` should return 2 or fewer (only in LSD file structure reference and possibly one other legitimate reference, not the header)
  - `grep -c "Sub-Command Reference" CLAUDE.md` should return 1
  - `grep -c "lucy-case-agent" CLAUDE.md` should return 1
  - `grep -c "lucy-diagnostic" CLAUDE.md` should return 1
  - `grep -c "supervisor" CLAUDE.md` should return 0
  - `grep -c "/lucy-ng:case" CLAUDE.md` should return at least 1
  </verify>
  <done>CLAUDE.md header points to sub-commands, has Sub-Command Reference section with all 5 commands + agent files, stale references updated, no supervisor mentions</done>
</task>

<task type="auto">
  <name>Task 2: Write v2.1 release notes</name>
  <files>.planning/phases/33-documentation-and-cleanup/v2.1-RELEASE-NOTES.md</files>
  <action>
  Create v2.1 release notes summarizing the milestone. Include:

  1. **Title:** v2.1 Working Multi-Agent CASE
  2. **Date:** 2026-02-09
  3. **Overview:** One paragraph explaining what v2.1 delivers vs what v2.0 left incomplete (paper architecture vs working orchestration)
  4. **Key changes:**
     - Sub-command skills replacing monolithic /lucy-ng skill (5 commands)
     - CASE orchestrator with real Task() agent spawning, progress monitoring, loop detection (4 patterns), advisory interventions
     - Autonomous CASE agent with hybrid context inlining (~528 lines critical knowledge)
     - Diagnostic specialist delegation after 2 failed basic interventions
     - AI-driven sanitisation workflow (no CLI, pure AI reasoning)
     - Old supervisor.md dissolved into case.md orchestrator
  5. **Architecture changes:**
     - Agent files: lucy-case-agent.md, lucy-diagnostic.md
     - Sub-command skills: status.md, dereplicate.md, predict.md, sanitise.md, case.md
     - Orchestration via Task() with model: inherit workaround
     - Hybrid context inlining (critical content inlined, detailed references via file paths)
  6. **Key decisions made during v2.1:**
     - Per-pattern intervention counters (not global), 10-cycle escalation
     - Advisory interventions say WHAT not HOW
     - Diagnostic delegation threshold = 2 failed interventions
     - [REDACTED] replacement for sanitisation
     - Batch monitoring over synchronous
  7. **Phases:** List all 7 phases (27-33) with one-line descriptions
  8. **Migration from v2.0:** What was removed (supervisor.md, diagnostic-specialist.md), what was renamed (lucy-diagnostic.md), what was added (sub-commands, case.md orchestrator)

  Keep it concise — this is a project record, not external marketing material. ~100-150 lines.
  </action>
  <verify>
  - File exists: `.planning/phases/33-documentation-and-cleanup/v2.1-RELEASE-NOTES.md`
  - Contains "Working Multi-Agent CASE" in content
  - Contains "v2.0" and "v2.1" for comparison
  - Contains all 5 sub-command names
  - Contains both agent file names
  </verify>
  <done>v2.1 release notes written with architecture overview, key changes, decisions, and migration guide from v2.0</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. CLAUDE.md has Sub-Command Reference section with 5 commands and 2 agent files listed
2. CLAUDE.md header references sub-commands, not skill/SKILL.md as primary workflow guide
3. No "supervisor" references remain in CLAUDE.md
4. v2.1 release notes exist and cover the v2.0 → v2.1 transition
5. All stale skill references in CLAUDE.md updated to point to sub-commands or correct paths
</verification>

<success_criteria>
- CLAUDE.md accurately reflects v2.1 architecture with sub-command entry points
- Release notes capture what v2.1 delivered and how it differs from v2.0's paper architecture
- A new Claude session reading CLAUDE.md would know to use /lucy-ng:* commands and understand the agent architecture
</success_criteria>

<output>
After completion, create `.planning/phases/33-documentation-and-cleanup/33-01-SUMMARY.md`
</output>
