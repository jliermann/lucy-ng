---
phase: 68-constraint-inventory-v2-schema
plan: 04
type: execute
wave: 3
depends_on:
  - 68-01
  - 68-02
files_modified:
  - /Users/steinbeck/.claude/agents/lucy-devils-advocate.md
autonomous: true
requirements:
  - INPUT-05

must_haves:
  truths:
    - "lucy-devils-advocate.md Section 5A uses 'lucy lsd validate-inventory --format json' instead of the grep/sed/awk pipeline (per D-12)"
    - "Section 5A includes the v1 legacy detection WARNING path: 'Legacy v1 inventory — using fallback validation' — does NOT block the run (per D-02)"
    - "Section 5B includes G1 (FORM/MULT consistency), G2 (no bare ELIM in pylsd_mode), G3 (annotation-vs-mode consistency) as CRITICAL/blocking checks (per D-07)"
    - "G2 grep pattern is anchored to line start: '^ELIM' — explicitly noted not to use unanchored 'ELIM' which would match '; ELIM' comment annotations (per RESEARCH.md Pitfall 1)"
    - "G2 text cites the v7.0 post-mortem: 'Statistical 4J detection non-viable — 100% FP rate' as context for why this gate is CRITICAL"
    - "Section 5 intro references v2 delimiters instead of v1 (per D-01)"
    - "No new override mechanism is introduced beyond existing APPROVED/BLOCKED/WARNING semantics (per D-08)"
    - "Devils-advocate validates pre-run constraint state only (the inventory block); it does NOT read or validate run_report.json content — that boundary belongs to the SolutionMerger from Phase 67 (per D-06)"
  artifacts:
    - path: "/Users/steinbeck/.claude/agents/lucy-devils-advocate.md"
      provides: "v2-aware validation gates and CLI-based inventory extraction for devils-advocate agent"
      contains: "lucy lsd validate-inventory --format json"
  key_links:
    - from: "lucy-devils-advocate.md §5A"
      to: "lucy lsd validate-inventory"
      via: "Bash call: RESULT=$(lucy lsd validate-inventory --format json <path>)"
      pattern: "validate-inventory --format json"
    - from: "lucy-devils-advocate.md §5B Check 4"
      to: "G1/G2/G3 gate logic"
      via: "CRITICAL blocking checks with explicit grep patterns"
      pattern: "G1.*G2.*G3"
---

<objective>
Update lucy-devils-advocate.md Section 5 to replace the grep/sed/awk inventory extraction pipeline with the lucy lsd validate-inventory --format json CLI call, add the v1 legacy WARNING path, and add G1/G2/G3 as CRITICAL blocking Check 4.

Purpose: The devils-advocate agent is the primary runtime enforcement point for the v2 schema. It must call the CLI validator (built in Plan 02) rather than parsing inventory manually, and it must enforce all three new gates before approving any LSD run in pylsd_mode.

Output: Updated ~/.claude/agents/lucy-devils-advocate.md with v2-aware Section 5A/5B
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/PROJECT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/ROADMAP.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/STATE.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/68-constraint-inventory-v2-schema/68-CONTEXT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/68-constraint-inventory-v2-schema/68-RESEARCH.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/68-constraint-inventory-v2-schema/68-PATTERNS.md

<interfaces>
<!-- Current Section 5A bash extraction pipeline (lines 222-233 of lucy-devils-advocate.md): -->
grep "^; " analysis/iteration_NN/compound.lsd | \
  sed -n '/CONSTRAINT INVENTORY v1/,/END CONSTRAINT INVENTORY/p' | \
  sed 's/^; //' | grep -v "===" > /tmp/current_inv.json

<!-- Replacement per D-12 (PATTERNS.md lines 284-290): -->
RESULT=$(lucy lsd validate-inventory --format json analysis/iteration_NN/compound.lsd)
VALID=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin)['valid'])")
if [ "$VALID" != "True" ]; then
    ERRORS=$(echo "$RESULT" | python3 -c "import sys,json; d=json.load(sys.stdin); [print(e['message']) for e in d.get('errors',[])]")
    # BLOCK with error details
fi

<!-- v1 legacy detection WARNING path (per D-02): -->
# If validate-inventory returns legacy v1 message:
# {"valid": false, "errors": [{"message": "Legacy v1 inventory detected...", "validator": "version"}]}
# -> emit WARNING: "Legacy v1 inventory — using fallback validation"
# -> do NOT block the run; proceed with legacy grep-based checks as fallback

<!-- Current Section 5 intro (line 219 of lucy-devils-advocate.md): -->
<!-- "between '; === CONSTRAINT INVENTORY v1 ===' and '; === END CONSTRAINT INVENTORY ===' delimiters" -->
<!-- Update to v2 -->

<!-- G1/G2/G3 Check 4 text (PATTERNS.md lines 302-320): -->
<!-- G1: FORM/MULT consistency — when pylsd_mode=true AND FORM present, carbon count in MULT must match formula -->
<!-- G2: No bare ELIM command — grep -n "^ELIM" file.lsd; anchored to line start; cite v7.0 post-mortem -->
<!-- G3: Annotation-vs-mode — HMBC lines with '; ELIM' require pylsd_mode=true AND elim_annotated=true AND len(deferred_4j)>0 -->

<!-- G2 grep anchoring CRITICAL detail (RESEARCH.md Pitfall 1, lines 394-403): -->
<!-- grep -n "^ELIM" compound.lsd — anchored to line start -->
<!-- "HMBC 4 8 2 4 ; ELIM" does NOT match "^ELIM" — verified -->
<!-- G2 must explicitly document this anchor to prevent false positives -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite lucy-devils-advocate.md Section 5A and 5B for v2 and G1/G2/G3 gates</name>
  <files>/Users/steinbeck/.claude/agents/lucy-devils-advocate.md</files>
  <read_first>
    - /Users/steinbeck/.claude/agents/lucy-devils-advocate.md — read the ENTIRE file before making any edits; extract exact current text of Section 5 (lines 217-296) before writing
    - .planning/phases/68-constraint-inventory-v2-schema/68-PATTERNS.md — Section "lucy-devils-advocate.md update" (lines 267-322) for exact replacement text of 5A and 5B Check 4
    - .planning/phases/68-constraint-inventory-v2-schema/68-RESEARCH.md — Key Finding 5 (lines 604-616) for exact subsections; Key Finding 6 (lines 618-627) for G2 grep anchor; Pitfall 1 (lines 394-403) for G2 false positive prevention; CONTEXT.md D-07 for gate severity definitions
    - .planning/phases/68-constraint-inventory-v2-schema/68-CONTEXT.md — D-02 (v1 WARNING not block), D-07 (all three gates CRITICAL/blocking), D-08 (no new override mechanism), D-12 (CLI replaces grep/sed/awk)
  </read_first>
  <action>
    Read the full file first, then make targeted edits to Section 5 only. Do NOT modify Sections 1-4 or the CASE-PROGRESS.md Contribution Protocol section.

    **Section 5 intro paragraph:**
    - Change "between `; === CONSTRAINT INVENTORY v1 ===`" to "between `; === CONSTRAINT INVENTORY v2 ===`"
    - Change any remaining v1 delimiter references to v2

    **Section 5A — Inventory Extraction:**
    Replace the two grep/sed bash blocks with the CLI-based extraction pattern. The new 5A content:

    - Primary path (v2 inventory):
      RESULT=$(lucy lsd validate-inventory --format json analysis/iteration_NN/compound.lsd)
      VALID=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin)['valid'])")
      If VALID == "True": proceed to 5B checks with the JSON already parsed from $RESULT
      If VALID == "False": check if any error has message containing "Legacy v1 inventory detected"
        - If v1 legacy message: emit WARNING "Legacy v1 inventory — using fallback validation"; do NOT block the run; fall back to the legacy grep-count comparison protocol described in Section 2
        - If other validation errors: BLOCK with error details — inventory is malformed or schema-invalid
    - If no inventory block found (VALID == "False" with "No v2 inventory block found" message): fall back to legacy protocol + WARNING "No inventory block found — using legacy validation"
    - Parse JSON body from $RESULT for use in 5B checks: INVENTORY=$(echo "$RESULT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(json.dumps(d))")
    - Note for the previous-iteration comparison (Check 2): run the same lucy lsd validate-inventory call on the previous iteration file to obtain PREV_INVENTORY

    Retain the fallback note at end of 5A: "If no inventory block found: Fall back to existing diff protocol (count-based comparison). Flag WARNING: 'No constraint inventory found — using legacy validation.'" — this was already present and still applies.

    **Section 5B — Three-Check Reconciliation Protocol:**
    - Keep Checks 1, 2, 3 unchanged
    - After Check 3, add Check 4 as a new CRITICAL section:

    "**Check 4: pyLSD Mode Consistency** (CRITICAL/blocking — all three sub-checks apply when pylsd_mode is true)

    Extract pylsd_mode from INVENTORY JSON. If false or absent: skip Check 4 entirely. If true:

    **G1: FORM/MULT Consistency**
    When pylsd_mode=true AND a line matching `^FORM` exists in the LSD file body (not in comment lines):
    - Extract formula from the FORM line (e.g. FORM C13H18O2 → C13H18O2)
    - Count carbons declared by FORM (parse element counts from formula string)
    - Count MULT lines defining C atoms: grep "^MULT" compound.lsd | grep " C " (or equivalent element extraction)
    - If carbon counts differ: BLOCK with message "FORM/MULT mismatch: FORM declares N carbons, MULT defines M carbons. Fix before running LSD."

    **G2: No Bare ELIM Command in pylsd_mode**
    When pylsd_mode=true:
    - Run: grep -n "^ELIM" analysis/iteration_NN/compound.lsd
    - CRITICAL: this grep MUST be anchored to line start (^ELIM). Do NOT use an unanchored pattern — 'HMBC 4 8 2 4 ; ELIM' contains 'ELIM' but does NOT match '^ELIM'.
    - If any match found: BLOCK with message "Bare ELIM command detected in pylsd_mode. Use 'HMBC X Y 2 4 ; ELIM' instead. [v7.0 post-mortem: Statistical 4J detection non-viable — 100% FP rate. Bare ELIM drops correlations entirely instead of permuting them. The v8.0 convention is HMBC X Y 2 4 (extended bond range) with '; ELIM' trailing comment.]"
    - If no matches: G2 passes

    **G3: Annotation-vs-Mode Consistency**
    When HMBC lines in the LSD file contain '; ELIM' trailing comment annotations:
    - grep -c "; ELIM" analysis/iteration_NN/compound.lsd (counts lines with the annotation)
    - If any '; ELIM' annotation exists, ALL of the following must be true from the inventory JSON:
      (a) pylsd_mode == true
      (b) elim_annotated == true
      (c) len(deferred_4j) > 0
    - If any condition is false: BLOCK with message "HMBC lines carry '; ELIM' annotations but constraint inventory is inconsistent. Check: pylsd_mode={pylsd_mode}, elim_annotated={elim_annotated}, deferred_4j count={len(deferred_4j)}. All three must be consistent when '; ELIM' annotations exist."

    All three gates G1/G2/G3 are CRITICAL severity and blocking. No new override mechanism is available (per design decision D-08). User may intervene via orchestrator advisory if a gate fires incorrectly."

    **Section 5 header comment:** Update delimiter references to v2 (where "v1" appears in the Section 5 header intro paragraph about what the inventory block is).
  </action>
  <verify>
    <automated>grep "lucy lsd validate-inventory --format json" ~/.claude/agents/lucy-devils-advocate.md && grep "G1\|G2\|G3" ~/.claude/agents/lucy-devils-advocate.md && grep "CONSTRAINT INVENTORY v2" ~/.claude/agents/lucy-devils-advocate.md && grep "100% FP rate" ~/.claude/agents/lucy-devils-advocate.md</automated>
  </verify>
  <done>Section 5A uses CLI-based extraction; Section 5B includes G1/G2/G3 as CRITICAL Check 4; intro references v2 delimiters; v1 WARNING path documented</done>
  <acceptance_criteria>
    - grep "lucy lsd validate-inventory --format json" ~/.claude/agents/lucy-devils-advocate.md exits 0
    - grep "CONSTRAINT INVENTORY v2" ~/.claude/agents/lucy-devils-advocate.md exits 0
    - grep "Legacy v1 inventory" ~/.claude/agents/lucy-devils-advocate.md exits 0 (WARNING path per D-02)
    - grep "G1.*FORM\|FORM.*G1" ~/.claude/agents/lucy-devils-advocate.md exits 0 (case-insensitive or specific)
    - grep "G2" ~/.claude/agents/lucy-devils-advocate.md exits 0
    - grep "G3" ~/.claude/agents/lucy-devils-advocate.md exits 0
    - grep "\^ELIM" ~/.claude/agents/lucy-devils-advocate.md exits 0 (anchored grep pattern documented)
    - grep "100% FP rate\|Statistical 4J detection non-viable" ~/.claude/agents/lucy-devils-advocate.md exits 0 (v7.0 post-mortem citation per CONTEXT.md specifics)
    - grep "APPROVED\|BLOCKED\|WARNING" ~/.claude/agents/lucy-devils-advocate.md exits 0 (existing severity model retained per D-08)
    - The file does NOT contain "override-inventory-check" or any new override CLI reference (per D-08 — no new override path)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill file → agent runtime | Agent reads skill markdown and acts on it; G2 anchor incorrectly stated would cause false positives or missed detections |
| agent Bash → LSD file | Devils-advocate runs grep commands on agent-written LSD files; all grep patterns must be anchored correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-07 | Tampering | G2 grep anchor | mitigate | Explicitly document "^ELIM" anchor in skill text; acceptance criteria checks for "\^ELIM" presence |
| T-68-08 | Elevation of Privilege | G1/G2/G3 as blocking gates | accept | Gates enforce known dangerous misconfigurations; existing APPROVED/BLOCKED/WARNING semantics cover all cases; no new override path per D-08 |
| T-68-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After task completes:
1. grep "lucy lsd validate-inventory --format json" ~/.claude/agents/lucy-devils-advocate.md exits 0
2. grep "CONSTRAINT INVENTORY v2" ~/.claude/agents/lucy-devils-advocate.md exits 0
3. grep "G1\|G2\|G3" ~/.claude/agents/lucy-devils-advocate.md exits 0
4. grep "\^ELIM" ~/.claude/agents/lucy-devils-advocate.md exits 0
5. grep "100% FP rate" ~/.claude/agents/lucy-devils-advocate.md exits 0
6. pytest exits 0 (full suite, no regressions)
</verification>

<success_criteria>
- Section 5A of lucy-devils-advocate.md uses lucy lsd validate-inventory --format json to extract and validate the inventory, instead of the grep/sed/awk pipeline
- v1 legacy detection produces WARNING (not BLOCK) and falls back to legacy validation
- Section 5B Check 4 documents G1 (FORM/MULT consistency), G2 (^ELIM anchor, v7.0 post-mortem citation), G3 (annotation-vs-mode consistency) as CRITICAL/blocking
- G2 grep is explicitly anchored to ^ELIM and explains why unanchored "ELIM" is wrong
- No new override mechanism introduced beyond existing APPROVED/BLOCKED/WARNING semantics
</success_criteria>

<output>
Create .planning/phases/68-constraint-inventory-v2-schema/68-04-SUMMARY.md when done
</output>
