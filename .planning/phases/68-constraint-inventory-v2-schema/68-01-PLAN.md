---
phase: 68-constraint-inventory-v2-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/constraint_inventory_v2.json
  - pyproject.toml
autonomous: true
requirements:
  - INPUT-05

must_haves:
  truths:
    - "schemas/constraint_inventory_v2.json exists at repo root and is a valid JSON Schema Draft 2020-12 document (per D-09)"
    - "The schema requires version=2 (const), pylsd_mode (bool), elim_annotated (bool), and deferred_4j (object array) as mandatory fields (per D-03, D-04)"
    - "deferred_4j items enforce atom1/atom2/shift1/shift2/correlation_type/annotation with additionalProperties:false (per D-04, D-05)"
    - "jsonschema>=4.18.0 is declared in pyproject.toml [project] dependencies (required for Draft202012Validator)"
    - "Schema rejects a v1 instance (version:1, string-array deferred_4j) with at least one validation error"
    - "Schema accepts a correct v2 instance (version:2, pylsd_mode:true, elim_annotated:true, deferred_4j object array)"
  artifacts:
    - path: "schemas/constraint_inventory_v2.json"
      provides: "Machine-readable Source of Truth for constraint inventory v2 format"
      contains: "Draft 2020-12"
    - path: "pyproject.toml"
      provides: "jsonschema dependency declaration"
      contains: "jsonschema>=4.18.0"
    - path: "tests/test_inventory_schema.py"
      provides: "Schema validation tests covering INPUT-05"
      min_lines: 60
  key_links:
    - from: "tests/test_inventory_schema.py"
      to: "schemas/constraint_inventory_v2.json"
      via: "json.load() + Draft202012Validator"
      pattern: "Draft202012Validator"
---

<objective>
Create the JSON Schema Draft 2020-12 source of truth file for the constraint inventory v2 format, add jsonschema to project dependencies, and write the test suite that validates the schema itself.

Purpose: The schema is the ground truth that both the CLI validator (Plan 02) and the agent skill documentation (Plans 03/04) reference. It must exist before the CLI can load it and before the skill docs can cite a path.

Output: schemas/constraint_inventory_v2.json, updated pyproject.toml, tests/test_inventory_schema.py
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/PROJECT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/ROADMAP.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/STATE.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/68-constraint-inventory-v2-schema/68-CONTEXT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/68-constraint-inventory-v2-schema/68-RESEARCH.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/68-constraint-inventory-v2-schema/68-PATTERNS.md

<interfaces>
<!-- Existing pyproject.toml [project] dependencies block (lines 26-34): -->
dependencies = [
    "click>=8.0",
    "nmrglue>=0.9",
    "numpy>=1.24",
    "pydantic>=2.0",
    "rdkit>=2023.0",
    "scipy>=1.10",
    "tqdm>=4.0",
]
<!-- Add "jsonschema>=4.18.0" in alphabetical order between "click" and "nmrglue" -->

<!-- Existing test pattern from tests/test_lsd_generator.py (TestPyLSDValidator): -->
class TestPyLSDValidator:
    def test_validate_pylsd_carbon_mismatch(self):
        from lucy_ng.lsd.generator import validate_pylsd_input
        problem = LSDProblem(molecular_formula="C13H18O2")
        for i in range(1, 13):
            problem.add_atom(LSDAtom(i, "C", Hybridization.SP3, 0))
        with pytest.raises(ValueError, match="FORM/MULT mismatch"):
            validate_pylsd_input(problem)
<!-- Mirror this class-per-feature structure in test_inventory_schema.py -->

<!-- JSON Schema structure per RESEARCH.md Pattern 4 — key fields: -->
<!--   "$schema": "https://json-schema.org/draft/2020-12/schema" -->
<!--   "required": ["version","iteration","formula","timestamp","mult_count","hsqc_count", -->
<!--                "hmbc_batches","hmbc_total","pylsd_mode","elim_annotated","deferred_4j"] -->
<!--   "version": {"type": "integer", "const": 2} -->
<!--   "pylsd_mode": {"type": "boolean"} -->
<!--   "elim_annotated": {"type": "boolean"} -->
<!--   deferred_4j items: required ["atom1","atom2","shift1","shift2","correlation_type","annotation"] -->
<!--   "correlation_type": {"type": "string", "const": "HMBC"} -->
<!--   "annotation": {"type": "string", "const": "; ELIM"} -->
<!--   "additionalProperties": true at top level; false on deferred_4j items -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create schemas/constraint_inventory_v2.json and add jsonschema dependency</name>
  <files>schemas/constraint_inventory_v2.json, pyproject.toml</files>
  <read_first>
    - pyproject.toml — read lines 26-38 (current dependencies block) before editing
    - .planning/phases/68-constraint-inventory-v2-schema/68-RESEARCH.md — Pattern 4 (lines 299-361) for exact schema structure
    - .planning/phases/68-constraint-inventory-v2-schema/68-CONTEXT.md — D-03 (version field), D-04 (deferred_4j schema), D-05 (identity tuple), D-09 (schema file path)
  </read_first>
  <behavior>
    - Schema loads without error via python -c "import json,jsonschema; jsonschema.Draft202012Validator.check_schema(json.load(open('schemas/constraint_inventory_v2.json')))"
    - Schema rejects {"version": 1, "deferred_4j": ["string"]} with validation error on version field
    - Schema accepts {"version": 2, "pylsd_mode": false, "elim_annotated": false, "deferred_4j": [], "iteration": 1, "formula": "C13H18O2", "timestamp": "2026-01-01T00:00:00Z", "mult_count": 13, "hsqc_count": 9, "hmbc_batches": [], "hmbc_total": 0}
    - Schema rejects deferred_4j item with string annotation value other than "; ELIM"
    - Schema rejects deferred_4j item missing required fields (e.g. missing atom2)
  </behavior>
  <action>
    Create the `schemas/` directory at repo root and write `schemas/constraint_inventory_v2.json` as a JSON Schema Draft 2020-12 document.

    Schema structure (per D-09, D-03, D-04, D-05 and RESEARCH.md Pattern 4):
    - "$schema": "https://json-schema.org/draft/2020-12/schema"
    - "$id": "https://lucy-ng/schemas/constraint_inventory_v2.json"
    - "title": "Constraint Inventory v2"
    - "required" array: version, iteration, formula, timestamp, mult_count, hsqc_count, hmbc_batches, hmbc_total, pylsd_mode, elim_annotated, deferred_4j
    - "additionalProperties": true at the top level (forward compatibility — agents may write extra fields)
    - "version": type integer, const 2
    - "iteration": type integer, minimum 1
    - "formula": type string
    - "timestamp": type string
    - "mult_count": type integer, minimum 0
    - "hsqc_count": type integer, minimum 0
    - "hmbc_total": type integer, minimum 0
    - "pylsd_mode": type boolean
    - "elim_annotated": type boolean
    - "hmbc_batches": array of objects, each required [batch (int), count (int), correlations (array of strings)], additionalProperties: false on items
    - "deferred_4j": array of objects, each required [atom1 (int, minimum 1), atom2 (int, minimum 1), shift1 (number), shift2 (number), correlation_type (string, const "HMBC"), annotation (string, const "; ELIM")], additionalProperties: false on items
    - Optional fields at top level (no validation beyond type when present): grouped_hmbc (string array), bond_constraints (string array), syme_pairs (string array), list_prop_constraints (string array), elim_value (integer or null), deff_not_patterns (string array), deff_fexp (object or null), detection_results (object), applied_from_detection (string array), pending_from_detection (string array)

    Then edit pyproject.toml: add "jsonschema>=4.18.0" to the [project] dependencies list in alphabetical order — between "click>=8.0" and "nmrglue>=0.9".
  </action>
  <verify>
    <automated>
      python -c "import json,jsonschema; jsonschema.Draft202012Validator.check_schema(json.load(open('schemas/constraint_inventory_v2.json')))" && grep '"jsonschema>=4.18.0"' pyproject.toml
    </automated>
  </verify>
  <done>schemas/constraint_inventory_v2.json exists and passes meta-schema check; pyproject.toml contains jsonschema>=4.18.0</done>
  <acceptance_criteria>
    - python -c "import json,jsonschema; jsonschema.Draft202012Validator.check_schema(json.load(open('schemas/constraint_inventory_v2.json')))" exits 0
    - grep '"jsonschema>=4.18.0"' pyproject.toml exits 0
    - grep '"const": 2' schemas/constraint_inventory_v2.json exits 0 (version=2 const enforced)
    - grep '"pylsd_mode"' schemas/constraint_inventory_v2.json exits 0
    - grep '"elim_annotated"' schemas/constraint_inventory_v2.json exits 0
    - grep '"deferred_4j"' schemas/constraint_inventory_v2.json exits 0
    - grep '"additionalProperties": false' schemas/constraint_inventory_v2.json exits 0 (on deferred_4j items)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test_inventory_schema.py with schema loading and validation tests</name>
  <files>tests/test_inventory_schema.py</files>
  <read_first>
    - tests/test_lsd_generator.py lines 1-9 and 500-537 — file header pattern and TestPyLSDValidator class structure to mirror
    - schemas/constraint_inventory_v2.json — just created in Task 1; executor reads it to confirm required fields for test fixtures
    - .planning/phases/68-constraint-inventory-v2-schema/68-VALIDATION.md — per-task verification map and Wave 0 requirements
  </read_first>
  <behavior>
    - TestSchemaLoading::test_schema_file_exists — file at repo_root/schemas/constraint_inventory_v2.json exists
    - TestSchemaLoading::test_schema_is_valid_json — json.load() succeeds without exception
    - TestSchemaLoading::test_schema_has_draft_2020_12 — "$schema" value contains "2020-12"
    - TestSchemaValidation::test_accepts_minimal_valid_v2 — Draft202012Validator raises no errors on a minimal correct v2 instance
    - TestSchemaValidation::test_rejects_v1_version — instance with "version": 1 fails with ValidationError on version field
    - TestSchemaValidation::test_rejects_deferred_4j_string_array — instance with deferred_4j: ["C4(129.4) H8(45.0)"] fails validation
    - TestSchemaValidation::test_rejects_missing_pylsd_mode — instance without pylsd_mode fails validation
    - TestSchemaValidation::test_rejects_deferred_4j_item_missing_atom2 — deferred_4j item with only atom1 present fails additionalProperties or required check
    - TestSchemaValidation::test_rejects_deferred_4j_wrong_annotation — annotation value "elim" (not "; ELIM") fails const check
  </behavior>
  <action>
    Create tests/test_inventory_schema.py with four test classes: TestSchemaLoading, TestSchemaValidation, TestDeferred4jSchema, TestSchemaOptionalFields.

    File header pattern (from test_lsd_generator.py analog):
    - Module docstring: "Tests for constraint inventory v2 JSON schema."
    - Imports: json, pytest, Path, jsonschema.Draft202012Validator
    - No imports from lucy_ng needed in this file (schema is a standalone JSON file)

    Helper fixture: load the schema once per class using a @pytest.fixture or a module-level function _load_schema() that resolves path via Path(__file__).parent.parent / "schemas" / "constraint_inventory_v2.json".

    Minimal valid v2 instance fixture to reuse across tests:
    - version: 2, iteration: 1, formula: "C13H18O2", timestamp: "2026-01-01T00:00:00Z"
    - mult_count: 13, hsqc_count: 9, hmbc_batches: [], hmbc_total: 0
    - pylsd_mode: false, elim_annotated: false, deferred_4j: []

    deferred_4j object fixture for valid item tests:
    - atom1: 4, atom2: 8, shift1: 129.38, shift2: 45.03, correlation_type: "HMBC", annotation: "; ELIM"

    Each test method: 5-10 lines, one assertion, docstring stating the single invariant. Follow the "# Should not raise" comment convention from test_lsd_generator.py line 519 for happy-path tests.

    Use iter_errors() not validate() so test assertions can check error count and fields. Use list(validator.iter_errors(instance)) pattern — list must be empty for valid, non-empty for invalid.
  </action>
  <verify>
    <automated>pytest tests/test_inventory_schema.py -x</automated>
  </verify>
  <done>All tests in tests/test_inventory_schema.py pass; full suite still green</done>
  <acceptance_criteria>
    - pytest tests/test_inventory_schema.py -x exits 0 with all tests passing
    - pytest exits 0 (full suite green, no regressions)
    - grep "TestSchemaLoading\|TestSchemaValidation" tests/test_inventory_schema.py exits 0
    - grep "Draft202012Validator" tests/test_inventory_schema.py exits 0
    - grep "test_rejects_v1_version\|test_rejects_deferred_4j_string_array" tests/test_inventory_schema.py exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → CLI | lsd_file path argument from user shell — could point to arbitrary file |
| agent-written LSD → schema validator | LSD file content written by autonomous agent — may contain malformed JSON in comment block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | schemas/constraint_inventory_v2.json path | accept | Schema file is repo-committed; no runtime writes; read-only by CLI |
| T-68-02 | DoS | json.loads() on large inventory block | accept | Inventory blocks are agent-written and bounded by practical CASE run size; json.loads is safe for this domain |
| T-68-SC | Tampering | jsonschema package install | mitigate | Package legitimacy audit: jsonschema [OK] — approved (see RESEARCH.md); no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
After both tasks complete:
1. python -c "import json,jsonschema; jsonschema.Draft202012Validator.check_schema(json.load(open('schemas/constraint_inventory_v2.json')))" exits 0
2. pytest tests/test_inventory_schema.py -x exits 0
3. pytest exits 0 (full suite, no regressions)
4. grep '"jsonschema>=4.18.0"' pyproject.toml exits 0
</verification>

<success_criteria>
- schemas/constraint_inventory_v2.json is a valid JSON Schema Draft 2020-12 document with version const:2, required fields pylsd_mode/elim_annotated/deferred_4j, and strict deferred_4j item validation
- jsonschema>=4.18.0 is declared in pyproject.toml dependencies
- tests/test_inventory_schema.py covers schema loading, valid v2 acceptance, v1 rejection, string-array deferred_4j rejection, and missing-required-field rejection
- Full pytest suite is green
</success_criteria>

<output>
Create .planning/phases/68-constraint-inventory-v2-schema/68-01-SUMMARY.md when done
</output>
