---
phase: 68-constraint-inventory-v2-schema
plan: 03
type: execute
wave: 3
depends_on:
  - 68-01
  - 68-02
files_modified:
  - /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
autonomous: true
requirements:
  - INPUT-05

must_haves:
  truths:
    - "lucy-lsd-engineer.md Section 5 uses v2 delimiters: '; === CONSTRAINT INVENTORY v2 ===' and '; === END CONSTRAINT INVENTORY ===' (per D-01)"
    - "The inline JSON example in Section 5B shows pylsd_mode, elim_annotated, and deferred_4j as object array with atom1/atom2/shift1/shift2/correlation_type/annotation (per D-04, D-11)"
    - "Section 5A schema table references schemas/constraint_inventory_v2.json as Source of Truth (per D-09, D-11)"
    - "Section 5A includes rows for pylsd_mode (bool), elim_annotated (bool), and deferred_4j (object array) with v2 semantics (per D-03, D-04)"
    - "An explicit UPGRADE NOTE in Section 5A warns that deferred_4j changed from string array to object array in v2"
    - "Section 5D Update Procedure references 'CONSTRAINT INVENTORY v2' delimiter string (not v1)"
    - "Workflow step 2a references structured objects (atom1/atom2/shift1/shift2) not string descriptions"
  artifacts:
    - path: "/Users/steinbeck/.claude/agents/lucy-lsd-engineer.md"
      provides: "v2 schema documentation and inline example for lsd-engineer agent"
      contains: "CONSTRAINT INVENTORY v2"
  key_links:
    - from: "lucy-lsd-engineer.md §5A"
      to: "schemas/constraint_inventory_v2.json"
      via: "explicit path reference in Section 5A intro"
      pattern: "schemas/constraint_inventory_v2.json"
---

<objective>
Update lucy-lsd-engineer.md Section 5 (Constraint Inventory System) from v1 to v2: replace delimiters, update schema table, replace inline example, update init/update procedures.

Purpose: The lsd-engineer agent reads this skill file before every CASE iteration. The v1 content will cause agents to write v1 inventories that fail schema validation — this update is the primary mechanism by which agents learn the v2 format.

Output: Updated ~/.claude/agents/lucy-lsd-engineer.md with v2 schema documentation throughout Section 5
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/PROJECT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/ROADMAP.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/STATE.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/68-constraint-inventory-v2-schema/68-CONTEXT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/68-constraint-inventory-v2-schema/68-RESEARCH.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/68-constraint-inventory-v2-schema/68-PATTERNS.md

<interfaces>
<!-- Current v1 Section 5A schema table (lines 316-336 of lucy-lsd-engineer.md): -->
<!--   version: int (always 1) -->
<!--   deferred_4j: string array — "Potential 4J correlations: ['C4(129.4) H8(45.0)']" -->
<!--   (no pylsd_mode row, no elim_annotated row) -->

<!-- Current v1 inline example (lines 343-376): -->
<!--   ; === CONSTRAINT INVENTORY v1 === -->
<!--   ;   "version": 1, ... -->
<!--   ;   "deferred_4j": ["C4(129.4) H8(45.0)"], -->
<!--   ; === END CONSTRAINT INVENTORY === -->

<!-- Current Section 5D delimiter reference (line 402): -->
<!--   "between `; === CONSTRAINT INVENTORY v1 ===` and `; === END CONSTRAINT INVENTORY ===`" -->

<!-- v2 inline example to replace it with (RESEARCH.md lines 492-516): -->
<!--   ; === CONSTRAINT INVENTORY v2 === -->
<!--   ;   "version": 2, ... -->
<!--   ;   "pylsd_mode": true, -->
<!--   ;   "elim_annotated": true, -->
<!--   ;   "deferred_4j": [ -->
<!--   ;     {"atom1": 4, "atom2": 8, "shift1": 129.38, "shift2": 45.03, "correlation_type": "HMBC", "annotation": "; ELIM"}, -->
<!--   ;     {"atom1": 6, "atom2": 9, "shift1": 127.26, "shift2": 44.90, "correlation_type": "HMBC", "annotation": "; ELIM"} -->
<!--   ;   ], -->
<!--   ; === END CONSTRAINT INVENTORY === -->
<!--   HMBC 4 8 2 4 ; ELIM -->
<!--   HMBC 6 9 2 4 ; ELIM -->

<!-- elim_value vs elim_annotated distinction (RESEARCH.md Pitfall 5): -->
<!--   elim_value: non-null only when bare ELIM N M command written (last-resort). If pylsd_mode=true, G2 blocks non-null elim_value. -->
<!--   elim_annotated: true when HMBC lines carry '; ELIM' trailing comment for PyLSDOrchestrator. If pylsd_mode=true AND elim_annotated=true, deferred_4j must be non-empty (G3). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite lucy-lsd-engineer.md Section 5 for v2 schema</name>
  <files>/Users/steinbeck/.claude/agents/lucy-lsd-engineer.md</files>
  <read_first>
    - /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md — read the ENTIRE file (not just lines 310-416) before making any edits; extract exact current text of all subsections to be changed before writing
    - .planning/phases/68-constraint-inventory-v2-schema/68-PATTERNS.md — Section "lucy-lsd-engineer.md update" (lines 218-264) for exact subsection-by-subsection change map
    - .planning/phases/68-constraint-inventory-v2-schema/68-RESEARCH.md — Key Finding 4 (lines 594-613) for the precise subsection-location table; Code Examples for v1→v2 example replacement (lines 459-516)
  </read_first>
  <action>
    Read the full file first, then make targeted edits to Section 5 only. Do NOT modify any content outside Section 5 (Sections 1-4, message_interface).

    Changes to make in Section 5:

    **Section 5 title line / intro:** Update any reference to "v1" in the title or intro to "v2".

    **5A JSON Schema Reference — schema table:**
    - Change `version` row: "int (always 1)" → "int (always 2)" and add const note
    - Change `deferred_4j` row: type from "string array" to "object array"; update Purpose from "Potential 4J correlations: ['C4(129.4) H8(45.0)']" to "Structured 4J suspect entries; each object: atom1 (int, LSD index), atom2 (int), shift1 (ppm), shift2 (ppm), correlation_type ('HMBC'), annotation ('; ELIM')"
    - Add new row after deferred_4j or in logical position: `pylsd_mode` | boolean | "True when PyLSDOrchestrator multi-run mode is active; controls G1/G2/G3 validation"
    - Add new row: `elim_annotated` | boolean | "True when HMBC lines carry '; ELIM' trailing comment for PyLSDOrchestrator permutation parsing"
    - Add intro paragraph before the table: "Source of Truth: schemas/constraint_inventory_v2.json (repo root, JSON Schema Draft 2020-12). The table below is a summary; the schema file is authoritative."
    - Add UPGRADE NOTE callout after the table: "UPGRADE NOTE: deferred_4j changed from string array to object array in v2. The old format ['C4(129.4) H8(45.0)'] will fail schema validation. Each entry must be a JSON object with atom1, atom2, shift1, shift2, correlation_type, annotation."
    - Add distinction note: "Note: elim_value (existing field) and elim_annotated (new field) serve different purposes. elim_value is non-null only when a bare ELIM N M command is written (last-resort zero-solution recovery). elim_annotated is true when HMBC lines carry '; ELIM' comments for PyLSDOrchestrator parsing."

    **5B LSD File Format:**
    - Replace delimiter line "; === CONSTRAINT INVENTORY v1 ===" with "; === CONSTRAINT INVENTORY v2 ==="
    - Replace "version": 1 with "version": 2 in the inline JSON example
    - Replace "deferred_4j": ["C4(129.4) H8(45.0)"] with the full object-array example (see interfaces block above)
    - Add pylsd_mode and elim_annotated fields to the inline example
    - After the closing "}" line and END delimiter, show the corresponding HMBC command lines: "HMBC 4 8 2 4 ; ELIM" and "HMBC 6 9 2 4 ; ELIM" — these appear BELOW the inventory block in the LSD file, showing the connection between the annotation in the inventory and the actual LSD command

    **5C Initialization Procedure:**
    - Step 2a (currently: "add them to deferred_4j in the constraint inventory (as string descriptions)"): change to "add them to deferred_4j in the constraint inventory as structured objects — each with atom1 (LSD index), atom2, shift1 (ppm from HSQC), shift2, correlation_type='HMBC', annotation='; ELIM'"
    - Add initialization for new fields: "Set pylsd_mode: true if nmr-chemist identified any 4J suspects; false otherwise. Set elim_annotated: true if any deferred_4j entries exist (i.e. len(deferred_4j) > 0); false otherwise."

    **5D Update Procedure:**
    - Step 2: change "CONSTRAINT INVENTORY v1" to "CONSTRAINT INVENTORY v2" in the delimiter string reference

    **Workflow steps (upstream of Section 5, wherever "string descriptions" appears):**
    - Find any reference to writing 4J correlations as string descriptions and update to "structured objects per v2 schema (atom1/atom2/shift1/shift2/correlation_type/annotation)"
  </action>
  <verify>
    <automated>grep "=== CONSTRAINT INVENTORY v2 ===" ~/.claude/agents/lucy-lsd-engineer.md && grep "schemas/constraint_inventory_v2.json" ~/.claude/agents/lucy-lsd-engineer.md && grep "elim_annotated" ~/.claude/agents/lucy-lsd-engineer.md && grep "pylsd_mode" ~/.claude/agents/lucy-lsd-engineer.md && grep "UPGRADE NOTE" ~/.claude/agents/lucy-lsd-engineer.md</automated>
  </verify>
  <done>Section 5 of lucy-lsd-engineer.md documents v2 schema with v2 delimiters, object-array deferred_4j example, pylsd_mode/elim_annotated rows, and schema file reference</done>
  <acceptance_criteria>
    - grep "=== CONSTRAINT INVENTORY v2 ===" ~/.claude/agents/lucy-lsd-engineer.md exits 0
    - grep "schemas/constraint_inventory_v2.json" ~/.claude/agents/lucy-lsd-engineer.md exits 0
    - grep "pylsd_mode" ~/.claude/agents/lucy-lsd-engineer.md exits 0
    - grep "elim_annotated" ~/.claude/agents/lucy-lsd-engineer.md exits 0
    - grep '"version": 2' ~/.claude/agents/lucy-lsd-engineer.md exits 0 (v2 in inline example)
    - grep "atom1.*atom2.*shift1" ~/.claude/agents/lucy-lsd-engineer.md exits 0 (object-array deferred_4j example)
    - grep "UPGRADE NOTE" ~/.claude/agents/lucy-lsd-engineer.md exits 0
    - grep -c "CONSTRAINT INVENTORY v1" ~/.claude/agents/lucy-lsd-engineer.md returns 0 (no remaining v1 references in Section 5 — earlier historical references in commit-log-style notes are acceptable only outside Section 5; confirm by context)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill file → agent runtime | Agent reads skill markdown and acts on it; incorrect content directly causes wrong LSD files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-05 | Information Disclosure | lucy-lsd-engineer.md skill update | accept | Skill file is in ~/.claude/agents/ (user-local); no sensitive data written |
| T-68-06 | Repudiation | v1 inline example removal | mitigate | v1 example is replaced (not just removed) with v2 example; UPGRADE NOTE explains the change — agents won't silently fall back to v1 format |
| T-68-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After task completes:
1. grep "=== CONSTRAINT INVENTORY v2 ===" ~/.claude/agents/lucy-lsd-engineer.md exits 0
2. grep "schemas/constraint_inventory_v2.json" ~/.claude/agents/lucy-lsd-engineer.md exits 0
3. grep "pylsd_mode" ~/.claude/agents/lucy-lsd-engineer.md exits 0
4. grep "elim_annotated" ~/.claude/agents/lucy-lsd-engineer.md exits 0
5. grep "UPGRADE NOTE" ~/.claude/agents/lucy-lsd-engineer.md exits 0
6. pytest exits 0 (full suite, no regressions — skill file changes do not affect Python tests)
</verification>

<success_criteria>
- lucy-lsd-engineer.md Section 5 uses v2 delimiters throughout
- Schema table includes pylsd_mode, elim_annotated, updated deferred_4j rows with object-array type
- Inline JSON example shows version:2, pylsd_mode:true, elim_annotated:true, and deferred_4j as object array with atom1/atom2/shift1/shift2
- schemas/constraint_inventory_v2.json referenced by path in Section 5A
- UPGRADE NOTE callout present warning about string→object migration
- Section 5C and 5D reference v2 delimiters and object-based deferred_4j initialization
</success_criteria>

<output>
Create .planning/phases/68-constraint-inventory-v2-schema/68-03-SUMMARY.md when done
</output>
