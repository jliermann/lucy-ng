---
phase: 30-diagnostic-specialist-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/.claude/agents/lucy-diagnostic.md"
  - "~/.claude/commands/lucy-ng/case.md"
files_deleted:
  - ".claude/agents/diagnostic-specialist.md"
autonomous: true

must_haves:
  truths:
    - "Diagnostic specialist agent file is at ~/.claude/agents/lucy-diagnostic.md (user-global) with frontmatter name: lucy-diagnostic"
    - "Old project-local .claude/agents/diagnostic-specialist.md is deleted"
    - "Orchestrator delegates to diagnostic specialist after 2 failed interventions with the same loop pattern"
    - "Orchestrator reads DIAGNOSTIC-REPORT.md after specialist completes, extracts root cause and primary fix"
    - "Specialist-informed advisory includes extracted root cause, fix action, and verification steps"
    - "Orchestrator falls back to basic advisory if DIAGNOSTIC-REPORT.md is missing after delegation"
    - "Counter increments after delegation (specialist delegation counts as 1 intervention cycle)"
  artifacts:
    - path: "~/.claude/agents/lucy-diagnostic.md"
      provides: "Renamed and relocated diagnostic specialist agent definition (user-global)"
      contains: "name: lucy-diagnostic"
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "Orchestrator with diagnostic specialist delegation"
      contains: "delegate_specialist"
  key_links:
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "~/.claude/agents/lucy-diagnostic.md"
      via: "Task tool agent_type parameter"
      pattern: "lucy-diagnostic"
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "DIAGNOSTIC-REPORT.md"
      via: "Read tool after specialist completes"
      pattern: "Root Cause|Recommended Fixes"
    - from: "track_and_decide step"
      to: "delegate_specialist step"
      via: "counter == 2 routing"
      pattern: "counter.*2.*delegate"
---

<objective>
Integrate the diagnostic specialist agent into the CASE orchestrator workflow so that after 2 failed basic interventions for the same loop pattern, the orchestrator delegates to the specialist for deep root cause analysis, reads the structured DIAGNOSTIC-REPORT.md, and generates a specialist-informed advisory for the next CASE agent spawn.

Purpose: Basic diagnosis (sp2 count, H budget, 1J artifacts) covers common failures, but difficult root causes (close carbon ambiguity, complex constraint conflicts, subtle spectral artifacts) require the deep systematic checks that only the diagnostic specialist can perform. This integration closes the gap between basic intervention and user escalation.

Output: Relocated and renamed agent definition at ~/.claude/agents/lucy-diagnostic.md (user-global, consistent with lucy-case-agent.md) and updated orchestrator at ~/.claude/commands/lucy-ng/case.md with working delegation trigger, specialist spawning, report parsing, and specialist-informed advisory generation.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-diagnostic-specialist-integration/30-RESEARCH.md
@.planning/phases/29-case-orchestrator-skill/29-01-SUMMARY.md

Key source files:
@.claude/agents/diagnostic-specialist.md (SOURCE - project-local, to be moved)
@~/.claude/commands/lucy-ng/case.md
@~/.claude/agents/lucy-case-agent.md (for Task tool spawning pattern reference and target directory)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move and rename diagnostic specialist agent to user-global lucy-diagnostic</name>
  <files>~/.claude/agents/lucy-diagnostic.md (created), .claude/agents/diagnostic-specialist.md (deleted)</files>
  <action>
  Move the diagnostic specialist agent from its legacy project-local location to the user-global agents directory, consistent with the Phase 28 decision ("Agent files live in ~/.claude/agents/") and the existing lucy-case-agent.md location.

  **Source:** Project-local `.claude/agents/diagnostic-specialist.md` (absolute: /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/diagnostic-specialist.md)
  **Target:** User-global `~/.claude/agents/lucy-diagnostic.md` (absolute: /Users/steinbeck/.claude/agents/lucy-diagnostic.md)

  1. **Read the source file** at the project-local path: `.claude/agents/diagnostic-specialist.md` (relative to project root /Users/steinbeck/Dropbox/develop/lucy-ng/)

  2. **Create the target file** at user-global path: `~/.claude/agents/lucy-diagnostic.md` (i.e., /Users/steinbeck/.claude/agents/lucy-diagnostic.md, same directory as lucy-case-agent.md)

  3. **Update YAML frontmatter in the new file:**
     - Change `name: diagnostic-specialist` to `name: lucy-diagnostic`
     - Change `model: sonnet` to `model: inherit` (consistent with lucy-case-agent.md pattern)
     - Keep description, tools (Read, Bash) unchanged

  4. **Update internal references throughout the new file:**
     - Replace all occurrences of "supervisor" spawning context with "orchestrator" (the v2.0 supervisor.md is being dissolved into case.md orchestrator in v2.1)
     - In "## Your Role" section: change "When the supervisor detects" to "When the orchestrator detects"
     - In "## What You Receive from Supervisor" section: rename heading to "## What You Receive from Orchestrator" and change "The supervisor spawns you" to "The orchestrator spawns you"
     - In "## What You Produce" section: change "The supervisor reads your report" to "The orchestrator reads your report" and "supervisor/CASE agent" to "orchestrator/CASE agent"
     - In "## Summary" section: change "supervisor detects" to "orchestrator detects" and "supervisor to advise" to "orchestrator to advise"
     - In "## Anti-Patterns to Avoid" item 6: change "only supervisor can spawn" to "only orchestrator can spawn"
     - In "## Metadata" template at bottom: change "Supervisor: lucy-supervisor" to "Orchestrator: /lucy-ng:case"
     - In "## Metadata" template: change "Specialist model: diagnostic-specialist subagent" to "Specialist model: lucy-diagnostic subagent"

  5. **Do NOT change** the diagnostic workflow, systematic checks, report template, domain knowledge references, important rules, example outputs, or anti-patterns logic. These are correct and unchanged from Phase 25.

  6. **Delete the old project-local file:** Remove `.claude/agents/diagnostic-specialist.md` from the project directory (absolute: /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/diagnostic-specialist.md). Confirm it no longer exists.
  </action>
  <verify>
  - Run: `ls ~/.claude/agents/lucy-diagnostic.md` and confirm it exists at the user-global location
  - Run: `ls ~/.claude/agents/lucy-case-agent.md` and confirm both agent files are in the same directory
  - Run: `ls /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/diagnostic-specialist.md 2>/dev/null` and confirm it does NOT exist (deleted)
  - Run: `head -15 ~/.claude/agents/lucy-diagnostic.md` and confirm frontmatter has `name: lucy-diagnostic` and `model: inherit`
  - Run: `grep -c "supervisor" ~/.claude/agents/lucy-diagnostic.md` and confirm count is 0 (all references updated to orchestrator)
  - Run: `wc -l ~/.claude/agents/lucy-diagnostic.md` and confirm approximately 455 lines (same as original, minor changes)
  </verify>
  <done>
  File ~/.claude/agents/lucy-diagnostic.md exists at user-global location (same directory as lucy-case-agent.md) with frontmatter name: lucy-diagnostic, model: inherit. All "supervisor" references updated to "orchestrator". Old project-local .claude/agents/diagnostic-specialist.md deleted. Agent definition content (domain knowledge, workflow, report template, rules) unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire diagnostic specialist delegation into case.md orchestrator</name>
  <files>~/.claude/commands/lucy-ng/case.md</files>
  <action>
  Modify ~/.claude/commands/lucy-ng/case.md to replace the diagnostic_specialist_placeholder with working delegation logic. Three changes required:

  **Change 1: Update track_and_decide step (lines ~308-327)**

  Replace the current decision logic:
  ```
  - If counter for this pattern < 10: Re-spawn agent with advisory
  - If counter for this pattern >= 10: Escalate to user
  ```

  With the expanded decision logic from 30-RESEARCH.md Pattern 1:
  ```
  - If counter for this pattern == 0 or 1: Basic intervention (diagnose + intervene steps, current behavior)
  - If counter for this pattern == 2: Delegate to diagnostic specialist (proceed to delegate_specialist step)
  - If counter for this pattern >= 3 and < 10: Use specialist-informed advisory if DIAGNOSTIC-REPORT.md exists in compound directory, else basic advisory
  - If counter for this pattern >= 10: Escalate to user (proceed to escalate step)
  ```

  Add explanation:
  ```
  **Why threshold = 2:**
  - First intervention (counter 0->1): Basic diagnosis sufficient for common issues (odd sp2, missing H)
  - Second intervention (counter 1->2): Pattern recurred despite basic fix, needs deeper analysis
  - Specialist after 2 failures prevents wasted cycles on difficult root causes
  ```

  **Change 2: Replace diagnostic_specialist_placeholder section (lines ~577-591) with two new steps**

  Delete the entire `<diagnostic_specialist_placeholder>` section and replace with:

  **Step: delegate_specialist** (insert between intervene and track_and_decide in the process flow, but only executed when track_and_decide routes to it at counter == 2):

  ```xml
  <step name="delegate_specialist">
  Delegate to diagnostic specialist agent for deep root cause analysis.

  **This step executes when:** counter for detected pattern == 2 (routed from track_and_decide).

  **Identify failure type from detected pattern:**
  - ZERO_SOLUTION_LOOP -> failure_type = "0 solutions"
  - SOLUTION_EXPLOSION -> failure_type = "1000+ solutions"
  - ELIM_THRASHING -> failure_type = "0 solutions" (ELIM usually causes 0)
  - CONSTRAINT_CHURNING -> failure_type = "other"

  **Extract latest LSD filename:**
  Read CASE-PROGRESS.md, find most recent iteration, extract the LSD file reference.

  **Spawn diagnostic specialist via Task tool:**
  ```
  Task(
    agent_type="lucy-diagnostic",
    instructions="Analyze LSD failure for compound at <compound_path>.

    Read:
    - <compound_path>/CASE-PROGRESS.md (iteration history)
    - <compound_path>/<latest_lsd_file> (latest LSD file)

    Failure type: <failure_type>

    Run systematic diagnostic checks per skill/diagnostic/SKILL.md.
    Document ALL checks (PASS and FAIL).
    Identify root cause with evidence.

    Write structured report to <compound_path>/DIAGNOSTIC-REPORT.md.
    Include: findings, root cause, recommended fixes with LSD command examples.
    Rate all findings and recommendations as HIGH/MEDIUM/LOW confidence.
    "
  )
  ```

  **After specialist Task completes**, proceed to extract_diagnostic_findings step.
  </step>
  ```

  **Step: extract_diagnostic_findings** (new step, executes after delegate_specialist):

  ```xml
  <step name="extract_diagnostic_findings">
  Read and parse DIAGNOSTIC-REPORT.md to extract root cause and primary fix for specialist-informed advisory generation.

  **Check report exists:**
  Verify file exists at <compound_path>/DIAGNOSTIC-REPORT.md.

  If DIAGNOSTIC-REPORT.md is missing:
  - Log: "Diagnostic specialist completed but DIAGNOSTIC-REPORT.md not found"
  - Fall back to basic advisory from the intervene step
  - Counter still increments (specialist delegation was attempted)
  - Proceed to respawn step with basic advisory

  **If report exists, extract from it:**

  1. **Root cause:** Locate "## Root Cause" section header. Extract "Primary:" line content. This is root_cause_primary.

  2. **Primary fix:** Locate "## Recommended Fixes" section header. Find the first subsection marked "(PRIMARY)". Extract "Action:" content (may include a code block with LSD commands). This is fix_action.

  3. **Verification:** From the same PRIMARY fix subsection, extract "Verification:" content. This is fix_verification.

  **Generate specialist-informed advisory:**
  ```
  Diagnostic specialist identified root cause: <root_cause_primary>

  See DIAGNOSTIC-REPORT.md for full analysis.

  Fix: <fix_action>
  <include LSD command examples from fix if present>

  Verification: <fix_verification>

  <optional pattern-specific guidance, e.g., for ELIM_THRASHING: "Do NOT add ELIM again until this fix is verified.">
  ```

  **Key difference from basic advisory:** Specialist-informed advisory is SPECIFIC (exact atom, exact command, exact verification) because the specialist ran systematic checks with quantitative evidence. Basic advisory is PROCEDURAL (check these conditions, verify these things).

  Proceed to respawn step with the specialist-informed advisory.
  </step>
  ```

  **Change 3: Update the process flow description**

  The orchestration loop now has these steps in order:
  1. parse_arguments
  2. validate_prerequisites
  3. check_dereplication_first
  4. spawn_case_agent
  5. monitor_progress
  6. detect_loops
  7. diagnose
  8. intervene
  9. track_and_decide (routes to: respawn at counter 0-1, delegate_specialist at counter 2, respawn at counter 3-9, escalate at counter 10+)
  10. delegate_specialist (NEW - only when counter == 2)
  11. extract_diagnostic_findings (NEW - after specialist completes)
  12. respawn (with basic OR specialist-informed advisory)
  13. escalate
  14. present_results

  Update the step ordering if there is an explicit step list or table of contents in case.md.

  **What NOT to change:**
  - Do NOT modify spawn_case_agent, monitor_progress, detect_loops, diagnose, intervene, escalate, respawn, or present_results steps — they are correct from Phase 29.
  - Do NOT modify the loop_detection_reference section — pattern definitions are correct.
  - Do NOT modify the anti_patterns section (but add one new anti-pattern, see below).

  **Add to anti_patterns section:**
  ```
  **Never delegate to specialist on first loop detection:**
  - First detection (counter = 0) -> basic diagnosis only
  - First recurrence (counter = 1) -> basic diagnosis again
  - Second recurrence (counter = 2) -> delegate to specialist
  - The specialist has 455 lines of deep diagnostic knowledge that is overkill for common issues (odd sp2, missing H) caught by basic diagnosis
  ```
  </action>
  <verify>
  - Run: `grep -c "delegate_specialist" ~/.claude/commands/lucy-ng/case.md` and confirm >= 3 occurrences (step name, reference from track_and_decide, step definition)
  - Run: `grep -c "extract_diagnostic_findings" ~/.claude/commands/lucy-ng/case.md` and confirm >= 2 occurrences
  - Run: `grep -c "lucy-diagnostic" ~/.claude/commands/lucy-ng/case.md` and confirm >= 1 occurrence (in Task agent_type)
  - Run: `grep -c "diagnostic_specialist_placeholder" ~/.claude/commands/lucy-ng/case.md` and confirm 0 occurrences (placeholder fully replaced)
  - Run: `grep -c "DIAGNOSTIC-REPORT.md" ~/.claude/commands/lucy-ng/case.md` and confirm >= 3 occurrences (existence check, read, advisory reference)
  - Run: `grep "counter.*2" ~/.claude/commands/lucy-ng/case.md` and confirm delegation threshold logic present
  - Run: `grep "counter.*3.*10" ~/.claude/commands/lucy-ng/case.md` or equivalent and confirm post-delegation advisory logic
  - Verify case.md still has all original steps (parse_arguments through present_results) plus the 2 new steps
  - Run: `wc -l ~/.claude/commands/lucy-ng/case.md` and confirm approximately 720-780 lines (was 622, adding ~100-150 lines for 2 new steps + expanded track_and_decide)
  </verify>
  <done>
  case.md orchestrator has working diagnostic specialist delegation: track_and_decide routes to delegate_specialist at counter == 2, delegate_specialist spawns lucy-diagnostic agent via Task tool, extract_diagnostic_findings reads DIAGNOSTIC-REPORT.md and generates specialist-informed advisory, falls back to basic advisory if report missing. Placeholder section fully replaced. All Phase 29 steps unchanged. Counter increments after delegation (counts as 1 intervention cycle).
  </done>
</task>

</tasks>

<verification>
Phase-level verification checks:

1. **DIAG-06 (Agent relocate and rename):**
   - `ls ~/.claude/agents/lucy-diagnostic.md` exists at user-global location
   - `ls /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/diagnostic-specialist.md` does NOT exist (deleted from project-local)
   - `grep "name: lucy-diagnostic" ~/.claude/agents/lucy-diagnostic.md` matches
   - `grep "model: inherit" ~/.claude/agents/lucy-diagnostic.md` matches
   - Both lucy-diagnostic.md and lucy-case-agent.md are in ~/.claude/agents/ (consistent)

2. **DIAG-07 (Delegation trigger):**
   - `grep "counter.*== 2.*delegate" ~/.claude/commands/lucy-ng/case.md` matches (delegation at threshold 2)
   - `grep "delegate_specialist" ~/.claude/commands/lucy-ng/case.md` matches (step exists)
   - `grep "lucy-diagnostic" ~/.claude/commands/lucy-ng/case.md` matches (correct agent name in Task call)

3. **DIAG-08 (Report parsing and advisory):**
   - `grep "extract_diagnostic_findings" ~/.claude/commands/lucy-ng/case.md` matches (extraction step exists)
   - `grep "DIAGNOSTIC-REPORT.md" ~/.claude/commands/lucy-ng/case.md` matches (report read)
   - `grep "Root Cause" ~/.claude/commands/lucy-ng/case.md` matches (extraction targets)
   - `grep "Recommended Fixes" ~/.claude/commands/lucy-ng/case.md` matches (extraction targets)
   - `grep "specialist-informed advisory" ~/.claude/commands/lucy-ng/case.md` matches (advisory generation)

4. **Cross-file consistency:**
   - Agent name in ~/.claude/agents/lucy-diagnostic.md frontmatter == agent_type in case.md Task call == "lucy-diagnostic"
   - DIAGNOSTIC-REPORT.md format expected by case.md extraction matches template in lucy-diagnostic.md Step 5
   - Both agent files (lucy-case-agent.md, lucy-diagnostic.md) live in ~/.claude/agents/ per Phase 28 decision
</verification>

<success_criteria>
- Agent definition at ~/.claude/agents/lucy-diagnostic.md (user-global) with name: lucy-diagnostic, model: inherit, zero "supervisor" references
- Old project-local .claude/agents/diagnostic-specialist.md deleted
- Agent location consistent with lucy-case-agent.md (both in ~/.claude/agents/)
- case.md orchestrator track_and_decide routes to specialist at counter == 2
- case.md has delegate_specialist step that spawns lucy-diagnostic via Task tool
- case.md has extract_diagnostic_findings step that reads DIAGNOSTIC-REPORT.md, extracts root cause + primary fix
- case.md generates specialist-informed advisory (specific, not procedural) for respawn
- case.md falls back to basic advisory if DIAGNOSTIC-REPORT.md missing
- case.md counter increments after delegation (counts as 1 cycle)
- diagnostic_specialist_placeholder section fully replaced (no remnants)
- All Phase 29 steps (spawn, monitor, detect, diagnose, intervene, escalate, respawn, present) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/30-diagnostic-specialist-integration/30-01-SUMMARY.md`
</output>
