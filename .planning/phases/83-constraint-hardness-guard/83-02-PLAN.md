---
phase: 83-constraint-hardness-guard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lucy_ng/detection/models.py
  - src/lucy_ng/cli/detect.py
  - tests/test_detection_neighbours.py
autonomous: true
requirements:
  - FIX-10

must_haves:
  truths:
    - "NeighbourResult model carries constraint_type and dominant_element fields that agents can read without manual frequency comparison"
    - "NeighbourResult carries an advisory field and advisory_note self-labeling the output as a statistical prior"
    - "lucy detect neighbours --format json output includes advisory: true, advisory_note, and dominant_element"
    - "lucy detect neighbours text output shows [ADVISORY] prefix and the dominant element prominently"
    - "Regression tests assert advisory label, dominant_element, and the CASE9-truth-not-excluded property"
    - "pytest + ruff + mypy stay green"
  artifacts:
    - path: "src/lucy_ng/detection/models.py"
      provides: "advisory field, advisory_note, dominant_element on NeighbourResult"
      contains: "advisory"
    - path: "src/lucy_ng/cli/detect.py"
      provides: "advisory note and dominant element in text output"
      contains: "advisory"
    - path: "tests/test_detection_neighbours.py"
      provides: "regression tests: advisory label, dominant_element, CASE9-truth-not-excluded"
      contains: "advisory"
  key_links:
    - from: "NeighbourResult.advisory + dominant_element"
      to: "lucy detect neighbours --format json"
      via: "model_dump_json()"
      pattern: "advisory.*true"
    - from: "FIX-10-conformant constraint set (no PROP from typical/carbon-dominant)"
      to: "CASE9 truth SMILES CC(O)c1ccc(cc1)C(=O)OC(C)C"
      via: "RDKit structural assertion in test docstring"
      pattern: "CC.O.c1ccc"
---

<objective>
Add `advisory`, `advisory_note`, and `dominant_element` fields to `NeighbourResult` so that every `lucy detect neighbours` output — in both JSON and text format — self-labels as a statistical prior and surfaces the dominant neighbour explicitly. Add regression tests asserting the advisory label, dominant_element, and the CASE9-truth-not-excluded property (Fix d, FIX-10).

Purpose: The advisory label and dominant_element field make the constraint encoded in Plan 01's skill edits machine-verifiable: agents consuming JSON output see `"advisory": true` and `"dominant_element": "carbon"` directly in the output object. When carbon is dominant, the text output says so prominently — making it harder to silently treat a 'typical' heteroatom value as a hard constraint.

Output: Two modified source files and one updated test file. All three quality gates (pytest, ruff, mypy) pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Phase-83 spec (authoritative): .planning/ROADMAP.md §Phase 83, success criterion 4.

EMPIRICAL GROUNDING FOR PLAN AUTHOR (do NOT embed in test docstrings or code comments beyond generic language):
CASE9 truth: CC(O)c1ccc(cc1)C(=O)OC(C)C, C12H16O3, DBE 5, 6 aromatic ring-C, 0 ring-oxygen.
At the defect shift (~150 ppm class, aromatic C bearing a carbon substituent), the real detect_neighbours output has carbon as dominant (~0.95, constraint_type "mandatory") and oxygen as "typical" (~0.55). A correct dominant_element field would show "carbon", making the advisory nature unambiguous to both agents and the G-PROP-EVIDENCE gate. The test verifies this pattern generically.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add advisory, advisory_note, and dominant_element fields to NeighbourResult model; surface in CLI text output</name>
  <files>src/lucy_ng/detection/models.py, src/lucy_ng/cli/detect.py</files>

  <read_first>
    Read `src/lucy_ng/detection/models.py` fully before editing. Focus on:
    - `NeighbourDistribution` (lines ~139-216): has fields carbon, oxygen, nitrogen, sulfur, halogen (all float); has `get_constraints()`, `forbidden_elements`, `mandatory_elements` properties.
    - `NeighbourResult` (lines ~219-298): Pydantic v2 BaseModel with fields shift_ppm, window_ppm, radius, forbidden_threshold, mandatory_threshold, distribution, constraints, total_observations, unique_hose_codes, has_data, warning. The `summary()` method builds `lines = []` then appends items.

    Read `src/lucy_ng/cli/detect.py` lines 97-197: the neighbours_command function. JSON path: `result.model_dump_json(indent=2)` at ~line 191; text path: `result.summary()` at ~line 193.
  </read_first>

  <behavior>
    - Test 1: `NeighbourResult` has `advisory: bool` field defaulting to `True`
    - Test 2: `NeighbourResult` has `advisory_note: str` field with default containing "statistical prior"
    - Test 3: `NeighbourResult` has `dominant_element: str | None` field; when distribution has a highest-frequency element, it is populated; when no data, it is None
    - Test 4: `result.model_dump_json()` includes `"advisory": true`, `"advisory_note"`, and `"dominant_element"` in serialized output
    - Test 5: `result.summary()` first line is "[ADVISORY] ..." containing the advisory note
    - Test 6: when dominant_element is "carbon", summary() includes a line like "Dominant neighbour: carbon — likely carbon-substituted; heteroatom placement uncertain"
    - Test 7: existing NeighbourResult construction (no advisory/dominant_element args) still works (all new fields have defaults)
  </behavior>

  <action>
    EDIT A — `src/lucy_ng/detection/models.py`, add a `dominant_element` property to `NeighbourDistribution`:

    Add a property `dominant_element` that returns the element name with the highest frequency, or None if all frequencies are zero. Check the five fields: carbon, oxygen, nitrogen, sulfur, halogen. Return the string name of the highest (e.g., "carbon", "oxygen"). If all zero: return None.

    EDIT B — `src/lucy_ng/detection/models.py` import line: change `from pydantic import BaseModel` (line 7) to `from pydantic import BaseModel, computed_field`. This is REQUIRED for EDIT C's computed field; without it ruff/mypy fail with NameError.

    EDIT C — `src/lucy_ng/detection/models.py`, class `NeighbourResult`:

    Add the `advisory` and `advisory_note` PLAIN fields immediately after the `warning: str | None = None` field (both have defaults, so existing construction sites are unaffected):

    ```
    advisory: bool = True
    advisory_note: str = (
        "statistical prior — do not encode as a hard LSD constraint "
        "without direct connectivity evidence (HMBC/HSQC/exchangeable-H) "
        "or convergent multi-source corroboration. See FIX-10."
    )
    ```

    Do NOT add `dominant_element` as a plain field. Instead add it as a `@computed_field` that delegates to the distribution (stays in sync automatically, no change to detector.py, no manual field-setting at construction). Add this method to the `NeighbourResult` class body:

    ```python
    @computed_field  # type: ignore[misc]
    @property
    def dominant_element(self) -> str | None:
        return self.distribution.dominant_element
    ```

    EDIT D — In `NeighbourResult.summary()`, make two additions:

    (1) Insert `lines.insert(0, f"[ADVISORY] {self.advisory_note}")` immediately after `lines = []` (before the header append). This puts the advisory as the very first output line.

    (2) In `NeighbourResult.summary()`, AFTER the Mandatory-elements block (the lines that append `f"Mandatory: ..."`) and BEFORE the data-coverage line, add the dominant-neighbour advisory. There is NO pre-existing "Dominant:" line in `NeighbourResult.summary()` — do not look for one; use the Mandatory-block / data-coverage anchors. Logic:
    If `self.dominant_element == "carbon"`:
      append `"Dominant neighbour: carbon — likely carbon-substituted; heteroatom placement uncertain at this shift."`
    Else if `self.dominant_element` is not None:
      append `f"Dominant neighbour: {self.dominant_element}"`

    EDIT E — `src/lucy_ng/cli/detect.py`, `neighbours_command` docstring: append a new paragraph inside the existing triple-quoted docstring after the "Examples:" block:

    "Output is a STATISTICAL PRIOR (advisory). Do not encode as a hard LSD constraint without direct connectivity evidence or convergent corroboration. Check dominant_element in JSON output: if 'carbon', heteroatom placement is uncertain. See FIX-10."
  </action>

  <verify>
    <automated>
      cd /Users/steinbeck/Dropbox/develop/lucy-ng && python -c "
from lucy_ng.detection.models import NeighbourResult, NeighbourDistribution
dist = NeighbourDistribution(carbon=0.95, oxygen=0.55)
r = NeighbourResult(shift_ppm=150.8, window_ppm=2.0, radius=3, distribution=dist, has_data=True)
assert r.advisory == True
assert 'statistical prior' in r.advisory_note
assert r.dominant_element == 'carbon'
import json; d = json.loads(r.model_dump_json())
assert d['advisory'] == True
assert d['dominant_element'] == 'carbon'
s = r.summary()
assert s.startswith('[ADVISORY]'), f'Got: {s[:50]}'
assert 'carbon' in s.lower()
print('All field checks PASSED')
"
    </automated>

    Also run:
    pytest tests/test_detection_neighbours.py -x -q 2>&1 | tail -5
    ruff check src/lucy_ng/detection/models.py src/lucy_ng/cli/detect.py 2>&1
    mypy src/lucy_ng/detection/models.py 2>&1 | tail -5
  </verify>

  <acceptance_criteria>
    - `NeighbourDistribution` has `dominant_element` property returning highest-frequency element name or None
    - `NeighbourResult` has `advisory: bool = True`, `advisory_note: str` (contains "statistical prior"), `dominant_element` computed from distribution
    - `r.model_dump_json()` includes `"advisory": true`, `"dominant_element": "carbon"` (for carbon-dominant distribution)
    - `r.summary()` starts with "[ADVISORY]" line
    - When dominant_element == "carbon", summary includes "carbon-substituted" or equivalent advisory line
    - All existing `test_detection_neighbours.py` tests still pass
    - `ruff check src/lucy_ng/detection/models.py src/lucy_ng/cli/detect.py` exits 0
    - `mypy src/lucy_ng/detection/models.py` exits 0 (or no new errors beyond baseline)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression tests — advisory label, dominant_element, and CASE9-truth-not-excluded check</name>
  <files>tests/test_detection_neighbours.py</files>

  <read_first>
    Read `tests/test_detection_neighbours.py` fully before editing. Note the existing test patterns (pytest fixtures, `test_db` fixture with sample HOSE data, CliRunner for CLI tests, StatisticalDetector for unit tests). Add new tests at the END of the file only — do NOT modify any existing tests.

    ROADMAP §Phase 83 success criterion 4: "A documented check (test or re-derivation) shows that, with heteroatom placement left open per the above, the CASE9 truth is NOT excluded by FIX-10-conformant constraints (it can appear in the LSD search space)."

    The truth has 0 ring-oxygen; the ~150 ppm class aromatic C bears a carbon substituent. Real detect_neighbours at that class of shift: carbon is dominant (~0.95, mandatory), oxygen is typical (~0.55). A FIX-10-conformant interpretation leaves heteroatom placement open: no PROP, no hard BOND at that carbon. The test verifies: (a) the advisory infrastructure is present, (b) carbon-dominant distribution produces dominant_element="carbon", (c) truth SMILES is structurally consistent with a carbon-substituent ring carbon (RDKit, already a project dependency).
  </read_first>

  <behavior>
    - Test A: `NeighbourResult` has `advisory=True` by default (unit test, no DB needed)
    - Test B: `NeighbourResult.summary()` starts with "[ADVISORY]" prefix
    - Test C: `NeighbourDistribution` with carbon as highest frequency yields `dominant_element="carbon"`
    - Test D: `NeighbourDistribution` with oxygen as highest frequency yields `dominant_element="oxygen"`
    - Test E: `lucy detect neighbours --format json` CLI output (CliRunner) includes `"advisory": true` and `"dominant_element"` key
    - Test F (CASE9-truth-not-excluded): RDKit structural assertion — the truth SMILES has the expected properties (formula C12H16O3, 0 ring-O, at least one aromatic C bearing a carbon substituent), confirming that leaving the corresponding aromatic C unconstrained (no PROP/BOND) does not exclude the truth from the LSD search space
  </behavior>

  <action>
    Add six new test functions to the END of `tests/test_detection_neighbours.py`. Do NOT modify any existing tests. Add `from lucy_ng.detection.models import NeighbourResult` to the imports if not already present (check the existing import block first).

    TEST A — `test_neighbour_result_advisory_default`:
    Create `NeighbourResult(shift_ppm=100.0, window_ppm=2.0, radius=3, distribution=NeighbourDistribution())`.
    Assert `result.advisory is True`.
    Assert `"statistical prior" in result.advisory_note`.
    Assert `"FIX-10" in result.advisory_note`.

    TEST B — `test_neighbour_result_summary_advisory_prefix`:
    Same minimal result. Call `result.summary()`. Assert it starts with "[ADVISORY]".
    Assert "statistical prior" appears somewhere in the string.

    TEST C — `test_dominant_element_carbon`:
    Create `NeighbourDistribution(carbon=0.95, oxygen=0.55, nitrogen=0.32)`.
    Assert `dist.dominant_element == "carbon"`.

    TEST D — `test_dominant_element_oxygen`:
    Create `NeighbourDistribution(carbon=0.30, oxygen=0.97)`.
    Assert `dist.dominant_element == "oxygen"`.

    TEST E — `test_detect_neighbours_json_advisory_and_dominant_element`:
    Use CliRunner with the `test_db` fixture. Invoke `cli` with `["detect", "neighbours", "170.5", "--db", str(test_db), "--format", "json"]`. Assert exit code 0. Parse JSON. Assert `parsed["advisory"] is True`. Assert `"dominant_element" in parsed`. Assert `"advisory_note" in parsed`. Assert `"statistical prior" in parsed["advisory_note"]`.

    TEST F — `test_case9_truth_not_excluded_by_fix10_conformant_constraints`:
    Docstring: "FIX-10 regression: verifies that the CASE9 truth compound (C12H16O3, 0 ring-O) is structurally consistent with a FIX-10-conformant constraint set. In the truth, the ~150 ppm class aromatic C bears a carbon substituent (not ring-O). detect_neighbours at this shift class has carbon as dominant neighbour, making it a 'typical'-O / carbon-dominant pattern. A FIX-10-conformant set leaves this heteroatom placement open (no PROP, no hard BOND). This test verifies the truth structure would appear in the LSD search space: (1) it has the expected formula, (2) it has 0 ring-O, (3) it has at least one aromatic C with a non-ring aliphatic C substituent (the benzylic carbon bearing an OH group)."

    Implementation:
    - Import: `from rdkit import Chem` and `from rdkit.Chem import rdMolDescriptors`
    - truth_smiles = "CC(O)c1ccc(cc1)C(=O)OC(C)C"
    - mol = Chem.MolFromSmiles(truth_smiles)
    - assert mol is not None, "truth SMILES must parse"
    - assert rdMolDescriptors.CalcMolFormula(mol) == "C12H16O3"
    - Verify 0 ring-oxygen — i.e. no oxygen atom is a RING MEMBER (this is the phase-goal property; it correctly distinguishes a benzene ring from furan/pyran, and does NOT confuse an O substituent bonded to a ring carbon with a ring-O). Do NOT use a "no O bonded to an aromatic C" check — that would wrongly reject a legitimate phenol/aryl-ether:
      ring_O_count = sum(1 for a in mol.GetAtoms() if a.GetIsAromatic() and a.GetAtomicNum() == 8)
      assert ring_O_count == 0, f"Expected 0 ring-oxygen atoms, found: {ring_O_count}"
    - Verify at least one aromatic C with non-ring aliphatic C substituent:
      aromatic_with_C_substituent = [
          a for a in mol.GetAtoms()
          if a.GetIsAromatic() and a.GetAtomicNum() == 6
          and any(
              n.GetAtomicNum() == 6 and not n.GetIsAromatic()
              for n in a.GetNeighbors()
          )
      ]
      assert len(aromatic_with_C_substituent) >= 1
    - Final assert comment: "Truth has 0 ring-O and at least one aromatic C with aliphatic C substituent. A FIX-10-conformant constraint set (no hard PROP/BOND from typical/carbon-dominant detect-neighbours) does not exclude this structure."

    After writing tests, verify imports are correct by checking the top of the file for `NeighbourDistribution` (already imported) and `NeighbourResult` (add if missing).
  </action>

  <verify>
    <automated>
      cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest tests/test_detection_neighbours.py -x -q 2>&1 | tail -10
    </automated>
    Expected: 0 failures, all new tests collected and passing.

    Full suite:
    pytest -x -q 2>&1 | tail -5
    ruff check src tests 2>&1 | head -10
    mypy src/lucy_ng 2>&1 | tail -5
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_detection_neighbours.py -x` exits 0 with all 6 new tests passing
    - Test A: advisory=True, "statistical prior" and "FIX-10" in advisory_note
    - Test B: summary starts with "[ADVISORY]"
    - Test C: NeighbourDistribution(carbon=0.95, oxygen=0.55).dominant_element == "carbon"
    - Test D: NeighbourDistribution(carbon=0.30, oxygen=0.97).dominant_element == "oxygen"
    - Test E: CLI JSON output has advisory=true and dominant_element key
    - Test F: truth parses, formula=C12H16O3, 0 ring-oxygen atoms, >= 1 aromatic C with aliphatic C substituent
    - `pytest` (full suite) exits 0 — no regressions
    - `ruff check src tests` exits 0
    - `mypy src/lucy_ng` exits 0 (or no new errors beyond pre-existing baseline)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI output -> agent JSON consumption | advisory and dominant_element fields are additive; no existing field semantics changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03 | Tampering | NeighbourResult/NeighbourDistribution model additions | accept | Additive Pydantic fields with defaults; existing callers unaffected; covered by test suite |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No new packages — rdkit already a project dependency |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/steinbeck/Dropbox/develop/lucy-ng

# Model field checks
python -c "
from lucy_ng.detection.models import NeighbourResult, NeighbourDistribution
import json

# Carbon-dominant distribution
dist = NeighbourDistribution(carbon=0.95, oxygen=0.55)
r = NeighbourResult(shift_ppm=100.0, window_ppm=2.0, radius=3, distribution=dist)
assert r.advisory is True
assert 'statistical prior' in r.advisory_note
assert r.dominant_element == 'carbon'
d = json.loads(r.model_dump_json())
assert d['advisory'] == True
assert d['dominant_element'] == 'carbon'
s = r.summary()
assert s.startswith('[ADVISORY]')
assert 'carbon' in s.lower()

# Oxygen-dominant distribution
dist2 = NeighbourDistribution(carbon=0.3, oxygen=0.97)
r2 = NeighbourResult(shift_ppm=170.0, window_ppm=2.0, radius=3, distribution=dist2)
assert r2.dominant_element == 'oxygen'

print('All checks PASSED')
"

# Test suite
pytest tests/test_detection_neighbours.py -q 2>&1 | tail -5
pytest -x -q 2>&1 | tail -5
ruff check src tests 2>&1 | head -10
mypy src/lucy_ng 2>&1 | tail -3
```
</verification>

<success_criteria>
1. `NeighbourDistribution` has `dominant_element` property returning the name of the highest-frequency element or None if all zero.
2. `NeighbourResult` has `advisory: bool = True`, `advisory_note: str` (contains "statistical prior" and "FIX-10"), `dominant_element` computed from distribution.
3. `r.summary()` first line is "[ADVISORY] ..."; when dominant_element == "carbon", summary includes "carbon-substituted" or equivalent advisory line.
4. `result.model_dump_json()` serializes `advisory: true` and `dominant_element`.
5. Six new tests in `test_detection_neighbours.py` all pass: advisory-default, summary-prefix, carbon-dominant, oxygen-dominant, CLI-JSON-fields, CASE9-truth-not-excluded.
6. CASE9-truth-not-excluded test uses RDKit to confirm: truth parses, formula=C12H16O3, 0 ring-oxygen atoms, >= 1 aromatic C with aliphatic C substituent.
7. Full `pytest` suite green. `ruff check src tests` green. `mypy src/lucy_ng` green (no new errors).
</success_criteria>

<output>
Create `.planning/phases/83-constraint-hardness-guard/83-02-SUMMARY.md` when done.
</output>
