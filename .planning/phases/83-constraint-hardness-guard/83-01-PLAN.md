---
phase: 83-constraint-hardness-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/lucy-nmr-chemist.md
  - .claude/agents/lucy-lsd-engineer.md
  - .claude/agents/lucy-devils-advocate.md
autonomous: true
requirements:
  - FIX-10

must_haves:
  truths:
    - "nmr-chemist reads the FULL detect-neighbours distribution (dominant neighbour matters, not just heteroatom frequency cherry-picked), knows constraint_type typical vs mandatory, and never hardens from a single typical value"
    - "nmr-chemist states the 145-160 ppm aromatic C ambiguity and re-asserts Pitfall 6"
    - "lsd-engineer forbids hard heteroatom PROP/BOND from a single detect-neighbours probability alone; permits only direct evidence OR convergent multi-source corroboration (the carbonyl exception)"
    - "devils-advocate G-PROP-EVIDENCE gate BLOCKS when a hard heteroatom PROP/BOND cites only a single detect-neighbours value without direct evidence or convergent corroboration — especially if that value is typical/borderline or carbon was the dominant neighbour"
    - "All three agent files remain compound-agnostic: no CASE9-specific shifts, formulas, or test-compound names anywhere in the edited .md files"
  artifacts:
    - path: ".claude/agents/lucy-nmr-chemist.md"
      provides: "FIX-10 full-distribution advisory rule in Pitfall 6 + Section 5 interpretation"
      contains: "statistical prior"
    - path: ".claude/agents/lucy-lsd-engineer.md"
      provides: "FIX-10 convergent-evidence rule + carbonyl exception"
      contains: "G-PROP-EVIDENCE"
    - path: ".claude/agents/lucy-devils-advocate.md"
      provides: "FIX-10 G-PROP-EVIDENCE gate"
      contains: "G-PROP-EVIDENCE"
  key_links:
    - from: "detect-neighbours full distribution output"
      to: "nmr-chemist interpretation rule"
      via: "constraint_type field + dominant element check"
      pattern: "statistical prior.*advisory"
    - from: "lsd-engineer PROP/BOND write path"
      to: "devils-advocate G-PROP-EVIDENCE gate"
      via: "LSD file validation"
      pattern: "G-PROP-EVIDENCE"
---

<objective>
Encode three skill-level fixes that prevent uncertain heteroatom inferences from becoming hard, solution-excluding LSD constraints (FIX-10).

Purpose: The CASE9 defect arose because the nmr-chemist cherry-picked an O frequency from `detect neighbours`, renormalized it (fabricating an inflated probability), and emitted a hard `PROP` constraint. The underlying value was a "typical" constraint_type (not "mandatory"), and the dominant neighbour at that shift was carbon. A correct reading of the full distribution would have flagged this as advisory-only. All three skill files need coordinated changes so the full-distribution advisory interpretation is enforced at every stage — and so no agent file contains compound-specific test values that would compromise blind UAT.

Output: Three edited agent .md files with grep-verifiable rule strings present and zero compound-specific leak content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

CRITICAL NOTE ON FILE LOCATIONS: Agent files live in the REPO at `.claude/agents/lucy-*.md` and are symlinked into `~/.claude`. Edit the REPO copies. They are git-tracked.

Phase-83 spec (authoritative): .planning/ROADMAP.md §Phase 83 (lines 539-561)

EMPIRICAL CONTEXT FOR PLAN AUTHOR ONLY (do NOT embed in agent files):
Real `detect neighbours` empirical findings motivate these rules. They must NOT appear in agent .md files.
The defect pattern: agent cherry-picked one heteroatom frequency, renormalized it by excluding other elements, inflated a "typical" (~0.55) value to claim ~81%, and emitted a hard PROP. The dominant neighbour at that shift was carbon (~0.95, mandatory), consistent with the truth (aromatic C bearing a carbon substituent). For a carbonyl shift (~166 ppm), oxygen was ~0.90 ("typical"), making BOND C=O correct only from convergent evidence (Cq + shift region + carbonyl chemistry), not from the single 0.90 value alone. At ~170 ppm, oxygen reaches 0.965 ("mandatory") — the paradigm case.
These motivate the generic thresholds in the rules below. Use only generic language in agent .md files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: nmr-chemist — full-distribution advisory rule + 145-160 ppm ambiguity + Pitfall 6 re-assertion</name>
  <files>.claude/agents/lucy-nmr-chemist.md</files>

  <read_first>
    Read `.claude/agents/lucy-nmr-chemist.md` fully before editing. Key sections to modify:

    - Section "2. Critical Pitfalls" — Pitfall 6 (currently at line ~57: "Pitfall 6 — Heteroatom constraints (CRITICAL): Express what you KNOW...") — expand with the full-distribution advisory rule and renormalization prohibition.
    - Section "5. Statistical Detection Protocol" — subsection "Interpretation Rules" (currently at line ~154: "Neighbours: Mandatory (>95%) -> report for PROP constraint...") — replace with the corrected 8-step reading protocol.

    ROADMAP §Phase 83 success criterion 1 (refined): The agent must read the FULL distribution (dominant neighbour, constraint_type, all frequencies), treat any single detect-neighbours probability as advisory/statistical prior only, and know the 145-160 ppm aromatic C ambiguity. The old "Mandatory -> PROP" shortcut is the defect.

    COMPOUND-AGNOSTIC RULE: Do NOT embed any specific ppm values, compound names, formulas, or CASE-run identifiers in the agent .md. Use generic language: "typical (~0.5-0.9)", "mandatory (>=0.95)", "borderline heteroatom probability", "dominant neighbour".
  </read_first>

  <action>
    Make two targeted edits to `.claude/agents/lucy-nmr-chemist.md`. Write the file back in its entirety after the replacements. Do NOT alter any content outside these two target blocks.

    EDIT A — Pitfall 6. Replace the existing Pitfall 6 block entirely with the following (verbatim, compound-agnostic):

    "**Pitfall 6 — Heteroatom constraints (CRITICAL — re-asserted FIX-10):** `detect neighbours` output is a STATISTICAL PRIOR / advisory signal — never the sole basis for a hard PROP or BOND heteroatom constraint. Read the FULL distribution, not just one element's frequency:

    **How to read the output correctly:**
    - Check `constraint_type` for each element: 'mandatory' (>=0.95 frequency) vs 'typical' (~0.5-0.9) vs 'forbidden' (<0.01).
    - Identify the DOMINANT neighbour (highest frequency). If carbon is dominant, the shift is more consistent with a carbon substituent than a heteroatom substituent — even if a heteroatom also shows 'typical' frequency.
    - A single 'typical' or borderline heteroatom probability (~0.5-0.9) never hardens. 'Typical' means the heteroatom appears sometimes in the database, not that it is required for this structure.
    - NEVER renormalize or inflate a frequency by excluding other elements. Use raw tool output values as-is. Inflating a typical value by renormalization (e.g., 'after N-exclusion') is fabrication and is forbidden.

    **145-160 ppm aromatic C ambiguity (CRITICAL):** A carbon in this shift range is ambiguous between (a) ring-O-substituted (phenol/aryl-ether) and (b) an aromatic C bearing an electron-donating or benzylic substituent with NO ring-oxygen — especially when an sp3 C-O signal at ~65-75 ppm could be a benzylic carbinol (CH(OH)CH3 or similar, directly attached to the ring carbon). Detection O-frequency is elevated for both cases; it cannot distinguish them. If carbon is the dominant neighbour at this range: do NOT emit PROP or BOND for the heteroatom.

    **Carbonyl exception (the ONLY safe hard heteroatom constraint that may draw on detection):** For a Cq in the 160-220 ppm range where (i) DEPT/HSQC confirms no attached H (Cq), (ii) shift is in the C=O region, and (iii) the carbonyl carbon's chemistry is unambiguous (ester/acid/amide context from formula and HMBC), convergent multi-source evidence supports BOND C=O. This is a multi-source inference, NOT a single detect-neighbours probability.

    **Rule:** For all other heteroatom placements: express what you KNOW from direct evidence (HMBC correlation to a heteroatom-bearing carbon, exchangeable H confirming OH/NH), not what detection suggests. Leave uncertain placement OPEN. Let LSD explore acid vs ester vs ether vs carbon substituent. Ranking (13C prediction) decides. This is 'don't over-constrain heteroatoms; let ranking decide.'"

    EDIT B — Section 5 Interpretation Rules, Neighbours line. Replace the existing Neighbours interpretation line (the one beginning "**Neighbours:** Mandatory (>95%) -> report for PROP constraint...") with the following 8-step protocol:

    "**Neighbours:** Output is a STATISTICAL PRIOR — advisory only. Protocol:
    1. Read the FULL distribution. Identify the dominant element (highest frequency) first.
    2. For each element, note its `constraint_type`: 'mandatory' (>=0.95), 'typical' (~0.5-0.9), 'forbidden' (<0.01).
    3. A 'typical' heteroatom value, alone, warrants only an advisory observation in [SETUP-COMPLETE] — never a hard PROP or BOND constraint.
    4. If carbon is the dominant element, report the shift as 'likely C-substituted' in [SETUP-COMPLETE]; do NOT propose a heteroatom constraint.
    5. For carbonyls (Cq 160-220 ppm, see Pitfall 6 carbonyl exception): convergent multi-source evidence supports BOND C=O — document all converging sources explicitly.
    6. 'Forbidden' (<1%) -> report as advisory exclusion only; applies only if formula has the element.
    7. CRITICAL: check formula first — ignore any 'mandatory' reading if formula has no such element.
    8. NEVER renormalize or reinterpret raw frequencies. Report them as-is from the tool output."
  </action>

  <verify>
    <automated>
      grep -c "statistical prior" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-nmr-chemist.md
    </automated>
    Expected: >= 2 (one in Pitfall 6, one in the Neighbours protocol header).

    Additional checks (run from /Users/steinbeck/Dropbox/develop/lucy-ng):
    grep -c "dominant" .claude/agents/lucy-nmr-chemist.md        # >= 2
    grep -c "FIX-10" .claude/agents/lucy-nmr-chemist.md          # >= 1
    grep -c "145-160 ppm" .claude/agents/lucy-nmr-chemist.md     # >= 1
    grep -c "renormali" .claude/agents/lucy-nmr-chemist.md       # >= 1

    Compound-agnostic guard (must all return 0):
    grep -c "150\.8\|166\.08\|C12H16O3\|isopropyl 4" .claude/agents/lucy-nmr-chemist.md
  </verify>

  <acceptance_criteria>
    - `grep -c "statistical prior" .claude/agents/lucy-nmr-chemist.md` >= 2
    - `grep -c "dominant" .claude/agents/lucy-nmr-chemist.md` >= 2
    - `grep -c "FIX-10" .claude/agents/lucy-nmr-chemist.md` >= 1
    - `grep -c "145-160 ppm" .claude/agents/lucy-nmr-chemist.md` >= 1
    - `grep -c "renormali" .claude/agents/lucy-nmr-chemist.md` >= 1
    - `grep -c "150\.8\|C12H16O3\|isopropyl 4" .claude/agents/lucy-nmr-chemist.md` == 0
    - File is valid markdown, YAML front-matter intact
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: lsd-engineer — convergent-evidence rule + carbonyl exception; forbid single-stat hardening</name>
  <files>.claude/agents/lucy-lsd-engineer.md</files>

  <read_first>
    Read `.claude/agents/lucy-lsd-engineer.md` fully before editing. Key locations:

    - Section "1. LSD Command Reference" — subsection "Bond and Property Constraints" (currently at line ~66, after the PROP/BOND examples) — add the FIX-10 PROHIBITION block here.
    - Section "1. LSD Command Reference" — "Manual Checklist" item 6 (currently: "Heteroatom constraints: BOND for C=O only; LIST/ELEM/PROP for flexible connectivity") — update to the convergent-evidence rule.

    ROADMAP §Phase 83 success criterion 2 (refined): The rule must be: direct evidence OR convergent multi-source corroboration (the carbonyl exception). A single detect-neighbours probability — even a high "typical" value (~0.9) — never suffices alone. Carbon-dominant shifts must not receive a heteroatom constraint. Renormalization is forbidden. Compound-agnostic language only.
  </read_first>

  <action>
    Make two targeted edits to `.claude/agents/lucy-lsd-engineer.md`. Write the file back in its entirety after the replacements.

    EDIT A — Bond and Property Constraints subsection. After the existing PROP example lines (ending with "PROP L1 1 L2 ; each atom in L1 has at least 1 neighbor from L2"), add a new block immediately after:

    "**FIX-10 PROHIBITION — No hard heteroatom PROP/BOND from a single statistical detection value (G-PROP-EVIDENCE):**

    A hard heteroatom constraint (`PROP X O n`, `PROP X N n`, or any `BOND` to a heteroatom outside the carbonyl exception) requires ONE of:

    (i) **Direct connectivity evidence:** an HMBC correlation establishing adjacency to a heteroatom-bearing carbon, an HSQC absence confirming Cq combined with formula O/N count and shift range, or an exchangeable H peak confirming OH/NH.

    (ii) **Convergent multi-source corroboration (carbonyl exception only):** Cq confirmed by DEPT/HSQC + shift in 160-220 ppm + unambiguous C=O context (ester/acid/amide chemistry from formula) + O present in formula. This is the ONLY case where a detect-neighbours O value contributes to a hard BOND — because three independent sources converge. Emit `BOND X O` (C=O) in this case only.

    **Forbidden patterns (devils-advocate G-PROP-EVIDENCE gate will BLOCK):**
    - Citing a single detect-neighbours 'typical' probability (~0.5-0.9) as the sole basis for PROP or BOND.
    - Renormalizing or inflating a detect-neighbours frequency (excluding elements and recalculating to a higher percentage). Use raw tool output only.
    - Emitting a hard heteroatom constraint when carbon is the dominant neighbour at that shift (highest frequency in the detect-neighbours distribution).
    - Citing detect-neighbours alone for any shift in the 145-160 ppm aromatic range (ambiguous: ring-O vs benzylic/EDG substituent — see nmr-chemist Pitfall 6).

    **When uncertain:** leave heteroatom placement OPEN. Use LIST/ELEM/PROP only when direct HMBC connectivity is established. Let LSD explore alternatives; 13C ranking (lucy lsd rank) discriminates. An incorrect hard PROP is a solution-excluding constraint that ranking cannot recover from. (FIX-10)"

    EDIT B — Manual Checklist item 6. Replace current text "Heteroatom constraints: BOND for C=O only; LIST/ELEM/PROP for flexible connectivity" with:

    "Heteroatom constraints: BOND for C=O only when convergent evidence confirms it (Cq via DEPT/HSQC + 160-220 ppm shift + unambiguous C=O context + O in formula — three independent sources). For ALL other heteroatom placements: leave OPEN unless direct HMBC/HSQC/exchangeable-H evidence establishes connectivity. NEVER emit `PROP X O n` or hard heteroatom BOND from a single detect-neighbours value alone — especially a 'typical' (~0.5-0.9) value or when carbon is the dominant neighbour. Devils-advocate G-PROP-EVIDENCE gate will BLOCK. (FIX-10)"
  </action>

  <verify>
    <automated>
      grep -c "G-PROP-EVIDENCE" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-lsd-engineer.md
    </automated>
    Expected: >= 2

    Additional checks:
    grep -c "FIX-10" .claude/agents/lucy-lsd-engineer.md          # >= 2
    grep -c "convergent" .claude/agents/lucy-lsd-engineer.md      # >= 2
    grep -c "dominant" .claude/agents/lucy-lsd-engineer.md        # >= 1
    grep -c "renormali" .claude/agents/lucy-lsd-engineer.md       # >= 1

    Compound-agnostic guard (must all return 0):
    grep -c "150\.8\|166\.08\|C12H16O3\|isopropyl 4" .claude/agents/lucy-lsd-engineer.md
  </verify>

  <acceptance_criteria>
    - `grep -c "G-PROP-EVIDENCE" .claude/agents/lucy-lsd-engineer.md` >= 2
    - `grep -c "FIX-10" .claude/agents/lucy-lsd-engineer.md` >= 2
    - `grep -c "convergent" .claude/agents/lucy-lsd-engineer.md` >= 2
    - `grep -c "dominant" .claude/agents/lucy-lsd-engineer.md` >= 1
    - `grep -c "renormali" .claude/agents/lucy-lsd-engineer.md` >= 1
    - `grep -c "150\.8\|C12H16O3\|isopropyl 4" .claude/agents/lucy-lsd-engineer.md` == 0
    - File is valid markdown, YAML front-matter intact
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: devils-advocate — add G-PROP-EVIDENCE gate covering full-distribution, renormalization, and carbon-dominant checks</name>
  <files>.claude/agents/lucy-devils-advocate.md</files>

  <read_first>
    Read `.claude/agents/lucy-devils-advocate.md` fully before editing. Key locations:

    - Section "5. Inventory Validation Protocol" — Check 5 "Failure Mode Detection" — the G7 gate block and the "Summary of Check 5 gates" table. New gate is inserted after G7, added to the table.
    - Workflow section — step 8 ("Run bug checklist") — add sub-step 8a for G-PROP-EVIDENCE.

    Follow the existing G-ELIM-*/G7 gate format: named identifier, trigger, check procedure with bash, severity, rationale. The gate must cover three sub-checks: (a) typical/borderline constraint_type, (b) carbon is dominant neighbour, (c) renormalized/inflated probability. Compound-agnostic language only.
  </read_first>

  <action>
    Make three targeted edits to `.claude/agents/lucy-devils-advocate.md`. Write the file back in its entirety.

    EDIT A — Insert G-PROP-EVIDENCE gate definition. In Section 5, Check 5, add it immediately after the G7 block and before the "Summary of Check 5 gates" table:

    "---

    **G-PROP-EVIDENCE: Hard Heteroatom PROP/BOND Without Sufficient Evidence (CRITICAL)**

    *Trigger:* The LSD file contains a hard heteroatom constraint (`PROP X O`, `PROP X N`, or `BOND X O`/`BOND X N` outside the Cq 160-220 ppm carbonyl exception) AND the constraint inventory `applied_from_detection` entry for that constraint does NOT cite (i) direct connectivity evidence (HMBC/HSQC/exchangeable-H) or (ii) convergent multi-source corroboration (Cq + 160-220 ppm shift + C=O context + O in formula).

    **Check procedure:**

    Step 1 — Detect hard heteroatom constraints:
    ```bash
    grep -E "^PROP [0-9]+ (O|N)" analysis/iteration_NN/compound.lsd
    grep -E "^BOND [0-9]+ [0-9]+" analysis/iteration_NN/compound.lsd
    ```
    For BOND lines, cross-reference with MULT definitions to determine if any BOND atom is a heteroatom. If no hard heteroatom constraints found: gate passes.

    Step 2 — For each hard heteroatom constraint, check the constraint inventory `applied_from_detection` array (Section 5A). The constraint PASSES if its entry cites one of:
    - 'HMBC' — direct 2-3J correlation establishing adjacency to the heteroatom-bearing carbon
    - 'HSQC Cq' with '160-220 ppm' — confirming Cq in the carbonyl region (carbonyl exception; BOND C=O permitted)
    - 'exchangeable H' — OH or NH confirmed independently

    Step 3 — Sub-checks (each triggers CRITICAL independently):

    **(a) constraint_type is 'typical':** If the inventory entry cites a detect-neighbours value but the constraint_type for that element is 'typical' (~0.5-0.9, not 'mandatory' >=0.95): CRITICAL — "G-PROP-EVIDENCE: constraint_type 'typical' is advisory-only; a borderline heteroatom probability is never a sufficient basis for a hard constraint without converging direct evidence."

    **(b) Carbon is dominant neighbour:** If the inventory entry shows carbon as the highest-frequency element at the queried shift: CRITICAL — "G-PROP-EVIDENCE: carbon is the dominant neighbour at this shift; heteroatom placement is uncertain. A carbon-dominant shift must be left open unless direct connectivity evidence establishes the heteroatom."

    **(c) Renormalized probability:** If the constraint inventory records a heteroatom frequency that does not match the raw detect-neighbours output for that shift, or if any 'after X-exclusion' or recalculated percentage is cited: CRITICAL — "G-PROP-EVIDENCE: renormalized/inflated detect-neighbours probability detected. Only raw tool output values are permitted. Recalculating by excluding elements is fabrication."

    Step 4 — Without inventory (legacy fallback): check CASE-PROGRESS.md for documented direct-evidence or convergent-evidence citation. If absent: flag CRITICAL.

    CRITICAL message template:
    'G-PROP-EVIDENCE: Hard heteroatom constraint [{constraint}] lacks sufficient evidence. Permitted basis: direct HMBC/HSQC/exchangeable-H OR convergent corroboration (Cq + 160-220 ppm + C=O context). Found only: [{cited_evidence}]. Remove the constraint or supply qualifying evidence. (FIX-10)'

    *Severity:* CRITICAL — blocks solver run. An incorrect hard heteroatom PROP/BOND is solution-excluding; 13C ranking cannot recover from it.

    *Rationale:* The failure mode is: agent reads a borderline 'typical' heteroatom frequency, inflates it by renormalization, and emits a hard constraint — while carbon is actually the dominant neighbour, consistent with a carbon substituent at that shift. An open placement lets LSD generate all variants; ranking then discriminates correctly. (FIX-10)"

    EDIT B — Add to the gate summary table after the G7 row:

    "| G-PROP-EVIDENCE | Pre-run, all iterations | CRITICAL | Hard heteroatom PROP/BOND: cites only single detect-neighbours value without direct/convergent evidence; OR constraint_type is 'typical'; OR carbon is dominant neighbour; OR probability is renormalized |"

    EDIT C — Workflow. In the workflow section, after the existing step 8 (run bug checklist), add:

    "8a. Run G-PROP-EVIDENCE gate (Section 5, Check 5): for each hard heteroatom PROP/BOND in the LSD file, verify the constraint inventory cites direct connectivity evidence or convergent corroboration (Cq + 160-220 ppm + C=O context). Sub-check: flag CRITICAL if constraint_type is 'typical', if carbon is dominant neighbour, or if any renormalized/inflated probability is documented."
  </action>

  <verify>
    <automated>
      grep -c "G-PROP-EVIDENCE" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-devils-advocate.md
    </automated>
    Expected: >= 4 (gate definition header, sub-check body, table row, workflow step)

    Additional checks:
    grep -c "FIX-10" .claude/agents/lucy-devils-advocate.md             # >= 2
    grep -c "dominant" .claude/agents/lucy-devils-advocate.md           # >= 2
    grep -c "renormali" .claude/agents/lucy-devils-advocate.md          # >= 1
    grep -c "typical" .claude/agents/lucy-devils-advocate.md            # >= 2

    Table row presence (Warning 1 fix):
    grep -c "| G-PROP-EVIDENCE |" .claude/agents/lucy-devils-advocate.md   # >= 1

    Existing gates intact:
    grep -c "G-ELIM-1" .claude/agents/lucy-devils-advocate.md           # >= 1
    grep -c "G7" .claude/agents/lucy-devils-advocate.md                 # >= 1

    Compound-agnostic guard (must all return 0):
    grep -c "150\.8\|166\.08\|C12H16O3\|isopropyl 4" .claude/agents/lucy-devils-advocate.md
  </verify>

  <acceptance_criteria>
    - `grep -c "G-PROP-EVIDENCE" .claude/agents/lucy-devils-advocate.md` >= 4
    - `grep -c "FIX-10" .claude/agents/lucy-devils-advocate.md` >= 2
    - `grep -c "dominant" .claude/agents/lucy-devils-advocate.md` >= 2
    - `grep -c "renormali" .claude/agents/lucy-devils-advocate.md` >= 1
    - `grep -c "| G-PROP-EVIDENCE |" .claude/agents/lucy-devils-advocate.md` >= 1  (pipe-delimited table row)
    - Gate appears in: (a) definition block, (b) pipe-delimited summary table row, (c) workflow step 8a
    - Existing G-ELIM-1..4 and G7 gates still present: `grep -c "G-ELIM-1\|G7" .claude/agents/lucy-devils-advocate.md` >= 2
    - `grep -c "150\.8\|C12H16O3\|isopropyl 4" .claude/agents/lucy-devils-advocate.md` == 0
    - File is valid markdown, YAML front-matter intact
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent skill files -> CASE runtime | Edited .md files are loaded verbatim as agent prompts; malformed markdown could cause syntax confusion but not code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | .claude/agents/*.md edits | accept | Files are git-tracked; any unintended change is diff-visible and reversible |
| T-83-02 | Information Disclosure | Compound-specific test values leaking into runtime skill | mitigate | Compound-agnostic guard in every task's acceptance_criteria; grep check for CASE9/C12H16O3/150.8 must return 0 |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After all three tasks complete:

```bash
cd /Users/steinbeck/Dropbox/develop/lucy-ng

# Presence checks
grep -c "statistical prior" .claude/agents/lucy-nmr-chemist.md   # >= 2
grep -c "dominant" .claude/agents/lucy-nmr-chemist.md            # >= 2
grep -c "renormali" .claude/agents/lucy-nmr-chemist.md           # >= 1
grep -c "G-PROP-EVIDENCE" .claude/agents/lucy-lsd-engineer.md    # >= 2
grep -c "convergent" .claude/agents/lucy-lsd-engineer.md         # >= 2
grep -c "G-PROP-EVIDENCE" .claude/agents/lucy-devils-advocate.md # >= 4
grep -c "| G-PROP-EVIDENCE |" .claude/agents/lucy-devils-advocate.md # >= 1
grep -c "dominant" .claude/agents/lucy-devils-advocate.md        # >= 2

# Compound-agnostic guard — all must return 0
grep -rn "150\.8\|166\.08\|C12H16O3\|isopropyl 4" \
  .claude/agents/lucy-nmr-chemist.md \
  .claude/agents/lucy-lsd-engineer.md \
  .claude/agents/lucy-devils-advocate.md

# Git diff
git diff --stat HEAD .claude/agents/
```
</verification>

<success_criteria>
1. nmr-chemist Pitfall 6 encodes: full-distribution reading (dominant element, constraint_type), the 145-160 ppm aromatic ambiguity, prohibition on renormalization, the convergent-evidence carbonyl exception, "let ranking decide" (greps: "statistical prior" >= 2, "dominant" >= 2, "renormali" >= 1, "145-160 ppm" >= 1, "FIX-10" >= 1).
2. nmr-chemist Section 5 Neighbours line replaced with 8-step full-distribution advisory protocol starting with "Output is a STATISTICAL PRIOR — advisory only."
3. lsd-engineer FIX-10 PROHIBITION block names G-PROP-EVIDENCE, specifies (i) direct evidence and (ii) convergent corroboration as the only permitted bases, lists forbidden patterns including renormalization and dominant-carbon override (greps: "G-PROP-EVIDENCE" >= 2, "convergent" >= 2, "dominant" >= 1, "renormali" >= 1, "FIX-10" >= 2).
4. lsd-engineer Manual Checklist item 6 updated to convergent-evidence rule with G-PROP-EVIDENCE reference.
5. devils-advocate G-PROP-EVIDENCE gate: check procedure (Step 1-4), three sub-checks (typical/dominant/renormalized), CRITICAL severity, FIX-10 rationale (greps: "G-PROP-EVIDENCE" >= 4, "| G-PROP-EVIDENCE |" >= 1, "dominant" >= 2, "renormali" >= 1).
6. G-PROP-EVIDENCE appears in pipe-delimited summary table row AND workflow step 8a.
7. All three files pass compound-agnostic guard: zero hits for CASE9-specific values (150.8, 166.08, C12H16O3, isopropyl 4).
8. All three files are valid markdown with YAML front-matter intact.
</success_criteria>

<output>
Create `.planning/phases/83-constraint-hardness-guard/83-01-SUMMARY.md` when done.
</output>
