---
phase: 83-constraint-hardness-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/lucy-nmr-chemist.md
  - .claude/agents/lucy-lsd-engineer.md
  - .claude/agents/lucy-devils-advocate.md
autonomous: true
requirements:
  - FIX-10

must_haves:
  truths:
    - "nmr-chemist explicitly names detect-neighbours O/N output a statistical prior / advisory signal, not a basis for a hard PROP constraint"
    - "nmr-chemist names the 145-160 ppm aromatic C ambiguity (ring-O vs EDG/benzylic substituent, esp. sp3 C-O at 65-75 ppm) and re-asserts Pitfall 6"
    - "lsd-engineer explicitly forbids PROP X O n (and hard heteroatom BONDs) derived from statistical detection alone; uncertain placement left open for ranking"
    - "devils-advocate has a G-PROP-EVIDENCE gate that BLOCKS any hard heteroatom-neighbour PROP or heteroatom BOND lacking a cited direct-evidence basis"
  artifacts:
    - path: ".claude/agents/lucy-nmr-chemist.md"
      provides: "FIX-10 advisory rule in Pitfall 6 + Section 5 interpretation rules"
      contains: "statistical prior"
    - path: ".claude/agents/lucy-lsd-engineer.md"
      provides: "FIX-10 no-hard-heteroatom-PROP-from-stats rule"
      contains: "G-PROP-EVIDENCE"
    - path: ".claude/agents/lucy-devils-advocate.md"
      provides: "FIX-10 G-PROP-EVIDENCE gate"
      contains: "G-PROP-EVIDENCE"
  key_links:
    - from: "nmr-chemist detect-neighbours output"
      to: "lsd-engineer constraint building"
      via: "[SETUP-COMPLETE] message"
      pattern: "statistical prior.*advisory"
    - from: "lsd-engineer PROP/BOND write path"
      to: "devils-advocate G-PROP-EVIDENCE gate"
      via: "LSD file validation"
      pattern: "G-PROP-EVIDENCE"
---

<objective>
Encode three skill-level fixes that prevent uncertain heteroatom inferences from becoming hard, solution-excluding LSD constraints (FIX-10).

Purpose: The CASE9 defect was caused by nmr-chemist turning a statistical `detect neighbours 150.80` -> O 81% signal into a hard `PROP 2 O 1` on an aromatic carbon that actually bears a benzylic CH(OH)CH3. This is the third instance of "uncertain interpretation -> hard constraint -> correct structure excluded." All three skill files need coordinated changes so the advisory-only interpretation is enforced at every stage.

Output: Three edited agent .md files with grep-verifiable rule strings present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

CRITICAL NOTE ON FILE LOCATIONS: Agent files live in the REPO at `.claude/agents/lucy-*.md` and are symlinked into `~/.claude`. Edit the REPO copies. They are git-tracked.

Phase-83 spec (authoritative): .planning/ROADMAP.md §Phase 83 (lines 539-561)
</context>

<tasks>

<task type="auto">
  <name>Task 1: nmr-chemist — advisory rule + 145-160 ppm ambiguity + Pitfall 6 re-assertion</name>
  <files>.claude/agents/lucy-nmr-chemist.md</files>

  <read_first>
    Read `.claude/agents/lucy-nmr-chemist.md` fully before editing. Key locations to modify:

    - Section "2. Critical Pitfalls" — specifically Pitfall 6 (currently at line ~57: "Pitfall 6 — Heteroatom constraints (CRITICAL): Express what you KNOW...") — this is the primary target.
    - Section "5. Statistical Detection Protocol" — subsection "Interpretation Rules" (currently at line ~154: "Neighbours: Mandatory (>95%) -> report for PROP constraint...") — needs advisory language.

    ROADMAP §Phase 83 success criterion 1: "The nmr-chemist agent states explicitly that `detect neighbours` O/N output is a statistical prior / advisory signal, and that a 145-160 ppm aromatic C is ambiguous (ring-O vs EDG/benzylic substituent, esp. when an sp3 C-O at 65-75 ppm could be benzylic); Pitfall 6 ('don't over-constrain heteroatoms; let ranking decide') is re-asserted."
  </read_first>

  <action>
    Make two targeted edits to `.claude/agents/lucy-nmr-chemist.md`:

    EDIT A — Pitfall 6 (the heteroatom over-constraint pitfall). Replace the existing Pitfall 6 block with an expanded version that includes all of the following text verbatim:

    "**Pitfall 6 — Heteroatom constraints (CRITICAL — re-asserted FIX-10):** `detect neighbours` O/N output is a STATISTICAL PRIOR / advisory signal — never the sole basis for a hard PROP or BOND heteroatom constraint. Express what you KNOW from direct evidence (HMBC to heteroatom-bearing carbon, HSQC absence confirming quaternary, exchangeable H confirming OH/NH), not what detection statistically suggests.

    **145-160 ppm aromatic C ambiguity:** A carbon in this range is ambiguous between (a) ring-O-substituted (phenol/aryl-ether, which IS O-substituted) and (b) an aromatic C bearing an electron-donating or benzylic substituent with NO ring-oxygen — especially when an sp3 C-O signal at 65-75 ppm could be a benzylic carbinol (e.g., CH(OH)CH3 directly attached to the ring carbon). Detection O-frequency at this shift is elevated for both cases; it cannot distinguish them. Do NOT emit PROP X O n for such a carbon based on detection alone.

    **Rule:** For carbonyls (160-220 ppm) where detection confirms O mandatory AND DEPT/HSQC confirms Cq: BOND C=O is safe (strong, direct evidence from three independent sources). For ALL other heteroatom placements: leave OPEN. Let LSD explore acid vs ester vs ether vs benzylic. Ranking (13C prediction) decides. This is 'don't over-constrain heteroatoms; let ranking decide.'"

    EDIT B — Section 5 Interpretation Rules, Neighbours line. Change the Neighbours interpretation line from its current text (which says "Mandatory (>95%) -> report for PROP constraint") to:

    "**Neighbours:** Output is a STATISTICAL PRIOR — advisory only. Mandatory (>95%) -> report in [SETUP-COMPLETE] as an advisory observation; do NOT unilaterally emit PROP or hard BOND from this alone. For carbonyls at 160-220 ppm where DEPT/HSQC also confirms Cq: safe to recommend BOND C=O. For O/N at 145-160 ppm aromatic C: ambiguous — leave open (see Pitfall 6). Forbidden (<1%) -> report as advisory exclusion. Typical (1-95%) -> no constraint. CRITICAL: check formula first — ignore 'O mandatory' if formula has no O."

    Do NOT remove or alter any other content in the file. Write the file back in its entirety after making these two targeted replacements.
  </action>

  <verify>
    <automated>
      grep -c "statistical prior" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-nmr-chemist.md
    </automated>
    Expected output: 2 (one in Pitfall 6, one in Section 5 Neighbours line).

    Also verify:
    grep -c "145-160 ppm aromatic C" .claude/agents/lucy-nmr-chemist.md
    Expected: >= 1

    grep -c "benzylic" .claude/agents/lucy-nmr-chemist.md
    Expected: >= 1

    grep -c "FIX-10" .claude/agents/lucy-nmr-chemist.md
    Expected: >= 1
  </verify>

  <acceptance_criteria>
    - `grep -c "statistical prior" .claude/agents/lucy-nmr-chemist.md` returns 2 or more
    - `grep -c "145-160 ppm aromatic C" .claude/agents/lucy-nmr-chemist.md` returns >= 1
    - `grep -c "benzylic" .claude/agents/lucy-nmr-chemist.md` returns >= 1
    - `grep -c "FIX-10" .claude/agents/lucy-nmr-chemist.md` returns >= 1
    - `grep -c "let ranking decide" .claude/agents/lucy-nmr-chemist.md` returns >= 1
    - File is valid markdown (no truncation, YAML front-matter intact)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: lsd-engineer — forbid hard heteroatom PROP/BOND from statistics; open placement rule</name>
  <files>.claude/agents/lucy-lsd-engineer.md</files>

  <read_first>
    Read `.claude/agents/lucy-lsd-engineer.md` fully before editing. Key locations:

    - Section "1. LSD Command Reference" — subsection "Bond and Property Constraints" (currently at line ~66: "BOND 1 14 ; atom 1 bonded to atom 14 ...") — add a HARD PROHIBITION block immediately after the existing PROP/BOND examples.
    - Section "1. LSD Command Reference" — "Manual Checklist" (currently at line ~222, item 6: "Heteroatom constraints: BOND for C=O only; LIST/ELEM/PROP for flexible connectivity") — update this item to include the FIX-10 prohibition.
    - The ELIM Escalation section's Step 0 comment (line ~122) about heteroatom constraints on first run also needs to reference G-PROP-EVIDENCE.

    ROADMAP §Phase 83 success criterion 2: "The lsd-engineer agent forbids emitting `PROP X O n` or a hard heteroatom BOND from statistical detection alone; uncertain heteroatom placement is left open for ranking to discriminate."
  </read_first>

  <action>
    Make two targeted edits to `.claude/agents/lucy-lsd-engineer.md`:

    EDIT A — Bond and Property Constraints subsection. After the existing PROP example lines (`PROP L1 1 L2 ; each atom in L1 has at least 1 neighbor from L2`), add a new block:

    "**FIX-10 PROHIBITION — No hard heteroatom PROP/BOND from statistical detection alone (G-PROP-EVIDENCE):**
    NEVER emit `PROP X O n`, `PROP X N n`, or any hard heteroatom BOND solely because `lucy detect neighbours` reported O or N as 'mandatory'. Statistical detection is an ADVISORY PRIOR, not structural proof. The only basis for a hard heteroatom constraint is DIRECT connectivity evidence:
    - HMBC correlation to a heteroatom-bearing carbon (proves adjacency)
    - HSQC absence confirming Cq at 160-220 ppm + formula O count (permits BOND C=O)
    - Exchangeable H peak confirming OH or NH (permits one heteroatom H assignment)

    **Carbonyl exception (the ONLY safe hard heteroatom constraint from detection):** A Cq in the 160-220 ppm range where detect-neighbours confirms O mandatory AND DEPT/HSQC confirms no attached H: emit `BOND X O` (C=O). This has three independent sources of evidence.

    **For all other heteroatom placements:** Leave OPEN. Use LIST/ELEM/PROP only when direct HMBC connectivity is established. Let LSD explore alternatives; let 13C ranking (lucy lsd rank) discriminate. An incorrect hard PROP is a solution-excluding constraint that ranking cannot recover from — this is the FIX-10 root cause."

    EDIT B — Manual Checklist item 6. The current text is: "Heteroatom constraints: BOND for C=O only; LIST/ELEM/PROP for flexible connectivity". Replace with:

    "Heteroatom constraints: BOND for C=O only (when Cq 160-220 ppm + detect-neighbours O mandatory + DEPT/HSQC confirms Cq — three independent sources). For ALL other heteroatom placements: leave OPEN; never emit PROP X O n or hard heteroatom BOND from detect-neighbours alone (see FIX-10 / G-PROP-EVIDENCE rule above). Devils-advocate will BLOCK if a hard heteroatom PROP/BOND lacks cited direct evidence."

    Do NOT remove or alter any other content. Write the file back in its entirety.
  </action>

  <verify>
    <automated>
      grep -c "G-PROP-EVIDENCE" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-lsd-engineer.md
    </automated>
    Expected: >= 2 (in the PROHIBITION block and in the checklist item)

    Also:
    grep -c "FIX-10" .claude/agents/lucy-lsd-engineer.md
    Expected: >= 2

    grep -c "Leave OPEN" .claude/agents/lucy-lsd-engineer.md
    Expected: >= 2
  </verify>

  <acceptance_criteria>
    - `grep -c "G-PROP-EVIDENCE" .claude/agents/lucy-lsd-engineer.md` returns >= 2
    - `grep -c "FIX-10" .claude/agents/lucy-lsd-engineer.md` returns >= 2
    - `grep -c "Leave OPEN" .claude/agents/lucy-lsd-engineer.md` returns >= 2
    - `grep -c "ADVISORY PRIOR" .claude/agents/lucy-lsd-engineer.md` returns >= 1
    - File is valid markdown (no truncation, YAML front-matter intact)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: devils-advocate — add G-PROP-EVIDENCE gate following existing G-* gate pattern</name>
  <files>.claude/agents/lucy-devils-advocate.md</files>

  <read_first>
    Read `.claude/agents/lucy-devils-advocate.md` fully before editing. Key locations:

    - Section "5. Inventory Validation Protocol" — subsection "B. Three-Check Reconciliation Protocol" — Check 4 (ELIM gates G-ELIM-1..4) and the G7 gate under Check 5. The new gate follows this exact naming and structure pattern.
    - The gate summary table at the end of Check 5 (around line ~374): "| Gate | When | Severity | Trigger |" — add the new gate to this table.
    - The [VALIDATION-BLOCKED] message template (around line ~432) — the new gate's CRITICAL findings should surface under "CRITICAL issues:".
    - The workflow step 8 (around line ~480: "Run bug checklist") — add the new gate check as a step 8a or within step 6.

    The new gate must follow the G-ELIM-* format: named gate identifier, when it applies, severity, trigger condition, and a concrete check procedure.

    ROADMAP §Phase 83 success criterion 3: "The devils-advocate has a gate that BLOCKS or CRITICAL-flags any hard heteroatom-neighbour PROP / heteroatom BOND that lacks a cited direct-evidence basis (HMBC/HSQC/exchangeable-H)."
  </read_first>

  <action>
    Make two targeted edits to `.claude/agents/lucy-devils-advocate.md`:

    EDIT A — Insert the G-PROP-EVIDENCE gate. Add it as a new subsection inside Section 5, "Check 5: Failure Mode Detection", immediately after the G7 gate block and before the "Summary of Check 5 gates" table. The block to insert:

    "---

    **G-PROP-EVIDENCE: Hard Heteroatom PROP/BOND Lacks Direct Evidence (CRITICAL)**

    *Trigger:* The LSD file contains `PROP X O`, `PROP X N`, or a hard heteroatom BOND (e.g. `BOND X O` outside the carbonyl 160-220 ppm Cq exception) AND no direct connectivity evidence is cited in the constraint inventory `applied_from_detection` array or in the nmr-chemist's [SETUP-COMPLETE] detection message.

    **Check procedure:**

    Step 1 — Detect hard heteroatom constraints:
    ```bash
    grep -E "^PROP [0-9]+ (O|N)" analysis/iteration_NN/compound.lsd
    grep -E "^BOND [0-9]+ (O|N)|^BOND (O|N) [0-9]+" analysis/iteration_NN/compound.lsd
    ```
    If no output: gate passes (no hard heteroatom constraints present).

    Step 2 — For each hard heteroatom constraint found, check for cited direct evidence. In the constraint inventory `applied_from_detection` array (extracted in Section 5A), look for an entry that cites one of:
    - 'HMBC' (direct HMBC connectivity to the heteroatom-bearing carbon)
    - 'HSQC' (HSQC-confirmed Cq at 160-220 ppm with O in formula — carbonyl exception)
    - 'exchangeable H' (OH or NH confirmed)

    The carbonyl exception: `BOND X O` where atom X is defined as Cq (MULT X C 2 0) at 160-220 ppm is PERMITTED if detect-neighbours O-mandatory evidence is cited — this is the safe case (three independent sources). All other hard heteroatom O/N constraints require direct HMBC/HSQC/exchangeable-H evidence.

    Step 3 — If a hard heteroatom constraint has no cited direct-evidence entry: flag CRITICAL.

    CRITICAL message: "G-PROP-EVIDENCE: Hard heteroatom constraint '{constraint}' found in LSD file but no direct connectivity evidence (HMBC/HSQC/exchangeable-H) is cited in the constraint inventory. Statistical detect-neighbours output alone is insufficient basis. Remove the constraint or supply direct-evidence citation in the inventory applied_from_detection array. (FIX-10)"

    **Without inventory (legacy fallback):** If no v2 inventory is present, check whether CASE-PROGRESS.md documents an explicit direct-evidence citation for each hard heteroatom constraint. If documentation is absent: flag CRITICAL with the same message.

    *Severity:* CRITICAL — blocks solver run. Incorrect hard heteroatom constraints are solution-excluding; ranking cannot recover from them.

    *Rationale:* This is the FIX-10 gate. The CASE9 defect was caused by a hard `PROP 2 O 1` derived from `detect neighbours 150.80` -> O 81%, forcing ring-oxygen onto an aromatic C that actually bears a benzylic CH(OH)CH3. Detection at 145-160 ppm cannot distinguish ring-O-substituted from benzylic-EDG-substituted. Ranking can — if the constraint is absent."

    EDIT B — Add to the gate summary table at the end of Check 5:

    In the row group after G7, add:
    "| G-PROP-EVIDENCE | Pre-run, all iterations | CRITICAL | Hard heteroatom PROP/BOND in LSD file without cited direct evidence (HMBC/HSQC/exchangeable-H) |"

    Also add G-PROP-EVIDENCE to the workflow (step 8 of the workflow section): add a sub-bullet "Run G-PROP-EVIDENCE gate (Section 5, Check 5): flag CRITICAL if hard heteroatom PROP/BOND lacks direct-evidence citation."

    Do NOT remove or alter any other content. Write the file back in its entirety.
  </action>

  <verify>
    <automated>
      grep -c "G-PROP-EVIDENCE" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-devils-advocate.md
    </automated>
    Expected: >= 4 (gate definition header, severity line, table row, workflow mention)

    Also:
    grep -c "FIX-10" .claude/agents/lucy-devils-advocate.md
    Expected: >= 2

    grep -c "direct connectivity evidence" .claude/agents/lucy-devils-advocate.md
    Expected: >= 1

    grep -c "CRITICAL" .claude/agents/lucy-devils-advocate.md
    Expected: >= 10 (existing + new gate, just confirm no reduction)
  </verify>

  <acceptance_criteria>
    - `grep -c "G-PROP-EVIDENCE" .claude/agents/lucy-devils-advocate.md` returns >= 4
    - `grep -c "FIX-10" .claude/agents/lucy-devils-advocate.md` returns >= 2
    - `grep -c "direct connectivity evidence" .claude/agents/lucy-devils-advocate.md` returns >= 1
    - Gate appears in both the gate definition block AND the summary table (check for table row with "G-PROP-EVIDENCE" and pipe characters)
    - File is valid markdown (no truncation, YAML front-matter intact)
    - Existing G-ELIM-1..4 and G7 gates are still present: `grep -c "G-ELIM-1\|G7" .claude/agents/lucy-devils-advocate.md` returns >= 2
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent skill files -> CASE runtime | Edited .md files are loaded verbatim as agent prompts; malformed markdown could cause syntax confusion but not code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | .claude/agents/*.md edits | accept | Files are git-tracked; any unintended change is diff-visible and reversible |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — skill-doc edits only |

No network, auth, or external-input attack surface introduced.
</threat_model>

<verification>
After all three tasks complete:

```bash
grep -c "statistical prior" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-nmr-chemist.md
# Expected: >= 2

grep -c "G-PROP-EVIDENCE" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-lsd-engineer.md
# Expected: >= 2

grep -c "G-PROP-EVIDENCE" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-devils-advocate.md
# Expected: >= 4

grep -c "FIX-10" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-nmr-chemist.md
# Expected: >= 1

grep -c "FIX-10" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-lsd-engineer.md
# Expected: >= 2

grep -c "FIX-10" /Users/steinbeck/Dropbox/develop/lucy-ng/.claude/agents/lucy-devils-advocate.md
# Expected: >= 2
```

Confirm git tracks the changes:
```bash
git -C /Users/steinbeck/Dropbox/develop/lucy-ng diff --stat HEAD .claude/agents/
```
</verification>

<success_criteria>
1. nmr-chemist Pitfall 6 is expanded with: advisory-signal language, the 145-160 ppm ambiguity rule, and the benzylic-substituent caveat (grep: "statistical prior", "145-160 ppm aromatic C", "benzylic" all present).
2. nmr-chemist Section 5 Neighbours interpretation explicitly says output is a STATISTICAL PRIOR and advisory only (grep: "statistical prior" in Neighbours line).
3. lsd-engineer has a named FIX-10/G-PROP-EVIDENCE PROHIBITION block forbidding PROP X O n from detection alone, with the carbonyl exception stated (grep: "G-PROP-EVIDENCE" >= 2, "Leave OPEN" >= 2).
4. lsd-engineer Manual Checklist item 6 updated to reference FIX-10 (grep: "FIX-10" >= 2).
5. devils-advocate has G-PROP-EVIDENCE gate definition with: check procedure (grep PROP/BOND lines), direct-evidence citation requirement, CRITICAL severity, FIX-10 rationale (grep: "G-PROP-EVIDENCE" >= 4).
6. G-PROP-EVIDENCE appears in the devils-advocate gate summary table.
7. All three files are valid markdown with YAML front-matter intact.
</success_criteria>

<output>
Create `.planning/phases/83-constraint-hardness-guard/83-01-SUMMARY.md` when done.
</output>
