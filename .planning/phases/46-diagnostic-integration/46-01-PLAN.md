---
phase: 46-diagnostic-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/lucy-ng/case.md
  - ~/.claude/agents/lucy-diagnostic.md
autonomous: true

must_haves:
  truths:
    - "Diagnostic specialist is spawned via Task() WITHOUT team_name (objectivity preserved)"
    - "Specialist instructions explicitly mention constraint inventory block in LSD file header"
    - "lucy-diagnostic.md Step 1 knows how to extract and use constraint inventory JSON"
    - "Diagnostic report delivered to coordinator via orchestrator advisory (extract_diagnostic_findings + deliver_advisory chain)"
    - "Delegation trigger remains counter == 2 for same pattern"
    - "DIAGNOSTIC-REPORT.md path uses analysis/ subdirectory for consistency with other CASE artifacts"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "Updated delegate_specialist instructions with inventory awareness and analysis/ path"
      contains: "CONSTRAINT INVENTORY"
    - path: "~/.claude/agents/lucy-diagnostic.md"
      provides: "Updated Step 1 Gather Context with inventory extraction procedure"
      contains: "CONSTRAINT INVENTORY"
  key_links:
    - from: "case.md delegate_specialist"
      to: "lucy-diagnostic.md Step 1"
      via: "Instructions tell specialist to parse inventory; Step 1 knows the format"
      pattern: "CONSTRAINT INVENTORY v1"
    - from: "case.md delegate_specialist"
      to: "case.md extract_diagnostic_findings"
      via: "Specialist writes DIAGNOSTIC-REPORT.md; orchestrator reads it"
      pattern: "analysis/DIAGNOSTIC-REPORT.md"
---

<objective>
Update diagnostic specialist integration for constraint inventory awareness and path consistency.

Purpose: The diagnostic specialist already receives the LSD file (which contains the constraint inventory JSON block since Phase 43), but is not explicitly told to parse the `; === CONSTRAINT INVENTORY v1 ===` block. Adding this awareness lets the specialist use inventory history (what changed when, what was dropped) for deeper root cause analysis. Also aligns DIAGNOSTIC-REPORT.md path with the `analysis/` convention used by all other CASE artifacts.

Output: Updated case.md (delegate_specialist + extract_diagnostic_findings) and lucy-diagnostic.md (Step 1 Gather Context) with inventory awareness and consistent paths.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-diagnostic-integration/46-RESEARCH.md

@~/.claude/commands/lucy-ng/case.md
@~/.claude/agents/lucy-diagnostic.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update case.md delegate_specialist and extract_diagnostic_findings for inventory awareness and analysis/ path</name>
  <files>~/.claude/commands/lucy-ng/case.md</files>
  <action>
  Three targeted edits in case.md. Do NOT touch any other steps (especially not track_and_decide — counter == 2 trigger must remain unchanged).

  **Edit 1: delegate_specialist instructions (lines ~947-968)**

  In the Task() instructions string, update the "Read:" block to:
  - Add "(iteration history with per-agent sections)" annotation to CASE-PROGRESS.md line
  - Change the LSD file path from `<compound_path>/<latest_lsd_file>` to `<compound_path>/analysis/<latest_iteration>/compound.lsd`
  - Add a note about the constraint inventory block after the Read: entries

  The updated instructions block should look like:
  ```
  Task(
    agent_type="lucy-diagnostic",
    model="opus",
    instructions="Analyze LSD failure for compound at <compound_path>.

    Read:
    - <compound_path>/analysis/CASE-PROGRESS.md (iteration history with per-agent sections)
    - <compound_path>/analysis/<latest_iteration>/compound.lsd (latest LSD file)
      Note: The LSD file header contains a JSON constraint inventory block between
      ; === CONSTRAINT INVENTORY v1 === and ; === END CONSTRAINT INVENTORY ===
      Extract this inventory in Step 1 — it tracks hmbc_batches, deff_not_patterns,
      syme_pairs, bond_constraints, list_prop_constraints, elim_value, and detection results.
      The inventory history reveals WHAT changed between iterations (drops, additions).

    Failure type: <failure_type>

    Run systematic diagnostic checks per your inlined knowledge.
    Document ALL checks (PASS and FAIL).
    Identify root cause with evidence.

    Write structured report to <compound_path>/analysis/DIAGNOSTIC-REPORT.md.
    Include: findings, root cause, recommended fixes with LSD command examples.
    Rate all findings and recommendations as HIGH/MEDIUM/LOW confidence.
    "
  )
  ```

  Key changes from current:
  - LSD path now follows `analysis/<latest_iteration>/compound.lsd` pattern (matches file org convention)
  - Added 5-line note about constraint inventory block
  - Changed report path from `<compound_path>/DIAGNOSTIC-REPORT.md` to `<compound_path>/analysis/DIAGNOSTIC-REPORT.md`
  - Changed "per skill/diagnostic/SKILL.md" to "per your inlined knowledge" (specialist has knowledge inlined since Phase 25)

  **Edit 2: extract_diagnostic_findings step (lines ~974-1011)**

  Update the file check path from `<compound_path>/DIAGNOSTIC-REPORT.md` to `<compound_path>/analysis/DIAGNOSTIC-REPORT.md` in three places:
  1. "Verify file exists at <compound_path>/analysis/DIAGNOSTIC-REPORT.md"
  2. The missing-report log message
  3. The advisory text "See analysis/DIAGNOSTIC-REPORT.md for full analysis."

  **Edit 3: track_and_decide reference (line ~620)**

  Update the DIAGNOSTIC-REPORT.md path reference in the counter >= 3 decision: change "DIAGNOSTIC-REPORT.md exists in compound directory" to "analysis/DIAGNOSTIC-REPORT.md exists in compound directory".

  **Verification before saving:** Confirm that:
  - track_and_decide counter == 2 threshold text is UNCHANGED
  - delegate_specialist still uses Task() without team_name parameter
  - deliver_advisory step is UNCHANGED (it uses advisory text, not file path)
  </action>
  <verify>
  Run these checks against the modified case.md:
  1. `grep "CONSTRAINT INVENTORY" ~/.claude/commands/lucy-ng/case.md` — should find inventory reference in delegate_specialist
  2. `grep "team_name" ~/.claude/commands/lucy-ng/case.md` — should NOT appear in delegate_specialist Task() call (only in team spawn code)
  3. `grep "counter.*== 2" ~/.claude/commands/lucy-ng/case.md` or `grep "counter for this pattern == 2" ~/.claude/commands/lucy-ng/case.md` — should still find the threshold
  4. `grep "analysis/DIAGNOSTIC-REPORT" ~/.claude/commands/lucy-ng/case.md` — should find updated paths
  5. `grep "compound_path.*DIAGNOSTIC-REPORT" ~/.claude/commands/lucy-ng/case.md` — should all use analysis/ prefix
  </verify>
  <done>
  delegate_specialist instructions mention constraint inventory block with field names; DIAGNOSTIC-REPORT.md path consistently uses analysis/ subdirectory across delegate_specialist, extract_diagnostic_findings, and track_and_decide; counter == 2 threshold untouched; Task() still has no team_name parameter.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update lucy-diagnostic.md Step 1 with constraint inventory extraction and analysis/ report path</name>
  <files>~/.claude/agents/lucy-diagnostic.md</files>
  <action>
  Two targeted edits in lucy-diagnostic.md.

  **Edit 1: Step 1 Gather Context (lines ~865-888)**

  After item 3 ("Spectral quality notes"), add item 4 for constraint inventory extraction:

  ```markdown
  4. **Constraint inventory (from LSD file header):**
     The LSD file contains a JSON inventory block at the top, delimited by:
     ```
     ; === CONSTRAINT INVENTORY v1 ===
     ; {"version": 1, "mult_count": 13, ...}
     ; === END CONSTRAINT INVENTORY ===
     ```

     Extract the JSON (strip leading `; ` from each line). Key fields for diagnosis:
     - `hmbc_batches`: Which HMBC correlations were added in each iteration — reveals if strategy was incremental or random
     - `deff_not_patterns`: Strained-ring exclusion count — should INCREASE or stay constant across iterations, never decrease (Bug 1 indicator)
     - `syme_pairs`: Symmetry constraints — if signal grouping was detected but this is empty, Bug 2 (grouping not applied)
     - `bond_constraints`, `list_prop_constraints`: Structural constraints from detection — check if detection ran but constraints are missing (Bug 5)
     - `elim_value`: If present, check per Section 2.2 Check 1 (ELIM miscalculation)
     - `pending_from_detection`: Detection results not yet translated to constraints — potential fix opportunity
     - `applied_from_detection`: Confirms which detection results were actually used

     The inventory supplements the raw LSD commands by providing HISTORY (what changed between iterations) that the raw commands alone cannot show. Compare inventory fields across iterations when CASE-PROGRESS.md documents multiple iterations.
  ```

  **Edit 2: DIAGNOSTIC-REPORT.md path references**

  Search for all occurrences of `DIAGNOSTIC-REPORT.md` in the file and update the path references to use `analysis/` subdirectory. Specifically:
  - Line ~932: "Write `DIAGNOSTIC-REPORT.md` to the compound directory" → "Write `DIAGNOSTIC-REPORT.md` to the compound's `analysis/` subdirectory"
  - Line ~1036: "`<compound_directory>/DIAGNOSTIC-REPORT.md`" → "`<compound_directory>/analysis/DIAGNOSTIC-REPORT.md`"
  - Line ~1083: "Write structured report to <path>/DIAGNOSTIC-REPORT.md" → "Write structured report to <path>/analysis/DIAGNOSTIC-REPORT.md"
  - Line ~1093: "Primary output: `DIAGNOSTIC-REPORT.md` written to the compound directory" → "Primary output: `DIAGNOSTIC-REPORT.md` written to the compound's `analysis/` subdirectory"
  - Line ~1137: same pattern

  Do NOT change the timestamped filename convention (line ~1038) — just update the directory reference to `analysis/`.

  **Note:** There may be minor line number drift from prior phases. Use the text content (not exact line numbers) to locate each occurrence.
  </action>
  <verify>
  Run these checks against the modified lucy-diagnostic.md:
  1. `grep "CONSTRAINT INVENTORY" ~/.claude/agents/lucy-diagnostic.md` — should find inventory extraction instructions in Step 1
  2. `grep "hmbc_batches" ~/.claude/agents/lucy-diagnostic.md` — should find field reference
  3. `grep "deff_not_patterns" ~/.claude/agents/lucy-diagnostic.md` — should find field reference with Bug 1 note
  4. `grep "pending_from_detection" ~/.claude/agents/lucy-diagnostic.md` — should find field reference
  5. `grep "analysis/DIAGNOSTIC-REPORT" ~/.claude/agents/lucy-diagnostic.md` — should find updated paths
  6. `grep -c "compound_directory./DIAGNOSTIC-REPORT" ~/.claude/agents/lucy-diagnostic.md` — should be 0 (all paths updated to use analysis/)
  </verify>
  <done>
  lucy-diagnostic.md Step 1 has item 4 documenting constraint inventory extraction with field-by-field diagnostic guidance; all DIAGNOSTIC-REPORT.md paths reference analysis/ subdirectory; no references to old compound-root path remain.
  </done>
</task>

</tasks>

<verification>
**Phase 46 Success Criteria verification:**

1. **SC1 — Specialist remains orchestrator-spawned (not team member):**
   `grep "team_name" ~/.claude/commands/lucy-ng/case.md` — delegate_specialist Task() call must NOT include team_name parameter.

2. **SC2 — Specialist receives team context (CASE-PROGRESS.md, constraint inventory):**
   - `grep "CASE-PROGRESS.md" ~/.claude/commands/lucy-ng/case.md` in delegate_specialist — must reference `analysis/CASE-PROGRESS.md`
   - `grep "CONSTRAINT INVENTORY" ~/.claude/commands/lucy-ng/case.md` — must appear in delegate_specialist instructions
   - `grep "CONSTRAINT INVENTORY" ~/.claude/agents/lucy-diagnostic.md` — must appear in Step 1

3. **SC3 — Diagnostic report delivered via orchestrator advisory:**
   - `grep "extract_diagnostic_findings" ~/.claude/commands/lucy-ng/case.md` — step must exist
   - `grep "deliver_advisory" ~/.claude/commands/lucy-ng/case.md` — step must exist
   - Both steps use `analysis/DIAGNOSTIC-REPORT.md` path

4. **SC4 — Delegation trigger unchanged:**
   - `grep "counter for this pattern == 2" ~/.claude/commands/lucy-ng/case.md` — must find exact threshold text in track_and_decide
</verification>

<success_criteria>
- delegate_specialist mentions constraint inventory with key field names
- lucy-diagnostic.md Step 1 has inventory extraction procedure with diagnostic field reference
- DIAGNOSTIC-REPORT.md path consistently uses analysis/ subdirectory in both files
- track_and_decide counter == 2 threshold is unchanged
- Task() in delegate_specialist has no team_name parameter
- All 4 Phase 46 success criteria verifiable via grep commands
</success_criteria>

<output>
After completion, create `.planning/phases/46-diagnostic-integration/46-01-SUMMARY.md`
</output>
