---
phase: 70-agent-skill-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/steinbeck/.claude/agents/lucy-devils-advocate.md
autonomous: true
requirements:
  - AGT-04

must_haves:
  truths:
    - "D-18: G4 is a hard BLOCK when K > 3 (deferred_4j array length exceeds 3), consistent with PyLSDOrchestrator.run() ValueError(K>3) from Phase 67"
    - "D-18a: No new override path — G4 uses the same APPROVED/BLOCKED/WARNING semantics as G1/G2/G3; no new override-inventory-check or bypass mechanism added"
    - "D-18b: G4 block message names: K value, 2^K permutation count, K≤3 cap, PyLSDOrchestrator ValueError reference, and nmr-chemist as responsible agent for deprioritizing suspects"
    - "AGT-04: §5B Check 4 contains G4 as a fourth sub-check below G3; the G4 block reuses $RESULT already set in §5A (no additional CLI call)"
    - "The 'All three gates G1/G2/G3' summary sentence is atomically updated to 'All four gates G1/G2/G3/G4 are CRITICAL severity and blocking'"
    - "The [VALIDATION-PASSED] template includes a 'pyLSD mode:' field after the existing Aromatic ring check line"
  artifacts:
    - path: "/Users/steinbeck/.claude/agents/lucy-devils-advocate.md"
      provides: "Updated devils-advocate skill with G4 permutation cap check"
      contains: "G4"
  key_links:
    - from: "lucy-devils-advocate.md §5B Check 4"
      to: "PyLSDOrchestrator.run() K-cap guard"
      via: "G4 K-count extraction from inventory.deferred_4j length"
      pattern: "len.*deferred_4j.*K.*3"
    - from: "G4 $RESULT variable"
      to: "§5A lucy lsd validate-inventory --format json call"
      via: "$RESULT reuse — no second CLI call"
      pattern: "RESULT.*validate-inventory"
---

<objective>
Insert G4 (Permutation Count Cap) as the fourth sub-check in lucy-devils-advocate.md §5B Check 4, update the "All three gates" summary line to "All four gates", and add a `pyLSD mode:` field to the [VALIDATION-PASSED] template.

Purpose: G4 mirrors the Python-layer K-cap guard in PyLSDOrchestrator.run() (Phase 67) at the agent-skill level. Without G4, an agent could assemble a constraint inventory with 4+ deferred_4j entries, pass G1/G2/G3, invoke lucy pylsd run, and hit an unrecoverable ValueError at runtime. G4 catches this before the solver is invoked.

Output: Updated ~/.claude/agents/lucy-devils-advocate.md with G4 block inserted after G3 (line ~331), summary line updated, VALIDATION-PASSED template extended
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/PROJECT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/ROADMAP.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/STATE.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-CONTEXT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-RESEARCH.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-PATTERNS.md

<interfaces>
<!-- Current Check 4 structure (lucy-devils-advocate.md lines 296-333, VERIFIED in RESEARCH.md): -->
<!-- G1: FORM/MULT consistency (lines 300-308) -->
<!-- G2: No bare ELIM in pylsd_mode (lines 310-318, anchored ^ELIM grep) -->
<!-- G3: Annotation-vs-mode consistency (lines 320-331) -->
<!-- Line 333 (current last line of Check 4): -->
<!--   "**All three gates G1/G2/G3 are CRITICAL severity and blocking.** No new override mechanism ..." -->

<!-- $RESULT variable source (lines 225-253 in §5A, VERIFIED): -->
<!-- RESULT=$(lucy lsd validate-inventory --format json analysis/iteration_NN/compound.lsd) -->
<!-- G4 reuses $RESULT — no additional CLI call needed -->

<!-- deferred_4j path confirmed (RESEARCH.md live CLI test): -->
<!-- echo "$RESULT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d.get('inventory',{}).get('deferred_4j', [])))" -->

<!-- [VALIDATION-PASSED] template (lines 363-380, VERIFIED): -->
<!-- Currently does not include pylsd_mode / permutation count field -->
<!-- Insertion point: after "Aromatic ring check:" line -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert G4 sub-check, update summary line, and extend VALIDATION-PASSED template</name>
  <files>/Users/steinbeck/.claude/agents/lucy-devils-advocate.md</files>
  <read_first>
    - /Users/steinbeck/.claude/agents/lucy-devils-advocate.md — read the ENTIRE file before any edits; locate exact current text of: (a) the last bullet of G3 block (~line 331), (b) the "All three gates G1/G2/G3" summary sentence (~line 333), (c) the [VALIDATION-PASSED] template section (~lines 363-380), specifically the "Aromatic ring check:" line
    - /Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-PATTERNS.md — §"Change: G4 Permutation Count Cap" for exact three-step atomic edit instructions
    - /Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-RESEARCH.md — "Change 4: G4 Permutation Cap Check" for exact G4 block text, Pitfall 3 (G4 and summary update must be atomic), Pitfall 7 (block message must name nmr-chemist)
  </read_first>
  <action>
    Read the full file. Make three targeted edits as a single atomic change — do NOT modify any content outside Check 4 §5B and the [VALIDATION-PASSED] template.

    **Edit 1 — INSERT G4 block BEFORE the "All three gates" summary sentence.**

    Find the line that reads:
    ```
    **All three gates G1/G2/G3 are CRITICAL severity and blocking.**
    ```
    (This line directly follows the last bullet of G3.)

    INSERT the following new block BEFORE that line (leave one blank line between G3's last bullet and G4):

    ```
    **G4: Permutation Count Cap**

    When `pylsd_mode=true`, count deferred_4j entries and check against the K≤3 cap:

    ```bash
    K=$(echo "$RESULT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d.get('inventory',{}).get('deferred_4j', [])))")
    ```

    - If K > 3: **BLOCK** with message: "K={K} exceeds permutation cap (K≤3, max 2^3=8 permutations). PyLSDOrchestrator.run() will raise ValueError before creating any files (K-cap is the first check in run()). Action: nmr-chemist must prioritize the Top-3 most likely 4J suspects (highest probability aromatic-to-aliphatic long-range correlations); demote the remaining entries from deferred_4j by removing their '; ELIM' annotation from the corresponding HMBC lines, or reclassify them as non-suspect."
    - If K ≤ 3: G4 passes.

    **Note:** `$RESULT` is already set from the `lucy lsd validate-inventory --format json` call in §5A. No additional CLI call is needed for G4.
    ```

    **Edit 2 — UPDATE the summary sentence ATOMICALLY with Edit 1.**

    REPLACE:
    ```
    **All three gates G1/G2/G3 are CRITICAL severity and blocking.**
    ```
    With:
    ```
    **All four gates G1/G2/G3/G4 are CRITICAL severity and blocking.**
    ```

    The remainder of the sentence (about no new override mechanism and user orchestrator advisory) is unchanged.

    **Edit 3 — ADD `pyLSD mode:` field to [VALIDATION-PASSED] template.**

    In the [VALIDATION-PASSED] template section (~lines 363-380), locate the `Aromatic ring check:` line. INSERT the following line AFTER it:

    ```
    pyLSD mode: N/A (pylsd_mode=false) / PASS (K=N, N≤3) / BLOCKED (K=N>3)
    ```

    Do NOT modify any other content in the [VALIDATION-PASSED] template or any other section.
  </action>
  <verify>
    <automated>grep "G4\|Permutation Count Cap" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md && grep "All four gates G1/G2/G3/G4" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md && grep "pyLSD mode:" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md && grep -c "All three gates G1/G2/G3 are CRITICAL" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md</automated>
  </verify>
  <acceptance_criteria>
    Positive (must match, exit 0):
    - grep "G4" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md
    - grep "Permutation Count Cap" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md
    - grep "K > 3\|K>3" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md
    - grep "deferred_4j" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md  (K-count extraction)
    - grep "PyLSDOrchestrator" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md  (ValueError reference per D-18b)
    - grep "nmr-chemist" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md  (action owner per D-18b / Pitfall 7)
    - grep "All four gates G1/G2/G3/G4" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md
    - grep "pyLSD mode:" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md

    Negative (old state gone):
    - grep -c "All three gates G1/G2/G3 are CRITICAL" /Users/steinbeck/.claude/agents/lucy-devils-advocate.md
      # should return 0 — summary line updated
  </acceptance_criteria>
  <done>G4 sub-check inserted before the summary line with K-count extraction, BLOCK message naming PyLSDOrchestrator ValueError and nmr-chemist as resolution owner; "All three gates" updated to "All four gates" atomically; [VALIDATION-PASSED] template has 'pyLSD mode:' field. AGT-04 and D-18/D-18a/D-18b satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill file → agent runtime | devils-advocate reads this markdown before every validation; G4 inserted after the "All three gates" summary line without updating the summary line would confuse future agents reading the file |
| G4 $RESULT dependency | G4 reuses $RESULT set in §5A — if §5A is skipped (no inventory found), $RESULT may be empty; the .get('inventory',{}).get('deferred_4j',[]) default handles this safely (returns 0 — G4 passes when no inventory, consistent with D-19 fallback) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04 | Tampering | G4 block + summary line atomicity | mitigate | Acceptance criteria has both positive (G4 present, new summary present) and negative (old summary absent) checks — executor cannot pass acceptance without the atomic update |
| T-70-05 | Elevation of Privilege | G4 as blocking gate | accept | Gate mirrors existing PyLSDOrchestrator ValueError — it enforces an already-existing Python invariant at the skill level; D-18a explicitly prohibits new override paths |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After task completes:
1. grep "G4\|Permutation Count Cap" ~/.claude/agents/lucy-devils-advocate.md exits 0
2. grep "All four gates G1/G2/G3/G4" ~/.claude/agents/lucy-devils-advocate.md exits 0
3. grep "pyLSD mode:" ~/.claude/agents/lucy-devils-advocate.md exits 0
4. grep "PyLSDOrchestrator\|ValueError" ~/.claude/agents/lucy-devils-advocate.md exits 0
5. grep "nmr-chemist" ~/.claude/agents/lucy-devils-advocate.md exits 0
6. grep -c "All three gates G1/G2/G3 are CRITICAL" ~/.claude/agents/lucy-devils-advocate.md returns 0
7. pytest -q exits 0 (no Python regression)
</verification>

<success_criteria>
- §5B Check 4 contains G4 as fourth sub-check with K-count python3 extraction reusing $RESULT from §5A
- G4 BLOCK message names K value, 2^K permutation count, K≤3 cap, PyLSDOrchestrator ValueError, nmr-chemist as resolution owner
- "All four gates G1/G2/G3/G4 are CRITICAL severity and blocking" replaces the old "All three" summary line
- [VALIDATION-PASSED] template includes 'pyLSD mode: N/A / PASS / BLOCKED' field
- No new override mechanism introduced (D-18a)
</success_criteria>

<output>
Create .planning/phases/70-agent-skill-updates/70-02-SUMMARY.md when done
</output>
