---
phase: 70-agent-skill-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
autonomous: true
requirements:
  - AGT-01
  - AGT-02
  - AGT-03

must_haves:
  truths:
    - "D-17: lsd-engineer.md §workflow step 11 contains a conditional routing block that reads inventory.pylsd_mode via 'lucy lsd validate-inventory --format json' and calls 'lucy pylsd run' when true and 'lucy lsd run' when false"
    - "D-17a: case.md is NOT modified — routing domain logic stays in lsd-engineer (the agent that writes the inventory)"
    - "D-17b: case.md spawn protocol is unchanged — lsd-engineer gets the same context block and decides internally"
    - "D-19: ABSENT inventory (validate-inventory returns valid:false with no-inventory-block message) falls through silently to lucy lsd run — the .get('inventory',{}).get('pylsd_mode', False) expression implements this without a warning"
    - "D-19a: MALFORMED inventory remains a hard error per Phase 69 WR-01 — the routing block does not suppress other validation failures"
    - "D-19b: v1 legacy WARNING+fallback path from Phase 68 D-02 is unchanged"
    - "D-20: When lucy pylsd run was invoked (pylsd_mode=True), lsd-engineer appends two blocks below the existing '4J status:' line in [ITERATION-COMPLETE]: Block 1 (per-permutation table with permutation_id/defer_set/solution_count/top_rank_quality columns) and Block 2 (aggregated merged_count + Top-3 SMILES)"
    - "D-20a: ITERATION-COMPLETE data comes from pylsd_output.json (lucy pylsd run --format json stdout) and pylsd_run/perm_NN/solutions.smi line counts + run_report.json provenance"
    - "D-20b: ITERATION-COMPLETE blocks are in a Markdown code-fence for mono-space rendering and case.md loop-detection parseability"
    - "AGT-01: §1 'LSD Command Reference' includes a new '### pyLSD Commands (pylsd_mode only)' subsection documenting SHIX/SHIH syntax, the '; FORM' comment convention (bare FORM rejected by LSD-3.4.9 error 102), and the HMBC X Y 2 4 / HMBC X Y 2 4 ; ELIM extended-range syntax"
    - "AGT-02: Both unconditional 'lucy lsd run' occurrences (line 268 documentation string, line 481 workflow step) are replaced — line 268 becomes a prose pointer to step 11; line 481 becomes the full conditional routing block"
    - "AGT-03: AGT-03 is satisfied by the routing in lsd-engineer (D-17) — case.md orchestration indirectly routes to pylsd path by spawning lsd-engineer which reads the inventory and selects the CLI command. No case.md edit is required or made."
  artifacts:
    - path: "/Users/steinbeck/.claude/agents/lucy-lsd-engineer.md"
      provides: "Updated lsd-engineer skill with pyLSD routing block, vocabulary subsection, and pylsd_mode ITERATION-COMPLETE blocks"
      contains: "lucy pylsd run"
  key_links:
    - from: "lsd-engineer §workflow step 11"
      to: "lucy pylsd run / lucy lsd run"
      via: "conditional bash block reading inventory.pylsd_mode"
      pattern: "PYLSD_MODE.*True.*lucy pylsd run"
    - from: "lsd-engineer §1 pyLSD Commands subsection"
      to: "LSDInputGenerator permutation files"
      via: "inspection/debugging reference for SHIX/SHIH/; FORM syntax"
      pattern: "SHIX.*SHIH.*; FORM"
    - from: "lsd-engineer [ITERATION-COMPLETE]"
      to: "pylsd_output.json + run_report.json"
      via: "python3 extraction of merged_count and per-permutation data"
      pattern: "pylsd_output.json.*merged_count"
---

<objective>
Update lucy-lsd-engineer.md with three targeted additions:
1. Replace both unconditional `lucy lsd run` invocations with a conditional routing block that reads `pylsd_mode` from the constraint inventory (D-17)
2. Insert a `### pyLSD Commands (pylsd_mode only)` subsection in §1 documenting SHIX/SHIH/; FORM/HMBC extended range (AGT-01)
3. Append conditional pylsd_mode ITERATION-COMPLETE blocks below the existing `4J status:` line (D-20)

Purpose: This is the primary Phase 70 deliverable. Once lsd-engineer reads its own inventory and calls `lucy pylsd run` when `pylsd_mode=true`, the Python infrastructure built in Phases 66–69 becomes reachable by the agent team. AGT-03 is satisfied here (not via case.md) because routing lives with the agent that writes the inventory, per D-17.

Output: Updated ~/.claude/agents/lucy-lsd-engineer.md (~15-20 lines added, 2 lines replaced)
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/PROJECT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/ROADMAP.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/STATE.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-CONTEXT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-RESEARCH.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-PATTERNS.md

<interfaces>
<!-- validate-inventory --format json confirmed response shape (RESEARCH.md, VERIFIED live): -->
<!-- { "valid": true, "file": "...", "version": 2, "inventory": { "pylsd_mode": true/false, "deferred_4j": [...], ... } } -->
<!-- pylsd_mode path: .inventory.pylsd_mode (boolean) -->

<!-- lucy pylsd run --format json confirmed output shape (src/lucy_ng/cli/pylsd.py lines 268-274): -->
<!-- { "permutations": 4, "merged_count": 15, "ranked_solutions": [{"smiles": "...", "matched_count": 10, "mae": 1.8, "quality": "excellent"}, ...], "run_report_path": "..." } -->
<!-- quality field name confirmed: "quality" (from sol.quality_label, lsd.py line 295) -->

<!-- run_report.json schema (orchestrator.py lines 385-400): -->
<!-- { "total_permutations": 4, "unique_solutions": 15, "solutions": [{ "inchi_key": "...", "smiles": "...", "provenance": [{ "perm_index": 0, "include_flags": [true, false], "active_correlations": [{"atom1": 4, "atom2": 8}] }] }] } -->
<!-- per-permutation solution counts: NOT in run_report.json — derive from wc -l on perm_NN/solutions.smi -->

<!-- Current lsd-engineer.md lines that MUST be replaced (RESEARCH.md lines 135-146, PATTERNS.md lines 35-68): -->
<!-- Line 268: "- Run LSD from within iteration dir: `cd analysis/iteration_NN && lucy lsd run compound.lsd`" -->
<!-- Line 481: "11. Run LSD: `cd analysis/iteration_NN && lucy lsd run compound.lsd`" -->

<!-- Current ITERATION-COMPLETE last field (line 299, MUST BE PRESERVED): -->
<!-- "4J status: <"N deferred" / "added as final batch" / "skipped — solutions converged" / "N/A — no potential 4J">" -->

<!-- Current §1 ELIM section insertion point (lines 78-84): -->
<!-- "### ELIM Command (LAST RESORT)" subsection — new pyLSD vocabulary subsection inserts AFTER line 84 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert pyLSD vocabulary subsection in §1</name>
  <files>/Users/steinbeck/.claude/agents/lucy-lsd-engineer.md</files>
  <read_first>
    - /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md — read the ENTIRE file before any edits; locate the exact text of the `### ELIM Command (LAST RESORT)` subsection (around line 78-84) to confirm insertion anchor
    - /Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-PATTERNS.md — §"Change B: §1 pyLSD Command Vocabulary Subsection" for exact text of new subsection
    - /Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-RESEARCH.md — "Change 2: pyLSD Command Vocabulary" for the exact syntax block and pitfall 5 (FORM must use `;` prefix)
  </read_first>
  <action>
    Read the full file. Locate the `### ELIM Command (LAST RESORT)` subsection (currently around line 78-84). The subsection ends with the line "Start with `ELIM 1 0`, increment." (or similar closing instruction — confirm from file read).

    INSERT the following new subsection IMMEDIATELY AFTER the last line of the `### ELIM Command (LAST RESORT)` block (leave one blank line between):

    ```
    ### pyLSD Commands (pylsd_mode only)

    When `pylsd_mode=true`, LSDInputGenerator generates permutation files using these additional commands. lsd-engineer does NOT write these directly — PyLSDOrchestrator generates permutation files automatically. Document here for inspection and debugging of generated files:

    ```
    ; FORM C13H18O2    ; molecular formula — LSD comment (not a real command)
                       ; bare FORM rejected by LSD-3.4.9 error 102 — always use '; FORM' comment form
                       ; reference: .planning/findings/form-tolerance.md
    SHIX 3 125.5       ; assign 13C chemical shift 125.5 ppm to atom 3
    SHIH 3 7.2         ; assign 1H chemical shift 7.2 ppm to atom 3
    HMBC X Y 2 4       ; HMBC correlation with extended bond range 2-4 (correct 4J mechanism)
    HMBC X Y 2 4 ; ELIM  ; same + trailing '; ELIM' comment marks X-Y as PyLSDOrchestrator suspect
    ```

    **ELIM in pylsd_mode:** Do NOT write bare `ELIM N M` when `pylsd_mode=true`. G2 gate blocks it. Use `HMBC X Y 2 4 ; ELIM` instead (extended bond range with trailing comment for PyLSDOrchestrator permutation parsing). See §2 4J Deferral Rule and §5 Constraint Inventory.
    ```

    Do NOT modify any other §1 content. Do NOT modify §2-§5 or the message_interface section.
  </action>
  <verify>
    <automated>grep "pyLSD Commands" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md && grep "SHIX\|SHIH" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md && grep "; FORM" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md && grep "bare FORM rejected\|error 102" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md && grep "HMBC X Y 2 4" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md</automated>
  </verify>
  <acceptance_criteria>
    Positive (must match, exit 0):
    - grep "pyLSD Commands (pylsd_mode only)" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "SHIX.*125\|SHIX 3" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "SHIH.*7\.2\|SHIH 3" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "bare FORM rejected" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "error 102" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "HMBC X Y 2 4" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "HMBC X Y 2 4 ; ELIM" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md

    Negative (old state gone):
    - The §1 section contains the ELIM subsection FOLLOWED by the new pyLSD Commands subsection — confirm by reading lines around the insertion point
  </acceptance_criteria>
  <done>§1 of lucy-lsd-engineer.md contains a new '### pyLSD Commands (pylsd_mode only)' subsection with SHIX/SHIH syntax, the '; FORM' comment convention with error-102 note, and the HMBC X Y 2 4 / HMBC X Y 2 4 ; ELIM extended-range syntax. AGT-01 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Replace unconditional lucy lsd run with conditional routing block (lines 268 and 481) and extend ITERATION-COMPLETE for pylsd_mode</name>
  <files>/Users/steinbeck/.claude/agents/lucy-lsd-engineer.md</files>
  <read_first>
    - /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md — read the ENTIRE file again (it was just modified by Task 1); locate line 268 (§3 File Organization), line 481 (workflow step 11), and line 299 (4J status field in ITERATION-COMPLETE template). Confirm exact current text of all three anchors before writing.
    - /Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-PATTERNS.md — §"Change A: §'Run LSD' Routing Block" for exact replacement text at both locations; §"Change C: §4 ITERATION-COMPLETE Template Extension" for the exact pylsd_mode blocks to append
    - /Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/70-agent-skill-updates/70-RESEARCH.md — "Change 1: §'Run LSD' Routing Block" (routing block bash code), "Change 3: ITERATION-COMPLETE Extension" (D-20 blocks with exact JSON extraction commands), Pitfall 2 (must update BOTH locations), Pitfall 4 (4J status line must be PRESERVED, D-20 adds blocks below it)
  </read_first>
  <action>
    Read the full file. Make three targeted edits — do NOT modify any other content.

    **Edit 1 — §3 File Organization documentation string (current line ~268):**

    REPLACE the line:
    ```
    - Run LSD from within iteration dir: `cd analysis/iteration_NN && lucy lsd run compound.lsd`
    ```
    With:
    ```
    - Run LSD via solver-mode routing block (see workflow step 11 below for the conditional branch based on pylsd_mode)
    ```

    **Edit 2 — Workflow step 11 (current line ~481):**

    REPLACE the line:
    ```
    11. Run LSD: `cd analysis/iteration_NN && lucy lsd run compound.lsd`
    ```
    With the following block (step number and surrounding context preserved; replace just the content):
    ```
    11. Run LSD — determine solver mode from constraint inventory:

    ```bash
    # Read inventory to determine solver mode
    RESULT=$(lucy lsd validate-inventory --format json analysis/iteration_NN/compound.lsd)
    PYLSD_MODE=$(echo "$RESULT" | python3 -c \
      "import sys,json; d=json.load(sys.stdin); print(d.get('inventory',{}).get('pylsd_mode', False))")

    if [ "$PYLSD_MODE" = "True" ]; then
      # Multi-run mode: use PyLSDOrchestrator via lucy pylsd run
      lucy pylsd run analysis/iteration_NN/compound.lsd \
        --shifts "<comma_separated_13C_shifts>" \
        --format json | tee analysis/iteration_NN/pylsd_output.json
    else
      # Single-run mode: classic lucy lsd run (also the silent fallback for ABSENT inventory per D-19)
      cd analysis/iteration_NN && lucy lsd run compound.lsd
    fi
    ```

    **Note:** If `validate-inventory` returns `valid: false` with "No v2 inventory block found" (ABSENT case), `pylsd_mode` defaults to `False` via `.get('inventory',{}).get('pylsd_mode', False)` — the silent fallback to `lucy lsd run` is automatic (D-19). MALFORMED inventory is caught by G2/G3 gates before this step is reached (D-19a).
    ```

    **Edit 3 — ITERATION-COMPLETE template extension (current line ~299, after `4J status:` line):**

    LOCATE the line:
    ```
    4J status: <"N deferred" / "added as final batch" / "skipped — solutions converged" / "N/A — no potential 4J">
    ```
    PRESERVE that line exactly. INSERT the following AFTER it (one blank line separator):

    ```
    When `lucy pylsd run` was invoked (`pylsd_mode=true`), append two additional blocks after the `4J status:` line above:

    **Block 1 — Per-permutation table** (derive `solution_count` from `wc -l < pylsd_run/perm_NN/solutions.smi`; derive `defer_set` from `run_report.json` provenance `active_correlations` per `perm_index`; derive `top_rank_quality` from `ranked_solutions[].quality` for solutions from that permutation):

    ```
    | permutation_id | defer_set                      | solution_count | top_rank_quality |
    |----------------|-------------------------------|----------------|-----------------|
    | perm_00        | none                           | 7              | excellent        |
    | perm_01        | atom4-atom8                    | 12             | good             |
    | perm_02        | atom6-atom9                    | 0              | —                |
    | perm_03        | atom4-atom8 + atom6-atom9      | 3              | excellent        |
    ```

    **Block 2 — Aggregated** (extract from `analysis/iteration_NN/pylsd_output.json` written by `lucy pylsd run --format json`):

    ```
    Merged (unique InChI): <merged_count>
    Top-3 SMILES: <ranked_solutions[0].smiles[:60]> | <ranked_solutions[1].smiles[:60]> | <ranked_solutions[2].smiles[:60]>
    ```

    **Data extraction commands:**
    - `merged_count`: `python3 -c "import json; print(json.load(open('analysis/iteration_NN/pylsd_output.json'))['merged_count'])"`
    - Top-3 SMILES: `python3 -c "import json; d=json.load(open('analysis/iteration_NN/pylsd_output.json')); print(' | '.join(s['smiles'][:60] for s in d['ranked_solutions'][:3]))"`
    - Per-permutation `solution_count`: `wc -l < pylsd_run/perm_NN/solutions.smi` for each perm_NN directory
    - `defer_set`: from `run_report.json` — for each `perm_index`, collect `active_correlations` from provenance entries; format as `atomN-atomM` pairs joined with ` + `; use `none` when no correlations were active (all include_flags false)
    - `top_rank_quality`: read `quality` from `ranked_solutions` in `pylsd_output.json` for solutions whose provenance includes that `perm_index`
    ```
  </action>
  <verify>
    <automated>grep "lucy pylsd run" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md && grep "PYLSD_MODE" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md && grep "pylsd_output.json" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md && grep "permutation_id\|defer_set\|merged_count" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md && grep "4J status:" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md</automated>
  </verify>
  <acceptance_criteria>
    Positive (must match, exit 0):
    - grep "lucy pylsd run" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "PYLSD_MODE" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "pylsd_output.json" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "permutation_id" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "defer_set" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "merged_count" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "Top-3 SMILES" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
    - grep "4J status:" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md  (preserved)
    - grep "solver-mode routing block" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md  (§3 updated)
    - grep "pylsd_mode.*False\|\.get.*pylsd_mode.*False" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md  (D-19 fallback documented)

    Negative (old unconditional invocations gone):
    - grep -v "^#\|pylsd_run\|pylsd_output" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md | grep -c "cd analysis/iteration_NN && lucy lsd run compound.lsd"
      # should return 0 — both old unconditional forms replaced
    - grep -c "Run LSD: \`cd analysis" /Users/steinbeck/.claude/agents/lucy-lsd-engineer.md
      # should return 0 — step-11 old form gone
  </acceptance_criteria>
  <done>Both unconditional 'lucy lsd run' occurrences replaced: line 268 is now a prose pointer, line 481 is the full conditional routing block with D-19 fallback note. ITERATION-COMPLETE template extended with per-permutation table and aggregated block conditional on pylsd_mode; existing '4J status:' field preserved. AGT-02 and D-17/D-19/D-20 all satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill file → agent runtime | lsd-engineer reads this markdown before every CASE iteration; incorrect routing prose directly causes wrong CLI invocations |
| agent Bash → lucy pylsd run | The routing block emitted as skill prose becomes a bash command the agent runs; wrong flags or wrong condition logic breaks multi-run mode silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | Routing block bash syntax | mitigate | Routing block uses `.get('inventory',{}).get('pylsd_mode', False)` — safe against ABSENT inventory per D-19; acceptance criteria grep verifies the fallback expression is present |
| T-70-02 | Repudiation | Both unconditional lucy lsd run removed | mitigate | Two negative grep acceptance criteria confirm both old occurrences are gone; executor must remove both or the plan is incomplete |
| T-70-03 | Information Disclosure | SHIX/SHIH syntax in §1 | accept | Skill file is in ~/.claude/agents/ (user-local); no sensitive data; syntax is derived from Phase 66 source — ASSUMED label noted in RESEARCH.md |
| T-70-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After all tasks complete:
1. grep "pyLSD Commands (pylsd_mode only)" ~/.claude/agents/lucy-lsd-engineer.md exits 0
2. grep "SHIX\|SHIH" ~/.claude/agents/lucy-lsd-engineer.md exits 0
3. grep "bare FORM rejected\|error 102" ~/.claude/agents/lucy-lsd-engineer.md exits 0
4. grep "HMBC X Y 2 4" ~/.claude/agents/lucy-lsd-engineer.md exits 0
5. grep "PYLSD_MODE\|lucy pylsd run" ~/.claude/agents/lucy-lsd-engineer.md exits 0
6. grep "pylsd_output.json\|merged_count\|permutation_id" ~/.claude/agents/lucy-lsd-engineer.md exits 0
7. grep "4J status:" ~/.claude/agents/lucy-lsd-engineer.md exits 0 (preserved)
8. grep -c "cd analysis/iteration_NN && lucy lsd run compound.lsd" ~/.claude/agents/lucy-lsd-engineer.md returns 0
9. grep -c "lucy lsd run\|lucy pylsd run" ~/.claude/commands/lucy-ng/case.md returns 0 (case.md unchanged — AGT-03)
10. pytest -q exits 0 (no Python regression)
</verification>

<success_criteria>
- §1 contains '### pyLSD Commands (pylsd_mode only)' with SHIX/SHIH/; FORM/HMBC X Y 2 4 syntax
- Workflow step 11 contains the conditional routing bash block; §3 documentation string points to step 11
- D-19 fallback behavior (ABSENT inventory → silent lucy lsd run) is documented inline in the routing block
- ITERATION-COMPLETE template has per-permutation table + aggregated block conditional on pylsd_mode; '4J status:' field preserved
- case.md is untouched (AGT-03 satisfied via D-17 encapsulation principle)
</success_criteria>

<output>
Create .planning/phases/70-agent-skill-updates/70-01-SUMMARY.md when done
</output>
