---
phase: 86-ranker-path-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lucy_ng/prediction/resolver.py
  - src/lucy_ng/ranking/ranker.py
  - tests/test_ranking.py
autonomous: true
requirements: [RANK-01]
must_haves:
  truths:
    - "A SolutionRanker can be built from the SQLite DB backend (not only the JSON table)"
    - "Both lucy lsd rank and lucy predict c13 can resolve their C13Predictor through one shared helper that applies an identical DB-first backend ladder"
  artifacts:
    - path: "src/lucy_ng/prediction/resolver.py"
      provides: "Shared resolve_c13_predictor() helper implementing the canonical 4-tier backend ladder"
      contains: "def resolve_c13_predictor"
    - path: "src/lucy_ng/ranking/ranker.py"
      provides: "SolutionRanker.from_database classmethod mirroring from_table_file"
      contains: "def from_database"
    - path: "tests/test_ranking.py"
      provides: "Unit tests for from_database and the shared resolver against a deterministic temp_db"
      contains: "def test"
  key_links:
    - from: "src/lucy_ng/ranking/ranker.py"
      to: "C13Predictor.from_database"
      via: "from_database classmethod"
      pattern: "C13Predictor\\.from_database"
    - from: "src/lucy_ng/prediction/resolver.py"
      to: "DatabaseFinder.find_hose_database"
      via: "DB-first auto-detect ladder"
      pattern: "find_hose_database"
---

<objective>
Add the missing DB-backed factory and a single shared backend-resolution helper so the ranker
CAN use the same 7.9M-entry SQLite DB that `lucy predict c13` uses. This plan delivers the
building blocks (no CLI wiring yet — that is Plan 02).

Purpose: Eliminate the only divergence between `lucy lsd rank` and `lucy predict c13` (the
lookup backend) by giving the ranker a `from_database` factory and extracting the canonical
backend-priority ladder into one place both CLIs will call. This is the structural guarantee
behind RANK-01 ("single prediction path; no divergent code").

Output: `src/lucy_ng/prediction/resolver.py` (new, shared helper), `SolutionRanker.from_database`
(new classmethod), and unit tests pinning both against a deterministic temp_db.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-ranker-path-unification/86-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- Exact signatures the executor must mirror. Extracted from codebase this session. -->

src/lucy_ng/ranking/ranker.py:301-323 — the factory to mirror:
```python
@classmethod
def from_table_file(
    cls,
    table_path: str,
    tolerance: float = 3.0,
    max_radius: int = 6,
) -> "SolutionRanker":
    from pathlib import Path
    predictor = C13Predictor.from_table_file(Path(table_path), max_radius=max_radius)
    return cls(predictor, tolerance=tolerance)
```

src/lucy_ng/prediction/predictor.py:61-75 — the DB factory to call:
```python
@classmethod
def from_database(cls, db_path: Path | str, **kwargs) -> "C13Predictor":
    from .db_lookup import DatabaseHOSELookup
    lookup = DatabaseHOSELookup.from_db_path(Path(db_path))
    return cls(lookup, **kwargs)
```

src/lucy_ng/cli/predict.py:72-115 — the canonical 4-tier ladder to extract verbatim
(behaviour, not the click.echo error strings):
  1. explicit db path     -> C13Predictor.from_database(Path(db), max_radius=...)
  2. explicit table path  -> C13Predictor.from_table_file(Path(table), max_radius=...)
  3. DatabaseFinder.find_hose_database()  -> from_database   (default hit)
  4. DatabaseFinder.find_hose_table()     -> from_table_file (fallback)
  else -> raise (no backend found)

src/lucy_ng/database/finder.py — finder methods (both exist):
  DatabaseFinder.find_hose_database() -> Path | None
  DatabaseFinder.find_hose_table() -> Path | None   # candidates do NOT include hose_nmrshiftdb.json.gz (see Trap)

tests/test_prediction.py:622-644 — temp_db fixture pattern to mirror:
```python
from lucy_ng.database import DatabaseManager
from lucy_ng.database.models import HOSEStatsRecord
db_path = tmp_path / "test_predict.db"
db = DatabaseManager(db_path); db.create_tables()
db.insert_hose_stats_batch([HOSEStatsRecord(hose_code=..., radius=..., mean=..., std=..., count=...)])
db.close()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SolutionRanker.from_database classmethod</name>
  <files>src/lucy_ng/ranking/ranker.py, tests/test_ranking.py</files>
  <read_first>
    - src/lucy_ng/ranking/ranker.py (read the whole class, especially from_table_file at lines 301-323 and the __init__ signature it calls — `cls(predictor, tolerance=...)`)
    - src/lucy_ng/prediction/predictor.py:61-75 (C13Predictor.from_database signature — note db_path is `Path | str`, accepts `max_radius` via **kwargs)
    - tests/test_prediction.py:622-672 (temp_db_with_ethanol fixture + test_from_database_classmethod / test_predict_from_database — mirror this exact fixture style)
    - tests/test_ranking.py (existing test conventions; locate test_from_table_file_integration ~line 455 and the MagicMock(spec=C13Predictor) tests ~line 478 to match house style)
  </read_first>
  <behavior>
    - Test: SolutionRanker.from_database(temp_db_path) returns a SolutionRanker instance whose predictor is DB-backed (predictor.lookup is a DatabaseHOSELookup).
    - Test: SolutionRanker.from_database(temp_db_path, tolerance=2.0, max_radius=4) propagates tolerance to the ranker and max_radius to the predictor.
    - Test: a ranker built from_database can rank a trivial SMILES list against shifts without raising (smoke).
  </behavior>
  <action>
    Add a `from_database` classmethod to `SolutionRanker` directly mirroring `from_table_file`
    (ranker.py:301-323): same signature shape with `db_path: str | Path`, `tolerance: float = 3.0`,
    `max_radius: int = 6`, returning `"SolutionRanker"`. Body: call
    `C13Predictor.from_database(Path(db_path), max_radius=max_radius)` then
    `return cls(predictor, tolerance=tolerance)`. Full type annotations required (mypy strict):
    accept `str | Path` for `db_path` and annotate the return type as the forward-ref string
    `"SolutionRanker"` exactly as from_table_file does. Add a docstring mirroring from_table_file's,
    swapping "HOSE lookup table file" wording for "SQLite database with HOSE statistics".
    Do NOT widen or change from_table_file. Do NOT touch _match_shifts, rank, or any matching/MAE
    logic — research confirms the algorithm is identical and correct.
    Add tests to tests/test_ranking.py: create a temp_db fixture mirroring
    tests/test_prediction.py:622-644 (DatabaseManager + create_tables + insert_hose_stats_batch
    of a few HOSEStatsRecord rows, then close), and add the three behavior tests above. Assert
    DB-backing via `isinstance(ranker.predictor.lookup, DatabaseHOSELookup)`.
  </action>
  <verify>
    <automated>pytest tests/test_ranking.py -k from_database -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def from_database" src/lucy_ng/ranking/ranker.py` returns a match inside the SolutionRanker class.
    - `pytest tests/test_ranking.py -k from_database -x` exits 0 with at least 3 tests collected.
    - `mypy src/lucy_ng/ranking/ranker.py` reports no errors on the new method.
    - `ruff check src/lucy_ng/ranking/ranker.py tests/test_ranking.py` is clean.
    - `grep -c "def _match_shifts" src/lucy_ng/ranking/ranker.py` still equals 1 and that function body is unchanged (no diff inside _match_shifts).
  </acceptance_criteria>
  <done>SolutionRanker.from_database exists, builds a DatabaseHOSELookup-backed ranker, propagates tolerance and max_radius, and its tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract shared resolve_c13_predictor() backend-ladder helper</name>
  <files>src/lucy_ng/prediction/resolver.py, tests/test_ranking.py</files>
  <read_first>
    - src/lucy_ng/cli/predict.py:48-115 (the canonical 4-tier ladder — copy its BEHAVIOUR exactly: explicit db, explicit table, find_hose_database(), find_hose_table(), else error)
    - src/lucy_ng/database/finder.py:97-136 (find_hose_database and find_hose_table — note find_hose_table candidates do NOT include hose_nmrshiftdb.json.gz; see Trap below)
    - src/lucy_ng/cli/lsd.py:113-139 (_get_default_table_path — the JSON fallback that DOES locate hose_nmrshiftdb.json.gz, the file that actually ships)
    - src/lucy_ng/prediction/predictor.py:35-75 (from_table_file and from_database factory signatures)
  </read_first>
  <behavior>
    - Test: resolve_c13_predictor(db="<temp_db>") returns a DB-backed C13Predictor (lookup is DatabaseHOSELookup).
    - Test: resolve_c13_predictor(table="<temp_json_table>") returns a table-backed C13Predictor.
    - Test: with no db/table args and DatabaseFinder.find_hose_database monkeypatched to return the temp_db, resolve_c13_predictor() returns the DB-backed predictor (priority 3 ahead of any table).
    - Test: with db=None, table=None, and both finders monkeypatched to return None, resolve_c13_predictor() raises a clear error (no backend found).
    - Test: max_radius is propagated to the constructed predictor.
  </behavior>
  <action>
    Create `src/lucy_ng/prediction/resolver.py` exposing one function
    `resolve_c13_predictor(db: str | Path | None = None, table: str | Path | None = None, max_radius: int = 6) -> C13Predictor`.
    Implement the exact 4-tier priority from cli/predict.py:72-115, in order:
    (1) explicit `db` -> `C13Predictor.from_database(Path(db), max_radius=max_radius)`;
    (2) explicit `table` -> `C13Predictor.from_table_file(Path(table), max_radius=max_radius)`;
    (3) `DatabaseFinder.find_hose_database()` truthy -> from_database;
    (4) JSON fallback: locate the shipped table. TRAP (research Pitfall 3): DatabaseFinder.find_hose_table()
    looks for `hose_lookup.json.gz` which does NOT exist on disk; the file that ships is
    `hose_nmrshiftdb.json.gz`, found by lsd.py's `_get_default_table_path()`. To avoid a regressed
    fallback, the helper's step 4 MUST try `DatabaseFinder.find_hose_table()` first and, if that
    returns None, fall back to the lsd.py `_get_default_table_path()` lookup so the real shipped
    table is found. (Import _get_default_table_path from cli.lsd, or replicate its three-location
    search — prefer importing to keep one source of truth; if importing creates a cycle, replicate
    the three candidate paths verbatim.)
    Else raise a `RuntimeError` (or FileNotFoundError) with a message naming the two remedies
    ("lucy database download" / "lucy predict build-table") mirroring predict.py:108-114.
    Full type annotations (mypy strict). Do NOT emit click.echo from the helper — it is library
    code; raise exceptions and let CLIs translate to SystemExit (Plan 02 wires that).
    Add tests to tests/test_ranking.py covering the five behaviors above. Use monkeypatch on
    `lucy_ng.database.finder.DatabaseFinder.find_hose_database` /`find_hose_table` for the
    auto-detect and no-backend cases. For the table-backed case, build a tiny JSON HOSE table
    file (reuse the HOSELookupTable save/load convention used by existing prediction tests, or a
    minimal .json the loader accepts).
  </action>
  <verify>
    <automated>pytest tests/test_ranking.py -k "resolver or resolve" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def resolve_c13_predictor" src/lucy_ng/prediction/resolver.py` returns a match.
    - The helper contains the DB-first ordering: `grep -n "find_hose_database" src/lucy_ng/prediction/resolver.py` appears BEFORE the table-fallback lookup in the file.
    - `pytest tests/test_ranking.py -k "resolver or resolve" -x` exits 0 with at least 5 tests collected.
    - The "no backend found" test asserts a raised exception (not a SystemExit) — `pytest.raises(Exception)` with a message containing "lucy database download".
    - `mypy src/lucy_ng/prediction/resolver.py` reports no errors.
    - `ruff check src/lucy_ng/prediction/resolver.py` is clean.
  </acceptance_criteria>
  <done>resolve_c13_predictor exists, applies the DB-first 4-tier ladder, locates the real shipped JSON table in fallback, raises cleanly when no backend exists, and its tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI → local SQLite DB / local JSON table | Operator-supplied local file paths read for prediction; no network, no auth, no untrusted external input introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Information Disclosure | local DB/table path handling in resolve_c13_predictor | accept | Offline single-user dev tooling on local files the operator already owns; no PII, no network surface, no privilege boundary crossed. Paths are operator-provided. |
| T-86-02 | Denial of Service | malformed/huge local DB passed via --db (Plan 02 wiring) | accept | Operator-controlled local input; failure is a local crash with no blast radius. Existing from_database/from_table_file error handling surfaces load errors. |
| T-86-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (existing-codebase refactor; RESEARCH.md confirms zero new dependencies). No slopcheck needed. |

No high-severity threats: this is offline developer tooling operating on local NMR data and a local SQLite DB — no network, no auth, no untrusted external input surface introduced.
</threat_model>

<verification>
- `pytest tests/test_ranking.py tests/test_prediction.py -x` green (new tests pass, no regressions).
- `mypy src/lucy_ng` clean (strict).
- `ruff check src tests` clean.
- The new from_database and resolve_c13_predictor are the ONLY new public symbols; _match_shifts unchanged.
</verification>

<success_criteria>
- SolutionRanker.from_database builds a DB-backed ranker (RANK-01 building block).
- resolve_c13_predictor applies one canonical DB-first ladder reusable by both CLIs (RANK-01 single-path guarantee).
- All new unit tests pass against a deterministic temp_db; full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/86-ranker-path-unification/86-01-SUMMARY.md` when done.
</output>
