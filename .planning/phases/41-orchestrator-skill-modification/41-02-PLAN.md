---
phase: 41-orchestrator-skill-modification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/.claude/commands/lucy-ng/case.md"
autonomous: true

must_haves:
  truths:
    - "case.md uses TeamCreate to initialize a team namespace instead of spawning a single Task()"
    - "case.md spawns 4 specialist teammates via Task(team_name) — nmr-chemist, lsd-engineer, solution-analyst, devils-advocate"
    - "Advisory interventions are delivered via SendMessage to running team (not agent re-spawn)"
    - "Team lifecycle is managed: spawn, monitor, terminate (shutdown_request + TeamDelete)"
    - "validate_prerequisites checks CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 env var"
    - "Loop detection logic and per-pattern counters preserved unchanged from v3.0"
    - "All unchanged steps preserved verbatim: parse_arguments, detect_loops, diagnose, intervene, track_and_decide, escalate, delegate_specialist, extract_diagnostic_findings, present_results"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "Team-based CASE orchestrator skill"
      contains: "TeamCreate"
  key_links:
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "~/.claude/agents/lucy-nmr-chemist.md"
      via: "Task(subagent_type='lucy-nmr-chemist', team_name=...)"
      pattern: "lucy-nmr-chemist"
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "~/.claude/agents/lucy-lsd-engineer.md"
      via: "Task(subagent_type='lucy-lsd-engineer', team_name=...)"
      pattern: "lucy-lsd-engineer"
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "~/.claude/agents/lucy-solution-analyst.md"
      via: "Task(subagent_type='lucy-solution-analyst', team_name=...)"
      pattern: "lucy-solution-analyst"
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "~/.claude/agents/lucy-devils-advocate.md"
      via: "Task(subagent_type='lucy-devils-advocate', team_name=...)"
      pattern: "lucy-devils-advocate"
---

<objective>
Rewrite the case.md orchestrator skill to use the Claude Code Agent Teams API. Replace the single-agent Task(lucy-case-agent) spawn pattern with TeamCreate + 4 specialist teammates. Replace agent re-spawn intervention with SendMessage advisory delivery. Add team lifecycle management (terminate step).

Purpose: This is the core Phase 41 deliverable. The orchestrator skill becomes a team lead that manages 4 specialists instead of spawning and re-spawning a monolithic autonomous agent.

Output: Modified ~/.claude/commands/lucy-ng/case.md with team-based orchestration
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-orchestrator-skill-modification/41-RESEARCH.md

# CRITICAL: Read current case.md in full before any modifications
@~/.claude/commands/lucy-ng/case.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontmatter and rewrite spawn + prerequisites steps</name>
  <files>~/.claude/commands/lucy-ng/case.md</files>
  <action>
Read the full current case.md (~693 lines). This task modifies the first half of the file.

**1. Update YAML frontmatter:**

Change the description and allowed-tools:

```yaml
---
name: lucy-ng:case
description: "Orchestrate 4-agent CASE team for NMR structure elucidation. Spawns specialist team via TeamCreate, monitors progress, detects loops, intervenes with advisory constraints via SendMessage."
argument-hint: "<compound_path> <formula>"
allowed-tools:
  - Bash
  - Read
  - Write
  - Glob
  - Grep
  - Task
  - TeamCreate
  - TeamDelete
  - TaskCreate
  - TaskList
  - TaskUpdate
  - SendMessage
---
```

**2. Update `<objective>` section:**

Replace "spawning the autonomous CASE agent" with "spawning a 4-agent specialist team (nmr-chemist, lsd-engineer, solution-analyst, devils-advocate)". Replace "delegates to the lucy-case-agent" with "delegates to a specialist team and handles supervision as team lead". Keep the dereplication prohibition unchanged.

**3. Keep `parse_arguments` step UNCHANGED — do not modify.**

**4. Rewrite `validate_prerequisites` step:**

Add a 5th prerequisite check BEFORE the existing 4 checks:

```markdown
0. **Agent Teams feature enabled:**
   ```bash
   echo $CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS
   ```
   If not "1": "Agent Teams required. Run: export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 (or add to ~/.claude/settings.json under env)"
```

Keep the existing 4 checks (lucy --version, lucy lsd check, prediction, compound directory) unchanged.

**5. Replace `spawn_case_agent` step with `spawn_case_team` step:**

Delete the entire existing spawn_case_agent step. Replace with:

```markdown
<step name="spawn_case_team">
Spawn the 4-agent CASE specialist team via TeamCreate and Task(team_name).

**CRITICAL:** The orchestrator skill IS the team lead (coordinator). Do NOT spawn a coordinator agent — the skill itself manages the team, creates tasks, delivers advisories, and handles lifecycle.

**Step 1: Initialize team namespace**

```
TeamCreate(
  team_name="case-{compound_name}",
  description="CASE workflow for {compound_path} — formula {formula}"
)
```

Where {compound_name} is the last path component of compound_path (e.g., "Ibuprofen" from "data/Ibuprofen").

**Step 2: Create initial shared tasks**

```
TaskCreate(
  subject="peak-picking",
  description="Pick 13C, HSQC, HMBC peaks for {compound_path} with formula {formula}.
               Run statistical detection (hybridisation, neighbours, hhb, grouping).
               Send structured peak assignments to lsd-engineer via SendMessage.
               Write analysis/ directory structure.
               Log results to analysis/CASE-PROGRESS.md.",
  activeForm="Picking NMR peaks"
)

TaskCreate(
  subject="lsd-iteration-01",
  description="Build initial LSD file from peak assignments. Use HSQC for MULT definitions,
               add first HMBC batch (3-5 high-confidence correlations). Run LSD from
               analysis/iteration_01/ directory. Log to analysis/CASE-PROGRESS.md.
               Create lsd-iteration-02 task when done.
               Convert solutions: outlsd 5 < compound.sol > solutions.smi",
  activeForm="Running LSD iteration 1"
)
```

**Step 3: Spawn 4 specialist teammates**

```
Task(
  name="nmr-chemist",
  team_name="case-{compound_name}",
  subagent_type="lucy-nmr-chemist",
  model="opus",
  prompt="You are the NMR chemist for CASE of {formula} at {compound_path}.
          Check TaskList for available tasks. Claim peak-picking task.
          Execute peak picking and statistical detection using lucy CLI.
          Send structured results to lsd-engineer via SendMessage.
          Mark task completed when done. Check TaskList for more work."
)

Task(
  name="lsd-engineer",
  team_name="case-{compound_name}",
  subagent_type="lucy-lsd-engineer",
  model="opus",
  prompt="You are the LSD engineer for CASE of {formula} at {compound_path}.
          Wait for peak assignments from nmr-chemist via SendMessage.
          Claim iteration tasks from TaskList. Build LSD files in
          analysis/iteration_NN/ directories. Run LSD solver. Log to
          analysis/CASE-PROGRESS.md after EVERY iteration.
          Create next iteration task when current one completes.
          Convert solutions: outlsd 5 < compound.sol > solutions.smi
          CRITICAL: Read previous LSD file, never reconstruct from memory.
          Follow incremental HMBC strategy: 3-5 correlations per iteration.
          Stop when solution_count <= 10 or ~10 iterations reached."
)

Task(
  name="solution-analyst",
  team_name="case-{compound_name}",
  subagent_type="lucy-solution-analyst",
  model="opus",
  prompt="You are the solution analyst for CASE of {formula} at {compound_path}.
          Monitor TaskList for ranking tasks. When solution_count <= 10,
          claim ranking task. Run: lucy lsd rank solutions.smi --shifts '...'
          Assess chemical plausibility of top candidates.
          Write analysis/final_results.md with ranked structures.
          Send summary to team lead via SendMessage."
)

Task(
  name="devils-advocate",
  team_name="case-{compound_name}",
  subagent_type="lucy-devils-advocate",
  model="opus",
  prompt="You are the devils advocate for CASE of {formula} at {compound_path}.
          Monitor analysis/CASE-PROGRESS.md after each iteration.
          After each LSD iteration logged by lsd-engineer:
          1. Diff current LSD file against previous iteration
          2. Check for dropped constraints (DEFF NOT, SYME, grouped notation)
          3. Verify sp2 count is even, H budget matches formula
          4. Flag issues to team lead via SendMessage if problems found
          Report constraint persistence status after every iteration."
)
```

The orchestrator then enters the monitor_progress step, waiting for team messages and polling progress.
</step>
```

  </action>
  <verify>
Read the modified case.md. Verify:

1. YAML frontmatter includes TeamCreate, TeamDelete, TaskCreate, TaskList, TaskUpdate, SendMessage in allowed-tools
2. Step `spawn_case_agent` is GONE — replaced by `spawn_case_team`
3. `spawn_case_team` contains TeamCreate, TaskCreate (x2 minimum), Task (x4 for each specialist)
4. validate_prerequisites has 5 checks (env var + original 4)
5. parse_arguments step is UNCHANGED from v3.0
6. No references to "lucy-case-agent" in spawn step (replaced by 4 specialist names)

```bash
grep -c "TeamCreate" ~/.claude/commands/lucy-ng/case.md
grep -c "spawn_case_agent" ~/.claude/commands/lucy-ng/case.md
grep -c "spawn_case_team" ~/.claude/commands/lucy-ng/case.md
grep -c "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" ~/.claude/commands/lucy-ng/case.md
```

Expected: TeamCreate >= 1, spawn_case_agent = 0, spawn_case_team >= 1, env var >= 1.
  </verify>
  <done>case.md frontmatter updated with team tools. validate_prerequisites includes Agent Teams env var check. spawn_case_agent fully replaced by spawn_case_team with TeamCreate + 4 Task(team_name) specialist spawns. parse_arguments unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Replace respawn with deliver_advisory, add terminate_team, update monitor_progress and anti_patterns</name>
  <files>~/.claude/commands/lucy-ng/case.md</files>
  <action>
Continue modifying case.md. This task handles the second half of the file.

**1. Rewrite `monitor_progress` step:**

Replace the current step (which reads CASE-PROGRESS.md after agent batch completes) with continuous team monitoring:

```markdown
<step name="monitor_progress">
Monitor team progress via TaskList polling and incoming SendMessage notifications.

**Monitoring loop:**

The orchestrator (team lead) monitors progress by:

1. **Receiving messages from teammates:** Messages from devils-advocate (constraint issues), solution-analyst (ranking complete), or lsd-engineer (iteration progress) arrive automatically as conversation turns.

2. **Polling TaskList periodically:** Check task completion status to track which iteration the team has reached.
   ```
   TaskList()
   ```

3. **Reading CASE-PROGRESS.md:** When lsd-engineer completes an iteration task, read the updated progress file:
   ```bash
   cat <compound_path>/analysis/CASE-PROGRESS.md
   ```

**Parse the iteration history from CASE-PROGRESS.md:**
- Extract solution count for each iteration
- Extract constraints added/removed from each iteration
- Extract sp2 checks and H budget status from Notes sections
- Count HMBC correlations used (X/Y from each iteration)
- Identify last iteration number

**Check for completion signals:**
- **Solution count <= 10:** Proceed to present_results
- **Ranking task completed:** Solution-analyst has written final_results.md, proceed to present_results
- **Devils-advocate flags issue:** Process the flag (may trigger detect_loops)
- **10 iterations reached:** Safety cap, proceed to present_results with caveats
- **No progress after extended wait:** Check if team is stuck, may need advisory intervention

Store parsed data for loop detection.
</step>
```

**2. Replace `respawn` step with `deliver_advisory` step:**

Delete the entire existing `respawn` step. Replace with:

```markdown
<step name="deliver_advisory">
Deliver advisory constraints to the running team via SendMessage. This REPLACES the v3.0 pattern of killing and re-spawning the agent.

**Why SendMessage instead of re-spawn:**
- Team retains all context and state (no restart overhead)
- Constraint inventory and progress are preserved
- Devils-advocate can immediately verify the fix
- No risk of losing team coordination state

**Deliver advisory to lsd-engineer (primary recipient):**
```
SendMessage(
  type="message",
  recipient="lsd-engineer",
  content="[SUPERVISOR ADVISORY] {advisory_text_from_intervene_step}

  Apply this advisory guidance in your next iteration. Read CASE-PROGRESS.md
  for current state. Resume from iteration {N+1}.

  Do NOT restart from scratch. Apply the fix to your existing constraint set.",
  summary="Supervisor advisory: {pattern_name} detected"
)
```

**Notify devils-advocate to monitor the fix:**
```
SendMessage(
  type="message",
  recipient="devils-advocate",
  content="[SUPERVISOR] {pattern_name} detected. Advisory sent to lsd-engineer:
           {brief_summary_of_advisory}
           Monitor the next iteration. Verify the fix is applied correctly.
           Report back if the issue persists or new problems arise.",
  summary="Monitor advisory fix for {pattern_name}"
)
```

After delivering advisory, return to monitor_progress step to check if issue resolves.
</step>
```

**3. Add new `terminate_team` step AFTER present_results:**

Insert this new step between `present_results` and the closing `</process>` tag:

```markdown
<step name="terminate_team">
Gracefully shut down all teammates and clean up team resources.

**This step executes after:** present_results (successful or failed CASE), OR escalate (user escalation).

**Step 1: Send shutdown requests to all 4 teammates:**
```
SendMessage(type="shutdown_request", recipient="nmr-chemist",
            content="CASE workflow complete. Shutting down team.")
SendMessage(type="shutdown_request", recipient="lsd-engineer",
            content="CASE workflow complete. Shutting down team.")
SendMessage(type="shutdown_request", recipient="solution-analyst",
            content="CASE workflow complete. Shutting down team.")
SendMessage(type="shutdown_request", recipient="devils-advocate",
            content="CASE workflow complete. Shutting down team.")
```

**Step 2: Wait for shutdown confirmations.**
Teammates approve shutdown automatically (they have no reason to reject after CASE completes).

**Step 3: Clean up team resources:**
```
TeamDelete()
```

This removes:
- ~/.claude/teams/case-{compound_name}/
- ~/.claude/tasks/case-{compound_name}/

**If TeamDelete fails (teammates still active):** Wait 30 seconds and retry. If still failing, report: "Team cleanup incomplete. Manual cleanup may be needed: rm -rf ~/.claude/teams/case-{compound_name} ~/.claude/tasks/case-{compound_name}"
</step>
```

**4. Update `track_and_decide` step:**

In the decision logic section, change all references from "proceed to respawn step" to "proceed to deliver_advisory step". Specifically:

- "proceed to respawn step" → "proceed to deliver_advisory step" (3 occurrences)
- The step logic itself is UNCHANGED — only the step name references change

**5. Update `anti_patterns` section:**

Replace the existing anti-pattern entries that reference the old single-agent pattern. Specifically:

- **Replace** "Never spawn agent per iteration" with:
  ```markdown
  **Never re-spawn the team to deliver an advisory:**
  - v3.0 killed and re-spawned the agent with advisory text — this is INVALID with teams
  - Use SendMessage to deliver advisory to the running team
  - Team retains context, constraint inventory, and coordination state
  - Re-spawning creates isolated agents outside the team
  ```

- **Add** new anti-pattern:
  ```markdown
  **Never call TeamDelete before shutdown_request:**
  - TeamDelete fails if teammates are still active
  - Always send shutdown_request to all 4 teammates first
  - Wait for shutdown confirmations before cleanup
  ```

- **Update** the "Never duplicate agent knowledge" entry:
  Replace "The lucy-case-agent already has 500-700 lines" with "The specialist agents each have their own domain knowledge (Phase 42 creates full definitions). Task(team_name) prompts should provide ONLY task-specific details (compound path, formula). Agent definitions contain the domain expertise."

- Keep all other anti-patterns unchanged: "Never use directive interventions", "Never use a global intervention counter", "Never attempt dereplication", "Never delegate to specialist on first loop detection"

**6. Update `delegate_specialist` step:**

The diagnostic specialist is spawned OUTSIDE the team (by design — objectivity). No change to the Task() call (it does NOT use team_name). However, update the text to clarify this is intentional:

Add after the "This step executes when" line:
```markdown
**Note:** The diagnostic specialist is spawned via Task() WITHOUT team_name — it operates independently from the team for objectivity. It reads team artifacts (CASE-PROGRESS.md, LSD files) but does not participate in team communication.
```

**7. Update `extract_diagnostic_findings` step:**

Change "Proceed to respawn step" references to "Proceed to deliver_advisory step" (2 occurrences in this step).
  </action>
  <verify>
Read the modified case.md. Verify:

1. No `respawn` step exists — replaced by `deliver_advisory`
2. `deliver_advisory` uses SendMessage (not Task re-spawn)
3. `terminate_team` step exists with shutdown_request x4 + TeamDelete
4. `monitor_progress` references TaskList polling and incoming messages
5. No references to "respawn" remain in track_and_decide or extract_diagnostic_findings
6. Anti-patterns section updated (no re-spawn, TeamDelete after shutdown)
7. delegate_specialist clarifies independent-from-team spawning
8. All UNCHANGED steps are still present and unmodified: parse_arguments, detect_loops, diagnose, intervene, escalate, present_results, loop_detection_reference

```bash
grep -c "deliver_advisory" ~/.claude/commands/lucy-ng/case.md
grep -c "terminate_team" ~/.claude/commands/lucy-ng/case.md
grep -c "respawn" ~/.claude/commands/lucy-ng/case.md
grep -c "SendMessage" ~/.claude/commands/lucy-ng/case.md
grep -c "TeamDelete" ~/.claude/commands/lucy-ng/case.md
grep -c "detect_loops" ~/.claude/commands/lucy-ng/case.md
grep -c "track_and_decide" ~/.claude/commands/lucy-ng/case.md
```

Expected: deliver_advisory >= 3, terminate_team >= 1, respawn = 0, SendMessage >= 5, TeamDelete >= 1, detect_loops >= 1, track_and_decide >= 1.
  </verify>
  <done>case.md fully converted to team-based orchestration. respawn step replaced by deliver_advisory using SendMessage. terminate_team step added with graceful shutdown sequence. monitor_progress updated for continuous team monitoring. All step name references updated (respawn -> deliver_advisory). Anti-patterns updated for team context. All unchanged steps (parse_arguments, detect_loops, diagnose, intervene, track_and_decide, escalate, delegate_specialist, extract_diagnostic_findings, present_results, loop_detection_reference) preserved verbatim.</done>
</task>

</tasks>

<verification>
- [ ] case.md YAML frontmatter includes all team tools (TeamCreate, TeamDelete, TaskCreate, TaskList, TaskUpdate, SendMessage)
- [ ] `spawn_case_agent` step is gone, replaced by `spawn_case_team`
- [ ] `spawn_case_team` calls TeamCreate once, TaskCreate 2+, Task(team_name) 4 times (one per specialist)
- [ ] 4 specialist names correct: nmr-chemist, lsd-engineer, solution-analyst, devils-advocate
- [ ] `respawn` step is gone, replaced by `deliver_advisory`
- [ ] `deliver_advisory` uses SendMessage to lsd-engineer and devils-advocate (not Task re-spawn)
- [ ] `terminate_team` step exists with shutdown_request to all 4 + TeamDelete
- [ ] `validate_prerequisites` checks CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS env var
- [ ] `monitor_progress` references TaskList polling and message reception
- [ ] `track_and_decide` references deliver_advisory (not respawn)
- [ ] `extract_diagnostic_findings` references deliver_advisory (not respawn)
- [ ] All unchanged steps preserved: parse_arguments, detect_loops, diagnose, intervene, escalate, delegate_specialist, extract_diagnostic_findings, present_results, loop_detection_reference
- [ ] Anti-patterns updated for team context
- [ ] No references to "lucy-case-agent" in spawn or respawn context (may remain in historical comments)
- [ ] Zero occurrences of step name "respawn" in the file
</verification>

<success_criteria>
case.md orchestrator skill fully converted from single-agent Task() pattern to team-based TeamCreate + Task(team_name) pattern. Advisory delivery uses SendMessage instead of re-spawn. Team lifecycle managed with terminate_team step. All loop detection, diagnosis, intervention, and escalation logic preserved from v3.0. File is a working orchestrator skill that can spawn and manage a 4-specialist CASE team.
</success_criteria>

<output>
After completion, create `.planning/phases/41-orchestrator-skill-modification/41-02-SUMMARY.md`
</output>
