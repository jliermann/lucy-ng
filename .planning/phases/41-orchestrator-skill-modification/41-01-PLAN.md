---
phase: 41-orchestrator-skill-modification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/.claude/agents/lucy-nmr-chemist.md"
  - "~/.claude/agents/lucy-lsd-engineer.md"
  - "~/.claude/agents/lucy-solution-analyst.md"
  - "~/.claude/agents/lucy-devils-advocate.md"
autonomous: true

must_haves:
  truths:
    - "4 stub agent definition files exist and are discoverable by Claude Code"
    - "Each stub has correct YAML frontmatter with name, description, tools"
    - "Stubs are minimal (15-30 lines each) — just enough to confirm team spawning works"
    - "Agent names match exactly what case.md will reference in spawn step"
  artifacts:
    - path: "~/.claude/agents/lucy-nmr-chemist.md"
      provides: "NMR chemist stub agent definition"
      contains: "name: lucy-nmr-chemist"
    - path: "~/.claude/agents/lucy-lsd-engineer.md"
      provides: "LSD engineer stub agent definition"
      contains: "name: lucy-lsd-engineer"
    - path: "~/.claude/agents/lucy-solution-analyst.md"
      provides: "Solution analyst stub agent definition"
      contains: "name: lucy-solution-analyst"
    - path: "~/.claude/agents/lucy-devils-advocate.md"
      provides: "Devils advocate stub agent definition"
      contains: "name: lucy-devils-advocate"
  key_links:
    - from: "~/.claude/agents/lucy-*.md"
      to: "~/.claude/commands/lucy-ng/case.md"
      via: "subagent_type in Task(team_name) spawn calls"
      pattern: "subagent_type.*lucy-(nmr-chemist|lsd-engineer|solution-analyst|devils-advocate)"
---

<objective>
Create 4 minimal stub agent definitions for the CASE team specialists. These stubs enable early validation of the Agent Teams API (TeamCreate + Task(team_name)) before Phase 42 creates the full agent definitions with distributed domain knowledge.

Purpose: Phase 41 modifies case.md to spawn 4 specialist teammates. Those Task(team_name, subagent_type) calls reference agent definition files that don't exist yet (Phase 42 creates them). Stubs bridge this gap — just enough YAML frontmatter and a placeholder role description for the agent to be discoverable and spawnable.

Output: 4 agent files at ~/.claude/agents/lucy-{role}.md (nmr-chemist, lsd-engineer, solution-analyst, devils-advocate)
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-orchestrator-skill-modification/41-RESEARCH.md

# Reference: existing agent definition patterns
@~/.claude/agents/lucy-case-agent.md (first 20 lines — YAML frontmatter pattern)
@~/.claude/agents/lucy-diagnostic.md (first 20 lines — YAML frontmatter pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 stub agent definitions</name>
  <files>
    ~/.claude/agents/lucy-nmr-chemist.md
    ~/.claude/agents/lucy-lsd-engineer.md
    ~/.claude/agents/lucy-solution-analyst.md
    ~/.claude/agents/lucy-devils-advocate.md
  </files>
  <action>
Create 4 minimal stub agent definition files. Each file follows the same YAML frontmatter pattern as the existing lucy-case-agent.md and lucy-diagnostic.md.

**Template for each stub** (adapt name, description, and role for each specialist):

```markdown
---
name: lucy-{role}
description: "STUB — v4.0 Phase 41 placeholder. Full definition created in Phase 42. {one-line role description}"
tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
---

<role>
You are the {Role Name} specialist in the CASE team for NMR structure elucidation.

**THIS IS A STUB DEFINITION** for Phase 41 API validation. Full domain knowledge and workflow instructions will be added in Phase 42.

Your responsibilities (placeholder):
- {2-3 bullet points describing the role's key responsibilities}

Claim tasks from TaskList. Report results via SendMessage to team lead. Mark tasks completed via TaskUpdate.
</role>
```

**Specific roles:**

1. **lucy-nmr-chemist** — NMR spectral analysis specialist
   - Pick 13C, HSQC, HMBC peaks from Bruker data
   - Run statistical detection (hybridisation, neighbours, hhb, grouping)
   - Send structured peak assignments to lsd-engineer

2. **lucy-lsd-engineer** — LSD constraint building specialist
   - Build LSD files from peak assignments and constraints
   - Manage constraint inventory (read previous file, never reconstruct from memory)
   - Run LSD solver and log results to CASE-PROGRESS.md

3. **lucy-solution-analyst** — Solution ranking and quality specialist
   - Rank LSD solutions using 13C shift prediction (two-tier: match count, then MAE)
   - Assess chemical plausibility of candidate structures
   - Write final results report

4. **lucy-devils-advocate** — Pre-run validation and quality gate specialist
   - Diff LSD file against previous iteration (check for dropped constraints)
   - Verify sp2 count, H budget, DEFF NOT persistence, SYME application
   - Flag issues to team lead before LSD solver runs

**Important:** Use flat file names (lucy-nmr-chemist.md, not in a subdirectory). Research finding: subdirectory agent discovery is unverified (LOW confidence). Flat names in ~/.claude/agents/ are the safe choice.
  </action>
  <verify>
Verify all 4 files exist and have valid YAML frontmatter:

```bash
for role in nmr-chemist lsd-engineer solution-analyst devils-advocate; do
  echo "=== lucy-$role ==="
  head -5 ~/.claude/agents/lucy-$role.md
  echo "---"
done
```

Each file must show `name: lucy-{role}` in frontmatter.
  </verify>
  <done>4 stub agent files exist at ~/.claude/agents/ with correct names, valid YAML frontmatter (name, description, tools), and placeholder role descriptions. Each file is 15-30 lines. All 4 names match what case.md Plan 02 will reference: lucy-nmr-chemist, lucy-lsd-engineer, lucy-solution-analyst, lucy-devils-advocate.</done>
</task>

</tasks>

<verification>
- [ ] 4 files exist: ~/.claude/agents/lucy-{nmr-chemist,lsd-engineer,solution-analyst,devils-advocate}.md
- [ ] Each file has valid YAML frontmatter with `name:` field
- [ ] Each file has `tools:` list including at minimum Read, Bash
- [ ] Each file has `<role>` section with placeholder responsibilities
- [ ] No files created in subdirectories (flat ~/.claude/agents/ only)
- [ ] Names match exactly: lucy-nmr-chemist, lucy-lsd-engineer, lucy-solution-analyst, lucy-devils-advocate
</verification>

<success_criteria>
4 stub agent definitions exist and can be referenced by subagent_type in Task(team_name) calls. Files are minimal (15-30 lines) — they exist solely to make Phase 41 team spawning work before Phase 42 creates full definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/41-orchestrator-skill-modification/41-01-SUMMARY.md`
</output>
