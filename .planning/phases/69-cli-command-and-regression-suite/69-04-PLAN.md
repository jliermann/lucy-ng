---
phase: 69-cli-command-and-regression-suite
plan: 04
type: execute
wave: 2
depends_on:
  - 69-01
files_modified:
  - tests/test_lsd_regression.py
  - tests/fixtures/regression/ibuprofen_no_4j.lsd
  - tests/fixtures/regression/ibuprofen_no_4j.expected_inchis.txt
autonomous: false
requirements:
  - CLI-03

must_haves:
  truths:
    - "lucy lsd run on ibuprofen_no_4j.lsd produces the same InChI set as the versioned baseline (set-equal, order-independent)"
    - "Regression test is skipped (not failed) on machines without LSD binary"
    - "Baseline file was inspected by the developer and confirmed to contain chemically plausible C13H18O2 isomers before committing (D-16a)"
    - "If LSD version changes and solution set changes, the test FAILS (not silently passes) — forcing manual re-verification"
  artifacts:
    - path: "tests/test_lsd_regression.py"
      provides: "set-equal InChI regression test for lucy lsd run"
      contains: "@pytest.mark.skipif(shutil.which"
    - path: "tests/fixtures/regression/ibuprofen_no_4j.lsd"
      provides: "ibuprofen LSD input — classic lucy lsd run form (no inventory block, no ; ELIM)"
      contains: "MULT"
    - path: "tests/fixtures/regression/ibuprofen_no_4j.expected_inchis.txt"
      provides: "sorted InChI baseline — one InChI per line, manually verified (D-16a)"
  key_links:
    - from: "test_lsd_regression.py"
      to: "ibuprofen_no_4j.lsd fixture"
      via: "LSDRunner.run_file() or CLI CliRunner"
      pattern: "ibuprofen_no_4j\\.lsd"
    - from: "test_lsd_regression.py"
      to: "ibuprofen_no_4j.expected_inchis.txt"
      via: "set equality comparison"
      pattern: "expected_inchis"
---

<objective>
Create a regression test confirming that `lucy lsd run` behavior is unchanged by the v8.0 changes: the ibuprofen LSD file (without 4J annotations) produces the same InChI set as a versioned baseline.

Purpose: CLI-03. Phases 66-68 added new code paths (FORM emission, inventory schema, validate-inventory) but must not have altered how `lucy lsd run` processes classic LSD files. The regression test catches accidental drift in LSD runner behavior. D-16 strategy: InChI-set comparison, baseline manually verified once (D-16a), no auto-update on LSD drift (D-16b).

Output: test_lsd_regression.py, ibuprofen_no_4j.lsd fixture (copied from Phase 65), ibuprofen_no_4j.expected_inchis.txt baseline (generated from Phase 65 sol file + developer verification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-cli-command-and-regression-suite/69-CONTEXT.md
@.planning/phases/69-cli-command-and-regression-suite/69-RESEARCH.md
@.planning/phases/69-cli-command-and-regression-suite/69-PATTERNS.md
</context>

<interfaces>
<!-- Key patterns extracted from codebase — 2026-05-19 -->

Phase 65 source files available:
  .planning/phases/65-hypothesis-gate/ibuprofen_no4j.lsd   — input fixture (copy verbatim)
  .planning/phases/65-hypothesis-gate/ibuprofen_no4j.smi   — 392 solution SMILES from Phase 65 run
  (ibuprofen_no4j.sol also present — outlsd 5 already applied to produce .smi)

InChI generation (from src/lucy_ng/lsd/orchestrator.py lines 408-428):
```python
from rdkit import Chem
from rdkit.Chem.inchi import MolToInchi
mol = Chem.MolFromSmiles(smiles)
if mol is None: return None
inchi = MolToInchi(mol)
```
Use MolToInchi (full InChI string, not InChIKey) for human-readable baseline.

LSDRunner.run_file pattern (from test_lsd_integration.py):
```python
runner = LSDRunner()
result = runner.run_file(lsd_path, output_dir=tmp_path, timeout=60)
assert result.success
```
After run_file succeeds, use LSDRunner.is_outlsd_available() + outlsd to get SMILES;
OR use the .smi file from output_dir directly if LSDRunner.run_file writes one.

skipif pattern per D-16:
```python
@pytest.mark.skipif(
    shutil.which("LSD") is None,
    reason="LSD binary not installed"
)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture files and test_lsd_regression.py</name>
  <files>
    tests/test_lsd_regression.py,
    tests/fixtures/regression/ibuprofen_no_4j.lsd
  </files>
  <read_first>
    - .planning/phases/65-hypothesis-gate/ibuprofen_no4j.lsd (full — to copy verbatim)
    - src/lucy_ng/lsd/runner.py lines 280-400 (LSDRunner.run_file, is_outlsd_available, is_available)
    - src/lucy_ng/lsd/parser.py lines 1-50 (LSDOutputParser.parse_smiles_file)
    - .planning/phases/69-cli-command-and-regression-suite/69-PATTERNS.md §"tests/test_lsd_regression.py"
    - .planning/phases/69-cli-command-and-regression-suite/69-RESEARCH.md §"Regression Test Design"
  </read_first>
  <action>
    Create tests/fixtures/regression/ directory (mkdir).

    Copy .planning/phases/65-hypothesis-gate/ibuprofen_no4j.lsd to tests/fixtures/regression/ibuprofen_no_4j.lsd verbatim (note filename difference: source has no underscore before 4j; destination uses underscores per RESEARCH.md naming). Verify the file has no v2 inventory block and no "; ELIM" annotations — it must be the "classic lucy lsd run" form per D-16.

    Create tests/test_lsd_regression.py. Module docstring must include:
    - What this test does (InChI-set comparison vs baseline)
    - What to do when it fails after LSD version update (analyze, verify chemical validity, regenerate baseline manually, commit with rationale — D-16b)
    - Baseline source: LSD-3.4.9 run, Phase 65 sol file, manually verified

    Imports: shutil, pathlib.Path, pytest, rdkit.Chem, rdkit.Chem.inchi.MolToInchi, from lucy_ng.lsd.runner import LSDRunner, from lucy_ng.lsd.parser import LSDOutputParser.

    Define module-level: FIXTURE_DIR = Path(__file__).parent / "fixtures" / "regression"

    Define helper: `_smiles_to_inchis(smiles_path: Path) -> set[str]` — reads smiles_path line by line, converts each non-empty line to InChI via MolToInchi (skipping invalid SMILES and None InChIs), returns a set of non-None InChI strings.

    Define helper: `_read_baseline(baseline_path: Path) -> set[str]` — reads sorted InChIs from baseline file, strips blank lines, returns set.

    Write test class TestLSDRegression with two tests:
    1. test_regression_fixtures_exist (no skipif — always runs):
       - assert (FIXTURE_DIR / "ibuprofen_no_4j.lsd").exists()
       - assert (FIXTURE_DIR / "ibuprofen_no_4j.expected_inchis.txt").exists(), "Baseline file missing — see plan Task 2 for generation instructions"
       - assert "=== CONSTRAINT INVENTORY v2 ===" not in (FIXTURE_DIR / "ibuprofen_no_4j.lsd").read_text(), "Fixture must not have v2 inventory block"
       - assert "; ELIM" not in (FIXTURE_DIR / "ibuprofen_no_4j.lsd").read_text(), "Fixture must not have ; ELIM annotations"

    2. test_ibuprofen_no_4j_inchi_set_stable(tmp_path):
       - @pytest.mark.skipif(shutil.which("LSD") is None, reason="LSD binary not installed")
       - Run runner.run_file(FIXTURE_DIR / "ibuprofen_no_4j.lsd", tmp_path, timeout=120)
       - assert result.success
       - Locate SMILES output: check result.output_files for a .smi file; if not found, look for outlsd.out in tmp_path; use LSDOutputParser.parse_smiles_file to get SMILES list
       - NOTE: Check runner.py for what run_file actually returns in output_files. If it doesn't run outlsd automatically, the test must run outlsd manually: find the .sol file in tmp_path, run outlsd 5 < compound.sol > solutions.smi via subprocess (this is the pattern from Phase 67 that bypasses the _run_outlsd bug)
       - actual_inchis = _smiles_to_inchis(smiles_path)
       - baseline_inchis = _read_baseline(FIXTURE_DIR / "ibuprofen_no_4j.expected_inchis.txt")
       - assert actual_inchis == baseline_inchis, detailed message showing added/removed InChIs

    NOTE: test_regression_fixtures_exist will FAIL until Task 2 provides the expected_inchis.txt baseline. This is intentional and expected — Wave 0 gap that Task 2 closes.
  </action>
  <verify>
    <automated>pytest tests/test_lsd_regression.py::TestLSDRegression::test_regression_fixtures_exist -v</automated>
  </verify>
  <done>
    - `pytest tests/test_lsd_regression.py::TestLSDRegression::test_regression_fixtures_exist -v` — this test will FAIL (expected_inchis.txt missing) until Task 2 completes; that is expected at this stage
    - `grep -n "shutil.which" tests/test_lsd_regression.py` shows skipif decorator
    - `grep -c "sys.exit\|sys\.exit" tests/test_lsd_regression.py` returns 0
    - `tests/fixtures/regression/ibuprofen_no_4j.lsd` exists and contains MULT lines
    - `grep -c "CONSTRAINT INVENTORY v2\|; ELIM" tests/fixtures/regression/ibuprofen_no_4j.lsd` returns 0 (clean classic form)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    tests/test_lsd_regression.py is written. tests/fixtures/regression/ibuprofen_no_4j.lsd is copied from Phase 65. The baseline file tests/fixtures/regression/ibuprofen_no_4j.expected_inchis.txt does NOT yet exist — it must be generated and verified by the developer.
  </what-built>
  <how-to-verify>
    The Phase 65 sol file already contains solutions from the same LSD run we need as baseline. To generate the baseline without re-running LSD:

    Step 1: Verify the Phase 65 smi file is valid:
      cat .planning/phases/65-hypothesis-gate/ibuprofen_no4j.smi | head -5

    Step 2: Convert SMILES to sorted InChIs using the helper script (or inline Python):
      python3 - <<'EOF'
      from pathlib import Path
      from rdkit import Chem
      from rdkit.Chem.inchi import MolToInchi
      smi_file = Path(".planning/phases/65-hypothesis-gate/ibuprofen_no4j.smi")
      inchis = set()
      for line in smi_file.read_text().splitlines():
          smiles = line.strip().split()[0] if line.strip() else ""  # SMILES is first field
          if not smiles: continue
          mol = Chem.MolFromSmiles(smiles)
          if mol is None: continue
          inchi = MolToInchi(mol)
          if inchi: inchis.add(inchi)
      out = Path("tests/fixtures/regression/ibuprofen_no_4j.expected_inchis.txt")
      out.write_text("\n".join(sorted(inchis)) + "\n")
      print(f"Wrote {len(inchis)} InChIs to {out}")
      EOF

    Step 3: Inspect the baseline. Are the InChIs chemically plausible C13H18O2 isomers?
      - Check a few: `python3 -c "from rdkit import Chem; from rdkit.Chem.inchi import InchiToInchi; ..."`
      - Or just count: how many InChIs? (should be around 392 minus invalid SMILES)
      - Spot-check: do they have molecular formula C13H18O2? (Can verify with RDKit)
      - Confirm: none look like nonsense (e.g., disconnected fragments, > 30 carbons)

    Step 4: If plausible, commit the baseline:
      git add tests/fixtures/regression/ibuprofen_no_4j.expected_inchis.txt
      [include in plan commit]

    Step 5: Run the regression test to confirm it passes:
      pytest tests/test_lsd_regression.py -v
      (The test_ibuprofen_no_4j_inchi_set_stable test should PASS or SKIP — not FAIL)
  </how-to-verify>
  <resume-signal>
    Type "baseline-committed: N InChIs" with the count of InChIs written to the baseline file. Claude will then update test_regression_fixtures_exist to assert the correct count and finalize the plan.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify regression test passes with baseline in place</name>
  <files>tests/test_lsd_regression.py</files>
  <read_first>
    - tests/fixtures/regression/ibuprofen_no_4j.expected_inchis.txt (count lines)
    - .planning/phases/69-cli-command-and-regression-suite/69-CONTEXT.md §D-16b
  </read_first>
  <action>
    After the human-verify checkpoint provides the baseline count:
    1. Update test_regression_fixtures_exist to also assert that the baseline file has a non-zero number of lines: `assert len([l for l in baseline_path.read_text().splitlines() if l.strip()]) > 0`
    2. Add a comment in the test file with the expected count (e.g., "# Baseline: ~389 InChIs from LSD-3.4.9 run of ibuprofen_no_4j.lsd (Phase 65)") for future reference
    3. Confirm `pytest tests/test_lsd_regression.py -v` passes (all tests PASSED or SKIPPED — none FAILED)

    No code changes beyond the comment + assertion update. All logic was written in Task 1.
  </action>
  <verify>
    <automated>pytest tests/test_lsd_regression.py -v -q</automated>
  </verify>
  <done>
    - `pytest tests/test_lsd_regression.py -v -q` shows all tests PASSED or SKIPPED (zero FAILED)
    - `tests/fixtures/regression/ibuprofen_no_4j.expected_inchis.txt` exists and has > 0 non-empty lines
    - `wc -l tests/fixtures/regression/ibuprofen_no_4j.expected_inchis.txt` matches the count from the human-verify checkpoint
    - When LSD binary present: test_ibuprofen_no_4j_inchi_set_stable PASSED (InChI sets match)
    - D-16b is enforced by test design: no auto-update logic exists; failure requires manual re-verification
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → LSD binary | LSD invoked via LSDRunner which uses subprocess; binary on trusted local PATH |
| Phase 65 sol file → baseline | One-time import from planning artifacts; developer verifies before committing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04-01 | Tampering | Baseline auto-update | mitigate | No auto-update code by design (D-16b); failure forces manual analysis |
| T-69-04-02 | Repudiation | Baseline source unclear | mitigate | Module docstring + commit message must state: "baseline from LSD-3.4.9, Phase 65 sol file, manually verified" |
| T-69-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `pytest tests/test_lsd_regression.py -v`: fixtures test PASSED, regression test PASSED (LSD installed) or SKIPPED (no LSD)
- `tests/fixtures/regression/ibuprofen_no_4j.expected_inchis.txt` has content (> 0 InChIs)
- `grep -c "CONSTRAINT INVENTORY v2\|; ELIM" tests/fixtures/regression/ibuprofen_no_4j.lsd` returns 0
- Full suite `pytest` green (wave merge check)
</verification>

<success_criteria>
1. test_regression_fixtures_exist PASSES unconditionally — confirming both fixture files are committed
2. test_ibuprofen_no_4j_inchi_set_stable PASSES when LSD is installed — InChI set matches baseline
3. If LSD is not installed, test SKIPPED (not FAILED) — CI-safe
4. Baseline file committed with a message citing LSD version and that it was manually verified (D-16a)
5. No auto-update logic anywhere — test failure on LSD version change is the desired behavior (D-16b)
</success_criteria>

<output>
Create `.planning/phases/69-cli-command-and-regression-suite/69-04-SUMMARY.md` when done
</output>
