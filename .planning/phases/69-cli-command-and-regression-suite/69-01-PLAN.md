---
phase: 69-cli-command-and-regression-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lucy_ng/cli/lsd.py
autonomous: true
requirements:
  - CLI-02

must_haves:
  truths:
    - "_perform_ranking() exists as a module-level helper in cli/lsd.py callable without a Click context"
    - "_validate_and_parse_inventory() exists as a module-level helper that returns the parsed inventory dict or None"
    - "lsd_rank behavior is byte-for-byte identical to before (pure refactor — no output change)"
  artifacts:
    - path: "src/lucy_ng/cli/lsd.py"
      provides: "_perform_ranking helper + _validate_and_parse_inventory helper"
      exports: ["_perform_ranking", "_validate_and_parse_inventory"]
  key_links:
    - from: "lsd_rank Click command"
      to: "_perform_ranking()"
      via: "direct function call after shift parsing"
      pattern: "_perform_ranking\\("
    - from: "pylsd.py (Wave 2)"
      to: "_perform_ranking()"
      via: "import from lucy_ng.cli.lsd"
      pattern: "from lucy_ng\\.cli\\.lsd import.*_perform_ranking"
---

<objective>
Extract two reusable helper functions from `lsd_rank` and `lsd_validate_inventory` in `src/lucy_ng/cli/lsd.py` so that the new `pylsd run` command (Plan 02) can call them directly in-process.

Purpose: D-14 mandates that `lucy pylsd run` calls ranking logic as a Python function call, not via subprocess. This requires `_perform_ranking()` to be importable. D-13 mandates inventory parsing via direct Python call; `_validate_and_parse_inventory()` makes this clean.

Output: Two module-private helpers in `cli/lsd.py`; `lsd_rank` and `lsd_validate_inventory` become thin wrappers. All existing tests pass unchanged (pure refactor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-cli-command-and-regression-suite/69-CONTEXT.md
@.planning/phases/69-cli-command-and-regression-suite/69-RESEARCH.md
@.planning/phases/69-cli-command-and-regression-suite/69-PATTERNS.md
</context>

<interfaces>
<!-- Key signatures extracted from src/lucy_ng/cli/lsd.py (read 2026-05-19) -->

Existing private helpers (follow same naming/style):
  _get_default_table_path() -> Path         # line 113
  _get_schema_path() -> Path                # line 142
  _extract_inventory_block(content: str) -> str | None  # line 174

lsd_rank body to be extracted (lines 441-537 of lsd.py):
  - starts at: solutions = LSDOutputParser.parse_smiles_file(smiles_file)
  - ends at: last `click.echo(f"WARNING: {warning}")` in warnings loop
  - requires: smiles_file, experimental_shifts (already parsed), top, tolerance, table, output_format

validate_inventory body to be extracted (lines 222-337 of lsd.py):
  - starts at: content = Path(lsd_file).read_text(...)
  - ends at: returning the parsed `instance` dict on success or raising SystemExit(1) on failure
  - new helper signature: _validate_and_parse_inventory(lsd_file: str | Path) -> dict | None

JSON output shape from lsd_rank (lines 479-507):
  {"total_solutions": int, "ranked_count": int, "skipped_count": int,
   "experimental_shifts": list[float], "tolerance": float,
   "solutions": [...], "warnings": [...]}

_perform_ranking return contract:
  - When output_format == "json": return the data dict (for pylsd_run to embed in its outer wrapper)
  - When output_format == "text": echo to stdout, return None
  - lsd_rank calls it for side-effect only and ignores return value
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract _perform_ranking() from lsd_rank</name>
  <files>src/lucy_ng/cli/lsd.py, tests/test_cli_lsd.py</files>
  <read_first>
    - src/lucy_ng/cli/lsd.py lines 341-537 (full lsd_rank command including body to extract)
    - src/lucy_ng/cli/lsd.py lines 113-139 (_get_default_table_path — used inside extracted helper)
    - tests/test_cli_lsd.py (existing lsd_rank tests — must stay green)
  </read_first>
  <behavior>
    - _perform_ranking(smiles_file, experimental_shifts, top, tolerance, table, output_format) callable with no Click context
    - When output_format=="json" returns the dict that lsd_rank previously echoed; lsd_rank caller does not echo it (lsd_rank ignores return value for text mode, but for json mode the dict return is for future pylsd_run use — lsd_rank still echoes it internally or delegates)
    - When output_format=="text" prints to stdout via click.echo and returns None
    - Passing an empty solutions list → click.echo("Error: No SMILES found in file", err=True) + raise SystemExit(1)
    - lsd_rank behavior is identical before and after (existing tests unchanged)
  </behavior>
  <action>
    Read lsd.py lines 341-537 fully. Extract lines 441-537 (starting from "Load solutions from SMILES file" comment) into a new module-level function `_perform_ranking(smiles_file: str | Path, experimental_shifts: list[float], top: int = 10, tolerance: float = 3.0, table: str | Path | None = None, output_format: str = "text") -> dict | None`. Place it immediately after `_extract_inventory_block` (after line 204), before the `@lsd.command("validate-inventory")` decorator.

    The function body is the lines 441-537 of the current lsd_rank handler verbatim, with the following changes only: (a) the `smiles_file`, `experimental_shifts`, `top`, `tolerance`, `table`, and `output_format` parameters come from the function signature, not from Click; (b) when `output_format == "json"`, build the data dict AND both `click.echo(json.dumps(data, indent=2))` AND `return data`; (c) when `output_format == "text"`, echo as before and `return None`.

    Rewrite `lsd_rank` to parse its arguments (lines 406-439 unchanged), then call `_perform_ranking(smiles_file, experimental_shifts, top, tolerance, table, output_format)`.

    Convention: use `raise SystemExit(1)` (never `sys.exit(1)`), `click.echo(..., err=True)` for errors. Helper is module-private (single underscore).

    Add a unit test in tests/test_cli_lsd.py (or alongside existing lsd rank tests) that imports `_perform_ranking` from `lucy_ng.cli.lsd` and calls it directly with a real SMILES file (use `tmp_path`) — confirming it is callable without a Click runner.
  </action>
  <verify>
    <automated>pytest tests/test_cli_lsd.py -x -q</automated>
  </verify>
  <done>
    - `grep -n "^def _perform_ranking" src/lucy_ng/cli/lsd.py` returns exactly 1 match
    - `python -c "from lucy_ng.cli.lsd import _perform_ranking; print('ok')"` exits 0
    - `pytest tests/test_cli_lsd.py -x -q` passes (all existing rank tests green)
    - `grep -c "raise SystemExit" src/lucy_ng/cli/lsd.py` returns >= 10 (no sys.exit introduced)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract _validate_and_parse_inventory() from lsd_validate_inventory</name>
  <files>src/lucy_ng/cli/lsd.py, tests/test_inventory_schema.py</files>
  <read_first>
    - src/lucy_ng/cli/lsd.py lines 207-338 (full lsd_validate_inventory command)
    - src/lucy_ng/cli/lsd.py lines 174-204 (_extract_inventory_block — called inside new helper)
    - tests/test_inventory_schema.py (existing validate-inventory tests — must stay green)
  </read_first>
  <behavior>
    - _validate_and_parse_inventory(lsd_file: str | Path) -> dict | None
    - Returns the parsed inventory instance dict when block is present and schema-valid
    - Returns None when no v2 block is present (no-inventory case — not an error, just missing)
    - Raises SystemExit(1) for: file read error, v1 block detected, JSON parse failure, schema validation failure; prints appropriate error to stderr via click.echo(err=True) before raising
    - lsd_validate_inventory behavior unchanged (existing tests pass)
  </behavior>
  <action>
    Read lsd.py lines 207-338. Extract the logic from lines 222-337 into a new module-level function `_validate_and_parse_inventory(lsd_file: str | Path) -> dict | None`. Place it immediately after `_perform_ranking` (after that function's last line, before `@lsd.command("validate-inventory")`).

    The function body covers: file read (PermissionError/OSError → SystemExit(1)), v1 block check (SystemExit(1)), `_extract_inventory_block()` call (returns None → return None from helper, NOT SystemExit — the "no block" case is handled by callers differently), JSON parse, schema load, schema validation, return `instance` on success.

    Rewrite `lsd_validate_inventory` to call `_validate_and_parse_inventory(lsd_file)` and handle the None return (no block) as a validation failure for the CLI (emit error JSON/text + raise SystemExit(1)), and handle the non-None return as success (emit success JSON/text). The CLI's behavior for "no block" is unchanged: it still exits 1 with an error message. The helper's None return simply means "no block found" without prescribing the exit code, so `pylsd_run` can treat it differently (as the D-13b fallback path).

    `_validate_and_parse_inventory` is module-private (single underscore). Convention: `raise SystemExit(1)`, `click.echo(..., err=True)`.

    Add a unit test in tests/test_inventory_schema.py importing `_validate_and_parse_inventory` directly and verifying: (a) valid v2 file → returns dict with deferred_4j key; (b) no-block file → returns None.
  </action>
  <verify>
    <automated>pytest tests/test_inventory_schema.py -x -q</automated>
  </verify>
  <done>
    - `grep -n "^def _validate_and_parse_inventory" src/lucy_ng/cli/lsd.py` returns exactly 1 match
    - `python -c "from lucy_ng.cli.lsd import _validate_and_parse_inventory; print('ok')"` exits 0
    - `pytest tests/test_inventory_schema.py -x -q` passes (all existing validate-inventory tests green)
    - No behavior change to `lucy lsd validate-inventory` output
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal refactor only | No new trust boundary — helper functions are internal-only, no new I/O surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Full suite must be green after this plan: `pytest tests/test_cli_lsd.py tests/test_inventory_schema.py -x`
All existing tests for `lsd rank` and `lsd validate-inventory` pass without modification.
Both new helpers are importable directly from `lucy_ng.cli.lsd`.
</verification>

<success_criteria>
1. `_perform_ranking` importable from `lucy_ng.cli.lsd` — callable with a SMILES file and shifts list, no Click context required
2. `_validate_and_parse_inventory` importable from `lucy_ng.cli.lsd` — returns dict on success, None when no block, raises SystemExit(1) on error
3. All existing `pytest tests/test_cli_lsd.py tests/test_inventory_schema.py` tests pass (zero regressions)
4. `lucy lsd rank` and `lucy lsd validate-inventory` produce identical output to before this plan
</success_criteria>

<output>
Create `.planning/phases/69-cli-command-and-regression-suite/69-01-SUMMARY.md` when done
</output>
