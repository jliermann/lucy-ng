---
phase: 69-cli-command-and-regression-suite
plan: 02
type: execute
wave: 2
depends_on:
  - 69-01
files_modified:
  - src/lucy_ng/cli/pylsd.py
  - src/lucy_ng/cli/main.py
  - tests/test_pylsd_cli.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02

must_haves:
  truths:
    - "lucy pylsd run <file> --shifts '...' executes orchestration, merges, ranks, and prints ranked solutions to stdout in the same format as lucy lsd rank"
    - "lucy pylsd run <file> --no-rank writes merged.smi + run_report.json to working dir and prints their paths"
    - "lucy pylsd run --shifts required when ranking active; missing shifts without --no-rank → exit 1 with clear message"
    - "lucy pylsd run --format json produces {permutations, merged_count, ranked_solutions, run_report_path}"
    - "Suspect extraction: inventory primary (D-13), ; ELIM sanity check (D-13a), grep fallback (D-13b), single-run when both empty (D-13c)"
    - "K > 3 suspect correlations → exit 1 (propagates ValueError from PyLSDOrchestrator)"
    - "Empty merged solutions set → exit 0 with clear message (not an error per CASE workflow)"
    - "raise SystemExit(1) used throughout — never sys.exit"
  artifacts:
    - path: "src/lucy_ng/cli/pylsd.py"
      provides: "pylsd Click group + pylsd_run command + _extract_suspects_from_file helper"
      exports: ["pylsd"]
    - path: "src/lucy_ng/cli/main.py"
      provides: "pylsd group registered in lucy CLI"
      contains: "cli.add_command(pylsd)"
    - path: "tests/test_pylsd_cli.py"
      provides: "CLI integration tests (mocked orchestrator)"
      contains: "TestPylsdRunCLI"
  key_links:
    - from: "pylsd_run"
      to: "PyLSDOrchestrator.run()"
      via: "direct instantiation and call"
      pattern: "PyLSDOrchestrator\\(.*\\)\\.run\\("
    - from: "pylsd_run"
      to: "_perform_ranking"
      via: "import from lucy_ng.cli.lsd"
      pattern: "from lucy_ng\\.cli\\.lsd import.*_perform_ranking"
    - from: "pylsd_run"
      to: "_validate_and_parse_inventory"
      via: "import from lucy_ng.cli.lsd"
      pattern: "from lucy_ng\\.cli\\.lsd import.*_validate_and_parse_inventory"
---

<objective>
Implement `lucy pylsd run` as a new Click subcommand in `src/lucy_ng/cli/pylsd.py`, register it in `main.py`, and provide full CLI integration tests.

Purpose: CLI-01 + CLI-02. The command is a thin orchestration wrapper: extract suspect correlations (D-13/D-13a/D-13b/D-13c), call PyLSDOrchestrator, call SolutionMerger, optionally rank via `_perform_ranking()` (D-14). The test suite uses mocked orchestrator/merger so no LSD binary is required to run.

Output: `src/lucy_ng/cli/pylsd.py` (new), one-line additions to `main.py`, `tests/test_pylsd_cli.py` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-cli-command-and-regression-suite/69-CONTEXT.md
@.planning/phases/69-cli-command-and-regression-suite/69-RESEARCH.md
@.planning/phases/69-cli-command-and-regression-suite/69-PATTERNS.md
@.planning/phases/69-cli-command-and-regression-suite/69-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from prior phases and Plan 01 — extracted 2026-05-19 -->

From src/lucy_ng/lsd/orchestrator.py:
```
class PyLSDOrchestrator:
    def __init__(self, lsd_path: str | Path | None = None, timeout: int = 120) -> None
    def run(self, base_problem: LSDProblem, suspect_correlations: list[LSDCorrelation],
            output_dir: Path) -> OrchestrationResult

class SolutionMerger:
    def merge(self, permutation_results: list[PermutationResult],
              output_dir: Path) -> MergeResult

@dataclass
class OrchestrationResult:
    permutation_results: list[PermutationResult]

@dataclass
class MergeResult:
    merged_solutions: list[MergedSolution]
    merged_smi: Path
    run_report: Path
```

From src/lucy_ng/lsd/parser.py:
```
class LSDInputParser:
    @staticmethod
    def parse_file(lsd_path: Path | str) -> LSDProblem
```

From src/lucy_ng/lsd/models.py (inferred from Phase 66/67 summaries):
```
@dataclass
class LSDCorrelation:
    atom1_index: int
    atom2_index: int
    correlation_type: str   # "HMBC", "HSQC", etc.
    min_bonds: int = 2
    max_bonds: int = 3
```

From src/lucy_ng/cli/lsd.py (Plan 01 output):
```
def _perform_ranking(
    smiles_file: str | Path,
    experimental_shifts: list[float],
    top: int = 10,
    tolerance: float = 3.0,
    table: str | Path | None = None,
    output_format: str = "text",
) -> dict | None

def _validate_and_parse_inventory(lsd_file: str | Path) -> dict | None
    # Returns parsed inventory dict on success, None if no block, raises SystemExit(1) on error
```

Existing pattern in src/lucy_ng/cli/lsd.py:
```
@click.group()
def lsd() -> None: ...

@lsd.command("run")
@click.argument("input_file", type=click.Path(exists=True))
@click.option("--format", "output_format", type=click.Choice(["text","json"]), default="text")
def lsd_run(...) -> None:
    if not LSDRunner.is_available():
        click.echo("Error: LSD is not installed or not in PATH", err=True)
        raise SystemExit(1)
```

D-14c JSON output shape for pylsd_run:
```json
{
  "permutations": 8,
  "merged_count": 47,
  "ranked_solutions": [...],   // same structure as lsd_rank solutions[]
  "run_report_path": "/tmp/pylsd_run_xyz/run_report.json"
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement src/lucy_ng/cli/pylsd.py with pylsd_run command</name>
  <files>src/lucy_ng/cli/pylsd.py</files>
  <read_first>
    - src/lucy_ng/cli/lsd.py lines 1-19 (import block + group pattern to mirror)
    - src/lucy_ng/cli/lsd.py lines 42-112 (lsd_run — decorator stacking pattern)
    - src/lucy_ng/lsd/orchestrator.py lines 112-160 (PyLSDOrchestrator constructor + run)
    - src/lucy_ng/lsd/orchestrator.py lines 303-360 (SolutionMerger.merge signature)
    - src/lucy_ng/lsd/parser.py lines 118-160 (LSDInputParser.parse_file)
    - .planning/phases/69-cli-command-and-regression-suite/69-PATTERNS.md §"src/lucy_ng/cli/pylsd.py"
  </read_first>
  <behavior>
    - pylsd group defined as @click.group() + def pylsd() + pass
    - pylsd_run command accepts: lsd_file (Path, exists=True), --shifts str, --no-rank flag,
      --working-dir Path, --timeout int default 120, --format "output_format" Choice text/json
    - First statement in pylsd_run: LSDRunner.is_available() check → SystemExit(1) if missing
    - D-14b: if not no_rank and shifts is None → click.echo error, raise SystemExit(1)
    - D-13 suspect extraction via _extract_suspects_from_file(lsd_path, lsd_content) helper:
        1. _validate_and_parse_inventory(lsd_path) → if non-None, read inventory["deferred_4j"]
           → extract [(atom1, atom2)] pairs; then cross-check ; ELIM annotations (D-13a):
           parse lines matching r"^HMBC\s+(\d+)\s+(\d+).*;\s*ELIM" from lsd_content,
           if mismatch between inventory pairs and annotation pairs → SystemExit(1) with message
        2. If inventory returns None (no block): grep lsd_content for ; ELIM → emit warning D-13b
        3. If both empty: emit warning D-13c, pairs = []
    - Reconstruct LSDCorrelation objects: call LSDInputParser.parse_file(lsd_path) → get
      base_problem.correlations → filter where (c.atom1_index, c.atom2_index) in suspect_pairs
      and c.correlation_type == "HMBC"; if a pair is not found in the parsed correlations,
      construct LSDCorrelation(atom1_index=a1, atom2_index=a2, correlation_type="HMBC") directly
    - working_dir: if --working-dir not given, default to Path(lsd_file).parent / "pylsd_run"
    - Call PyLSDOrchestrator(timeout=timeout).run(base_problem, suspect_correlations, output_dir)
      → catch ValueError (K > 3) → click.echo error, raise SystemExit(1)
    - Call SolutionMerger().merge(orch_result.permutation_results, output_dir)
    - If merge_result.merged_solutions is empty → click.echo("No solutions found..."), exit 0
    - If --no-rank: echo merged_smi path + run_report path → exit 0
    - Else: parse shifts, call _perform_ranking(merge_result.merged_smi, experimental_shifts,
      table=None, output_format=output_format)
    - For --format json: wrap _perform_ranking return dict in outer D-14c shape:
      {"permutations": K, "merged_count": N, "ranked_solutions": data["solutions"],
       "run_report_path": str(merge_result.run_report)}
    - All error exits use raise SystemExit(1); all error messages go to stderr via click.echo(err=True)
  </behavior>
  <action>
    Create src/lucy_ng/cli/pylsd.py from scratch. Module docstring: "CLI commands for PyLSD multi-run orchestration." Imports: json, re, tempfile (not used if default is parent/pylsd_run), pathlib.Path, click, from lucy_ng.lsd.orchestrator import PyLSDOrchestrator, SolutionMerger, from lucy_ng.lsd.models import LSDCorrelation, LSDProblem (if needed — check if LSDProblem needed explicitly), from lucy_ng.lsd.parser import LSDInputParser, from lucy_ng.lsd import LSDRunner, from lucy_ng.cli.lsd import _validate_and_parse_inventory, _perform_ranking.

    Define module-level helper `_extract_suspects(lsd_path: str | Path) -> list[LSDCorrelation]` implementing D-13/D-13a/D-13b/D-13c branching as described in behavior above. This helper raises SystemExit(1) on mismatch (D-13a) and emits warnings (D-13b, D-13c) via click.echo (no err=True for warnings — stdout).

    Define `@click.group() def pylsd()` group.

    Define `@pylsd.command("run") def pylsd_run(...)` with full decorator stack per PATTERNS.md. Implement the five-step pipeline: extract suspects → orchestrate → merge → (optional) rank → output.

    Default working_dir = Path(lsd_file).parent / "pylsd_run"; create it with mkdir(parents=True, exist_ok=True) before passing to orchestrator.

    For --format json with ranking: _perform_ranking returns a dict when output_format=="json". Build outer wrapper. For --format text: _perform_ranking echoes internally and returns None; pylsd_run adds nothing.

    Per PATTERNS.md: use `"output_format"` as the parameter rename for --format to avoid shadowing the Python builtin; use `catch_exceptions=False` in tests.
  </action>
  <verify>
    <automated>python -c "from lucy_ng.cli.pylsd import pylsd; print(pylsd.name)"</automated>
  </verify>
  <done>
    - `python -c "from lucy_ng.cli.pylsd import pylsd; print(pylsd.name)"` prints "pylsd"
    - `grep -n "raise SystemExit" src/lucy_ng/cli/pylsd.py | wc -l` returns >= 4 (no sys.exit calls)
    - `grep -n "def pylsd_run\|def _extract_suspects\|def pylsd" src/lucy_ng/cli/pylsd.py` shows all three definitions
    - File contains no `sys.exit` (grep -c "sys.exit" returns 0)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register pylsd in main.py and write test_pylsd_cli.py</name>
  <files>src/lucy_ng/cli/main.py, tests/test_pylsd_cli.py</files>
  <read_first>
    - src/lucy_ng/cli/main.py (full — 57 lines, for exact import + add_command location)
    - tests/test_inventory_schema.py lines 499-703 (TestValidateInventoryCLI class — CliRunner pattern)
    - .planning/phases/69-cli-command-and-regression-suite/69-PATTERNS.md §"tests/test_pylsd_cli.py"
    - .planning/phases/69-cli-command-and-regression-suite/69-RESEARCH.md §"CliRunner pattern for pylsd CLI tests"
  </read_first>
  <behavior>
    main.py changes:
    - `from lucy_ng.cli.pylsd import pylsd` added to imports (after the lsd import line)
    - `cli.add_command(pylsd)` added after `cli.add_command(lsd)`
    - No other changes; existing commands unchanged

    test_pylsd_cli.py contains three test classes:
    - TestPylsdRunCLI: basic invocations with mocked PyLSDOrchestrator + SolutionMerger
        - test_no_rank_exits_0: --no-rank path, exit 0, merged.smi path in stdout
        - test_shifts_required_without_no_rank: missing --shifts → exit 1 with error message
        - test_format_json_structure: --format json output has {permutations, merged_count, ranked_solutions, run_report_path}
        - test_no_solutions_exits_0: empty merge result → exit 0 (not 1)
        - test_k_gt_3_exits_1: orchestrator raises ValueError → exit 1
        - test_lsd_unavailable_exits_1: LSDRunner.is_available() mocked False → exit 1
    - TestSuspectExtraction: D-13 logic (no real LSD needed)
        - test_inventory_primary_used: LSD file with v2 block → suspects from deferred_4j
        - test_elim_annotation_mismatch_exits_1: inventory says atom pair (4,8) but ; ELIM says (5,9) → exit 1
        - test_grep_fallback_when_no_inventory: no v2 block + ; ELIM annotations → warning + pairs from grep
        - test_single_run_when_both_empty: no block + no ; ELIM → warning + empty list (K=0)
    - TestRankingIntegration: verifies _perform_ranking is called (not subprocess)
        - test_perform_ranking_called_not_subprocess: patch _perform_ranking, verify called with expected args
  </behavior>
  <action>
    Edit main.py: add `from lucy_ng.cli.pylsd import pylsd` after line 12 (after `from lucy_ng.cli.lsd import lsd`). Add `cli.add_command(pylsd)` after `cli.add_command(lsd)` (line 52). Optionally extend the CLI docstring with a pylsd entry.

    Create tests/test_pylsd_cli.py. Import: json, pathlib.Path, unittest.mock.MagicMock + patch + call, pytest, click.testing.CliRunner, lucy_ng.cli.pylsd.pylsd, lucy_ng.lsd.orchestrator.MergeResult + MergedSolution + OrchestrationResult + PermutationResult.

    For mock setup in TestPylsdRunCLI: each test creates a tmp_path LSD file ("; minimal\nEXIT\n"), constructs MagicMock(spec=OrchestrationResult) with .permutation_results = [], MagicMock(spec=MergeResult) with .merged_solutions = [], .merged_smi = tmp_path/"merged.smi" (create the file with write_text("")), .run_report = tmp_path/"run_report.json" (create). Patches on "lucy_ng.cli.pylsd.PyLSDOrchestrator" and "lucy_ng.cli.pylsd.SolutionMerger" (the module where they are used, not where they are defined). Also patch "lucy_ng.cli.pylsd.LSDRunner.is_available" returning True.

    For TestSuspectExtraction: create real LSD content strings in tmp_path files with/without v2 inventory blocks and ; ELIM annotations. Import and call `_extract_suspects` from lucy_ng.cli.pylsd directly (or via runner.invoke if it's not exposed; prefer direct import).

    For TestRankingIntegration: patch "lucy_ng.cli.pylsd._perform_ranking", invoke pylsd run with --shifts, assert the patch was called once with the merged_smi path and parsed shifts list.

    Use `catch_exceptions=False` on all positive-path (exit 0) tests. Use plain `runner.invoke` without catch_exceptions=False on tests expecting exit 1.
  </action>
  <verify>
    <automated>pytest tests/test_pylsd_cli.py -x -q</automated>
  </verify>
  <done>
    - `pytest tests/test_pylsd_cli.py -x -q` passes (all 10+ tests green)
    - `lucy pylsd run --help` lists --shifts, --no-rank, --working-dir, --timeout, --format options
    - `lucy pylsd --help` is reachable (group is registered)
    - `grep -n "cli.add_command(pylsd)" src/lucy_ng/cli/main.py` returns 1 match
    - `grep -c "subprocess" tests/test_pylsd_cli.py` returns 0 (no subprocess calls in tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → pylsd_run | lsd_file path validated by click.Path(exists=True); shifts string parsed with float() in try/except |
| pylsd_run → LSD binary | Invoked by PyLSDOrchestrator with subprocess; working_dir is caller-controlled Path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-02-01 | Tampering | --shifts parse path (comma CSV floats) | mitigate | float(s.strip()) in try/except ValueError → SystemExit(1); same pattern as lsd_rank (lsd.py:419-423) |
| T-69-02-02 | Denial of Service | K-cap not enforced before I/O | mitigate | PyLSDOrchestrator.run() raises ValueError as FIRST statement when K>3; CLI catches and exits 1 |
| T-69-02-03 | Tampering | working_dir path traversal | accept | CLI operates on local filesystem; no multi-tenant context; user-supplied --working-dir is trusted |
| T-69-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `pytest tests/test_pylsd_cli.py tests/test_cli_lsd.py tests/test_inventory_schema.py -x` all green
- `lucy pylsd run --help` shows expected options
- `lucy pylsd --help` visible in CLI
- `lucy --help` shows `pylsd` in command list
- `grep -c "sys\.exit" src/lucy_ng/cli/pylsd.py` returns 0
</verification>

<success_criteria>
1. `lucy pylsd run compound.lsd --shifts "180.5,140.8" --format json` (with mocked orchestrator) produces JSON with permutations/merged_count/ranked_solutions/run_report_path keys
2. Missing --shifts without --no-rank returns exit 1 with "Error: --shifts is required unless --no-rank" on stderr
3. K > 3 correlations returns exit 1 with message about permutation limit
4. Suspect extraction reads v2 inventory deferred_4j first (D-13), falls back to ; ELIM grep (D-13b), runs single-pass when both empty (D-13c)
5. _perform_ranking is called as a Python function (not subprocess) — verifiable by the TestRankingIntegration mock assertion
6. All 10+ tests in test_pylsd_cli.py pass without requiring an LSD binary
</success_criteria>

<output>
Create `.planning/phases/69-cli-command-and-regression-suite/69-02-SUMMARY.md` when done
</output>
