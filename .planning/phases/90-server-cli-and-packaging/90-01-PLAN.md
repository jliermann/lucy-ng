---
phase: 90-server-cli-and-packaging
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_cli_webview.py
  - tests/conftest.py
autonomous: true
requirements: [WV-01, WV-02, WV-08]

must_haves:
  truths:
    - "tests/test_cli_webview.py exists with all six test classes covering the WV-01/WV-02/WV-08 test map"
    - "conftest.py exposes webview_analysis_dir and webview_server fixtures with guaranteed subprocess teardown (no orphan processes)"
    - "The test module parses and collects; assertions target the real module APIs (create_app, WebviewState, server lifecycle, webview CLI group)"
  artifacts:
    - path: "tests/test_cli_webview.py"
      provides: "Full Nyquist test scaffold for phase 90 (11 tests / 6 classes)"
      contains: "class TestWebviewLifecycle"
    - path: "tests/conftest.py"
      provides: "webview_analysis_dir + webview_server fixtures"
      contains: "def webview_analysis_dir"
  key_links:
    - from: "tests/test_cli_webview.py"
      to: "lucy_ng.cli.webview / lucy_ng.webview.app / lucy_ng.webview.server / lucy_ng.webview.state"
      via: "top-level imports (RED until waves 1-2 land)"
      pattern: "from lucy_ng.(cli.webview|webview)"
---

<objective>
Create the complete test infrastructure for phase 90 BEFORE any implementation exists (Nyquist Wave 0). This plan writes `tests/test_cli_webview.py` with all six test classes and the eleven named tests from 90-VALIDATION.md, plus two conftest fixtures (`webview_analysis_dir`, `webview_server`) with guaranteed teardown.

Purpose: every downstream implementation task (waves 1-2) has an automated `pytest` acceptance command that already exists. Tests are RED until the modules they exercise are built.
Output: `tests/test_cli_webview.py` + fixtures added to `tests/conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-server-cli-and-packaging/90-VALIDATION.md
@.planning/phases/90-server-cli-and-packaging/90-PATTERNS.md
@.planning/phases/90-server-cli-and-packaging/90-RESEARCH.md

<interfaces>
<!-- Contracts the tests must assert against. Waves 1-2 implement these exactly. -->

lucy_ng.webview.state.WebviewState (Pydantic v2 BaseModel):
  fields: pid: int, port: int, host: str, url: str, analysis_dir: str, started_at: str
  classmethod create(pid: int, port: int, host: str, analysis_dir: Path) -> WebviewState
  save(analysis_dir: Path) -> None            # writes <analysis_dir>/.webview.json
  classmethod load(analysis_dir: Path) -> WebviewState
  is_alive() -> bool                          # os.kill(pid, 0) probe

lucy_ng.webview.app.create_app(analysis_dir: Path) -> FastAPI
  GET /health -> {"status": "ok", "analysis_dir": "<abs path>"}

lucy_ng.webview.server:
  _pick_free_port(host: str = "127.0.0.1") -> int
  status(analysis_dir: Path) -> WebviewState | None   # None if no file OR stale (dead pid); cleans stale file
  start(analysis_dir: Path, port: int | None, host: str) -> WebviewState
  stop(analysis_dir: Path) -> tuple[bool, int | None]  # (stopped, pid)

lucy_ng.cli.webview.webview  # Click group with serve/stop/status + hidden _run; each supports --format {text,json}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add webview fixtures to conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (whole file — existing `@pytest.fixture` style; module header imports numpy/pytest ONLY, no fastapi)
    - .planning/phases/90-server-cli-and-packaging/90-PATTERNS.md lines 450-515 (conftest fixture sketch)
    - .planning/phases/90-server-cli-and-packaging/90-VALIDATION.md lines 57-63 (Wave 0 fixture requirements)
  </read_first>
  <action>
    Append two fixtures to tests/conftest.py. Do NOT add any fastapi/uvicorn/subprocess import at module top level — all runtime imports go inside the fixture bodies so the core test collection stays import-safe.

    `webview_analysis_dir(tmp_path: Path) -> Path`: create and return `tmp_path / "analysis"` (mkdir). Add `from pathlib import Path` at module top (stdlib only).

    `webview_server(webview_analysis_dir)`: a yielding fixture that starts a REAL server by invoking the serve command (not `_run` directly, so that `.webview.json` is actually written by the lifecycle code under test). Inside the body: import subprocess, sys, json, os, signal, time. Launch `[sys.executable, "-m", "lucy_ng.cli", "webview", "serve", str(webview_analysis_dir), "--format", "json"]`, capture stdout, parse the JSON payload (`url`/`pid`/`port`). Poll up to ~2s for `<analysis_dir>/.webview.json` to appear. Yield `(state_dict_or_None, webview_analysis_dir)`. Teardown MUST guarantee no orphan process: read `.webview.json` if present, `os.kill(pid, SIGTERM)` inside try/except ProcessLookupError, then `unlink(missing_ok=True)` the state file. Mark the fixture so it skips (pytest.skip) if `import fastapi` fails — the webview extra may be absent in a core env.
  </action>
  <acceptance_criteria>
    - `python -m py_compile tests/conftest.py` exits 0.
    - `grep -c "def webview_analysis_dir\|def webview_server" tests/conftest.py` reports 2.
    - `grep -v '^#' tests/conftest.py | grep -c "^import fastapi\|^import uvicorn"` reports 0 (no module-level webview imports).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the full test scaffold tests/test_cli_webview.py</name>
  <files>tests/test_cli_webview.py</files>
  <read_first>
    - tests/test_cli_lsd.py lines 1-10 (imports + CliRunner setup) and lines 109-128 (tmp_path class + help-assertion patterns)
    - tests/test_cli_main.py lines 9-60 (class-per-feature grouping)
    - .planning/phases/90-server-cli-and-packaging/90-VALIDATION.md lines 39-53 (the exact 11 test names + their req/type mapping)
    - .planning/phases/90-server-cli-and-packaging/90-PATTERNS.md lines 274-364 (test analogs + FastAPI TestClient usage)
    - <interfaces> block in this plan's <context> (the APIs to assert against)
  </read_first>
  <action>
    Create tests/test_cli_webview.py with six classes and the eleven tests named verbatim in 90-VALIDATION.md. Import `from click.testing import CliRunner` and `from lucy_ng.cli.webview import webview` at top (these resolve after wave 2 — RED until then, expected).

    TestImportSafety: `test_main_importable_without_fastapi` — importlib.import_module("lucy_ng.cli.main") succeeds AND assert `"fastapi" not in sys.modules` after a clean import (do NOT uninstall anything; if fastapi is already loaded by another test, reload in a subprocess via `subprocess.run([sys.executable, "-c", "import lucy_ng.cli.main, sys; assert 'fastapi' not in sys.modules"])` and assert returncode 0). `test_serve_without_webview_extra` — assert the `_require_webview` friendly-error path: monkeypatch so fastapi import fails, invoke `webview serve <dir>` via CliRunner, assert exit_code != 0 and "pip install lucy-ng[webview]" in output (no raw ImportError traceback).

    TestPackaging: `test_webview_extra_declared` — parse pyproject.toml (tomllib), assert `project.optional-dependencies.webview` exists and lists fastapi + uvicorn, and assert none of fastapi/uvicorn appear in `project.dependencies`.

    TestWebviewApp: `test_health_endpoint` — `create_app(tmp_path)` + FastAPI TestClient GET /health returns 200 and json()["status"] == "ok".

    TestFreePort: `test_pick_free_port` — `server._pick_free_port()` returns an int in 1024..65535 and a socket can bind it.

    TestWebviewStatus: `test_status_no_file` — `server.status(webview_analysis_dir)` returns None on a clean dir. `test_status_stale_pid` — write a `.webview.json` (via WebviewState) with a guaranteed-dead pid (e.g. an unused high pid probed with os.kill), call `server.status`, assert it returns None AND the stale file was removed.

    TestWebviewLifecycle (subprocess integration, use the webview_server fixture): `test_serve_writes_state_file` — after serve, `.webview.json` exists and contains pid/port/url. `test_serve_idempotent` — a second serve on the same live dir returns the SAME url and does not change the pid. `test_stop_terminates` — after stop, `.webview.json` is gone and the pid is dead (os.kill raises ProcessLookupError). `test_status_running` — status reports running while the fixture server is live.

    Every subprocess/lifecycle test must be robust to the webview extra being absent (pytest.skip if fastapi is not importable). No `time.sleep` longer than needed; prefer short poll loops.
  </action>
  <acceptance_criteria>
    - `python -m py_compile tests/test_cli_webview.py` exits 0.
    - `grep -c "def test_" tests/test_cli_webview.py` reports >= 11.
    - `grep -c "class TestImportSafety\|class TestPackaging\|class TestWebviewApp\|class TestFreePort\|class TestWebviewStatus\|class TestWebviewLifecycle" tests/test_cli_webview.py` reports 6.
    - All eleven method names from 90-VALIDATION.md are present: `grep -c "test_main_importable_without_fastapi\|test_serve_without_webview_extra\|test_webview_extra_declared\|test_health_endpoint\|test_pick_free_port\|test_status_no_file\|test_status_stale_pid\|test_serve_writes_state_file\|test_serve_idempotent\|test_stop_terminates\|test_status_running" tests/test_cli_webview.py` reports 11.
    - NOTE: `pytest tests/test_cli_webview.py` is expected RED at this wave (target modules not built yet). Green is asserted by plans 90-02 and 90-03.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → tmp_path fixtures | Tests read/write only pytest-provided temp dirs; no external/untrusted input |
| webview_server fixture → OS process | Fixture spawns a real subprocess; teardown must not leak processes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Denial of Service | webview_server fixture orphan process | mitigate | Teardown sends SIGTERM in try/except and unlinks `.webview.json`; fixture bound to tmp_path, single server per test |
| T-90-02 | Tampering | conftest module-level imports | mitigate | No fastapi/uvicorn/subprocess imports at module top; all inside fixture bodies to preserve core import-safety |
| T-90-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. fastapi/uvicorn already audited [OK] in 90-RESEARCH Package Legitimacy Audit; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `python -m py_compile tests/test_cli_webview.py tests/conftest.py` exits 0.
- Test scaffold present: 6 classes, 11 tests (grep gates above).
- Fixtures present with fastapi-free module header.
</verification>

<success_criteria>
- tests/test_cli_webview.py exists with all six classes and eleven named tests from 90-VALIDATION.md.
- tests/conftest.py has webview_analysis_dir + webview_server fixtures with guaranteed teardown and no module-level webview imports.
- Files compile; tests are RED (implementation pending), which is the correct Wave 0 state.
</success_criteria>

<output>
Create `.planning/phases/90-server-cli-and-packaging/90-01-SUMMARY.md` when done.
</output>
