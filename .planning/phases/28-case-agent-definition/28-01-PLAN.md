---
phase: 28-case-agent-definition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/.claude/agents/lucy-case-agent.md"
autonomous: true

must_haves:
  truths:
    - "Agent file exists at ~/.claude/agents/lucy-case-agent.md with valid YAML frontmatter"
    - "Agent has inlined critical knowledge (~500-700 lines) covering NMR background, CASE workflow, LSD basics, incremental HMBC strategy, and CASE-PROGRESS.md format"
    - "Agent NEVER mentions dereplication as part of its workflow (absolute separation)"
    - "Agent understands advisory constraints from supervisor (WHAT to fix, decides HOW autonomously)"
    - "Agent writes CASE-PROGRESS.md after every LSD iteration with all required fields"
  artifacts:
    - path: "~/.claude/agents/lucy-case-agent.md"
      provides: "Autonomous CASE agent definition with hybrid context inlining"
      min_lines: 700
  key_links:
    - from: "~/.claude/agents/lucy-case-agent.md"
      to: "skill/SKILL.md"
      via: "file path reference in <detailed_references> section"
      pattern: "skill/SKILL\\.md"
    - from: "~/.claude/agents/lucy-case-agent.md"
      to: "skill/diagnostic/SKILL.md"
      via: "file path reference in <detailed_references> section"
      pattern: "skill/diagnostic/SKILL\\.md"
    - from: "~/.claude/agents/lucy-case-agent.md"
      to: "skill/CASE/SKILL.md"
      via: "file path reference in <detailed_references> section"
      pattern: "skill/CASE/SKILL\\.md"
---

<objective>
Create the autonomous CASE agent definition file at ~/.claude/agents/lucy-case-agent.md with YAML frontmatter, inlined critical domain knowledge, workflow instructions, and CASE-PROGRESS.md format.

Purpose: This is the core deliverable of Phase 28 -- a spawnable agent that can execute the full CASE workflow autonomously, write structured progress logs for supervisor monitoring, and understand advisory constraints from the orchestrator. This agent proves multi-agent CASE works before the orchestrator (Phase 29) is built.

Output: A single agent definition file (~800-1000 lines) containing YAML frontmatter + role + inlined critical knowledge from skill files + detailed file path references + workflow + progress format + advisory constraint handling.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-case-agent-definition/28-RESEARCH.md

Source files to READ and extract content from:
@skill/SKILL.md (1,098 lines -- extract Sections 1, 6, 7, 9)
@skill/supervisor/SKILL.md (827 lines -- extract Section 8: CASE-PROGRESS.md format)
@skill/CASE/SKILL.md (721 lines -- reference for workflow detail)
@skill/diagnostic/SKILL.md (1,876 lines -- file path reference only)

Pattern reference (follow this agent structure):
@~/.claude/agents/gsd-executor.md (first 50 lines for YAML + role pattern)

Existing v2.0 agents for reference (being replaced/updated):
@.claude/agents/supervisor.md (existing project-level supervisor -- being dissolved into case.md orchestrator in Phase 29)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lucy-case-agent.md with YAML frontmatter and inlined critical knowledge</name>
  <files>~/.claude/agents/lucy-case-agent.md</files>
  <action>
  Read the following skill files to extract content for inlining:

  1. **skill/SKILL.md** -- Read the FULL file. Extract these sections:
     - Section 1 (NMR Background): excerpt the Experiment Types table, 13C Chemical Shift Regions table, and Common Pitfalls subsection (~80-120 lines)
     - Section 6 (LSD Reference): excerpt the core LSD command format and rules. Include MULT, HSQC, HMBC, BOND, LIST, PROP, ELIM command reference. Include the critical rule "NO ELIM on first run" and "HSQC before HMBC" ordering. (~100-150 lines)
     - Section 7 (Incremental HMBC Strategy): inline the FULL section (~150 lines). This is critical for preventing the "dump all correlations" anti-pattern.
     - Section 9 (CASE Workflow): excerpt the step-by-step procedure, including prerequisite checks, data assessment, LSD file creation, iterative refinement, and ranking. (~100-150 lines)

  2. **skill/supervisor/SKILL.md** -- Read the FULL file. Extract:
     - Section 8 (CASE-PROGRESS.md Format): inline the COMPLETE section including all required fields, example format, and writing protocol. This is what the supervisor monitors. (~150-200 lines)

  3. **skill/SKILL.md** additional sections for excerpts:
     - Section 2 (Spectral Quality Assessment): brief excerpt of key quality checks (~30-50 lines)
     - Section 10 (Error Tolerance and Ambiguity Detection): brief excerpt of close carbon handling and DEPT conflict resolution (~50-80 lines)
     - Section 11 (Confidence Scoring): brief excerpt of confidence levels and how to assign them (~30-50 lines)

  Create the file at ~/.claude/agents/lucy-case-agent.md with this structure:

  **YAML Frontmatter:**
  ```yaml
  ---
  name: lucy-case-agent
  description: >
    Autonomous CASE agent for NMR structure elucidation. Performs full de novo
    structure determination from Bruker NMR spectra using LSD solver. Writes
    CASE-PROGRESS.md after every LSD iteration. NEVER performs dereplication.
    Use when orchestrator spawns agent with compound path and molecular formula.
  tools: Read, Write, Bash, Glob, Grep
  model: inherit
  color: blue
  ---
  ```

  **Body structure (in this order):**

  ```
  <role>
    - Identity: autonomous CASE agent for NMR structure elucidation
    - Spawned by: /lucy-ng:case orchestrator
    - Job: determine molecular structure from NMR, write progress, follow incremental HMBC strategy
    - ABSOLUTE PROHIBITION: NEVER attempt dereplication (no `lucy dereplicate` commands, no database matching, no formula-based compound lookup)
    - Advisory constraints: understand WHAT supervisor says to fix, decide HOW to implement it autonomously
  </role>

  <absolute_prohibitions>
    - NEVER run `lucy dereplicate` in any form
    - NEVER attempt formula-based compound lookup or database matching
    - NEVER skip writing CASE-PROGRESS.md after an LSD iteration
    - Dereplication is a completely separate workflow handled by the orchestrator BEFORE this agent is spawned
  </absolute_prohibitions>

  <inlined_critical_knowledge>
    [~500-700 lines of extracted skill content as described above]
    Organize into numbered sections:
    1. NMR Background (Essential Concepts)
    2. Spectral Quality Assessment (Key Checks)
    3. LSD Command Reference (Core Commands)
    4. Incremental HMBC Strategy (Full Section)
    5. CASE Workflow (Step-by-Step)
    6. Error Tolerance and Ambiguity Detection (Key Patterns)
    7. Confidence Scoring (Levels and Assignment)
    8. CASE-PROGRESS.md Format (MANDATORY -- Full Section)
  </inlined_critical_knowledge>

  <detailed_references>
    File paths for comprehensive knowledge when deeper detail needed:
    - Full NMR domain: /Users/steinbeck/Dropbox/develop/lucy-ng/skill/SKILL.md
    - CASE-specific procedures: /Users/steinbeck/Dropbox/develop/lucy-ng/skill/CASE/SKILL.md
    - LSD diagnostics and manual: /Users/steinbeck/Dropbox/develop/lucy-ng/skill/diagnostic/SKILL.md
    - Project CLI reference: /Users/steinbeck/Dropbox/develop/lucy-ng/CLAUDE.md
  </detailed_references>

  <workflow>
    Step-by-step execution flow:
    1. Parse task instructions (compound path, formula, any advisory constraints)
    2. Check prerequisites (lucy --version, lucy lsd check)
    3. Read and assess NMR data quality
    4. Pick peaks (1D 13C, DEPT, HSQC, HMBC) using thin CLI commands
    5. Assess symmetry
    6. Build initial LSD file with core constraints (MULT, HSQC)
    7. Run LSD and write CASE-PROGRESS.md iteration 1
    8. Apply incremental HMBC strategy (add 5-10 high-confidence correlations per iteration)
    9. Iterate: run LSD, check solution count, add/refine constraints, write progress
    10. When solutions <= 10: rank via `lucy lsd rank` with `lucy predict c13`
    11. Report final results with confidence assessment

    If advisory constraints received from supervisor:
    - Read the advisory (WHAT to fix)
    - Read CASE-PROGRESS.md to understand current state
    - Resume from last iteration, applying the advisory fix
    - Continue normal workflow from there
  </workflow>

  <advisory_constraint_handling>
    When spawned with advisory constraints from the orchestrator:
    - The advisory tells you WHAT needs to change (e.g., "sp2 count is odd -- verify and correct")
    - You decide HOW to implement the fix (e.g., re-examine DEPT data, check atom multiplicities, adjust MULT definitions)
    - Read the existing CASE-PROGRESS.md to understand iteration history
    - Continue from the last iteration number, do NOT restart from scratch
    - Apply the fix, run LSD, and write the next CASE-PROGRESS.md iteration
  </advisory_constraint_handling>

  <output_format>
    Final output should be a summary including:
    - Best candidate structures (SMILES)
    - Ranking by 13C prediction MAE
    - Confidence assessment
    - Key decisions and ambiguities documented
    - Path to CASE-PROGRESS.md for full iteration history
  </output_format>
  ```

  **IMPORTANT content extraction rules:**
  - When extracting from skill files, preserve the markdown formatting (tables, headers, code blocks)
  - Do NOT paraphrase -- use the actual skill content (it has been carefully written for AI consumption)
  - For excerpts, include the most critical subsections and trim verbose explanations
  - For full sections (Section 7, supervisor Section 8), include everything
  - Total inlined content target: 500-700 lines (check with wc -l after writing)
  - Total file target: 800-1000 lines including structure and formatting
  </action>
  <verify>
  Run these checks:
  ```bash
  # File exists
  test -f ~/.claude/agents/lucy-case-agent.md && echo "EXISTS"

  # YAML frontmatter is valid (has opening and closing ---)
  head -1 ~/.claude/agents/lucy-case-agent.md | grep -q '^---$' && echo "YAML START OK"

  # Has required YAML fields
  grep -q '^name: lucy-case-agent' ~/.claude/agents/lucy-case-agent.md && echo "NAME OK"
  grep -q 'tools:' ~/.claude/agents/lucy-case-agent.md && echo "TOOLS OK"
  grep -q 'model: inherit' ~/.claude/agents/lucy-case-agent.md && echo "MODEL OK"

  # Line count in expected range
  wc -l ~/.claude/agents/lucy-case-agent.md

  # Has required structural sections
  grep -c '<role>' ~/.claude/agents/lucy-case-agent.md
  grep -c '<inlined_critical_knowledge>' ~/.claude/agents/lucy-case-agent.md
  grep -c '<detailed_references>' ~/.claude/agents/lucy-case-agent.md
  grep -c '<workflow>' ~/.claude/agents/lucy-case-agent.md
  grep -c 'CASE-PROGRESS.md' ~/.claude/agents/lucy-case-agent.md

  # Anti-pattern checks
  grep -c 'lucy dereplicate' ~/.claude/agents/lucy-case-agent.md  # Should only appear in prohibitions
  grep -ci 'Task' ~/.claude/agents/lucy-case-agent.md  # Should NOT list Task as a tool
  ```
  </verify>
  <done>
  File ~/.claude/agents/lucy-case-agent.md exists with:
  - Valid YAML frontmatter (name: lucy-case-agent, tools: Read/Write/Bash/Glob/Grep, model: inherit)
  - Role section with absolute dereplication prohibition
  - Inlined critical knowledge section with 500-700 lines of extracted skill content
  - Detailed references section with absolute file paths to skill files
  - Workflow section with step-by-step execution
  - Advisory constraint handling section
  - CASE-PROGRESS.md format section (full content from supervisor skill)
  - Total file length 700-1100 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate agent definition against all Phase 28 requirements</name>
  <files>~/.claude/agents/lucy-case-agent.md</files>
  <action>
  Perform comprehensive validation of the agent definition file against all 5 CASE requirements:

  **CASE-01: YAML Frontmatter Validation**
  - Verify `name: lucy-case-agent` (exact match)
  - Verify `description:` field exists and describes when to use agent
  - Verify `tools:` lists exactly Read, Write, Bash, Glob, Grep (NO Task tool)
  - Verify `model: inherit` (workaround for Task tool bug #18873)

  **CASE-02: Inlined Skill Content Validation**
  - Count lines in <inlined_critical_knowledge> section (target: 500-700)
  - Verify NMR background content present (Experiment Types table, 13C shifts table)
  - Verify LSD command reference present (MULT, HSQC, HMBC commands documented)
  - Verify incremental HMBC strategy present (Section 7 content)
  - Verify CASE workflow present (step-by-step procedure)
  - Verify CASE-PROGRESS.md format present (all required fields from supervisor Section 8)
  - Verify file path references present for skill/SKILL.md, skill/diagnostic/SKILL.md, skill/CASE/SKILL.md

  **CASE-03: CASE-PROGRESS.md Format Validation**
  - Verify progress format includes: iteration number, LSD file name, solution count
  - Verify format includes: constraints added, constraints removed, reasoning
  - Verify format includes: confidence assessment, sp2/H checks
  - Verify format includes: HMBC correlations used count
  - Verify "after EVERY LSD iteration" instruction is present

  **CASE-04: No Dereplication Validation**
  - Search entire file for `lucy dereplicate` -- should ONLY appear in prohibition sections
  - Verify <absolute_prohibitions> section exists with dereplication ban
  - Verify role section states "NEVER attempt dereplication"
  - Verify workflow does NOT include any dereplication step

  **CASE-05: Advisory Constraint Handling Validation**
  - Verify <advisory_constraint_handling> section exists
  - Verify it describes receiving WHAT to fix from supervisor
  - Verify it describes deciding HOW autonomously
  - Verify it describes resuming from last iteration (not restarting)

  If any check fails, fix the issue in the agent file before marking done.
  </action>
  <verify>
  Run a comprehensive validation script:
  ```bash
  AGENT=~/.claude/agents/lucy-case-agent.md
  PASS=0
  FAIL=0

  # CASE-01: YAML
  grep -q '^name: lucy-case-agent' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'description:' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'tools:.*Read.*Write.*Bash.*Glob.*Grep' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'model: inherit' $AGENT && ((PASS++)) || ((FAIL++))

  # CASE-02: Content presence
  grep -q 'Experiment Types' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'MULT' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'Incremental HMBC' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'CASE-PROGRESS.md' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'skill/SKILL.md' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'skill/diagnostic/SKILL.md' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'skill/CASE/SKILL.md' $AGENT && ((PASS++)) || ((FAIL++))

  # CASE-03: Progress format fields
  grep -q 'Solution count' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'Constraints added' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'sp2 count' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'H budget' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'EVERY LSD iteration' $AGENT && ((PASS++)) || ((FAIL++))

  # CASE-04: No dereplication in workflow
  grep -q 'NEVER.*dereplicat' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q '<absolute_prohibitions>' $AGENT && ((PASS++)) || ((FAIL++))

  # CASE-05: Advisory handling
  grep -q '<advisory_constraint_handling>' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'WHAT.*fix' $AGENT && ((PASS++)) || ((FAIL++))
  grep -q 'HOW.*autonomously\|decides HOW\|decide HOW' $AGENT && ((PASS++)) || ((FAIL++))

  echo "PASS: $PASS / FAIL: $FAIL / TOTAL: $((PASS + FAIL))"
  ```
  </verify>
  <done>
  All 5 CASE requirements validated:
  - CASE-01: YAML frontmatter correct (name, description, tools without Task, model: inherit)
  - CASE-02: Inlined content covers NMR background, LSD reference, HMBC strategy, CASE workflow, progress format; file path references point to all 3 skill files
  - CASE-03: CASE-PROGRESS.md format includes all required fields (solution count, constraints, reasoning, confidence, sp2/H checks)
  - CASE-04: No dereplication in workflow; absolute prohibition section present
  - CASE-05: Advisory constraint handling section present with WHAT/HOW separation and resume-from-last behavior
  Validation script reports 0 failures.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. File existence: `~/.claude/agents/lucy-case-agent.md` exists alongside other GSD agents
2. File size: 700-1100 lines (check with `wc -l`)
3. YAML parsing: frontmatter has name, description, tools, model fields
4. Content completeness: 8 inlined knowledge sections present
5. Anti-patterns absent: no Task tool, no dereplication in workflow, no directive commands
6. References valid: all 3 skill file paths are absolute and correct
7. All 5 CASE requirements (CASE-01 through CASE-05) satisfied

Note: Actual agent spawning via Task() is tested in Phase 32 (End-to-End Validation, VALD-01).
The executor cannot spawn agents -- only the orchestrator (Phase 29) can do that.
Phase 28 proves the agent DEFINITION is correct; Phase 32 proves it WORKS when spawned.
</verification>

<success_criteria>
- lucy-case-agent.md exists at ~/.claude/agents/ with valid YAML frontmatter
- Inlined critical knowledge contains 500-700 lines from skill/SKILL.md and skill/supervisor/SKILL.md
- All 5 CASE requirements (CASE-01 through CASE-05) pass validation
- Agent file follows gsd-executor.md structural pattern (role, knowledge, workflow, output)
- No anti-patterns: no Task tool, no dereplication workflow steps, no over-inlining (under 1100 total lines)
</success_criteria>

<output>
After completion, create `.planning/phases/28-case-agent-definition/28-01-SUMMARY.md`
</output>
