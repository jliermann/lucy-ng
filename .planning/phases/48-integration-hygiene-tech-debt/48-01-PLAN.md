---
phase: 48-integration-hygiene-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/lucy-ng/case.md
  - ~/.claude/agents/lucy-diagnostic.md
  - ~/.claude/agents/lucy-devils-advocate.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "case.md monitor_progress explicitly relays DA approval/block decision to lsd-engineer via SendMessage after writing DA section to CASE-PROGRESS.md"
    - "lsd-engineer spawn prompt in case.md does not contain 'Stop when solution_count <= 10' — uses task-driven stop semantics instead"
    - "solution-analyst spawn prompt in case.md does not contain '--shifts ...' literal — references task description for shift data"
    - "lucy-diagnostic.md 'What You Receive' example block uses analysis/ paths matching the actual delegate_specialist Task() call"
    - "lucy-devils-advocate.md Aromatic Ring Expectation check documents the CASE-PROGRESS.md read path for aromatic expectation data"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "DA relay SendMessage, corrected spawn prompts"
      contains: "[DA-APPROVED]"
    - path: "~/.claude/agents/lucy-diagnostic.md"
      provides: "Updated example paths"
      contains: "analysis/CASE-PROGRESS.md"
    - path: "~/.claude/agents/lucy-devils-advocate.md"
      provides: "Aromatic data relay path documentation"
      contains: "CASE-PROGRESS.md"
  key_links:
    - from: "case.md monitor_progress"
      to: "lsd-engineer"
      via: "SendMessage [DA-APPROVED] or [DA-BLOCKED]"
      pattern: "DA-APPROVED.*lsd-engineer"
---

<objective>
Fix 3 integration gaps in CASE team instruction files: add explicit DA approval relay from orchestrator to lsd-engineer (MISSING-01), correct 2 spawn prompt anti-patterns in case.md, update stale example paths in lucy-diagnostic.md, and document the aromatic data relay path in lucy-devils-advocate.md.

Purpose: Close medium-severity integration gaps identified in the v4.0 milestone audit. These gaps work in practice (Team runtime delivers messages implicitly) but violate the principle of explicit instruction-file completeness.

Output: 3 updated instruction files with 5 discrete edits.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-integration-hygiene-tech-debt/48-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DA approval relay and fix spawn prompts in case.md</name>
  <files>~/.claude/commands/lucy-ng/case.md</files>
  <action>
Make 3 targeted edits to case.md. Do NOT restructure any step or add new steps.

**Edit 1 — MISSING-01: DA approval relay in monitor_progress step:**

In the `monitor_progress` step, find the paragraph:
"After receiving [ITERATION-COMPLETE] from lsd-engineer AND [VALIDATION-PASSED] from devils-advocate, AND after writing both sections to CASE-PROGRESS.md:"

Immediately BEFORE the numbered list that starts with "1. Extract solution_count...", insert this block:

```
**Relay DA decision to lsd-engineer:**

After writing the Devils-Advocate section to CASE-PROGRESS.md, relay the approval decision to lsd-engineer:

If [VALIDATION-PASSED] was received:
\`\`\`
SendMessage(
  type="message",
  recipient="lsd-engineer",
  content="[DA-APPROVED] Iteration {N} — Validation passed. Proceed with solver run.
           DA findings: {brief summary from [VALIDATION-PASSED] message}",
  summary="DA approved iteration {N} — proceed with solver"
)
\`\`\`

If [VALIDATION-BLOCKED] was received:
\`\`\`
SendMessage(
  type="message",
  recipient="lsd-engineer",
  content="[DA-BLOCKED] Iteration {N} — Validation blocked. Critical issues:
           {issues from [VALIDATION-BLOCKED] message}
           Fix these before running solver.",
  summary="DA blocked iteration {N} — fix required"
)
\`\`\`
```

**Edit 2 — lsd-engineer spawn prompt stop condition:**

In the `spawn_case_team` step, find the lsd-engineer Task() prompt. Locate the line:
"Stop when solution_count <= 10 or ~10 iterations reached."

Replace it with:
"Continue claiming tasks from TaskList. You will receive a shutdown_request when CASE is complete."

**Edit 3 — solution-analyst spawn prompt shifts reference:**

In the `spawn_case_team` step, find the solution-analyst Task() prompt. Locate:
"Run: lucy lsd rank solutions.smi --shifts '...'"

Replace with:
"Read experimental 13C shifts from the task description (coordinator embeds the full shift list when creating the ranking task)."

Verify after all edits:
- Grep for "DA-APPROVED" in case.md — must find the new SendMessage block
- Grep for "Stop when solution_count" in case.md — must NOT find any matches
- Grep for "shifts '...'" in case.md — must NOT find any matches
  </action>
  <verify>
Read case.md after edits. Verify:
1. "[DA-APPROVED]" appears in monitor_progress step with SendMessage to lsd-engineer
2. "[DA-BLOCKED]" appears in monitor_progress step with SendMessage to lsd-engineer
3. lsd-engineer spawn prompt says "Continue claiming tasks from TaskList"
4. solution-analyst spawn prompt says "Read experimental 13C shifts from the task description"
5. No occurrences of "Stop when solution_count <= 10"
6. No occurrences of "--shifts '...'"
  </verify>
  <done>
case.md has explicit DA approval relay to lsd-engineer (MISSING-01 closed), lsd-engineer spawn prompt uses task-driven stop semantics, and solution-analyst spawn prompt references task description for shifts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update stale paths in lucy-diagnostic.md and document aromatic relay in lucy-devils-advocate.md</name>
  <files>~/.claude/agents/lucy-diagnostic.md, ~/.claude/agents/lucy-devils-advocate.md</files>
  <action>
Make 2 targeted edits across 2 files.

**Edit 4 — lucy-diagnostic.md stale example paths:**

Find the `## What You Receive from Orchestrator` section (around line 1085). Within the code block that starts with "Analyze LSD failure for compound at <path>.", find these two lines:

```
- <path>/CASE-PROGRESS.md (iteration history)
- <path>/<filename>.lsd (latest LSD file)
```

Replace the entire "Read:" subsection within that code block with:

```
Read:
- <compound_path>/analysis/CASE-PROGRESS.md (iteration history with per-agent sections)
- <compound_path>/analysis/<latest_iteration>/compound.lsd (latest LSD file)
  Note: The LSD file header contains a JSON constraint inventory block between
  ; === CONSTRAINT INVENTORY v1 === and ; === END CONSTRAINT INVENTORY ===
  Extract this inventory in Step 1 for diagnosis.
```

Also update the "Analyze LSD failure for compound at <path>" line to use "<compound_path>" for consistency, and update the "Diagnose:" and "Output:" subsections to use "<compound_path>" instead of "<path>":

```
Analyze LSD failure for compound at <compound_path>.

Read:
- <compound_path>/analysis/CASE-PROGRESS.md (iteration history with per-agent sections)
- <compound_path>/analysis/<latest_iteration>/compound.lsd (latest LSD file)
  Note: The LSD file header contains a JSON constraint inventory block between
  ; === CONSTRAINT INVENTORY v1 === and ; === END CONSTRAINT INVENTORY ===
  Extract this inventory in Step 1 for diagnosis.

Diagnose:
- Why did LSD return <0 solutions | 1000+ solutions>?
- Run systematic checks for <failure type>
- Identify root cause with evidence

Output:
- Write structured report to <compound_path>/analysis/DIAGNOSTIC-REPORT.md
- Include: findings, root cause, recommended fixes with LSD command examples

Confidence: Rate all findings and recommendations as HIGH/MEDIUM/LOW
```

Do NOT change anything in Step 1 body text (lines ~866-906) — that is already correct.

**Edit 5 — lucy-devils-advocate.md aromatic data relay path:**

Find the `### Aromatic Ring Expectation` subsection in Section 3 (Structural Integrity Checks). It starts with:
"If nmr-chemist's [SETUP-COMPLETE] message reports 'aromatic ring expected'..."

Add the following note immediately AFTER the first sentence of this subsection (before "AND ranking results are available"):

"**Data source:** To determine whether nmr-chemist flagged aromatic expectation, read `<compound_path>/analysis/CASE-PROGRESS.md` and look for the `Aromatic expectation:` field in the `### NMR-Chemist` subsection of `## Setup`. The coordinator writes this data from the [SETUP-COMPLETE] message."

Verify after edits:
- Grep for "analysis/CASE-PROGRESS.md" in lucy-diagnostic.md — must find in example block
- Grep for "analysis/<latest_iteration>/compound.lsd" in lucy-diagnostic.md — must find in example block
- Grep for "CASE-PROGRESS.md" in the Aromatic Ring Expectation section of lucy-devils-advocate.md — must find the data source note
  </action>
  <verify>
Read both files after edits. Verify:
1. lucy-diagnostic.md "What You Receive" example block uses "analysis/CASE-PROGRESS.md" and "analysis/<latest_iteration>/compound.lsd" paths
2. lucy-diagnostic.md example block uses "<compound_path>" (not bare "<path>")
3. lucy-diagnostic.md Step 1 body is unchanged
4. lucy-devils-advocate.md Aromatic Ring Expectation check contains "Data source:" note referencing CASE-PROGRESS.md
  </verify>
  <done>
lucy-diagnostic.md example block uses correct analysis/ paths matching the actual delegate_specialist Task() call. lucy-devils-advocate.md documents how DA reads aromatic expectation data from CASE-PROGRESS.md.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Read case.md — confirm [DA-APPROVED] relay, corrected spawn prompts
2. Read lucy-diagnostic.md — confirm analysis/ paths in example block
3. Read lucy-devils-advocate.md — confirm aromatic data relay documentation
4. No Python code changed, no tests changed, no other agent files changed
</verification>

<success_criteria>
1. case.md monitor_progress has explicit SendMessage relay of DA approval/block to lsd-engineer
2. lsd-engineer spawn prompt uses "Continue claiming tasks from TaskList" (no "Stop when")
3. solution-analyst spawn prompt references task description for shifts (no "--shifts '...'")
4. lucy-diagnostic.md example block matches analysis/ path convention
5. lucy-devils-advocate.md documents CASE-PROGRESS.md read path for aromatic data
</success_criteria>

<output>
After completion, create `.planning/phases/48-integration-hygiene-tech-debt/48-01-SUMMARY.md`
</output>
