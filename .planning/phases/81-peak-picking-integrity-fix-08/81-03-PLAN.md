---
phase: 81-peak-picking-integrity-fix-08
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md
autonomous: true
requirements:
  - FIX-08

must_haves:
  truths:
    - "nmr-chemist skill states SNR ≥ 5 = signal, SNR < 5 = noise (not just a quality tier)"
    - "nmr-chemist skill states more signals than carbons = noise, not symmetry"
    - "nmr-chemist skill states a 160–180 ppm signal with SNR ≥ 5 is a C=O — never discard it"
  artifacts:
    - path: "/Users/steinbeck/.claude/agents/lucy-nmr-chemist.md"
      provides: "Three SNR/carbonyl rules for blind agent"
      contains: "SNR ≥ 5"
  key_links:
    - from: "lucy-nmr-chemist.md Section 2 or Section 5"
      to: "peak picking decision"
      via: "explicit SNR floor rules and carbonyl-never-discard rule"
      pattern: "SNR.*5.*signal"
---

<objective>
Update the nmr-chemist agent skill with three explicit rules that encode the lessons from the Phase-80 CASE9 UAT failure. The rules must be prominent and unambiguous so a fresh blind instance does not repeat the carbonyl-discard pattern.

Purpose: The Phase-80 blind agent manually curated 76 peaks to 8 and dropped the ester carbonyl (SNR 17, 166.08 ppm). The agent had no skill rule saying "keep everything ≥ SNR 5", "overcount = noise not symmetry", or "160–180 ppm ≥ SNR 5 = C=O, don't touch it". These three rules close the skill gap.

Output: Updated lucy-nmr-chemist.md with three new rules in Section 2 (Critical Pitfalls) or a new dedicated SNR section placed before the quality tiers table.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-UAT-VERDICT.md

<interfaces>
<!-- From /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md (extracted structure) -->

Section 2: Critical Pitfalls (Condensed) — Pitfalls 1–9, lines ~46–63
Section 4: Spectral Quality Assessment — S/N Quality Tiers table, lines ~83–101
  (currently maps SNR ranges to quality labels but states NO accept/reject cutoff)
Section 5: Statistical Detection Protocol — lines ~107+
Section 5a: DBE Self-Check (MANDATORY) — lines ~156+
  (currently says "any peak in 160-220 ppm with SNR 3-20?" — contradicts the FIX-08 floor)

Current Pitfall 1 (Symmetry): "Signal count < atom count usually means equivalent atoms".
This is the UNDERCOUNT direction only. No pitfall covers OVERCOUNT (observed > expected = noise).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three SNR/carbonyl rules to lucy-nmr-chemist.md</name>
  <files>/Users/steinbeck/.claude/agents/lucy-nmr-chemist.md</files>
  <read_first>
    - /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md (full file — extract Section 2 Pitfalls list end, Section 4 S/N Quality Tiers header, and Section 5a DBE Self-Check "SNR 3-20" language to find exact insertion points)
    - .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-UAT-VERDICT.md (evidence table: snr_floor=5.0 → 29 peaks, 0 impossibles, carbonyl alone above 150.80 ppm)
  </read_first>
  <action>
    Make four targeted edits to the skill file. Do NOT rewrite sections — surgical insertions only.

    EDIT 1 — Add Pitfall 10 after the existing Pitfall 9 block in Section 2:

    "**Pitfall 10 — SNR floor (CRITICAL):** The default picker returns only peaks with SNR >= 5.
    Peaks at SNR < 5 are baseline ripple, not real carbons. If you ever see more peaks than expected
    carbons, the excess is noise — NOT symmetry. Do NOT manually curate by deleting peaks; instead
    re-pick with `lucy pick 1d <path> --snr-floor 5 --format json` and use that list as-is.
    Overcount (observed > formula carbons) → run `lucy analyze symmetry` first; if it reports
    OVERCOUNT ALARM or Warning: more signals than carbons, discard your current list and re-pick
    at snr_floor 5."

    EDIT 2 — Add Pitfall 11 after Pitfall 10:

    "**Pitfall 11 — Carbonyl never discard:** A peak in 160-180 ppm with SNR >= 5 is an ester or
    carboxylic acid carbonyl (C=O). It is ALWAYS real. Never remove it during manual curation.
    Loss of this peak misallocates DBE, reads the aromatic ring as monosubstituted instead of
    para-disubstituted, and excludes the correct structure before LSD runs."

    EDIT 3 — Update the DBE Self-Check language in Section 5a:
    Change "any peak in 160-220 ppm with SNR 3-20?" to
    "any peak in 160-220 ppm with SNR >= 5?" (SNR 3-4 peaks at this region are baseline ripple;
    a genuine carbonyl will have SNR >= 8 in typical Bruker 13C at 400+ MHz)

    EDIT 4 — Add a cutoff note before or after the S/N Quality Tiers table header in Section 4:
    "**SNR cutoff: SNR >= 5 = signal (keep), SNR < 5 = noise (discard). This is independent of
    spectrum quality tier — even in a 'poor' spectrum, SNR < 5 peaks are not real carbons.**"
  </action>
  <verify>
    <automated>grep -c "SNR ≥ 5\|SNR >= 5" /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md</automated>
  </verify>
  <done>
    grep "SNR ≥ 5\|SNR >= 5" returns ≥ 3 matches (Pitfall 10, Pitfall 11, Section 5a update);
    grep -i "never discard" returns ≥ 1 match in the 160-180 ppm context;
    grep -i "more signals than carbons\|OVERCOUNT" returns ≥ 1 match;
    grep "Pitfall 10" returns 1 match;
    grep "Pitfall 11" returns 1 match;
    file frontmatter YAML is valid (verify command below)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill file | ~/.claude/agents/ — local user-owned agent skill; no network exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-03-SC | Tampering | No packages installed | accept | Text-only edit to a Markdown agent skill file; no code execution risk |

Note: Skill file edit is purely instructional text. No new code paths, no new attack surface.
</threat_model>

<verification>
Run after editing:

```bash
grep -c "SNR ≥ 5\|SNR >= 5" /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md
grep -in "never discard" /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md
grep -in "overcount\|more signals than carbons" /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md
python -c "import yaml; yaml.safe_load(open('/Users/steinbeck/.claude/agents/lucy-nmr-chemist.md').read().split('---')[1])"
```

All four must succeed (grep commands return ≥1 match each; Python exits 0 with no output).
</verification>

<success_criteria>
- Pitfall 10 present: "more peaks than expected carbons = noise, not symmetry; re-pick at snr_floor 5"
- Pitfall 11 present: "160-180 ppm SNR >= 5 = C=O, never discard it"
- Section 4 S/N Tiers: explicit "SNR >= 5 = signal, SNR < 5 = noise" cutoff statement
- Section 5a DBE-check updated from "SNR 3-20" to "SNR >= 5"
- grep -c returns ≥3 for "SNR ≥ 5" or "SNR >= 5"
- Frontmatter YAML parses cleanly (python yaml.safe_load exits 0)
</success_criteria>

<output>
Create `.planning/phases/81-peak-picking-integrity-fix-08/81-03-SUMMARY.md` when done
</output>
