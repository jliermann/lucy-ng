---
phase: 81-peak-picking-integrity-fix-08
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lucy_ng/processing/peak_picker.py
  - src/lucy_ng/cli/pick.py
autonomous: true
requirements:
  - FIX-08

must_haves:
  truths:
    - "AdaptivePeakPicker.pick_peaks / pick_peaks_instance default snr_floor is 5.0 (not 3.0)"
    - "lucy pick 1d <path> --snr-floor 5 routes snr_floor=5.0 to AdaptivePeakPicker"
    - "lucy pick 1d <path> -t 0.05 still works (backward compat)"
    - "CASE9/12 re-picked with snr_floor=5.0 returns ≤30 peaks and the carbonyl is the highest-ppm peak"
  artifacts:
    - path: "src/lucy_ng/processing/peak_picker.py"
      provides: "AdaptivePeakPicker with snr_floor default = 5.0"
      contains: "snr_floor: float = 5.0"
    - path: "src/lucy_ng/cli/pick.py"
      provides: "lucy pick 1d --snr-floor flag"
      contains: "--snr-floor"
  key_links:
    - from: "cli/pick.py pick_1d()"
      to: "AdaptivePeakPicker.pick_peaks(snr_floor=...)"
      via: "new snr_floor kwarg forwarded from CLI option"
      pattern: "snr_floor"
---

<objective>
Raise the 13C peak-picker SNR floor default from 3.0 (IUPAC LoD — detects baseline ripple) to 5.0 (signal-vs-noise cutoff — retains every real carbon, eliminates the 50 noise peaks that led the Phase-80 blind agent to discard the CASE9 ester carbonyl). Also expose `--snr-floor` in `lucy pick 1d` so the floor can be tuned from the CLI without writing Python.

Purpose: A fresh blind agent picking CASE9/12 at default snr_floor=5.0 receives ≤30 peaks with the carbonyl as the highest-ppm signal, not 76 peaks including 13 physically impossible >220 ppm artifacts.

Output: Modified peak_picker.py (default changed in two method signatures) + modified pick.py (new `--snr-floor` option forwarded to picker).
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-UAT-VERDICT.md

<interfaces>
<!-- Extracted from src/lucy_ng/processing/peak_picker.py -->

AdaptivePeakPicker.pick_peaks signature (static):
  pick_peaks(spectrum, threshold=0.05, detect_negative=False, snr_floor=3.0, use_snr=True) -> PeakList1D

AdaptivePeakPicker.pick_peaks_instance signature (instance):
  pick_peaks_instance(self, spectrum, threshold=0.05, detect_negative=False, snr_floor=3.0, use_snr=True) -> PeakList1D

_compute_snr_threshold signature:
  _compute_snr_threshold(data, ppm_scale, solvent=None, k=3.0) -> tuple[float, float]
  # k=3.0 is a DIFFERENT parameter — it is passed snr_floor at call time; this function's
  # own default is irrelevant because callers always pass k=snr_floor explicitly.

From src/lucy_ng/cli/pick.py pick_1d():
  - Accepts --threshold / -t (float, optional); when None uses SNR mode
  - Calls AdaptivePeakPicker.pick_peaks(..., use_snr=use_snr) without passing snr_floor
  - When threshold is None: use_snr=True, no snr_floor kwarg → takes the method default (currently 3.0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raise snr_floor default to 5.0 in peak_picker.py</name>
  <files>src/lucy_ng/processing/peak_picker.py</files>
  <read_first>
    - src/lucy_ng/processing/peak_picker.py (lines 157–200: pick_peaks static method signature; lines 193–218: pick_peaks_instance signature — both have `snr_floor: float = 3.0`)
    - .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-UAT-VERDICT.md (SNR evidence table: at snr_floor=5.0, 0 impossible peaks, carbonyl stands alone above 150.80 ppm)
    - tests/test_peak_picker_snr.py (TestCASE9Regression and TestCASE1Regression — existing assertions to keep green)
  </read_first>
  <behavior>
    - After change: AdaptivePeakPicker.pick_peaks(spectrum) with CASE9/12 data → ≤30 peaks, 0 above 230 ppm, peak near 164–168 ppm present
    - After change: AdaptivePeakPicker.pick_peaks(spectrum) with CASE1 (ibuprofen) data → ≥9 peaks (existing TestCASE1Regression floor still passes)
    - Explicit snr_floor kwarg still overrides: pick_peaks(spectrum, snr_floor=3.0) → behavior unchanged from prior default
    - _compute_snr_threshold k parameter is unaffected (it is always called with the snr_floor value, not its own default)
  </behavior>
  <action>
    PRE-FLIGHT BEFORE CHANGING THE DEFAULT: Before writing the default change, run the first
    automated verify command below. If it returns fewer than 9 ibuprofen peaks at snr_floor=5.0,
    DO NOT make the default change — stop and consult the user.

    In peak_picker.py, change `snr_floor: float = 3.0` to `snr_floor: float = 5.0` in TWO places:
    (1) AdaptivePeakPicker.pick_peaks() static method parameter (line ~163)
    (2) AdaptivePeakPicker.pick_peaks_instance() method parameter (line ~200)

    Update the docstring k description in both methods from "IUPAC LoD: k=3" to "k=5 (signal/noise separation; use k=3 for exploratory re-pick)".

    Do NOT change _compute_snr_threshold's own k=3.0 default — it is always called with an explicit k argument and is not part of the public API for this fix.

    No other logic changes. The k parameter in detect_intensity_symmetry is unrelated (that function does not use snr_floor).
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && python -c "from lucy_ng.processing.peak_picker import AdaptivePeakPicker; from lucy_ng.readers import BrukerReader; s=BrukerReader.read_1d('data/Ibuprofen/2'); p=AdaptivePeakPicker.pick_peaks(s, snr_floor=5.0); assert len(p.peaks)>=9, f'CASE1 drops to {len(p.peaks)} at snr_floor=5 -- STOP, do not change default'; print(f'PRE-FLIGHT OK: {len(p.peaks)} peaks at snr_floor=5')"</automated>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && python -c "from lucy_ng.processing.peak_picker import AdaptivePeakPicker; import inspect; sig = inspect.signature(AdaptivePeakPicker.pick_peaks); assert sig.parameters['snr_floor'].default == 5.0, f'Got {sig.parameters[chr(34)+chr(115)+chr(110)+chr(114)+chr(95)+chr(102)+chr(108)+chr(111)+chr(111)+chr(114)+chr(34)].default}'; print('OK: snr_floor default = 5.0')"</automated>
  </verify>
  <done>snr_floor default is 5.0 in both pick_peaks and pick_peaks_instance; pre-flight passes (ibuprofen ≥9 peaks at snr_floor=5.0); existing tests/test_peak_picker_snr.py::TestCASE1Regression::test_case1_count_unchanged passes; CASE9 regression (skipif) passes on machines with CASE9 data</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose --snr-floor in lucy pick 1d CLI</name>
  <files>src/lucy_ng/cli/pick.py</files>
  <read_first>
    - src/lucy_ng/cli/pick.py (full file — 80 lines; current options: --threshold/-t; current call site: AdaptivePeakPicker.pick_peaks without snr_floor kwarg)
    - tests/test_cli_pick.py (existing test patterns to follow; add new test cases in the same file)
  </read_first>
  <behavior>
    - `lucy pick 1d <path> --snr-floor 5` (or equivalently `-s 5`) → calls pick_peaks with snr_floor=5.0
    - `lucy pick 1d <path> --snr-floor 3` → calls pick_peaks with snr_floor=3.0 (explicit override to old behavior)
    - `lucy pick 1d <path>` (no flags) → calls pick_peaks with default snr_floor (now 5.0 after Task 1)
    - `lucy pick 1d <path> -t 0.1` → unchanged legacy behavior (use_snr=False, snr_floor irrelevant)
    - `--snr-floor` and `-t` are mutually exclusive: if both provided, -t takes priority (existing use_snr=False branch already handles this)
    - `--snr-floor` does not affect 2D picking commands (only the `1d` subcommand)
  </behavior>
  <action>
    In pick.py pick_1d() function:

    1. Add a new Click option above the existing `--format` option:
       `--snr-floor` / `-s`, type=float, default=None, help="SNR floor multiplier k (default: 5.0 signal/noise; use 3.0 for exploratory re-pick)."

    2. Add `snr_floor: float | None` to the pick_1d() function signature.

    3. In the SNR-mode branch (when threshold is None, use_snr=True), forward the flag:
       - If snr_floor is not None: pass `snr_floor=snr_floor` to AdaptivePeakPicker.pick_peaks()
       - If snr_floor is None: do not pass snr_floor (let the method default apply)

    4. In the JSON output dict, add `"snr_floor_used"` field showing the effective snr_floor value.
       When use_snr=True: snr_floor_used = snr_floor if snr_floor is not None else 5.0 (the new method default).
       When use_snr=False: snr_floor_used = None.

    Keep `-t/--threshold` unchanged. No other changes to output format.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && python -m pytest tests/test_cli_pick.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    `lucy pick 1d data/Ibuprofen/2 --snr-floor 3 --format json` exits 0 and returns JSON with "snr_floor_used": 3.0;
    `lucy pick 1d data/Ibuprofen/2 --format json` exits 0 and returns JSON with "snr_floor_used": 5.0;
    all existing test_cli_pick.py tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → CLI | Bruker experiment directory path supplied by user; already trusted (user-owned local data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01-SC | Tampering | No new packages installed in this plan | accept | No npm/pip/cargo installs; pure Python stdlib changes only |

Note: This is a local NMR CLI bugfix. No network exposure, no new attack surface. The `--snr-floor` option accepts a float from the command line; it is passed directly to scipy `find_peaks` as a multiplier — no path traversal, no injection risk. Negative or zero values would produce a zero threshold (all peaks pass), which is a user error, not a security issue. Existing bounds are acceptable for a trusted-user local tool.
</threat_model>

<verification>
Run from `/Users/steinbeck/Dropbox/develop/lucy-ng`:

```
pytest tests/test_peak_picker_snr.py tests/test_cli_pick.py -x -q
ruff check src/lucy_ng/processing/peak_picker.py src/lucy_ng/cli/pick.py
mypy src/lucy_ng/processing/peak_picker.py src/lucy_ng/cli/pick.py
```

All must exit 0.
</verification>

<success_criteria>
- Pre-flight passes: ibuprofen (data/Ibuprofen/2) yields ≥9 peaks at explicit snr_floor=5.0 before the default is changed
- `AdaptivePeakPicker.pick_peaks` and `pick_peaks_instance` both show `snr_floor: float = 5.0` in their signatures
- `lucy pick 1d <path> --snr-floor 3` routes snr_floor=3.0 to the picker
- `lucy pick 1d <path>` (no flags) uses the new 5.0 default
- `-t/--threshold` still works; JSON output gains `snr_floor_used` field
- Existing test_peak_picker_snr.py and test_cli_pick.py test suites pass
- `ruff check` and `mypy` clean on modified files
</success_criteria>

<output>
Create `.planning/phases/81-peak-picking-integrity-fix-08/81-01-SUMMARY.md` when done
</output>
