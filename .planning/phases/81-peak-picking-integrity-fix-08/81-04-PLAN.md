---
phase: 81-peak-picking-integrity-fix-08
plan: 04
type: execute
wave: 2
depends_on:
  - 81-01
  - 81-02
files_modified:
  - tests/test_peak_picking_integrity.py
autonomous: true
requirements:
  - FIX-08

must_haves:
  truths:
    - "CASE9/12 picked at snr_floor=5 returns ≤30 peaks, none >230 ppm, carbonyl in 164–168 ppm present"
    - "Overcount guard fires on synthetic 76-vs-12 scenario — CLI emits overcount_alarm:true, symmetry_analysis emits OVERCOUNT ALARM"
    - "CASE1 ibuprofen is not regressed — peak count still ≥9 at snr_floor=5 (the new default)"
    - "Full test suite (pytest) stays green"
  artifacts:
    - path: "tests/test_peak_picking_integrity.py"
      provides: "Phase-81 FIX-08 regression test suite"
      contains: "TestFIX08"
  key_links:
    - from: "tests/test_peak_picking_integrity.py"
      to: "AdaptivePeakPicker.pick_peaks(snr_floor=5.0)"
      via: "CASE9 skipif test + synthetic test"
      pattern: "snr_floor.*5"
    - from: "tests/test_peak_picking_integrity.py"
      to: "analyze_symmetry CLI / SymmetryAnalysisResult.summary()"
      via: "synthetic overcount scenario"
      pattern: "overcount_alarm"
---

<objective>
Write the FIX-08 regression test suite that proves the Phase-81 fixes (Plans 01+02) hold. Three test classes:

1. CASE9 integration: re-pick CASE9/12 at k=5 → ≤30 peaks, none >230 ppm, carbonyl in 164–168 ppm present (skipif CASE9 data not on machine).
2. Overcount guard: synthetic spectrum generating >expected peaks → CLI `analyze symmetry` emits "overcount_alarm": true; `SymmetryAnalysisResult.summary()` contains "OVERCOUNT ALARM".
3. CASE1 non-regression: ibuprofen picked at new default snr_floor=5.0 → ≥9 peaks (unchanged from prior).

Purpose: Lock in the Phase-81 invariants so future changes cannot silently reintroduce the noise-flood or overcount-blindness defect without a failing test.

Scope note: This plan delivers the code fixes only. The blind re-UAT (CASE9 + CASE1, fresh instances per feedback_blind_uat) is a separate gate run by the orchestrator after Phase 81 completes — this plan does NOT claim or test that the UAT is passed.

Output: New `tests/test_peak_picking_integrity.py` (self-contained for the overcount guard; uses CASE9 data under `pytest.mark.skipif` for the integration tests, mirroring the pattern in `test_peak_picker_snr.py`).
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-UAT-VERDICT.md

<!-- Summary references from Wave 1 plans -->
@.planning/phases/81-peak-picking-integrity-fix-08/81-01-SUMMARY.md
@.planning/phases/81-peak-picking-integrity-fix-08/81-02-SUMMARY.md

<interfaces>
<!-- From tests/test_peak_picker_snr.py — the pattern to follow for CASE9 skipif tests -->

CASE9_C13 path:
  Path("/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/12")

skipif pattern:
  @pytest.mark.skipif(not CASE9_C13.exists(), reason="CASE9 dataset not available on this machine")

In-repo CASE1 path (always available):
  DATA_DIR = Path(__file__).parent.parent / "data"
  CASE1_C13 = DATA_DIR / "Ibuprofen" / "2"

<!-- From src/lucy_ng/cli/analyze.py after Plan 02 -->
analyze_symmetry JSON output fields (after FIX-08 plan 02):
  {"formula", "expected_carbons", "observed_peaks", "difference", "overcount_alarm": bool, "overcount_excess": int}

<!-- From src/lucy_ng/analysis/symmetry_analysis.py after Plan 02 -->
SymmetryAnalysisResult.summary() contains "OVERCOUNT ALARM" when missing_carbons < 0

<!-- For synthetic overcount test: create a Spectrum1D with a flat noise floor + 20 sharp peaks,
     then invoke SymmetryAnalysisResult with signal_count=20 and expected_carbons=12 -->
Spectrum1D constructor (from lucy_ng/models):
  Spectrum1D(data: np.ndarray, ppm_scale: np.ndarray, nucleus: str, frequency: float, solvent: str | None = None)

SymmetryAnalysisResult constructor:
  SymmetryAnalysisResult(molecular_formula, hydrogen_budget, intensity_report, signal_count, expected_carbons, missing_carbons)
  missing_carbons = expected_carbons - signal_count
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test_peak_picking_integrity.py (FIX-08 regression suite)</name>
  <files>tests/test_peak_picking_integrity.py</files>
  <read_first>
    - tests/test_peak_picker_snr.py (lines 1–30: imports, DATA_DIR, CASE9_C13, CASE1_C13 paths; lines 104–145: TestCASE9Regression and TestCASE1Regression — structure to follow for the new file)
    - tests/test_cli_analyze.py (lines 1–10: imports and CliRunner pattern for the analyze symmetry CLI test)
    - .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-UAT-VERDICT.md (evidence: snr_floor=5.0 → 29 peaks, none >220, carbonyl at highest ppm ≈166.08; snr_floor=3.0 → 76 peaks, 13 impossible >220)
    - .planning/phases/81-peak-picking-integrity-fix-08/81-01-SUMMARY.md (confirm snr_floor default is 5.0 as shipped)
    - .planning/phases/81-peak-picking-integrity-fix-08/81-02-SUMMARY.md (confirm overcount_alarm JSON field name and OVERCOUNT ALARM text)
  </read_first>
  <behavior>
    Class TestFIX08CASE9Integration (all skipif CASE9_C13.exists()):
      - test_case9_snr5_peak_count: pick CASE9/12 with snr_floor=5.0 → len(peaks.peaks) ≤ 30
      - test_case9_snr5_no_impossible_peaks: pick CASE9/12 with snr_floor=5.0 → no peak.position > 230.0
      - test_case9_snr5_carbonyl_present: pick CASE9/12 with snr_floor=5.0 → at least one peak in 164.0–168.0 ppm
      - test_case9_default_snr_is_5: pick CASE9/12 with default args → same results as snr_floor=5.0 explicit (asserts default=5 is actually in effect)

    Class TestFIX08OvercountGuard (fully synthetic — no external data):
      - test_overcount_symmetry_analysis_summary: build SymmetryAnalysisResult with signal_count=76, expected_carbons=12, missing_carbons=-64 → "OVERCOUNT ALARM" in summary()
      - test_overcount_cli_json: use CliRunner to invoke `analyze symmetry C4H8 <path>` where the 13C data has many peaks → overcount_alarm=True in JSON output. Use CASE1 path (C13H18O2) with formula "C4H8" so observed_peaks(~13) > expected_carbons(4) → simple overcount case; no need to fabricate a separate spectrum
      - test_overcount_cli_text: same invocation → "more signals than carbons" in text output

    Class TestFIX08CASE1Regression (uses in-repo CASE1 data — no skipif needed):
      - test_case1_snr5_count_floor: pick data/Ibuprofen/2 with snr_floor=5.0 → ≥ 9 peaks (same floor as existing TestCASE1Regression in test_peak_picker_snr.py)

    Total: ~8 test methods.
  </behavior>
  <action>
    Create tests/test_peak_picking_integrity.py with the four test classes described above.
    
    File-level imports needed: Path, numpy, pytest, json, CliRunner (from click.testing),
    AdaptivePeakPicker (from lucy_ng.processing.peak_picker), BrukerReader (from lucy_ng.readers),
    SymmetryAnalysisResult (from lucy_ng.analysis.symmetry_analysis),
    HydrogenBudgetResult (from lucy_ng.analysis.hydrogen_budget),
    IntensityReport (from lucy_ng.analysis.intensity_reporter),
    analyze (from lucy_ng.cli.analyze).
    
    CASE9 path: mirror exactly from test_peak_picker_snr.py:
      CASE9_C13 = Path("/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/12")
    
    For TestFIX08OvercountGuard.test_overcount_cli_json and test_overcount_cli_text:
      Use the CliRunner with mix_stderr=False. Invoke analyze with ["symmetry", "C4H8", str(CASE1_C13)].
      C13H18O2 ibuprofen picks ~9-13 peaks; C4H8 has expected_carbons=4 → difference < 0 → overcount.
      Do NOT fabricate a new spectrum file — reuse the in-repo ibuprofen 13C path.
    
    For TestFIX08OvercountGuard.test_overcount_symmetry_analysis_summary:
      Build a minimal SymmetryAnalysisResult directly (no spectrum file needed):
        hb = HydrogenBudgetResult(expected_h=16, total_accounted=16, missing_h=0, has_equivalents=False)
        ir = IntensityReport(peaks=[], has_potential_equivalents=False)
        r = SymmetryAnalysisResult(molecular_formula="C12H16O3", hydrogen_budget=hb, intensity_report=ir, signal_count=76, expected_carbons=12, missing_carbons=-64)
        assert "OVERCOUNT ALARM" in r.summary()
    
    Keep the file under 120 lines. No fenced code blocks inside action — see above for identifiers.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && python -m pytest tests/test_peak_picking_integrity.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    tests/test_peak_picking_integrity.py exists;
    all non-skipif tests pass (TestFIX08OvercountGuard + TestFIX08CASE1Regression);
    CASE9 skipif tests are skipped on machines without CASE9 data and pass where data is present;
    full `pytest` suite stays green (≥1054 passing, 0 new failures);
    NOTE: this plan does not run or claim the blind UAT gate — that is a separate orchestrator step
  </done>
</task>

<task type="auto">
  <name>Task 2: Full suite health check</name>
  <files></files>
  <read_first>
    (no files — this task just runs the suite)
  </read_first>
  <action>
    Run the full test suite, ruff, and mypy to confirm no regressions were introduced by Plans 01–02 or the new test file. Record counts in SUMMARY.md.
    
    Commands:
      pytest --tb=short -q
      ruff check src/lucy_ng
      mypy src/lucy_ng
    
    Expected: pytest ≥1054 passing, 0 new failures; ruff exits 0; mypy exits 0.
    
    If any test fails due to a snr_floor default change in test_peak_picker_snr.py::TestCASE1Regression (the existing "≥9 peaks" floor), inspect whether the new default of 5.0 drops ibuprofen below 9 peaks and add a note in SUMMARY.md. The acceptance floor is 9 peaks — if the new default yields fewer than 9, that is a blocker requiring Plan 01 to be revised (consult the user).
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && python -m pytest --tb=short -q 2>&1 | tail -5 && ruff check src/lucy_ng && mypy src/lucy_ng --no-error-summary 2>&1 | tail -3</automated>
  </verify>
  <done>
    pytest exits 0 with ≥1054 passing and 0 failures;
    ruff exits 0;
    mypy exits 0 (or exits with only pre-existing errors — compare against the pre-Phase-81 baseline);
    the blind re-UAT (CASE9 + CASE1, fresh instances) is run separately by the orchestrator after this plan completes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → test | CASE9 external path accessed read-only under skipif guard; CASE1 in-repo path read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-04-SC | Tampering | No new packages installed in this plan | accept | New test file only; no new production dependencies |

Note: Test-only plan. No new production code. External CASE9 data accessed read-only under skipif. No security exposure.
</threat_model>

<verification>
Run from `/Users/steinbeck/Dropbox/develop/lucy-ng`:

```
pytest tests/test_peak_picking_integrity.py -v
pytest --tb=short -q
ruff check src/lucy_ng
mypy src/lucy_ng
```

All must exit 0. CASE9 tests may be marked SKIPPED on machines without the dataset — that is expected and correct.
</verification>

<success_criteria>
- tests/test_peak_picking_integrity.py exists with TestFIX08CASE9Integration, TestFIX08OvercountGuard, TestFIX08CASE1Regression classes
- Overcount guard tests (fully synthetic) pass without any external data
- CASE1 regression test passes (ibuprofen ≥9 peaks at snr_floor=5.0)
- Full pytest suite ≥1054 passing, 0 new failures
- ruff + mypy clean
</success_criteria>

<output>
Create `.planning/phases/81-peak-picking-integrity-fix-08/81-04-SUMMARY.md` when done
</output>
