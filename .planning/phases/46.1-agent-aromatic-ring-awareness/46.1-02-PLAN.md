---
phase: 46.1-agent-aromatic-ring-awareness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/lucy-nmr-chemist.md
  - ~/.claude/agents/lucy-devils-advocate.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "NMR-chemist reports aromatic/sp2 expectation in [SETUP-COMPLETE] message based on 13C shift count in 110-160 ppm"
    - "Devils-advocate checks aromatic ring expectation against ranking results when available (WARNING severity, defense-in-depth)"
    - "Devils-advocate reports aromatic ring check result in [VALIDATION-PASSED] template"
  artifacts:
    - path: "~/.claude/agents/lucy-nmr-chemist.md"
      provides: "Aromatic expectation field in [SETUP-COMPLETE] template"
      contains: "Aromatic expectation"
    - path: "~/.claude/agents/lucy-devils-advocate.md"
      provides: "Aromatic Ring Expectation check in Structural Integrity section, aromatic check field in [VALIDATION-PASSED] template"
      contains: "Aromatic Ring Expectation"
  key_links:
    - from: "nmr-chemist [SETUP-COMPLETE] Aromatic expectation field"
      to: "devils-advocate Aromatic Ring Expectation check"
      via: "Coordinator relays nmr-chemist setup data to team; devils-advocate reads aromatic expectation"
      pattern: "Aromatic expectation.*110-160 ppm"
---

<objective>
Add aromatic ring awareness to the nmr-chemist and devils-advocate agent definitions.

Purpose: The nmr-chemist currently detects sp2 carbons but does not explicitly flag aromatic ring expectation in its [SETUP-COMPLETE] message. The devils-advocate has no post-ranking aromatic sanity check. Adding both creates an early warning (nmr-chemist) and a defense-in-depth safety net (devils-advocate) for the aromatic mismatch scenario that caused v4.0 UAT failure.

Output: Updated `~/.claude/agents/lucy-nmr-chemist.md` with Aromatic expectation field in [SETUP-COMPLETE] template. Updated `~/.claude/agents/lucy-devils-advocate.md` with Aromatic Ring Expectation check and [VALIDATION-PASSED] template extension.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46.1-agent-aromatic-ring-awareness/46.1-RESEARCH.md
@~/.claude/agents/lucy-nmr-chemist.md
@~/.claude/agents/lucy-devils-advocate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Aromatic expectation field to nmr-chemist [SETUP-COMPLETE] template</name>
  <files>~/.claude/agents/lucy-nmr-chemist.md</files>
  <action>
Make one additive edit to `~/.claude/agents/lucy-nmr-chemist.md`. Do not remove or restructure existing content.

**Edit: Add Aromatic expectation field to [SETUP-COMPLETE] template**

In the `## CASE-PROGRESS.md Contribution Protocol` section, within the `### [SETUP-COMPLETE]` template block, insert a new line AFTER the `HHB:` line and BEFORE the `Conflicts with NMR evidence:` line:

```
Aromatic expectation: <N carbons in 110-160 ppm — aromatic ring expected> / <N carbons in 110-160 ppm — sp2 carbons present (check DEPT for CH2 to distinguish alkene vs aromatic)> / <fewer than 4 carbons in 110-160 ppm — no aromatic expectation>
```

The threshold (>= 4 carbons in 110-160 ppm) matches the ranking code's aromatic warning trigger. The field uses three-way conditional:
- >= 4 carbons in 110-160 ppm AND no CH2 in that region (from DEPT): "aromatic ring expected"
- >= 4 carbons in 110-160 ppm AND some CH2 in that region: "sp2 carbons present" (could be alkene, not definitive aromatic)
- < 4 carbons in 110-160 ppm: "no aromatic expectation"

This field is computed from the already-picked 13C peaks — no new CLI command needed. The agent counts shifts in 110-160 ppm from its picked peak list.

**Important constraints:**
- Do NOT modify existing fields in the [SETUP-COMPLETE] template
- Do NOT add new workflow steps (the count is trivial arithmetic on already-picked data, done during step 7 compilation)
- Do NOT restructure any sections
  </action>
  <verify>
Read `~/.claude/agents/lucy-nmr-chemist.md` and verify:
1. "Aromatic expectation:" field appears in [SETUP-COMPLETE] template after "HHB:" and before "Conflicts with NMR evidence:"
2. Field contains the three-way conditional (aromatic ring expected / sp2 carbons present / no aromatic expectation)
3. All existing template fields are unchanged
4. File structure (YAML frontmatter, XML tags) is intact
5. Workflow step numbering is unchanged (1-10)
  </verify>
  <done>
NMR-chemist agent definition has Aromatic expectation field in [SETUP-COMPLETE] template with three-way conditional based on 13C shift count in 110-160 ppm, DEPT-aware to distinguish aromatic from alkene sp2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Aromatic Ring Expectation check and template field to devils-advocate</name>
  <files>~/.claude/agents/lucy-devils-advocate.md</files>
  <action>
Make two additive edits to `~/.claude/agents/lucy-devils-advocate.md`. Do not remove or restructure existing content.

**Edit 1: Add Aromatic Ring Expectation check to Section 3 (Structural Integrity Checks)**

Insert a new subsection AFTER `### ELIM Usage` and BEFORE `## 4. Severity Classification`:

```
### Aromatic Ring Expectation

If nmr-chemist's [SETUP-COMPLETE] message reports "aromatic ring expected" (>= 4 carbons in 110-160 ppm) AND ranking results are available for this compound (from current or previous iteration): check whether `lucy lsd rank --format json` output has any solution with `has_aromatic_ring: true`, OR whether the `warnings` array contains an aromatic mismatch message.

If aromatic expected AND all solutions are non-aromatic: flag WARNING — "Aromatic ring expected from NMR but all solutions are non-aromatic. Solution-analyst should flag for 4J HMBC removal."

**Important:** This check is defense-in-depth. The solution-analyst is the primary responder (via Check 6). Do NOT flag CRITICAL — use WARNING severity only. Do NOT block the solver run for this check (it is a post-ranking observation, not a pre-run validation). This check is conditional on ranking results being available; it does not apply to iteration 1 before any ranking has occurred.
```

**Edit 2: Add Aromatic ring check field to [VALIDATION-PASSED] template**

In the `## CASE-PROGRESS.md Contribution Protocol` section, within the `### [VALIDATION-PASSED]` template block, insert a new line AFTER the `Concerns:` line:

```
Aromatic ring check: N/A (no ranking yet or no aromatic expectation) / PASS (solutions contain aromatic ring) / WARNING: all solutions non-aromatic despite aromatic expectation from NMR
```

**Important constraints:**
- Do NOT modify existing checks in Section 3
- Do NOT change severity of any existing check
- Do NOT add this as a CRITICAL check (WARNING only)
- Do NOT restructure any sections
- Preserve existing workflow step numbering
  </action>
  <verify>
Read `~/.claude/agents/lucy-devils-advocate.md` and verify:
1. "### Aromatic Ring Expectation" subsection appears in Section 3 after "### ELIM Usage" and before "## 4. Severity Classification"
2. Subsection specifies WARNING severity (not CRITICAL)
3. Subsection notes defense-in-depth role and conditional on ranking availability
4. "Aromatic ring check:" field appears in [VALIDATION-PASSED] template after "Concerns:" line
5. All existing Section 3 checks are unchanged
6. File structure (YAML frontmatter, XML tags) is intact
7. Workflow step numbering is unchanged (1-12)
  </verify>
  <done>
Devils-advocate agent definition has:
- Aromatic Ring Expectation check in Section 3 with WARNING severity and defense-in-depth framing
- Aromatic ring check field in [VALIDATION-PASSED] template with three-way result (N/A / PASS / WARNING)
  </done>
</task>

</tasks>

<verification>
1. Read both updated files and confirm all insertions are present at correct locations
2. Verify no existing content was modified in either file (only additions)
3. Verify YAML frontmatter and XML tag structure is valid in both files
4. nmr-chemist: [SETUP-COMPLETE] template should have field order: ...HHB → Aromatic expectation → Conflicts with NMR evidence → Key observations
5. devils-advocate: Section 3 should end with "### Aromatic Ring Expectation" before Section 4
6. devils-advocate: [VALIDATION-PASSED] template should have "Aromatic ring check:" after "Concerns:"
7. Verify devils-advocate check explicitly says WARNING severity and defense-in-depth
</verification>

<success_criteria>
- NMR-chemist has Aromatic expectation field in [SETUP-COMPLETE] with DEPT-aware three-way conditional
- Devils-advocate has Aromatic Ring Expectation check with WARNING severity and defense-in-depth framing
- Devils-advocate has Aromatic ring check field in [VALIDATION-PASSED] template
- No existing content removed or modified in either file
- Both files are valid markdown with intact YAML frontmatter and XML tags
</success_criteria>

<output>
After completion, create `.planning/phases/46.1-agent-aromatic-ring-awareness/46.1-02-SUMMARY.md`
</output>
