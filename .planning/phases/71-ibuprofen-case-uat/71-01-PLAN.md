---
phase: 71-ibuprofen-case-uat
plan: 01
type: uat
wave: 1
depends_on: []
autonomous: false
requirements:
  - UAT-01
  - UAT-02

must_haves:
  truths:
    - "D-22: CASE run executed by a FRESH Claude instance (not the build/bookkeeping instance) to preserve blind-test validity"
    - "D-21: Run targets sanitised dataset ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1 with formula C13H18O2"
    - "D-23 (UAT-01): A structure with verified aromatic ring AND formula C13H18O2 appears in top-3 of merged.smi"
    - "D-23 (UAT-02): The 3 known 4J correlations (atoms 4-8, 6-9, 8-4) handled via lucy pylsd run with >=2 permutations; deferred_4j written by lsd-engineer"
    - "D-23a: Verification is INDEPENDENT — build instance checks merged.smi SMILES via RDKit (aromatic ring + formula), NOT via agent self-report (v4.0 lesson: solution-analyst hallucinated rank #1)"
    - "D-24: A failing UAT is a valid result documenting a real v8.0 gap — no force-pass; route to forensics + fix + re-run"
  artifacts:
    - path: "CASE-PROGRESS.md (in CASE1 analysis dir, produced by fresh instance)"
      provides: "narrative of which permutation produced the aromatic-ring solution + which correlations deferred as 4J"
      contains: "pylsd_mode"
    - path: "merged.smi (in CASE1 pylsd_run dir)"
      provides: "deduplicated ranked solution set across permutations"
    - path: "run_report.json (in CASE1 pylsd_run dir)"
      provides: "per-permutation solution distribution + provenance"
  key_links:
    - from: "fresh /lucy-ng:case run"
      to: "lucy pylsd run multi-permutation orchestration"
      via: "lsd-engineer routing on pylsd_mode=true"
      pattern: "pylsd run"
---

<objective>
Execute the v8.0 milestone-complete UAT: a fresh Claude instance solves the (sanitised) ibuprofen CASE blind via /lucy-ng:case, and the build instance independently verifies the result against the four ROADMAP success criteria. This is the litmus test for whether v8.0's pyLSD multi-run integration fixes the v4.0 aromatic-ring failure.

This plan is HUMAN-RUN (autonomous: false). It is NOT executed by a gsd-executor agent — the UAT operator runs the blind CASE in a fresh session and returns artifacts for verification.
</objective>

<task id="1" type="manual">
  <name>Run blind CASE in fresh instance</name>
  <read_first>
    - .planning/phases/71-ibuprofen-case-uat/71-CONTEXT.md (D-21..D-24)
  </read_first>
  <action>
    UAT operator opens a FRESH Claude Code session (new terminal — no build context) and runs:

      /lucy-ng:case ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1 C13H18O2

    Operator gives NO hints about compound identity, the 3 4J correlations, or expected structure (D-22b). The agent must autonomously: pick peaks, flag aromatic 4J risk (nmr-chemist), write v2 inventory with pylsd_mode=true + deferred_4j (lsd-engineer), pass G1-G4 (devils-advocate), route to lucy pylsd run, rank merged solutions.

    On completion, operator collects from the CASE1 analysis directory:
    - CASE-PROGRESS.md
    - merged.smi (+ ranked output)
    - run_report.json
    and returns them to the build/bookkeeping instance for verification.
  </action>
  <verify>
    <manual>Fresh-instance CASE run completes; CASE-PROGRESS.md + merged.smi + run_report.json exist in the CASE1 analysis dir</manual>
  </verify>
  <done>
    - A fresh-session /lucy-ng:case run finished (not the build instance)
    - CASE-PROGRESS.md, merged.smi, run_report.json produced
    - Artifacts handed back for independent verification
  </done>
</task>

<task id="2" type="manual">
  <name>Independent verification against UAT criteria</name>
  <read_first>
    - .planning/phases/71-ibuprofen-case-uat/71-CONTEXT.md (D-23, D-23a)
    - returned CASE-PROGRESS.md, merged.smi, run_report.json
  </read_first>
  <action>
    Build/bookkeeping instance verifies INDEPENDENTLY (D-23a — do NOT trust agent self-report):

    1. UAT-01 / SC2: Parse merged.smi top-3 SMILES. For each, RDKit check:
       - Chem.MolFromSmiles(smi) parses
       - has aromatic 6-membered ring (mol.GetRingInfo + GetAromaticAtoms)
       - CalcMolFormula(mol) == "C13H18O2"
       At least one top-3 solution must satisfy all three.
    2. UAT-02 / SC1: Confirm run_report.json shows >=2 permutations covering the 3 known 4J correlations (atoms 4-8, 6-9, 8-4). Confirm lucy pylsd run was the solver path.
    3. SC3: CASE-PROGRESS.md documents which permutation produced the aromatic solution + which correlations were deferred.
    4. SC4: run_report.json shows solution distribution; >=1 permutation yields an aromatic-ring solution.

    Write VERIFICATION.md with PASS/FAIL per criterion + RDKit evidence per top-3 SMILES.
  </action>
  <verify>
    <manual>VERIFICATION.md created with per-criterion verdict and RDKit-derived evidence (aromatic ring + formula) for each top-3 solution</manual>
  </verify>
  <done>
    - All 4 ROADMAP success criteria assessed with independent evidence
    - VERIFICATION.md status: passed (all TRUE) OR gaps_found (with failure-mode forensics per D-24)
    - If passed: phase 71 + v8.0 milestone ready to close
    - If failed: forensic note on which stage failed → routes to fix + re-run (no force-pass)
  </done>
</task>

<verification>
- Phase goal achieved when a verified aromatic-ring C13H18O2 structure sits in merged.smi top-3, produced via lucy pylsd run multi-permutation orchestration, independently confirmed by RDKit (not agent claim).
- This is the v8.0 milestone-complete gate.
</verification>
