---
phase: 45-team-coordination-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/lucy-ng/case.md
  - ~/.claude/agents/lucy-lsd-engineer.md
  - ~/.claude/agents/lucy-solution-analyst.md
autonomous: true

must_haves:
  truths:
    - "Orchestrator creates next-iteration tasks after each non-converging iteration (lsd-engineer does not create tasks)"
    - "Ranking task description contains the full experimental 13C shift list extracted from CASE-PROGRESS.md"
    - "After a non-converging iteration, orchestrator creates both hmbc-selection and ranking tasks in parallel where appropriate"
    - "present_results reports elapsed time and iteration-count comparison to v3.0 baseline"
    - "lsd-engineer spawn prompt no longer instructs the agent to create next iteration tasks"
    - "solution-analyst workflow says shifts come from task description (coordinator provides)"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "Iteration task creation, shift list embedding, parallel tasks, time measurement"
      contains: "TaskCreate.*lsd-iteration"
    - path: "~/.claude/agents/lucy-lsd-engineer.md"
      provides: "Cleaned spawn prompt (no TaskCreate instruction)"
    - path: "~/.claude/agents/lucy-solution-analyst.md"
      provides: "Updated workflow step 2 referencing task description for shifts"
  key_links:
    - from: "case.md monitor_progress"
      to: "TaskCreate for next iteration"
      via: "After [ITERATION-COMPLETE] + [VALIDATION-PASSED] + solution_count > 10"
      pattern: "TaskCreate.*lsd-iteration"
    - from: "case.md monitor_progress"
      to: "TaskCreate for ranking"
      via: "After solution_count <= 10, with shift list embedded"
      pattern: "TaskCreate.*ranking.*shifts"
    - from: "case.md present_results"
      to: "Elapsed time report"
      via: "Parse Started timestamp from CASE-PROGRESS.md header"
      pattern: "Time to solution"
---

<objective>
Close 4 coordination gaps identified in Phase 45 research: (1) next-iteration task creation by orchestrator, (2) shift list delivery to ranking tasks, (3) parallel task execution for hmbc-selection and ranking, (4) time-to-solution measurement. Also clean up spawn prompt mismatches.

Purpose: Without these gaps closed, the workflow stops after one LSD iteration (Gap 1), ranking runs without experimental shifts (Gap 2), agents wait unnecessarily (Gap 3), and time cannot be measured (Gap 4).

Output: Updated case.md, lucy-lsd-engineer.md, lucy-solution-analyst.md with all 4 gaps closed.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-team-coordination-protocol/45-RESEARCH.md
@~/.claude/commands/lucy-ng/case.md
@~/.claude/agents/lucy-lsd-engineer.md
@~/.claude/agents/lucy-solution-analyst.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add iteration task creation and shift list embedding to case.md monitor_progress</name>
  <files>~/.claude/commands/lucy-ng/case.md</files>
  <action>
Modify the `monitor_progress` step in case.md to add the iteration-management decision logic. After the existing "Check for completion signals" section, add a new section titled "Iteration management (create next tasks)". This section implements the decision tree:

**Decision tree (add after "Check for completion signals"):**

```
**Iteration management (create next tasks):**

After receiving [ITERATION-COMPLETE] from lsd-engineer AND [VALIDATION-PASSED] from devils-advocate, AND after writing both sections to CASE-PROGRESS.md:

1. Extract solution_count from the [ITERATION-COMPLETE] message
2. Run detect_loops (existing step)
3. **If no loop AND solution_count > 10 AND iterations < 10 (safety cap):**

   Retain the shift list from the [SETUP-COMPLETE] message received earlier (the orchestrator wrote these to ## Setup / ### NMR-Chemist in CASE-PROGRESS.md — read from there if needed).

   Create next iteration task:
   ```
   TaskCreate(
     subject="lsd-iteration-{next_iter:02d}",
     description="Build LSD file for iteration {next_iter} of {formula} at {compound_path}.
                  Read previous: analysis/iteration_{current_iter:02d}/compound.lsd
                  Use constraint inventory to copy ALL constraints forward.
                  Add next HMBC batch (3-5 correlations, best remaining from CASE-PROGRESS.md Setup).
                  Send validation request to devils-advocate.
                  WAIT for approval before running solver.
                  Run LSD from analysis/iteration_{next_iter:02d}/.
                  Convert solutions: outlsd 5 < compound.sol > solutions.smi
                  Send [ITERATION-COMPLETE] to coordinator.",
     activeForm="LSD iteration {next_iter}"
   )
   ```

   **Parallel task creation (when solution_count is 10-50 and iterations >= 2):**
   Additionally create an HMBC selection task for nmr-chemist:
   ```
   TaskCreate(
     subject="hmbc-selection-{next_iter:02d}",
     description="Select next HMBC batch (3-5 correlations) for iteration {next_iter}.
                  Review CASE-PROGRESS.md for already-used correlations.
                  Apply selection criteria: isolated carbons, unique protons, strong intensity.
                  Send selected batch to lsd-engineer via SendMessage.",
     activeForm="Selecting HMBC batch {next_iter}"
   )
   ```

4. **If solution_count <= 10:**

   Read the shift list from CASE-PROGRESS.md ## Setup / ### NMR-Chemist section (the 13C peak positions written from [SETUP-COMPLETE]).

   Create ranking task with embedded shifts:
   ```
   TaskCreate(
     subject="ranking-iteration-{current_iter:02d}",
     description="Rank LSD solutions from iteration {current_iter}.
                  solutions.smi path: analysis/iteration_{current_iter:02d}/solutions.smi
                  Experimental 13C shifts: {shift_list}
                  Run: lucy lsd rank analysis/iteration_{current_iter:02d}/solutions.smi --shifts '{shift_list}'
                  Assess chemical plausibility of top candidates.
                  Write analysis/final_results.md with full report.
                  Send [RANKING-COMPLETE] to coordinator.",
     activeForm="Ranking LSD solutions"
   )
   ```

   **Note on shift list extraction:** The shift list is a comma-separated string of 13C chemical shifts (e.g., "155.08,151.58,136.87,..."). The orchestrator received this data in nmr-chemist's [SETUP-COMPLETE] message and wrote it to CASE-PROGRESS.md. At ranking time, read back from CASE-PROGRESS.md if no longer in scope.

5. **If iterations >= 10 (safety cap) AND solution_count > 10:**

   Create ranking task (same as above) AND proceed to present_results with caveat.

Return to monitoring after creating tasks.
```

Also add a note at the start of monitor_progress: "**Shift list retention:** When processing [SETUP-COMPLETE] from nmr-chemist, extract and retain the experimental 13C shift list (comma-separated ppm values). This is needed later for creating ranking tasks."

**IMPORTANT:** Do NOT change any existing content in monitor_progress. Append the new "Iteration management" section after the existing "Check for completion signals" bullet list.

Also clean up the spawn prompt for lsd-engineer in spawn_case_team (Step 3). In the Task() call for lsd-engineer, remove these two lines:
- "Create next iteration task when current one completes."
- The line in lsd-iteration-01 TaskCreate: "Create lsd-iteration-02 task when done."

Replace "Create next iteration task when current one completes." with "Claim iteration tasks from TaskList as they become available."

Replace the lsd-iteration-01 TaskCreate description to remove "Create lsd-iteration-02 task when done." (the orchestrator will create subsequent iteration tasks).
  </action>
  <verify>
Read case.md and verify:
1. monitor_progress contains "Iteration management (create next tasks)" section
2. TaskCreate for lsd-iteration-{next_iter} appears in monitor_progress
3. TaskCreate for ranking-iteration with shift list placeholder appears in monitor_progress
4. TaskCreate for hmbc-selection appears for parallel execution
5. Shift list retention note appears at start of monitor_progress
6. lsd-engineer spawn prompt does NOT contain "Create next iteration task"
7. lsd-iteration-01 TaskCreate does NOT contain "Create lsd-iteration-02 task when done"
  </verify>
  <done>
monitor_progress has a complete decision tree for creating next-iteration tasks (orchestrator-driven), ranking tasks with embedded shift lists, and parallel hmbc-selection tasks. Spawn prompt cleanup removes task creation instructions from lsd-engineer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add time measurement to present_results and update agent files</name>
  <files>~/.claude/commands/lucy-ng/case.md, ~/.claude/agents/lucy-lsd-engineer.md, ~/.claude/agents/lucy-solution-analyst.md</files>
  <action>
**Part A: Time measurement in case.md present_results**

In the `present_results` step, add elapsed time reporting to all three result formats (success, incomplete convergence, failure).

After the existing line "Read the latest iteration from CASE-PROGRESS.md and any solution files generated." add:

```
**Compute elapsed time:**
Parse the `**Started:** <timestamp>` line from the CASE-PROGRESS.md file header.
Record current time. Compute elapsed time = current - started.
Report in the results section below.

**v3.0 baseline reference:** The v3.0 Ibuprofen UAT completed in 4 iterations (wall-clock time not recorded). Use iteration count as surrogate: if v4.0 uses N iterations, the ratio is N/4.
```

In the success result template (the "## CASE Results" block), after `**Final solution count:** <count>` add:

```
**Time to solution:** ~<elapsed_minutes> minutes (<N> iterations)
**v3.0 comparison:** <N> iterations (v3.0 baseline: 4 iterations, ratio: <N/4:.1f>x)
```

In the incomplete convergence template (the "## CASE Results (Incomplete Convergence)" block), after `**Final solution count:** <count>` add:

```
**Time to solution:** ~<elapsed_minutes> minutes (<N> iterations, safety cap)
**v3.0 comparison:** <N> iterations (v3.0 baseline: 4 iterations, ratio: <N/4:.1f>x)
```

In the failure template (the "## CASE Failed" block), after `**Final solution count:** 0` add:

```
**Time to solution:** ~<elapsed_minutes> minutes (<N> iterations, failed)
```

**Part B: Clean up lucy-lsd-engineer.md spawn reference**

The agent definition itself does NOT need changes (it already has no TaskCreate in its tool list or workflow). However, check if any text in the agent's `<role>` or `<workflow>` section mentions creating tasks or TaskCreate. If found, remove it. The agent should only: claim tasks from TaskList, mark tasks completed via TaskUpdate, and send messages via SendMessage.

**Part C: Update lucy-solution-analyst.md workflow step 2**

In the `<workflow>` section, change step 2 from:
```
2. Read solutions.smi path and experimental shifts from task/messages
```
to:
```
2. Read solutions.smi path and experimental 13C shifts from task description (coordinator embeds the full shift list when creating the ranking task)
```

Also in the `<message_interface>` INPUTS section, change the nmr-chemist line from:
```
- From **nmr-chemist:** Experimental shift list (for ranking --shifts argument), detection results (for confidence)
```
to:
```
- From **orchestrator:** Task assignments with embedded experimental shift list (for ranking --shifts argument)
- From **nmr-chemist:** Detection results (for confidence context)
```
  </action>
  <verify>
1. Read case.md present_results step: verify "Time to solution" and "v3.0 comparison" appear in all three result templates
2. Read lucy-lsd-engineer.md: verify no TaskCreate references exist in role or workflow sections
3. Read lucy-solution-analyst.md: verify workflow step 2 says "from task description (coordinator embeds the full shift list)"
4. Read lucy-solution-analyst.md: verify INPUTS section lists orchestrator as source of shift list
  </verify>
  <done>
Time measurement added to all present_results output formats with v3.0 iteration-count baseline comparison. Solution-analyst knows shifts come from task description. LSD-engineer has no task creation references.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Gap 1 closed:** Read case.md monitor_progress — verify TaskCreate for lsd-iteration-{N+1} exists after convergence check
2. **Gap 2 closed:** Read case.md monitor_progress — verify ranking TaskCreate includes "{shift_list}" in description
3. **Gap 3 closed:** Read case.md monitor_progress — verify hmbc-selection TaskCreate exists alongside iteration task
4. **Gap 4 closed:** Read case.md present_results — verify "Time to solution" in all result templates
5. **Spawn cleanup:** Read case.md spawn_case_team — verify lsd-engineer prompt says "Claim iteration tasks" not "Create next iteration task"
6. **Agent alignment:** Read lucy-solution-analyst.md — verify workflow step 2 says "from task description"
7. **No TaskCreate in agent:** Grep lucy-lsd-engineer.md for "TaskCreate" — should return 0 matches
</verification>

<success_criteria>
- case.md monitor_progress creates iteration tasks after non-converging iterations (orchestrator-driven)
- Ranking tasks embed the experimental shift list in the task description
- Parallel hmbc-selection task created alongside iteration tasks when solution_count is 10-50
- present_results reports elapsed time and v3.0 iteration-count comparison
- lsd-engineer spawn prompt cleaned: no "Create next iteration task" instruction
- solution-analyst workflow says shifts come from task description
</success_criteria>

<output>
After completion, create `.planning/phases/45-team-coordination-protocol/45-01-SUMMARY.md`
</output>
