---
phase: 88-aliphatic-multiplicity-robustness
plan: 03
type: execute
wave: 3
depends_on: ["88-02"]
files_modified:
  - .claude/commands/lucy-ng/case.md
  - .claude/commands/lucy-ng/references/loop-patterns.md
  - .claude/commands/lucy-ng/references/advisory-templates.md
  - .claude/agents/lucy-devils-advocate.md
autonomous: true
requirements: [MULT-02, MULT-03]
must_haves:
  truths:
    - "D-04/MULT-02: a deterministic pre-accept coverage gate runs before the run may accept, independent of MAE — if a [MULTIPLICITY-AMBIGUOUS] record exists and ≥2 viable families were identified but not all were SEARCHED, the run does NOT accept; it reopens and searches the missing family(ies)"
    - "D-04: the gate counts families SEARCHED (each got its own constrained LSD run + [ITERATION-COMPLETE]), NOT families that produced a ranked solutions.smi (the SEARCHED-not-RANKED rule from Plan 88-02)"
    - "D-06/MULT-03: a devils-advocate 'evidence FOR model X' multiplicity flag is a MANDATORY-search item — model X MUST enter the search space and the flag is closeable ONLY by showing model X was actually searched (an iteration_NN_X/ run), never by the convergence narrative"
    - "The coverage gate fires at the identity_gate lifecycle point (after the union is ranked / final_results.md exists, before accept), guarded to only fire when a [MULTIPLICITY-AMBIGUOUS] record exists"
  artifacts:
    - path: ".claude/commands/lucy-ng/case.md"
      provides: "pre-accept multiplicity coverage gate + CASE-PROGRESS.md searched-families ledger"
      contains: "coverage"
    - path: ".claude/commands/lucy-ng/references/loop-patterns.md"
      provides: "coverage-triggered reopen pattern (alongside Pattern 5)"
      contains: "coverage"
    - path: ".claude/agents/lucy-devils-advocate.md"
      provides: "binding multiplicity gate emitting [MULT-EVIDENCE-FOR] model=X"
      contains: "MULT-EVIDENCE-FOR"
  key_links:
    - from: "case.md coverage gate"
      to: "CASE-PROGRESS.md viable_families vs searched_families"
      via: "viable ⊆ searched check before accept"
      pattern: "searched"
    - from: "lucy-devils-advocate.md [MULT-EVIDENCE-FOR]"
      to: "case.md mandatory-search requirement"
      via: "DA flag recorded as a search requirement the coverage gate enforces"
      pattern: "MULT-EVIDENCE-FOR"
---

<objective>
Add the MAE-independent guardrails that make coverage binding: a pre-accept coverage gate in the orchestrator (D-04/MULT-02) and a binding devils-advocate multiplicity flag (D-06/MULT-03). Together they close the exact CASE4 defeat — where a wrong-class structure scored MAE 1.75 "PLAUSIBLE", the MAE>4 guardrail stayed silent, and the DA's "evidence for ethyl" flag was rationalized away as gem-dimethyl coupling instead of forcing an ethyl-family search.

Purpose: Even with per-family search wired (Plan 88-02), nothing yet STOPS the run from accepting when a viable family was never searched, and nothing makes a DA multiplicity flag binding. This plan adds the coverage check (viable ⊆ searched, counted by SEARCHED not RANKED) and makes a DA "evidence FOR model X" flag closeable only by an actual iteration_NN_X/ run.

Output: prompt edits to case.md, loop-patterns.md, advisory-templates.md, and lucy-devils-advocate.md. Repo .claude/ files symlinked into ~/.claude — MARKDOWN PROMPT EDITS, NOT unit-testable this session, requiring a FRESH Claude Code session to reload. Functional validation is the blind CASE4 re-run (UAT-01, Phase 89). Grep-asserted now, blind-UAT-validated in Phase 89.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-aliphatic-multiplicity-robustness/88-CONTEXT.md
@.planning/phases/88-aliphatic-multiplicity-robustness/88-RESEARCH.md
@.planning/todos/pending/2026-06-23-multiplicity-model-hardcoding-defect.md

<interfaces>
<!-- Gate-lifecycle and signal anchors. Read the files; do not reconstruct. -->

case.md:
- monitor_progress + completion signals (lines ~265-310): [SETUP-COMPLETE] / [ITERATION-COMPLETE] / [RANKING-COMPLETE] handling; the orchestrator is the SOLE author of CASE-PROGRESS.md (line ~218).
- identity_gate step (lines ~308-310, ~632-655): "never go straight from [RANKING-COMPLETE] to present_results"; post-solution G-IDENT gate runs on final_results.md before present_results. The coverage gate sits at this SAME lifecycle point (pre-accept), guarded by a [MULTIPLICITY-AMBIGUOUS] record.
- detect_loops Pattern 5 "Quality Convergence Failure" (lines ~432-469): the reopen-the-converged-run precedent. The coverage gate is the analogous reopen but COVERAGE-triggered, not MAE-triggered.

loop-patterns.md:
- "Quality Convergence Failure" (line ~60) — the reopen pattern to model the new coverage pattern on.

advisory-templates.md:
- advisory wording precedents for reopen directives.

lucy-devils-advocate.md:
- G-IDENT post-solution gate (line ~416) — structural template for the new DA multiplicity gate. KEY DIFFERENCE: G-IDENT is advisory/POST-solution; the multiplicity gate is PRE-accept and BINDING (closeable only by an actual search). There is currently NO DA multiplicity gate — it must be ADDED.

Contracts produced upstream (Plan 88-02): nmr-chemist [MULTIPLICITY-AMBIGUOUS] with viable_families; lsd-engineer per-family iteration_NN_<family>/ runs each sending [ITERATION-COMPLETE]; SEARCHED-not-RANKED coverage rule.

CASE4 worked example: DA flagged HMBC 11→13 as "evidence FOR the ethyl model"; it was re-explained as 3-bond gem-dimethyl coupling within isopropyl and the ethyl family was never searched. The gate must make that impossible.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Binding DA multiplicity gate emitting [MULT-EVIDENCE-FOR] model=X</name>
  <files>.claude/agents/lucy-devils-advocate.md</files>
  <read_first>
    - .claude/agents/lucy-devils-advocate.md:416-455 (G-IDENT — the post-solution gate structure to model the new gate on; note it is advisory whereas the multiplicity gate is binding)
    - .claude/agents/lucy-devils-advocate.md:227-241 (severity classification + validation decision — where a binding/mandatory severity fits)
    - .planning/todos/pending/2026-06-23-multiplicity-model-hardcoding-defect.md (the HMBC 11→13 "evidence for ethyl" defeat the gate must prevent)
  </read_first>
  <action>
    Add a new gate to lucy-devils-advocate.md (e.g. "G-MULT: Multiplicity Evidence — BINDING mandatory-search flag (MULT-03)") modeled structurally on G-IDENT but with two differences stated explicitly: it is PRE-accept (not post-solution) and BINDING (not advisory). Specify: when the DA finds positive evidence FOR a specific aliphatic multiplicity model X that is not currently being searched — e.g. an HMBC/COSY correlation that a CH3–CH2 (ethyl) would produce as genuine 2–3J but which the leading model cannot explain — it emits a structured "[MULT-EVIDENCE-FOR] model=<X>" message naming model X and the supporting correlation. State the BINDING rule in the prompt: model X becomes a MANDATORY-search item; the flag is closeable ONLY by demonstrating model X was actually searched (an iteration_NN_X/ run exists with an [ITERATION-COMPLETE]); it CANNOT be closed by argument or convergence narrative. Embed the CASE4 example verbatim: HMBC 11→13 is genuine evidence for ethyl (the two methyls of 1,4-dimethylazulene are far apart; a CH3–CH2 gives 2–3J) and MUST NOT be re-explained away as gem-dimethyl coupling within isopropyl. Add the fresh-session reload note as an HTML comment in the new gate (repo .claude/ symlinked to ~/.claude; FRESH Claude Code session required; validated by blind CASE4 UAT-01 / Phase 89).
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && grep -q "MULT-EVIDENCE-FOR" .claude/agents/lucy-devils-advocate.md && grep -qiE "binding|mandatory" .claude/agents/lucy-devils-advocate.md && grep -qi "narrative" .claude/agents/lucy-devils-advocate.md && grep -qi "fresh" .claude/agents/lucy-devils-advocate.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lucy-devils-advocate.md contains a G-MULT (or equivalently named) gate emitting "[MULT-EVIDENCE-FOR] model=<X>", states the flag is BINDING/mandatory-search, says it is closeable ONLY by an actual iteration_NN_X/ search (never by narrative), and embeds the CASE4 HMBC 11→13 ethyl example.
    - The verify grep gate exits 0; a fresh-session reload note is present.
  </acceptance_criteria>
  <done>The DA emits a binding [MULT-EVIDENCE-FOR] flag that forces model X into the search and cannot be narrated away.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-accept coverage gate in case.md + CASE-PROGRESS.md searched ledger</name>
  <files>.claude/commands/lucy-ng/case.md</files>
  <read_first>
    - .claude/commands/lucy-ng/case.md:216-310 (CASE-PROGRESS.md authorship + monitor_progress + completion signals + the "never go straight from [RANKING-COMPLETE] to present_results" rule and identity_gate lifecycle point)
    - .claude/commands/lucy-ng/case.md:432-469 (detect_loops Pattern 5 — the reopen-the-converged-run precedent the coverage gate mirrors, but coverage-triggered)
    - .claude/commands/lucy-ng/case.md:632-655 (identity_gate step — the gate-lifecycle template; the coverage gate runs at the same pre-accept point)
    - .claude/agents/lucy-lsd-engineer.md (the [MULTIPLICITY-AMBIGUOUS] / per-family [ITERATION-COMPLETE] / SEARCHED-not-RANKED contracts from Plan 88-02 that the gate reads)
    - .claude/agents/lucy-devils-advocate.md (the [MULT-EVIDENCE-FOR] model=X mandatory-search contract from Task 1)
  </read_first>
  <action>
    Add a "Multiplicity Coverage" ledger and a pre-accept coverage gate to case.md. LEDGER: as the SOLE author of CASE-PROGRESS.md, the coordinator adds a "## Multiplicity Coverage" section recording, when a [MULTIPLICITY-AMBIGUOUS] record exists: viable_families (from the nmr-chemist signal), searched_families (one entry per family that produced its own iteration_NN_<family>/ run + [ITERATION-COMPLETE] — count a family as SEARCHED even if its solutions.smi conversion was skipped for size, per the SEARCHED-not-RANKED rule; do NOT require a ranked solutions.smi), DA-mandated models (from any [MULT-EVIDENCE-FOR] model=X message), and the gate verdict. GATE: add a coverage_gate step at the identity_gate lifecycle point (after the union is ranked and final_results.md exists, BEFORE accept/present_results), GUARDED to only run when a [MULTIPLICITY-AMBIGUOUS] record exists (otherwise skip — the single-family flow is unaffected). The gate is deterministic and MAE-INDEPENDENT: if (viable_families ⊆ searched_families) AND (every DA-mandated model ∈ searched_families), PASS → proceed to identity_gate; otherwise FAIL → do NOT accept, REOPEN and push the lsd-engineer to search the missing family(ies)/model(s) in their own iteration_NN_<family>/ dirs, then re-run the union rank and re-enter the gate. State explicitly this gate triggers on COVERAGE, not MAE (the CASE4 wrong class scored MAE 1.75 PLAUSIBLE and the MAE>4 guardrail stayed silent). Update the completion-signal flow so [RANKING-COMPLETE] routes through coverage_gate (when ambiguous) before identity_gate. Add the fresh-session reload note as an HTML comment in the new step.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && grep -qi "Multiplicity Coverage" .claude/commands/lucy-ng/case.md && grep -qi "coverage" .claude/commands/lucy-ng/case.md && grep -q "searched" .claude/commands/lucy-ng/case.md && grep -q "MULTIPLICITY-AMBIGUOUS" .claude/commands/lucy-ng/case.md && grep -qi "MAE-independent\|independent of MAE\|not MAE" .claude/commands/lucy-ng/case.md && grep -qi "fresh" .claude/commands/lucy-ng/case.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - case.md contains a "## Multiplicity Coverage" ledger (viable_families, searched_families counted by SEARCHED-not-RANKED, DA-mandated models, verdict) and a pre-accept coverage_gate guarded by a [MULTIPLICITY-AMBIGUOUS] record.
    - The gate is stated as MAE-independent, runs at the identity_gate lifecycle point, reopens to search missing families/DA-mandated models, and re-enters after re-ranking.
    - The verify grep gate exits 0; a fresh-session reload note is present; the single-family flow is explicitly unaffected.
  </acceptance_criteria>
  <done>The orchestrator will not accept an ambiguous-multiplicity run until every viable family and every DA-mandated model has been actually searched, counted by SEARCHED not RANKED, independent of MAE.</done>
</task>

<task type="auto">
  <name>Task 3: Coverage reopen pattern + advisory wording in the references</name>
  <files>.claude/commands/lucy-ng/references/loop-patterns.md, .claude/commands/lucy-ng/references/advisory-templates.md</files>
  <read_first>
    - .claude/commands/lucy-ng/references/loop-patterns.md:60-89 (Quality Convergence Failure — the reopen pattern to model the coverage pattern on)
    - .claude/commands/lucy-ng/references/advisory-templates.md (advisory wording precedents for reopen directives)
    - .claude/commands/lucy-ng/case.md (the coverage_gate step from Task 2 these references support)
  </read_first>
  <action>
    In loop-patterns.md, add a new pattern (e.g. "Multiplicity Coverage Gap") alongside Quality Convergence Failure: state its trigger is a [MULTIPLICITY-AMBIGUOUS] record where viable_families ⊄ searched_families OR a [MULT-EVIDENCE-FOR] model=X is not in searched_families; its data source is the CASE-PROGRESS.md "## Multiplicity Coverage" section; its action is REOPEN — push the lsd-engineer to run the missing family(ies)/model(s) in their own iteration_NN_<family>/ dirs, then re-union-rank and re-enter the coverage gate. Emphasize it is COVERAGE-triggered, not MAE-triggered, and counts SEARCHED not RANKED. In advisory-templates.md, add the reopen advisory wording the coordinator pushes for a multiplicity-coverage reopen (WHAT not HOW: name the missing family/model and that it must be searched in its own iteration dir; do not dictate the MULT lines). Add a fresh-session reload note as an HTML comment in each edited reference.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && grep -qi "Multiplicity Coverage" .claude/commands/lucy-ng/references/loop-patterns.md && grep -qi "reopen" .claude/commands/lucy-ng/references/loop-patterns.md && grep -qiE "multiplicity|MULT-EVIDENCE-FOR|coverage" .claude/commands/lucy-ng/references/advisory-templates.md && grep -qi "fresh" .claude/commands/lucy-ng/references/loop-patterns.md && grep -qi "fresh" .claude/commands/lucy-ng/references/advisory-templates.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - loop-patterns.md contains a "Multiplicity Coverage Gap" pattern (coverage-triggered reopen, SEARCHED-not-RANKED, data from the CASE-PROGRESS.md ## Multiplicity Coverage section).
    - advisory-templates.md contains the multiplicity-coverage reopen advisory wording (WHAT-not-HOW).
    - The verify grep gate exits 0; both references carry a fresh-session reload note.
  </acceptance_criteria>
  <done>The reopen mechanics and advisory wording for the coverage gate exist in the references, consistent with the case.md gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teammate signals → orchestrator gate | Internal CASE-team structured messages ([MULTIPLICITY-AMBIGUOUS], [ITERATION-COMPLETE], [MULT-EVIDENCE-FOR]); no untrusted external input; coordinator is sole author of CASE-PROGRESS.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-05 | Repudiation (silent accept) | Run accepting before all viable families searched | mitigate | Deterministic pre-accept coverage gate (viable ⊆ searched, MAE-independent); reopens until covered; guarded by a [MULTIPLICITY-AMBIGUOUS] record |
| T-88-06 | Tampering (flag rationalized away) | DA "evidence FOR model X" closed by narrative | mitigate | Binding [MULT-EVIDENCE-FOR] flag closeable ONLY by an actual iteration_NN_X/ search; enforced by the coverage gate's DA-mandated-models check |
| T-88-07 | Information disclosure (coverage miscount) | Counting RANKED instead of SEARCHED drops a conversion-skipped family | mitigate | Ledger counts SEARCHED families (own LSD run + [ITERATION-COMPLETE]), not ranked solutions.smi |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (prompt edits only); Package Legitimacy Audit N/A per RESEARCH.md |

Block-on severity: high — none expected (prompt edits only; no network/auth/untrusted-parse surface).
</threat_model>

<verification>
- All three grep gates (Tasks 1-3) exit 0.
- Every edited file carries the fresh-session reload note (HTML comment).
- pytest suite stays green (no Python changed in this plan).
- Cross-consistency: the [MULT-EVIDENCE-FOR] and [MULTIPLICITY-AMBIGUOUS] tokens and the "## Multiplicity Coverage" ledger name match across devils-advocate.md, case.md, loop-patterns.md, and the Plan 88-02 producer agents.
</verification>

<success_criteria>
- MULT-02: a deterministic, MAE-independent pre-accept coverage gate reopens the run when a viable family was not searched (counted SEARCHED-not-RANKED), closing the CASE4 silent-guardrail failure.
- MULT-03: a DA "evidence FOR model X" flag is binding — model X must be searched and the flag cannot be narrated away.
- Functional acceptance deferred to the blind CASE4 re-run (UAT-01, Phase 89).
</success_criteria>

<output>
Create `.planning/phases/88-aliphatic-multiplicity-robustness/88-03-SUMMARY.md` when done.
</output>
