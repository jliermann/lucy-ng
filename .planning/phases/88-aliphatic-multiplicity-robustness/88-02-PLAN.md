---
phase: 88-aliphatic-multiplicity-robustness
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - .claude/agents/lucy-nmr-chemist.md
  - .claude/agents/lucy-lsd-engineer.md
autonomous: true
requirements: [MULT-04, MULT-01]
must_haves:
  truths:
    - "D-05/MULT-04: the nmr-chemist emits a deterministic [MULTIPLICITY-AMBIGUOUS] signal listing the viable whole-molecule multiplicity families, fired when multiplicity_edited is false AND/OR APT/DEPT is phase-unreliable (combines the programmatic HSQC flag with the existing APT/DEPT reliability assessment, not the boolean alone)"
    - "D-01/MULT-01: when ambiguous, the lsd-engineer runs EACH viable family as a SEPARATE fully-constrained LSD run in its own iteration_NN_<family>/ sub-dir, then unions per-family solutions.smi (deduped) into ONE lucy lsd rank"
    - "D-02: viable families are whole-molecule CH3/CH2/CH partitions consistent with formula/H-count/DBE (not per-center cross-product)"
    - "D-03: families capped at 3; if more are valid, rank by chemical plausibility, search the top 3, and document the truncation explicitly in CASE-PROGRESS.md (no silent drop)"
    - "Pitfall: coverage counts SEARCHED families (each family got its own constrained LSD run + an [ITERATION-COMPLETE]), not RANKED — a large family whose solutions.smi conversion was skipped is still recorded as searched"
    - "Pitfall: the union solutions.smi is deduped by canonical SMILES before ranking (the parser does NOT dedup)"
  artifacts:
    - path: ".claude/agents/lucy-nmr-chemist.md"
      provides: "deterministic multiplicity-ambiguity detection + [MULTIPLICITY-AMBIGUOUS] emission"
      contains: "MULTIPLICITY-AMBIGUOUS"
    - path: ".claude/agents/lucy-lsd-engineer.md"
      provides: "per-family LSD runs in iteration_NN_<family>/ + deduped union ranking"
      contains: "iteration_NN_"
  key_links:
    - from: "lucy-nmr-chemist.md [MULTIPLICITY-AMBIGUOUS]"
      to: "lucy-lsd-engineer.md per-family runs"
      via: "structured signal listing viable families consumed deterministically"
      pattern: "MULTIPLICITY-AMBIGUOUS"
    - from: "lucy-nmr-chemist.md"
      to: "lucy pick hsqc multiplicity_edited"
      via: "reads the programmatic boolean from Plan 88-01"
      pattern: "multiplicity_edited"
---

<objective>
Wire the multiplicity-ambiguous branch into the two CASE producer agents: the nmr-chemist emits a deterministic [MULTIPLICITY-AMBIGUOUS] signal (D-05/MULT-04), and the lsd-engineer searches every viable family as a separate fully-constrained LSD run and ranks across the deduped union (D-01/D-02/D-03/MULT-01).

Purpose: This is the core CASE4 fix — the lsd-engineer must STOP hard-coding one multiplicity model (the iPr path that excluded chamazulene's ethyl truth) and instead give LSD every viable whole-molecule partition. The nmr-chemist's "is it ambiguous?" decision becomes programmatic: it reads the multiplicity_edited boolean from lucy pick hsqc (Plan 88-01) and combines it with its existing APT/DEPT reliability assessment.

Output: prompt edits to lucy-nmr-chemist.md and lucy-lsd-engineer.md. These are repo .claude/ files symlinked into ~/.claude; behavior changes are MARKDOWN PROMPT EDITS — NOT unit-testable this session and requiring a FRESH Claude Code session to reload the edited agents. Functional validation is the blind CASE4 re-run (UAT-01, Phase 89). This plan is grep-asserted now and blind-UAT-validated in Phase 89.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-aliphatic-multiplicity-robustness/88-CONTEXT.md
@.planning/phases/88-aliphatic-multiplicity-robustness/88-RESEARCH.md
@.planning/todos/pending/2026-06-23-multiplicity-model-hardcoding-defect.md

<interfaces>
<!-- Skill-file anchors the edits attach to. Read the files; do not reconstruct. -->

lucy-nmr-chemist.md:
- "Multiplicity Conflicts" / multiplicity assessment + [SETUP-COMPLETE] message (peak-assignment table: Atom#, Shift, Multiplicity CH3/CH2/CH/Cq, Confidence), lines ~338-362.
- Evidence-priority table: DEPT-135 sign 100% (never override); HSQC 95%, lines ~237-242.
- DBE self-check (Section 5a, lines ~205-235) supplies formula/H-budget/DBE for partition enumeration.

lucy-lsd-engineer.md:
- §1 MULT reference (CH3 = MULT i C 3 3, CH2 = MULT i C 3 2, CH = MULT i C 3 1), lines ~62-71.
- §3 File Organization: analysis/iteration_NN/compound.lsd|.sol|solutions.smi convention; read-previous-never-reconstruct carry-forward rule.
- Anti-stall conversion-skip rule (lines 28-45): count > ~50 do NOT convert; convert only when ≤ ~20. THIS IS THE COVERAGE TRAP — see action.
- lucy lsd run auto-writes solutions.smi per output dir; lucy lsd rank <file> --shifts ranks one SMILES/line, parser does NOT dedup.

CASE4 worked example (canonical illustration throughout): truth = 7-ethyl-1,4-dimethylazulene (2×CH3 + ethyl); the hard-coded leading model was iPr (3×CH3 + CH); HSQC non-edited (0 negatives) AND APT phase-unreliable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: nmr-chemist emits deterministic [MULTIPLICITY-AMBIGUOUS] + viable families</name>
  <files>.claude/agents/lucy-nmr-chemist.md</files>
  <read_first>
    - .claude/agents/lucy-nmr-chemist.md:235-242 (evidence-priority hierarchy — DEPT sign 100%, HSQC 95%; ambiguity declared only when these high-confidence signals are absent/unreliable)
    - .claude/agents/lucy-nmr-chemist.md:205-235 (Section 5a DBE self-check — formula, H-budget, DBE used for whole-molecule partitions)
    - .claude/agents/lucy-nmr-chemist.md:338-362 ([SETUP-COMPLETE] message + peak-assignment multiplicity table + workflow steps where the new signal is emitted)
    - .planning/todos/pending/2026-06-23-multiplicity-model-hardcoding-defect.md (CASE4 worked example to embed as the illustration)
  </read_first>
  <action>
    Add a new numbered section to lucy-nmr-chemist.md titled "Aliphatic Multiplicity Ambiguity Detection (MULT-04)" and reference it from the workflow steps before [SETUP-COMPLETE]. Define the DETERMINISTIC trigger as an AND/OR: declare multiplicity-ambiguous when (a) "lucy pick hsqc <hsqc_path> --format json" reports multiplicity_edited false (the boolean from Plan 88-01 — instruct the chemist to READ this field, not eyeball the spectrum) AND/OR (b) APT/DEPT is phase-unreliable/inconsistent by the chemist's existing reliability assessment (phase varies across range, signs inconsistent with shift regions). State explicitly that multiplicity_edited false is NECESSARY-but-NOT-SUFFICIENT alone, and that an edited HSQC with clean DEPT-135 is NOT ambiguous (combine both inputs — addresses RESEARCH Pitfall 3). On the ambiguous condition, enumerate VIABLE FAMILIES per D-02: whole-molecule CH3/CH2/CH integer partitions of the ambiguous aliphatic carbons plus attached protons that are consistent with molecular formula / H-count / DBE (CASE4 example: same H-budget block yields 3×CH3+CH iPr-family vs 2×CH3+CH2+CH2 ethyl-family). Apply the cap of 3 (D-03): if more than 3 partitions are formally valid, rank by prior chemical plausibility, list the top 3, and state the truncation so the coordinator records it (no silent drop). Then emit a structured [MULTIPLICITY-AMBIGUOUS] message carrying the ambiguity basis (multiplicity_edited value + APT/DEPT verdict) and an explicit numbered "viable_families:" list, each family naming per-atom multiplicity. State the signal is IN ADDITION to [SETUP-COMPLETE] (coordinator records both). Add a fresh-session reload note as an HTML comment near the top of the new section stating the file is a repo .claude/ skill prompt symlinked to ~/.claude, that a FRESH Claude Code session is required to reload, and that behavior is validated by the blind CASE4 re-run (UAT-01 / Phase 89), not unit tests this session. Do NOT put a numeric threshold in the prompt (the detector lives in pick.py per D-05).
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && grep -q "MULTIPLICITY-AMBIGUOUS" .claude/agents/lucy-nmr-chemist.md && grep -q "multiplicity_edited" .claude/agents/lucy-nmr-chemist.md && grep -q "viable_families" .claude/agents/lucy-nmr-chemist.md && grep -qi "fresh" .claude/agents/lucy-nmr-chemist.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lucy-nmr-chemist.md contains a "MULTIPLICITY-AMBIGUOUS" section, references "multiplicity_edited" and a "viable_families" list, the CASE4 iPr-vs-ethyl example, the cap-of-3 + truncation rule, and the AND/OR trigger wording (multiplicity_edited false necessary-but-not-sufficient).
    - The verify grep gate exits 0; a fresh-session reload note is present.
    - No numeric detector threshold appears in the prompt (the detector stays in pick.py).
  </acceptance_criteria>
  <done>The nmr-chemist deterministically declares ambiguity from the programmatic HSQC boolean combined with APT/DEPT reliability, enumerates capped whole-molecule families, and emits [MULTIPLICITY-AMBIGUOUS] with the viable_families list.</done>
</task>

<task type="auto">
  <name>Task 2: lsd-engineer runs each viable family separately + deduped union ranking</name>
  <files>.claude/agents/lucy-lsd-engineer.md</files>
  <read_first>
    - .claude/agents/lucy-lsd-engineer.md:26-45 (anti-stall conversion-skip rule — the SEARCHED-not-RANKED coverage trap: a >50-solution family may skip solutions.smi conversion yet was still searched)
    - .claude/agents/lucy-lsd-engineer.md:62-71 (§1 MULT reference — CH3/CH2/CH MULT lines used to author the two per-family blocks)
    - .claude/agents/lucy-lsd-engineer.md (§3 File Organization — iteration_NN/ convention + read-previous-never-reconstruct carry-forward rule the per-family dirs extend)
    - .claude/agents/lucy-nmr-chemist.md (the [MULTIPLICITY-AMBIGUOUS] viable_families contract authored in Task 1 — this is the input the lsd-engineer consumes)
    - .planning/phases/88-aliphatic-multiplicity-robustness/88-RESEARCH.md Pitfalls 1 and 2 (dedup; conversion-skip suppression)
  </read_first>
  <action>
    Add a new section to lucy-lsd-engineer.md titled "Per-Family Multiplicity Search (MULT-01)" wired to the [MULTIPLICITY-AMBIGUOUS] signal. Specify: on receiving viable_families, the engineer runs ONE LSD run PER FAMILY, each in its own iteration sub-directory named iteration_NN_<family> (e.g. iteration_03_iPr, iteration_03_ethyl) — NOT a single relaxed-MULT run and NOT sequential-with-fallback (call out sequential-with-fallback explicitly as the CASE4 failure mode where the leading family scored MAE 1.75 PLAUSIBLE and silently excluded the truth). Each per-family compound.lsd is FULLY constrained: it carries forward ALL identical constraints (HSQC, HMBC, ring exclusion DEFF F/FEXP, BOND C=O, COSY equiv pairs) per the read-previous rule, and differs ONLY in its MULT block, authored by hand from the family's per-atom multiplicity (CH3 = MULT i C 3 3, CH2 = MULT i C 3 2, CH = MULT i C 3 1). Show the CASE4 two-block illustration (iPr family vs ethyl family). State the SEARCHED-not-RANKED coverage rule: every viable family must be actually run (its own iteration_NN_<family>/ dir + an [ITERATION-COMPLETE] reporting its solution count), and a family whose solutions.smi conversion is skipped because the count is large (>~50, the existing anti-stall rule) is STILL recorded as SEARCHED — it must NOT be dropped from coverage or the union just because it produced no solutions.smi; note "searched, N solutions, conversion deferred" in that family's [ITERATION-COMPLETE]. For UNION RANKING (D-01 discretion, concatenate-then-one-rank): once each family has produced a solutions.smi, build a deduped union file (e.g. analysis/union_solutions.smi) by concatenating the per-family solutions.smi and DEDUPING by canonical SMILES (state the parser does NOT dedup — RESEARCH Pitfall 1 — so duplicates would distort counts/rank), then run ONE "lucy lsd rank analysis/union_solutions.smi --shifts <13C list> --format json" across the union. State that the family identity of the top-ranked solution must be reported. Add the fresh-session reload note as an HTML comment in the new section (repo .claude/ symlinked to ~/.claude; FRESH Claude Code session required; validated by blind CASE4 UAT-01 / Phase 89, not unit tests).
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && grep -q "iteration_NN_" .claude/agents/lucy-lsd-engineer.md && grep -qi "MULTIPLICITY-AMBIGUOUS" .claude/agents/lucy-lsd-engineer.md && grep -qiE "dedup|deduplicat" .claude/agents/lucy-lsd-engineer.md && grep -qi "searched" .claude/agents/lucy-lsd-engineer.md && grep -qi "fresh" .claude/agents/lucy-lsd-engineer.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lucy-lsd-engineer.md contains the per-family run pattern with iteration_NN_<family> sub-dirs, two MULT families (iPr and ethyl illustration), the deduped union + single lucy lsd rank, and the SEARCHED-not-RANKED coverage rule (a conversion-skipped large family still counts as searched).
    - The verify grep gate exits 0; sequential-with-fallback is explicitly rejected; the dedup-before-rank step is present (parser does not dedup); a fresh-session reload note is present.
  </acceptance_criteria>
  <done>The lsd-engineer runs every viable family as a separate fully-constrained LSD run, records each as searched even when conversion is skipped, and ranks across one deduped union.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nmr-chemist signal → lsd-engineer | Internal CASE-team structured messages; no untrusted external input; both agents run locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03 | Information disclosure (coverage gap) | lsd-engineer dropping a conversion-skipped family from the union | mitigate | SEARCHED-not-RANKED rule: a >50-solution family is recorded as searched and not dropped; enforced downstream by the Plan 88-03 coverage gate |
| T-88-04 | Tampering | Duplicate SMILES inflating union ranking | mitigate | Canonical-SMILES dedup before lucy lsd rank (RESEARCH Pitfall 1); the parser does not dedup |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (prompt edits only); Package Legitimacy Audit N/A per RESEARCH.md |

Block-on severity: high — none expected (prompt edits only; no network/auth/untrusted-parse surface).
</threat_model>

<verification>
- Both grep gates exit 0 (nmr-chemist signal + lsd-engineer per-family/union/coverage).
- Both edited files carry the fresh-session reload note (HTML comment).
- pytest suite stays green (no Python changed in this plan; run the suite to confirm no regression).
</verification>

<success_criteria>
- MULT-04: nmr-chemist emits a deterministic [MULTIPLICITY-AMBIGUOUS] signal with the viable_families list, combining the programmatic HSQC boolean with APT/DEPT reliability.
- MULT-01: lsd-engineer searches every viable family separately (capped at 3, truncation documented) and ranks across one deduped union; the SEARCHED-not-RANKED rule prevents coverage loss.
- Functional acceptance deferred to the blind CASE4 re-run (UAT-01, Phase 89).
</success_criteria>

<output>
Create `.planning/phases/88-aliphatic-multiplicity-robustness/88-02-SUMMARY.md` when done.
</output>
