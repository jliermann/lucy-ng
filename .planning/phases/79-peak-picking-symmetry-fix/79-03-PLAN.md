---
phase: 79-peak-picking-symmetry-fix
plan: "03"
type: execute
wave: 2
depends_on: ["79-01", "79-02"]
files_modified:
  - /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md
  - /Users/steinbeck/.claude/commands/lucy-ng/case.md
  - /Users/steinbeck/.claude/commands/lucy-ng/references/loop-patterns.md
  - /Users/steinbeck/.claude/commands/lucy-ng/references/advisory-templates.md
autonomous: false
requirements: [FIX-06]

must_haves:
  truths:
    - "lucy-nmr-chemist.md has a mandatory DBE self-check section after statistical detection, before [SETUP-COMPLETE], covering O→carbonyl 160-220 and N→amide/nitrile"
    - "lucy-nmr-chemist.md has a mandatory intensity-symmetry check for aromatic CH before statistical detection"
    - "[SETUP-COMPLETE] format in lucy-nmr-chemist.md includes DBE balance and Intensity-symmetry fields"
    - "loop-patterns.md has a Quality Convergence Failure pattern block after Constraint Churning"
    - "case.md detect_loops step references Pattern 5 Quality Convergence Failure with primary trigger (all top-3 IMPLAUSIBLE/QUESTIONABLE) and OR-trigger (best-MAE > 4.0 AND solution_count <= 20)"
    - "case.md track_and_decide step has QUALITY_CONVERGENCE_FAILURE counter with budget=1 cycle and honest-termination at count >= 1 (no diagnostic specialist escalation)"
    - "advisory-templates.md has a quality_convergence_advisory step with reactivation advisory text and nmr-chemist SendMessage target"
  artifacts:
    - path: "/Users/steinbeck/.claude/agents/lucy-nmr-chemist.md"
      provides: "DBE self-check + intensity-symmetry procedural sections"
      contains: "DBE Self-Check"
    - path: "/Users/steinbeck/.claude/commands/lucy-ng/references/loop-patterns.md"
      provides: "5th loop-pattern definition"
      contains: "Quality Convergence Failure"
    - path: "/Users/steinbeck/.claude/commands/lucy-ng/case.md"
      provides: "Pattern 5 detection + QUALITY_CONVERGENCE_FAILURE counter"
      contains: "QUALITY_CONVERGENCE_FAILURE"
    - path: "/Users/steinbeck/.claude/commands/lucy-ng/references/advisory-templates.md"
      provides: "Assumption-reexamination advisory template"
      contains: "quality_convergence_advisory"
  key_links:
    - from: "/Users/steinbeck/.claude/commands/lucy-ng/case.md"
      to: "/Users/steinbeck/.claude/commands/lucy-ng/references/loop-patterns.md"
      via: "detect_loops step reads loop-patterns.md then checks Pattern 5"
      pattern: "Quality Convergence Failure"
    - from: "/Users/steinbeck/.claude/commands/lucy-ng/case.md"
      to: "/Users/steinbeck/.claude/commands/lucy-ng/references/advisory-templates.md"
      via: "deliver_advisory_and_results step reads advisory-templates.md; quality_convergence_advisory section provides the template"
      pattern: "quality_convergence_advisory"
---

<objective>
Extend four agent/command skill markdown files to add the Layer 2 feedback loop (FIX-06): a mandatory DBE self-check and intensity-symmetry procedural step in the nmr-chemist workflow, plus a new 5th quality loop-pattern in case.md / loop-patterns.md / advisory-templates.md.

Purpose: CASE9 converged cleanly (211→4→18→12) with no loop pattern firing, despite all top solutions being IMPLAUSIBLE. The 4 existing patterns key only on solution_count. This plan adds a quality-based 5th pattern that fires when LSD is healthy but the NMR interpretation going in was wrong.
Output: 4 markdown files updated; manual grep verifications confirm all required insertions are present.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/PROJECT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-CONTEXT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-RESEARCH.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-PATTERNS.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-01-SUMMARY.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-02-SUMMARY.md

<interfaces>
<!-- Current structure of the 4 skill files — confirmed by direct inspection. -->

~/.claude/agents/lucy-nmr-chemist.md (264 lines):
  - Section "## 5. Statistical Detection Protocol" at line 106
    - Detection command table ends around line 116
    - "Run detection ONCE per compound, before first LSD iteration." at line 117
  - Workflow steps in <workflow> block at lines 250-264:
    step 5: lucy analyze symmetry
    step 6: Run statistical detection
    step 6a: Scan HMBC for 4J couplings
    step 7: Compile results
    step 8: Send [SETUP-COMPLETE]
  - [SETUP-COMPLETE] format at lines 201-222

~/.claude/commands/lucy-ng/case.md (632 lines):
  - <step name="detect_loops"> at line 343
    - Pattern 1-4 quick criteria at lines 352-358
    - Convergence check block at lines 360-365
  - <step name="track_and_decide"> at line 476
    - Per-pattern counters list at lines 479-484
    - Decision logic at lines 486-495

~/.claude/commands/lucy-ng/references/loop-patterns.md (60 lines):
  - Four pattern blocks; last one "### Constraint Churning" ends at line 59
  - Closing </loop_detection_reference> tag at line 60

~/.claude/commands/lucy-ng/references/advisory-templates.md (284 lines):
  - Contains several <step> blocks for advisories
  - Find the closing </anti_patterns> or equivalent end tag to append before it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mandatory DBE self-check and intensity-symmetry sections to lucy-nmr-chemist.md</name>
  <files>/Users/steinbeck/.claude/agents/lucy-nmr-chemist.md</files>
  <read_first>
    ~/.claude/agents/lucy-nmr-chemist.md — full file; locate:
      (a) Section "## 5. Statistical Detection Protocol" (line 106) and "Run detection ONCE per compound" (line 117) — insertion point for intensity-symmetry check
      (b) The end of section 5 / beginning of section 6 (around line 137) — insertion point for DBE self-check (MANDATORY section 5a)
      (c) The [SETUP-COMPLETE] format block (lines 201–222) — where to add two new fields
      (d) The <workflow> block steps 5-8 (lines 250-264) — where to add a step 6b or 5a reference
    .planning/phases/79-peak-picking-symmetry-fix/79-PATTERNS.md — section "lucy-nmr-chemist.md" for exact markdown text to insert (lines 566-648)
  </read_first>
  <action>
    Make three targeted insertions in ~/.claude/agents/lucy-nmr-chemist.md:

    INSERTION 1 — Intensity-symmetry check (after the detection table in Section 5, before "Run detection ONCE per compound"):
    Add a new subsection titled "### Intensity-Symmetry Check (MANDATORY for aromatic compounds)" with numbered steps:
    1. Collect ppm positions of all HSQC-confirmed aromatic CH carbons (correlations in 110–165 ppm)
    2. Compare 13C peak intensities within this class: compute median intensity of the aromatic CH class; any peak with intensity >= 1.6× the median is a 2C-equivalence candidate
    3. Report candidates under "Intensity-symmetry" in [SETUP-COMPLETE]: "<ppm> (ratio=<R>, 2C candidate)" for each flagged peak; "none detected" if none
    4. Pass confirmed 2C-equivalence candidates to lucy detect aromatic-cosy as grouped shifts
    Include a "**Why:**" rationale: "13C intensity for aromatic CH reflects the number of equivalent carbons. Para-disubstituted rings produce two 2C-equivalent pairs at ~2× the intensity of a mono-substituted ring carbon. This feeds detect_aromatic_cosy_pairs correctly."

    INSERTION 2 — DBE self-check (after Section 5 closing / before Section 6 "Chemistry-First Hierarchy"):
    Add a new section "## 5a. DBE Self-Check (MANDATORY — before [SETUP-COMPLETE])" with:
    - Formula: DBE = (2C + 2 + N − H) / 2
    - Account for: benzene/aromatic ring = 4 DBE; each additional ring = 1; each C=C = 1; each C=O (carbonyl, ester, amide) = 1
    - If DBE_found < DBE_formula: deficit = DBE_formula − DBE_found
      - O in formula AND 160-220 ppm has no picked peak → FLAG: "DBE deficit of N; O in formula; 160-220 ppm region appears empty. Check SNR annotation for peaks in 160-220 with SNR 3-20."
      - N in formula AND 150-180 ppm (amide) or 100-120 ppm (nitrile) empty → FLAG similar
      - No O or N but deficit > 0 → flag as possible additional ring
    - Output: add to [SETUP-COMPLETE] "DBE balance: accounted X of Y DBE" (or "DBE deficit: N DBE; suspected: <region>")
    - Closing: "This check is a diagnostic flag, not a decision gate. The agent decides whether to act on it."

    INSERTION 3 — [SETUP-COMPLETE] format fields (after "Quality assessment:" line, before "Statistical detection:"):
    Add two new fields:
    - "DBE balance: <accounted N of M DBE> or <deficit N DBE; suspected: region>"
    - "Intensity-symmetry: <ppm: ratio, 2C candidate> or <none detected>"

    Also add a step "6b" (or renumber as needed) in the <workflow> block to reference the intensity-symmetry check and DBE self-check — these must appear as named steps so the agent does not skip them. Step 6b: "Perform intensity-symmetry check on HSQC-confirmed aromatic CH peaks (Section 5, Intensity-Symmetry Check)". Between step 6 and step 7 add: "6b. Perform DBE self-check (Section 5a, mandatory)".

    Follow the exact markdown conventions from PATTERNS.md: ## headings with (MANDATORY — condition), numbered checklists, bold flag phrases, short "**Why:**" rationale.
  </action>
  <verify>
    <automated>grep -c "DBE Self-Check\|Intensity-Symmetry Check\|DBE balance\|Intensity-symmetry" /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md</automated>
    Expected: >= 6 (each phrase appears at least once; the fields appear in both the section and the [SETUP-COMPLETE] template).

    <automated>grep -n "MANDATORY" /Users/steinbeck/.claude/agents/lucy-nmr-chemist.md</automated>
    Expected: at least 2 matches (one for intensity-symmetry heading, one for DBE self-check heading).
  </verify>
  <done>
    lucy-nmr-chemist.md has a mandatory intensity-symmetry check after analyze symmetry, a mandatory DBE self-check section 5a before [SETUP-COMPLETE], two new fields in the [SETUP-COMPLETE] template, and workflow steps referencing both.
    Commit: "feat(79-03a): add mandatory DBE self-check + intensity-symmetry to nmr-chemist skill (FIX-06)"
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire 5th quality loop-pattern into loop-patterns.md, case.md, and advisory-templates.md</name>
  <files>
    /Users/steinbeck/.claude/commands/lucy-ng/references/loop-patterns.md,
    /Users/steinbeck/.claude/commands/lucy-ng/case.md,
    /Users/steinbeck/.claude/commands/lucy-ng/references/advisory-templates.md
  </files>
  <read_first>
    ~/.claude/commands/lucy-ng/references/loop-patterns.md — full file (60 lines); locate closing </loop_detection_reference> tag on line 60 — insert new pattern block before this tag
    ~/.claude/commands/lucy-ng/case.md — lines 343-366 (detect_loops step: 4 pattern quick-criteria + convergence block), lines 476-495 (track_and_decide: per-pattern counters + decision logic); these are the two insertion points
    ~/.claude/commands/lucy-ng/references/advisory-templates.md — read the last 50 lines to find the correct append point (before or after closing tag)
    .planning/phases/79-peak-picking-symmetry-fix/79-PATTERNS.md — sections "loop-patterns.md", "case.md detect_loops step", "case.md track_and_decide counter", "advisory-templates.md" for exact markdown text (lines 697-822)
    .planning/phases/79-peak-picking-symmetry-fix/79-RESEARCH.md — Pattern 5 (lines 354-416), Pitfall 6 (lines 469-477) for the counter logic and the prohibition on diagnostic-specialist escalation
  </read_first>
  <action>
    EDIT 1 — loop-patterns.md: Before the closing </loop_detection_reference> tag, append a new block:

    ### Quality Convergence Failure
    **Definition:** Solutions converge to a small count but ALL top-K candidates are IMPLAUSIBLE or QUESTIONABLE per the solution-analyst's verdict.

    **Detection criteria (primary — check FIRST):**
    - solution-analyst's most recent [RANKING-COMPLETE] record lists Chemical plausibility as "IMPLAUSIBLE" or "QUESTIONABLE" for ALL top-3 candidates
    - A [RANKING-COMPLETE] record for the current iteration must exist (guard — do not fire before ranking has run)

    **Detection criteria (OR trigger):**
    - best MAE in latest [RANKING-COMPLETE] > 4.0 ppm AND solution_count <= 20

    **Common root causes:**
    - A key peak was missed in initial 13C picking (e.g., weak quaternary carbonyl masked by solvent-dominated max-relative threshold)
    - DBE deficit: missing carbonyl → forced extra ring → correct structure excluded
    - Para-symmetry not detected: 2C-equivalent aromatic peaks not passed to detect_aromatic_cosy_pairs → emergent ring never activated
    - Incorrect molecular formula

    **Why this is different from Patterns 1–4:**
    Patterns 1–4 detect LSD-level symptoms (zero solutions, explosion, churning, ELIM thrash). This pattern fires when LSD is healthy but the **interpretation going into LSD was wrong from the start**. A solution_count of <= 20 looks like SUCCESS to the other patterns — that is exactly the CASE9 trap.

    **Budget:** Exactly 1 re-examination cycle. After that, honest termination. Do NOT escalate to the diagnostic specialist (it is LSD-focused, not peak-picking focused).

    Also update the title line "## Four Loop Patterns" to "## Five Loop Patterns".

    EDIT 2 — case.md detect_loops step (lines 343-366): After the Pattern 4 quick criterion line, and BEFORE the convergence check block that starts "If no loop pattern detected, check convergence:", insert:

    **Pattern 5: Quality Convergence Failure** — Most recent [RANKING-COMPLETE] message (or CASE-PROGRESS.md ## Ranking section for current iteration) shows Chemical plausibility = IMPLAUSIBLE or QUESTIONABLE for ALL top-3 candidates.
    OR-trigger: best-MAE > 4.0 ppm AND solution_count <= 20.
    Guard: only fire if a [RANKING-COMPLETE] record exists for the current iteration. Do NOT fire if solution_count = 0 (Pattern 2 covers zero-solution cases).

    Also update the intro "Four patterns to detect:" line to "Five patterns to detect:".

    EDIT 3 — case.md track_and_decide step (lines 476-495): After the CONSTRAINT_CHURNING counter line in the per-pattern counters list, add:
    - QUALITY_CONVERGENCE_FAILURE: count_quality

    After the decision logic block, add a new Decision section for QUALITY_CONVERGENCE_FAILURE:
    Decision for QUALITY_CONVERGENCE_FAILURE:
    - count_quality == 0: deliver assumption-reexamination advisory (proceed to deliver_advisory_and_results step)
    - count_quality >= 1: honest termination message — do NOT escalate to diagnostic specialist (the diagnostic specialist is LSD-focused; quality convergence failure is a peak-picking issue). Send to coordinator: "Assumption re-examination complete. No correctable peak-picking defect found after 1 re-examination cycle. Additional experiments may be needed."

    EDIT 4 — advisory-templates.md: Append a new <step name="quality_convergence_advisory"> block at the end (before any closing XML tag). The block must include:
    - Header comment: "Deliver to: nmr-chemist (reactivation advisory) via SendMessage"
    - "When to use:" trigger condition
    - The advisory text with header [SUPERVISOR ADVISORY - QUALITY CONVERGENCE FAILURE]
    - A numbered re-examination checklist:
      1. DBE balance check: count accounted DBE; if deficit, check SNR annotation for peaks in 160-220 ppm with SNR 3-20
      2. Intensity-symmetry: were 2C candidates flagged? Were they passed to lucy detect aromatic-cosy?
      3. Multiplicity spot-check for suspect peaks
    - After re-examination: describe defect in [DETECTION-COMPLETE] if found; else "Re-examination complete: no correctable peak-picking defect found"
    - Budget warning: "THIS IS THE ONLY re-examination cycle. Do not initiate a further re-pick unless a concrete suspicion exists (empty expected-region despite DBE deficit with SNR 3-20 evidence)."
    - Meta: SendMessage target = nmr-chemist (NOT lsd-engineer); after delivering advisory, return to monitor_progress

    Exact text for each edit is in PATTERNS.md sections "loop-patterns.md new block", "case.md detect_loops", "case.md track_and_decide", "advisory-templates.md" (pages 696–822). Use those excerpts as the authoritative source.
  </action>
  <verify>
    <automated>grep -c "Quality Convergence Failure\|QUALITY_CONVERGENCE_FAILURE" /Users/steinbeck/.claude/commands/lucy-ng/references/loop-patterns.md /Users/steinbeck/.claude/commands/lucy-ng/case.md /Users/steinbeck/.claude/commands/lucy-ng/references/advisory-templates.md</automated>
    Expected: loop-patterns.md: 1, case.md: 2, advisory-templates.md: 1 (or higher — multiple mentions OK)

    <automated>grep -n "quality_convergence_advisory" /Users/steinbeck/.claude/commands/lucy-ng/references/advisory-templates.md</automated>
    Expected: 1 match (the step name attribute).

    <automated>grep -n "count_quality" /Users/steinbeck/.claude/commands/lucy-ng/case.md</automated>
    Expected: at least 2 matches (counter definition + decision logic).

    <automated>grep -n "Pattern 5" /Users/steinbeck/.claude/commands/lucy-ng/case.md</automated>
    Expected: 1 match in the detect_loops step.

    <automated>grep -n "Five Loop Patterns\|Five patterns to detect" /Users/steinbeck/.claude/commands/lucy-ng/references/loop-patterns.md /Users/steinbeck/.claude/commands/lucy-ng/case.md</automated>
    Expected: 1 match per file.
  </verify>
  <done>
    loop-patterns.md has Quality Convergence Failure block with detection criteria, root causes, budget note, and updated count to 5.
    case.md detect_loops has Pattern 5 criterion line (primary + OR-trigger + guard).
    case.md track_and_decide has QUALITY_CONVERGENCE_FAILURE counter and 1-cycle-then-terminate decision logic with no diagnostic-specialist escalation.
    advisory-templates.md has quality_convergence_advisory step with nmr-chemist SendMessage target and 3-item re-examination checklist.
    Commit: "feat(79-03b): wire QUALITY_CONVERGENCE_FAILURE 5th loop-pattern into case/loop-patterns/advisory (FIX-06)"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Tasks 1 and 2 added mandatory DBE self-check + intensity-symmetry sections to nmr-chemist, plus wired the 5th quality loop-pattern (QUALITY_CONVERGENCE_FAILURE) into all three case.md/loop-patterns.md/advisory-templates.md files.
  </what-built>
  <how-to-verify>
    1. Read ~/.claude/agents/lucy-nmr-chemist.md sections 5a and the intensity-symmetry subsection. Confirm: (a) DBE self-check is labelled MANDATORY; (b) it covers O→carbonyl 160-220 and N→amide/nitrile; (c) [SETUP-COMPLETE] template has "DBE balance:" and "Intensity-symmetry:" fields; (d) workflow steps reference both checks before step 7.

    2. Read ~/.claude/commands/lucy-ng/references/loop-patterns.md. Confirm: Quality Convergence Failure block is present with: primary trigger (all top-3 IMPLAUSIBLE/QUESTIONABLE), OR-trigger (MAE > 4.0 AND count <= 20), budget = 1 cycle, no diagnostic specialist escalation.

    3. Read ~/.claude/commands/lucy-ng/case.md lines 343-400 and 476-510. Confirm: Pattern 5 criterion is wired into detect_loops; QUALITY_CONVERGENCE_FAILURE counter and 1-cycle termination logic is in track_and_decide.

    4. Read ~/.claude/commands/lucy-ng/references/advisory-templates.md last section. Confirm: quality_convergence_advisory step exists with nmr-chemist as SendMessage target and 3-item re-examination checklist.

    5. Run the full Python test suite to confirm no Python regressions from the markdown-only edits:
       cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest -q 2>&1 | tail -5
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem write | Edits local skill markdown files under ~/.claude/; no network, no auth, no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Tampering | skill markdown edits | accept | Files are local agent skill definitions; changes are plain text insertions; no secrets, no untrusted input path |
| T-79-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; this plan is markdown-only edits |

**Note:** This plan consists entirely of local markdown text file edits. No network, no auth, no untrusted input, no DB writes. The only security-relevant concern is unintentional removal of existing skill sections during edits — mitigated by reading the full file before editing (per critical_rules) and targeted insertions rather than full rewrites.
</threat_model>

<verification>
Full FIX-06 phase-gate check (grep assertions — no fenced code blocks):
```bash
grep -c "DBE Self-Check\|Intensity-Symmetry Check" ~/.claude/agents/lucy-nmr-chemist.md
grep -c "Quality Convergence Failure" ~/.claude/commands/lucy-ng/references/loop-patterns.md
grep -c "QUALITY_CONVERGENCE_FAILURE" ~/.claude/commands/lucy-ng/case.md
grep -c "quality_convergence_advisory" ~/.claude/commands/lucy-ng/references/advisory-templates.md
cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest -q 2>&1 | tail -5
```
</verification>

<success_criteria>
- lucy-nmr-chemist.md: mandatory DBE self-check section 5a (covering O and N), mandatory intensity-symmetry check in section 5, both new fields in [SETUP-COMPLETE] template, workflow references to both steps
- loop-patterns.md: Quality Convergence Failure block present; "Five Loop Patterns" header
- case.md: Pattern 5 in detect_loops; QUALITY_CONVERGENCE_FAILURE counter + 1-cycle termination in track_and_decide; "Five patterns to detect" intro
- advisory-templates.md: quality_convergence_advisory step with nmr-chemist SendMessage target
- Human checkpoint approved (visual/textual review confirms all insertions are correct)
- Python test suite still fully green
</success_criteria>

<output>
Create `.planning/phases/79-peak-picking-symmetry-fix/79-03-SUMMARY.md` when done.
</output>
