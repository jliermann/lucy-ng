---
phase: 79-peak-picking-symmetry-fix
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/test_peak_picker_snr.py
  - tests/test_intensity_symmetry.py
autonomous: true
requirements: [FIX-04, FIX-05, FIX-06]

must_haves:
  truths:
    - "FIX-04, FIX-05, FIX-06 rows appear in the REQUIREMENTS.md traceability table under Post-UAT Fixes, mapped to Phase 79 (Wave-0 stubs assert D-04: per-peak SNR, D-05: and D-06: intensity-symmetry scope)"
    - "tests/test_peak_picker_snr.py exists with failing stubs for test_case9_carbonyl_picked, test_case1_count_unchanged, test_mad_lower_than_max_for_cdcl3_dominated_spectrum, test_pick_1d_json_has_snr, test_pick_1d_json_has_noise_sigma"
    - "tests/test_intensity_symmetry.py exists with failing stubs for test_case9_flags_two_candidates, test_estimated_carbon_count_is_2, test_non_aromatic_peaks_excluded, test_empty_aromatic_ch_list_returns_empty, test_single_aromatic_ch_returns_empty"
    - "Stub tests fail with ImportError or NotImplementedError (RED state confirmed)"
    - "Full pytest suite remains at 1023 passed (no regressions from stub files alone)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "FIX-04/05/06 in traceability table"
      contains: "FIX-04"
    - path: "tests/test_peak_picker_snr.py"
      provides: "SNR threshold test stubs"
      contains: "test_case9_carbonyl_picked"
    - path: "tests/test_intensity_symmetry.py"
      provides: "intensity-symmetry test stubs"
      contains: "test_case9_flags_two_candidates"
  key_links:
    - from: "tests/test_peak_picker_snr.py"
      to: "src/lucy_ng/processing/peak_picker.py"
      via: "import AdaptivePeakPicker, _compute_snr_threshold"
      pattern: "from lucy_ng.processing.peak_picker import"
    - from: "tests/test_intensity_symmetry.py"
      to: "src/lucy_ng/processing/peak_picker.py"
      via: "import detect_intensity_symmetry"
      pattern: "from lucy_ng.processing.peak_picker import detect_intensity_symmetry"
---

<objective>
Prepare the traceability table and failing test stubs that Plans 01 and 02 will make green.

Purpose: Wave 0 ensures that (a) FIX-04/05/06 are formally tracked in REQUIREMENTS.md, and (b) test stubs exist in RED state so execution is test-driven from the start.
Output: Updated REQUIREMENTS.md with 3 new rows; two new test files with failing stubs; full suite still green (stub files alone do not break anything).
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/PROJECT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/ROADMAP.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/STATE.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/REQUIREMENTS.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-CONTEXT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-RESEARCH.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-PATTERNS.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-VALIDATION.md

<interfaces>
<!-- Key types the test stubs import against. -->

From src/lucy_ng/models/peaks.py:
  class Peak1D(BaseModel):
      position: float  # ppm
      intensity: float
      assignment: str | None = None
      multiplicity: str | None = None

  class PeakList1D(BaseModel):
      peaks: list[Peak1D]
      nucleus: str
      spectrum_id: str | None = None

From src/lucy_ng/processing/peak_picker.py (current signatures — stubs import against future signatures):
  class AdaptivePeakPicker:
      @staticmethod
      def pick_peaks(spectrum: Spectrum1D, threshold: float = 0.05, detect_negative: bool = False) -> PeakList1D

  # _compute_snr_threshold and detect_intensity_symmetry do NOT yet exist — stubs will ImportError until Plan 01/02 implement them

From tests/test_peak_picker_2d.py (skip-guard pattern to mirror):
  CASE9_C13 = Path("/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/12")
  @pytest.mark.skipif(not CASE9_C13.exists(), reason="CASE9 dataset not available on this machine")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FIX-04/05/06 to REQUIREMENTS.md traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md — full file; locate the "Post-UAT Fixes" subsection (FIX-01/02/03 rows) and the Traceability table at the bottom; confirm current content before editing.
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md, make two targeted additions:

    1. Under the "### Post-UAT Fixes" subsection (after the FIX-03 row), add three new requirement rows:
       - `- [ ] **FIX-04**: Peak-picker threshold replaced with SNR/MAD-absolute (noise = 1.4826·MAD, floor k=3 IUPAC LoD); solvent multiplet (CDCl₃ etc.) excluded before threshold/scale computation; per-peak SNR annotation added to Peak1D and JSON output; backwards-compatible (threshold param kept; use_snr=True is new default)`
       - `- [ ] **FIX-05**: 13C intensity class-normalized 2C-equivalence detection for protonated aromatic CH (HSQC-confirmed, 100–165 ppm scope); output feeds `lucy analyze symmetry` / `lucy detect aromatic-cosy`; DO NOT modify detect_aromatic_cosy_pairs`
       - `- [ ] **FIX-06**: Skill feedback loop — (a) DBE self-check procedural/mandatory in nmr-chemist after picking (O→carbonyl 160–220; N→amide/nitrile); (b) 5th quality loop-pattern QUALITY_CONVERGENCE_FAILURE in case.md detect_loops + loop-patterns.md + advisory-templates.md; budget = 1 re-look cycle; does NOT escalate to diagnostic specialist`

    2. In the Traceability table at the bottom, add three new rows under the FIX-03 row:
       ```
       | FIX-04 | Phase 79 | Pending |
       | FIX-05 | Phase 79 | Pending |
       | FIX-06 | Phase 79 | Pending |
       ```

    Also update the "v1 requirements: 10 total" count to 13 and "Mapped to phases: 10" to 13.
  </action>
  <verify>
    <automated>grep -c "FIX-04\|FIX-05\|FIX-06" /Users/steinbeck/Dropbox/develop/lucy-ng/.planning/REQUIREMENTS.md</automated>
    Expected output: 9 (3 items × 3 mentions each in requirements + traceability)
  </verify>
  <done>REQUIREMENTS.md contains FIX-04, FIX-05, FIX-06 in both the requirements list and the traceability table, each mapped to Phase 79 with status Pending.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create failing test stubs for FIX-04 and FIX-05</name>
  <files>
    tests/test_peak_picker_snr.py,
    tests/test_intensity_symmetry.py
  </files>
  <read_first>
    tests/test_peak_picker_2d.py — import pattern, Path constants, BrukerReader usage, skipif guard (lines 1–30)
    tests/test_symmetry_analysis.py — pytest.fixture pattern, synthetic Peak1D fixtures, class structure (lines 1–50)
    .planning/phases/79-peak-picking-symmetry-fix/79-PATTERNS.md — sections "tests/test_peak_picker_snr.py" and "tests/test_intensity_symmetry.py" for exact import/fixture patterns to follow
  </read_first>
  <behavior>
    tests/test_peak_picker_snr.py:
    - TestSNRThreshold.test_mad_lower_than_max_for_cdcl3_dominated_spectrum: sigma_mad < 5e5 on a synthetic CDCl3-dominated spectrum; threshold < 1e6
    - TestCASE9Regression.test_case9_carbonyl_picked: ester C=O at 166.08 ppm present in picked list (±2 ppm); skipif CASE9_C13 absent
    - TestCASE9Regression.test_case9_all_peaks_have_snr: all picked peaks have non-None, positive snr; skipif CASE9_C13 absent
    - TestCASE1Regression.test_case1_count_unchanged: ibuprofen yields >= 9 peaks (no skip — in-repo data)
    - TestCLIPick1D.test_pick_1d_json_has_snr: JSON output has "snr" key in peaks[0], non-null, > 0
    - TestCLIPick1D.test_pick_1d_json_has_noise_sigma: JSON output has top-level "noise_sigma" key, non-null, > 0

    tests/test_intensity_symmetry.py:
    - TestDetectIntensitySymmetry.test_case9_flags_two_candidates: case9_like_aromatic_peaks + aromatic_ch_ppms_case9 fixture → len(result) == 2
    - TestDetectIntensitySymmetry.test_estimated_carbon_count_is_2: all result[i][2] == 2
    - TestDetectIntensitySymmetry.test_non_aromatic_peaks_excluded: all result ppm in 100.0..165.0
    - TestDetectIntensitySymmetry.test_empty_aromatic_ch_list_returns_empty: aromatic_ch_ppms=[] → []
    - TestDetectIntensitySymmetry.test_single_aromatic_ch_returns_empty: single peak → []
  </behavior>
  <action>
    Create tests/test_peak_picker_snr.py following the PATTERNS.md template exactly:
    - Module docstring citing FIX-04
    - Imports: Path, pytest, numpy, from lucy_ng.models import PeakList1D (no need to import Peak1D for these tests), from lucy_ng.processing.peak_picker import AdaptivePeakPicker, _compute_snr_threshold, from lucy_ng.readers import BrukerReader
    - CASE1_C13 = Path(__file__).parent.parent / "data" / "Ibuprofen" / "2"  (no skip — in-repo)
    - CASE9_C13 = Path("/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/12")
    - Three classes: TestSNRThreshold, TestCASE9Regression (skipif guard), TestCASE1Regression
    - Stubs MUST import _compute_snr_threshold (which does not yet exist) so they fail with ImportError on first run (RED state)
    - Add two CLI JSON methods to a separate class TestCLIPick1D that uses click.testing.CliRunner with the pick CLI

    Create tests/test_intensity_symmetry.py following the PATTERNS.md template exactly:
    - Imports: pytest, numpy, from lucy_ng.models import Peak1D, from lucy_ng.processing.peak_picker import detect_intensity_symmetry
    - Fixtures: case9_like_aromatic_peaks (two 2C aromatic CH at 129.94/125.31 intensities 1.81e7/1.72e7; two 1C Cq at 150.80/130.16 intensities 3.1e6/2.9e6; non-aromatic CH3 at 22.10 intensity 1.70e7), aromatic_ch_ppms_case9 = [129.94, 125.31]
    - Class TestDetectIntensitySymmetry with 5 test methods
    - Stub imports detect_intensity_symmetry (which does not yet exist) → ImportError on first run (RED state)

    After creating both files, verify RED state: pytest should fail with ImportError on both new test files, while all existing 1023 tests continue to pass.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest tests/test_peak_picker_snr.py tests/test_intensity_symmetry.py -q 2>&1 | head -20</automated>
    Expected: ImportError or collection errors on the new files (RED state confirmed).

    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest --ignore=tests/test_peak_picker_snr.py --ignore=tests/test_intensity_symmetry.py -q 2>&1 | tail -5</automated>
    Expected: original suite still passes (1023 passed or equivalent count).
  </verify>
  <done>
    Both test files exist; stub imports cause collection failures (RED state); existing suite unaffected.
    Commit message: "test(79-00): add failing test stubs for FIX-04 and FIX-05 (RED state)"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read | REQUIREMENTS.md and test files are local project files; no network, no auth, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-00-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan; Wave 0 is documentation + test stubs only |

**Note:** This plan has no realistic security threat surface. It edits a local planning document and creates test stub files. No network access, no authentication, no external input, no DB writes.
</threat_model>

<verification>
Full phase gate check after Wave 0:
```bash
cd /Users/steinbeck/Dropbox/develop/lucy-ng
grep -c "FIX-04\|FIX-05\|FIX-06" .planning/REQUIREMENTS.md   # expect >= 9
pytest --ignore=tests/test_peak_picker_snr.py --ignore=tests/test_intensity_symmetry.py -q 2>&1 | tail -3
```
</verification>

<success_criteria>
- REQUIREMENTS.md traceability table has FIX-04, FIX-05, FIX-06 each mapped to Phase 79
- tests/test_peak_picker_snr.py exists with 7+ failing stubs (ImportError on _compute_snr_threshold)
- tests/test_intensity_symmetry.py exists with 5+ failing stubs (ImportError on detect_intensity_symmetry)
- All 1023 pre-existing tests still pass
</success_criteria>

<output>
Create `.planning/phases/79-peak-picking-symmetry-fix/79-00-SUMMARY.md` when done.
</output>
