---
phase: 79-peak-picking-symmetry-fix
plan: "01"
type: execute
wave: 1
depends_on: ["79-00"]
files_modified:
  - src/lucy_ng/processing/peak_picker.py
  - src/lucy_ng/models/peaks.py
  - src/lucy_ng/models/peak_list.py
  - src/lucy_ng/cli/pick.py
  - tests/test_peak_picker_snr.py
autonomous: true
requirements: [FIX-04]

must_haves:
  truths:
    - "lucy pick 1d on CASE9/12 lists the ester carbonyl at ~166 ppm (SNR≈17)"
    - "lucy pick 1d on CASE1/2 (ibuprofen) still yields >= 9 peaks (unchanged)"
    - "Every peak in JSON output has a non-null positive snr field"
    - "JSON output has a top-level noise_sigma field"
    - "The old threshold parameter still works when use_snr=False (backwards compat)"
    - "All 1023 pre-existing tests continue to pass"
  artifacts:
    - path: "src/lucy_ng/processing/peak_picker.py"
      provides: "SNR/MAD threshold + solvent exclusion + detect_intensity_symmetry export"
      contains: "1.4826"
    - path: "src/lucy_ng/models/peaks.py"
      provides: "Peak1D.snr optional field"
      contains: "snr: float | None = None"
    - path: "src/lucy_ng/cli/pick.py"
      provides: "noise_sigma and snr in JSON output"
      contains: "noise_sigma"
    - path: "tests/test_peak_picker_snr.py"
      provides: "All stubs now GREEN"
      contains: "test_case9_carbonyl_picked"
  key_links:
    - from: "src/lucy_ng/cli/pick.py"
      to: "src/lucy_ng/processing/peak_picker.py"
      via: "AdaptivePeakPicker.pick_peaks_instance() captures sigma_mad via PeakList1D.noise_sigma"
      pattern: "noise_sigma"
    - from: "src/lucy_ng/processing/peak_picker.py"
      to: "src/lucy_ng/models/peaks.py"
      via: "Peak1D(position=..., intensity=..., snr=snr)"
      pattern: "snr=snr"
---

<objective>
Replace the max-relative threshold in AdaptivePeakPicker with a MAD/SNR-based absolute threshold (FIX-04). Add solvent-region exclusion before computing noise. Annotate every picked peak with its SNR. Surface noise_sigma and per-peak snr in the CLI JSON contract.

Purpose: The CASE9 ester carbonyl at 166.08 ppm (SNR≈17) is currently masked by the CDCl₃-dominated `0.05 × max` threshold (= 2.30×10⁶ > carbonyl 2.08×10⁶). The MAD-based noise estimate (σ_MAD = 1.226×10⁵) gives SNR = 17, far above k=3.
Output: Modified peak_picker.py, peaks.py, PeakList1D model update, pick.py; all FIX-04 stubs in tests/test_peak_picker_snr.py turn GREEN.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/PROJECT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/ROADMAP.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-CONTEXT.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-RESEARCH.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-PATTERNS.md
@/Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/79-peak-picking-symmetry-fix/79-00-SUMMARY.md

<interfaces>
<!-- Current signatures — read these before modifying. -->

From src/lucy_ng/processing/peak_picker.py (lines 37–125):
  class AdaptivePeakPicker:
      @staticmethod
      def pick_peaks(spectrum: Spectrum1D, threshold: float = 0.05, detect_negative: bool = False) -> PeakList1D
      def pick_peaks_instance(self, spectrum: Spectrum1D, threshold: float = 0.05, detect_negative: bool = False) -> PeakList1D
      # Lines 87-89: max_intensity = np.max(np.abs(data)); abs_threshold = threshold * max_intensity  ← REPLACE

From src/lucy_ng/models/peaks.py (lines 8–14):
  class Peak1D(BaseModel):
      position: float
      intensity: float
      assignment: str | None = None
      multiplicity: str | None = None
      # ADD: snr: float | None = None

  class PeakList1D(BaseModel):
      peaks: list[Peak1D]
      nucleus: str
      spectrum_id: str | None = None
      # ADD: noise_sigma: float | None = None

From src/lucy_ng/cli/pick.py (lines 62–74):
  # Current JSON block — lacks snr and noise_sigma fields
  data = {"count": len(peaks.peaks), "negative_detected": has_significant_negative, "peaks": [{"ppm": p.position, "intensity": p.intensity}]}

From src/lucy_ng/readers/bruker.py (confirmed by RESEARCH.md):
  # BrukerReader.read_1d() already reads SOLVENT from acqus and stores it in Spectrum1D.solvent
  # Spectrum1D.solvent: str | None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SNR/MAD threshold, solvent exclusion, and SNR annotation to peak_picker.py + models</name>
  <files>
    src/lucy_ng/processing/peak_picker.py,
    src/lucy_ng/models/peaks.py
  </files>
  <read_first>
    src/lucy_ng/processing/peak_picker.py — full file; locate lines 87–89 (threshold computation), lines 37–63 (static pick_peaks signature), lines 65–125 (pick_peaks_instance body)
    src/lucy_ng/models/peaks.py — lines 8–26 (Peak1D model); lines 43–75 (PeakList1D model)
    .planning/phases/79-peak-picking-symmetry-fix/79-PATTERNS.md — sections "peak_picker.py — MAD threshold" and "peaks.py — add snr field" for the exact code patterns (IUPAC k=3, solvent table, _compute_snr_threshold signature)
  </read_first>
  <behavior>
    Test assertions (from test_peak_picker_snr.py stubs):
    - _compute_snr_threshold(noise_array, ppm, solvent="CDCl3") → sigma_mad < 5e5; threshold < 1e6 on synthetic CDCl3-dominated data
    - AdaptivePeakPicker.pick_peaks(case9_spectrum) includes a peak with 164.0 <= ppm <= 168.0
    - All picked peaks have p.snr is not None and p.snr > 0
    - AdaptivePeakPicker.pick_peaks(case1_spectrum) yields >= 9 peaks
  </behavior>
  <action>
    In src/lucy_ng/models/peaks.py:
    - Add `snr: float | None = None` to Peak1D after the multiplicity field (line 14), following the existing optional-field pattern; no validator needed
    - Add `noise_sigma: float | None = None` to PeakList1D after the spectrum_id field (line 48), same pattern

    In src/lucy_ng/processing/peak_picker.py:
    1. Add module-level dict _SOLVENT_EXCLUSION_13C before the AdaptivePeakPicker class. Keys: "CDCl3" (72.0, 82.0), "DMSO" (37.0, 42.0), "DMSO-d6" (37.0, 42.0), "CD3OD" (46.0, 52.0), "MeOD" (46.0, 52.0), "CD3CN" (1.0, 5.0), "acetone" (27.0, 33.0), "C6D6" (125.0, 131.0).
    2. Add module-level function _compute_snr_threshold(data: np.ndarray, ppm_scale: np.ndarray, solvent: str | None = None, k: float = 3.0) -> tuple[float, float] that:
       - Looks up _SOLVENT_EXCLUSION_13C.get(solvent or "", None); if found builds mask = (ppm_scale >= lo) & (ppm_scale <= hi); clean_data = data[~mask]; else clean_data = data
       - Computes mad = float(np.median(np.abs(clean_data - np.median(clean_data))))
       - sigma_mad = 1.4826 * mad
       - Returns (k * sigma_mad, sigma_mad)
    3. Update pick_peaks static method signature to add snr_floor: float = 3.0 and use_snr: bool = True parameters, forwarding them to pick_peaks_instance.
    4. Update pick_peaks_instance signature to add snr_floor: float = 3.0 and use_snr: bool = True. In the body, replace lines 87–89 (max_intensity / abs_threshold computation) with:
       - If use_snr: call abs_threshold, sigma_mad = _compute_snr_threshold(data, ppm_scale, solvent=spectrum.solvent, k=snr_floor)
       - Else: abs_threshold = threshold * np.max(np.abs(data)); sigma_mad = None
    5. After finding peak_indices with find_peaks, update the loop to compute snr = float(abs(data[idx]) / sigma_mad) if sigma_mad is not None else None and pass snr=snr to each Peak1D constructor.
    6. Same SNR annotation for the detect_negative branch (abs value, so SNR is always positive even for negative DEPT peaks per RESEARCH.md Pitfall 3).
    7. Set return value: create PeakList1D(peaks=peaks, nucleus=spectrum.nucleus, noise_sigma=sigma_mad if sigma_mad is not None else None).

    Backwards-compatibility guarantee: callers that pass an explicit threshold and use_snr=False get the old behavior unchanged. Callers that pass threshold=<value> with use_snr=True (default) use the new threshold; the threshold param is silently ignored in SNR mode. No existing test asserts that the old threshold number is used.

    Also update estimate_line_widths (lines 127–) to keep its own threshold calculation unchanged — do NOT refactor it to use _compute_snr_threshold (it is used only for line-width estimation, not peak picking).
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest tests/test_peak_picker_snr.py -x -q 2>&1 | tail -10</automated>
    Expected: all test_peak_picker_snr.py tests pass (except CASE9 tests if data absent — those are marked skipif).

    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && grep -n "1.4826" src/lucy_ng/processing/peak_picker.py</automated>
    Expected: one line containing "1.4826" (the sigma_mad computation).

    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && grep -n "snr: float" src/lucy_ng/models/peaks.py</automated>
    Expected: one match.
  </verify>
  <done>
    _compute_snr_threshold is importable; Peak1D.snr and PeakList1D.noise_sigma exist; pick_peaks defaults to use_snr=True; CASE9 carbonyl (if data present) is in the peak list; CASE1 still yields >= 9 peaks; per-peak SNR is non-null for all peaks in SNR mode.
    Commit: "feat(79-01): replace max-relative threshold with MAD/SNR absolute (FIX-04)"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface noise_sigma and per-peak snr in pick.py CLI JSON contract</name>
  <files>
    src/lucy_ng/cli/pick.py,
    tests/test_peak_picker_snr.py
  </files>
  <read_first>
    src/lucy_ng/cli/pick.py — lines 37–84 (pick_1d function, threshold handling, JSON block at 62–74)
    .planning/phases/79-peak-picking-symmetry-fix/79-PATTERNS.md — section "pick.py — surface noise_sigma and per-peak snr" for the exact JSON dict change and has_significant_negative note
    tests/test_peak_picker_snr.py — TestCLIPick1D methods test_pick_1d_json_has_snr and test_pick_1d_json_has_noise_sigma
  </read_first>
  <behavior>
    Test assertions (from test_peak_picker_snr.py stubs):
    - CLI runner invokes `pick 1d data/Ibuprofen/2 --format json` → exit_code 0
    - data["peaks"][0]["snr"] is not None and > 0
    - data["noise_sigma"] is not None and > 0
  </behavior>
  <action>
    In src/lucy_ng/cli/pick.py pick_1d function:
    1. The current call to AdaptivePeakPicker.pick_peaks returns a PeakList1D. After Task 1, that PeakList1D now carries noise_sigma. No change to the call site is needed (use_snr=True is the new default). If threshold is explicitly passed by the user, the CLI should pass use_snr=False to preserve legacy behavior for callers who explicitly set a threshold — add `use_snr = threshold is None` and pass it: `AdaptivePeakPicker.pick_peaks(spectrum, threshold=effective_threshold, detect_negative=has_significant_negative, use_snr=use_snr)`.
    2. In the JSON output block (lines 62–74), update the data dict to:
       - Add top-level key "noise_sigma": peaks.noise_sigma (will be None if threshold was explicit, which JSON serializes as null — acceptable)
       - Add per-peak key "snr": p.snr in the peaks list comprehension
    3. The `has_significant_negative` gate on lines 49–51 uses effective_threshold * max_abs; this is independent of the new SNR threshold and should remain unchanged per PATTERNS.md note.

    The CLI text output (non-JSON) does NOT need to change — it already shows ppm and intensity.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest tests/test_peak_picker_snr.py::TestCLIPick1D -x -q 2>&1 | tail -5</automated>
    Expected: 2 passed.

    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest tests/test_cli_pick.py -x -q 2>&1 | tail -5</automated>
    Expected: all existing tests still pass (no regression).

    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest -q 2>&1 | tail -5</automated>
    Expected: full suite passes (1023 + new tests).
  </verify>
  <done>
    `lucy pick 1d data/Ibuprofen/2 --format json` returns JSON with noise_sigma (non-null) and snr per peak (non-null, positive); all FIX-04 stubs green; full suite green.
    Commit: "feat(79-01): add noise_sigma and per-peak snr to pick 1d JSON contract (FIX-04)"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read | Bruker NMR binary files read locally via nmrglue; no network access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01-01 | Tampering | peak_picker.py numpy array ops | accept | Input is a local Bruker numpy array; no external/untrusted data; array shape validation already in BrukerReader |
| T-79-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; numpy/scipy already in project dependencies |

**Note:** This plan processes local Bruker NMR files (numerical numpy arrays) and modifies local Python source. No network, no auth, no user-supplied string input beyond file paths. The attack surface is minimal; no high-severity threats apply.
</threat_model>

<verification>
Full FIX-04 phase-gate check:
```bash
cd /Users/steinbeck/Dropbox/develop/lucy-ng
pytest tests/test_peak_picker_snr.py tests/test_cli_pick.py -x -q
pytest -q 2>&1 | tail -5
grep -n "1.4826" src/lucy_ng/processing/peak_picker.py
grep -n "noise_sigma" src/lucy_ng/cli/pick.py
grep -n "snr: float" src/lucy_ng/models/peaks.py
```
</verification>

<success_criteria>
- src/lucy_ng/processing/peak_picker.py contains `1.4826` (sigma_mad computation)
- src/lucy_ng/models/peaks.py contains `snr: float | None = None` in Peak1D
- src/lucy_ng/models/peaks.py contains `noise_sigma: float | None = None` in PeakList1D
- src/lucy_ng/cli/pick.py contains `noise_sigma` in JSON output
- All test_peak_picker_snr.py tests pass (CASE9 tests skipped only if data absent)
- All pre-existing tests pass (no regressions)
- When CASE9 data is present: `lucy pick 1d .../CASE9/12 --format json | python -c "import json,sys; peaks=json.load(sys.stdin)['peaks']; assert any(164<=p['ppm']<=168 for p in peaks)"` exits 0
</success_criteria>

<output>
Create `.planning/phases/79-peak-picking-symmetry-fix/79-01-SUMMARY.md` when done.
</output>
