---
phase: 21-skill-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/SKILL.md
autonomous: true

must_haves:
  truths:
    - "skill/SKILL.md contains the full CASE workflow from dereplication through ranking"
    - "Every NMR domain knowledge paragraph from CLAUDE.md has a canonical home in SKILL.md"
    - "No rule is stated more than once within SKILL.md (sp2 even count, ELIM usage, correlation order, score thresholds, MAE quality)"
    - "An AI agent reading only SKILL.md has enough domain knowledge to perform structure elucidation"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Canonical CASE domain knowledge document"
      contains: "NMR Background, Peak Picking Strategy, Symmetry Detection, Dereplication, LSD Reference, Ranking and Prediction, CASE Workflow, Quick Reference"
  key_links:
    - from: "skill/SKILL.md"
      to: "AUDIT-REPORT.md Section 5.3"
      via: "section outline derived from audit"
      pattern: "NMR Background|Peak Picking Strategy|Symmetry Detection|Dereplication|LSD Reference|Ranking|CASE Workflow|Quick Reference"
---

<objective>
Write the new canonical skill/SKILL.md (~500 lines) containing all CASE domain knowledge, deduplicated and organized per the AUDIT-REPORT.md Section 5.3 outline.

Purpose: This is the foundation for Phase 21. SKILL.md becomes the single source of truth for all NMR/CASE domain knowledge that the AI agent needs during structure elucidation. All other documents (CLAUDE.md, subskills) will reference or defer to this document.

Output: A complete skill/SKILL.md file (~450-550 lines) with YAML frontmatter, 8-9 sections of domain knowledge, zero internal duplication.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-system-audit/AUDIT-REPORT.md
@.planning/phases/21-skill-restructure/21-RESEARCH.md

Source files to extract content from:
@CLAUDE.md
@skill/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write new skill/SKILL.md with deduplicated CASE domain knowledge</name>
  <files>skill/SKILL.md</files>
  <action>
Replace the current skill/SKILL.md (which is a ~1,042 line mirror of CLAUDE.md) with a new ~500-line document following the AUDIT-REPORT.md Section 5.3 outline.

**YAML Frontmatter:** Keep the existing frontmatter (name: lucy-ng, description, tools) -- it is correct.

**Section structure (follow AUDIT-REPORT.md Section 5.3 exactly):**

1. **NMR Background** (~87 lines)
   - Experiment types table (1H, 13C, DEPT-135, DEPT-90, HSQC, HMBC, COSY) -- what each provides
   - 13C chemical shift regions table (0-50, 50-90, 90-120, 120-160, 160-180, 180-220 ppm)
   - Common pitfalls (consolidated from current Pitfalls 1-5):
     - Pitfall 1 (symmetry): signal count != atom count, HSQC intensity check, common motifs
     - Pitfall 2 (quaternary C): invisible in DEPT/HSQC, only HMBC connects them
     - Pitfall 3 (HMBC noise): always use guided picking, validates against 13C and HSQC
     - Pitfall 4 (too many LSD solutions): insufficient constraints, check ELIM usage
     - Pitfall 5 (heteroatoms): infer from formula + shifts, use BOND or LIST/PROP
   - IMPORTANT: Each pitfall should be 8-12 lines max (currently ~15-20 each). Compress by removing repetition.

2. **Peak Picking Strategy** (~85 lines)
   - Scientific rationale for guided picking (~15 lines): raw 2D has noise, use 1D as ground truth
   - 1D adaptive picker: threshold 0.05, FWHM factor 1.5, when to override (~10 lines)
   - HSQC DEPT-guided strategy (~25 lines): iterative threshold (0.10->0.005), DEPT sign-based multiplicity (positive=CH/CH3, negative=CH2), DEPT-90 for CH vs CH3. Include the Python API call pattern: `DEPTGuidedPicker.pick_hsqc_peaks(hsqc, dept135)` and `DEPTGuidedPicker.pick_hsqc_peaks_with_dept90(hsqc, dept135, dept90)`
   - HMBC cross-validation (~25 lines): tolerances (13C 1.5 ppm, 1H 0.1 ppm), noise model, when to adjust. Include `HMBCGuidedPicker.pick_hmbc_peaks_from_spectra(hmbc, carbon_spectrum, hsqc, dept135)` call pattern
   - APT as DEPT alternative (~10 lines): positive=CH/CH3, negative=CH2/qC

3. **Symmetry Detection** (~51 lines)
   - Expected vs observed signal count
   - Intensity-based equivalence (>=1.5x threshold)
   - Shift-based multiplicity guessing (<30 ppm = likely CH3, >100 ppm = aromatic CH)
   - Common symmetric motifs (para-benzene, isopropyl, gem-dimethyl)
   - Handling symmetry decision tree (from CLAUDE.md section 12.2)

4. **Dereplication** (~33 lines)
   - When to use (always first, before full CASE)
   - CLI syntax: `lucy dereplicate c13 <path> <formula>` and `--shifts` variant
   - Region-specific tolerances and scoring explanation
   - Score interpretation: >0.85 strong, 0.65-0.85 possible, <0.50 no match (ONE canonical table, not three)

5. **LSD Reference** (~133 lines) -- MERGE current "LSD Integration" + "Manual LSD File Construction" + "LSD Troubleshooting" into ONE section
   - Command format: MULT, HSQC, HMBC, BOND, LIST/PROP/ELEM (~30 lines)
   - Correlation order rule: HSQC before HMBC -- state ONCE with example (~8 lines)
   - Hybridization rules: sp2 even count, atom classification table -- state ONCE (~20 lines)
   - Heteroatom attachment: BOND vs LIST/PROP decision logic with when-to-use table (~25 lines)
   - ELIM: single canonical statement -- "only after exhausting diagnostics" (~8 lines)
   - Solution count interpretation (0, 1, 2-10, 10-100, >100) (~20 lines)
   - Manual file construction checklist (~10 lines)
   - Troubleshooting table (~12 lines)
   - Converting solutions to SMILES: `outlsd 5 < compound.sol > solutions.smi` (~5 lines)

6. **Ranking and Prediction** (~43 lines)
   - HOSE prediction: radius fallback (6->1), confidence meaning
   - N:1 symmetry matching explanation
   - MAE quality thresholds table: <2.0 excellent, 2-3.5 good, 3.5-5 moderate, >5 poor -- ONE table
   - Ranking output format and interpretation
   - Why correct structures may not rank #1

7. **CASE Workflow** (~60 lines)
   - Step-by-step: dereplication -> symmetry -> picking -> LSD -> ranking
   - Stopping criteria: 1-10 solutions is the target
   - When to proceed vs request more data (decision tree from CLAUDE.md 12.1)
   - Result reporting templates (dereplication match, possible match, no match; LSD results)

8. **Quick Reference** (~19 lines)
   - Key tolerances (13C matching, HSQC validation, HMBC validation, dereplication, MAE)
   - Red flags (fewer signals, more signals, zero solutions, thousands of solutions)
   - When to ask for help

**CRITICAL DEDUPLICATION RULES:**
- sp2 even count: stated ONCE in section 5 (LSD Reference > Hybridization rules)
- ELIM usage: stated ONCE in section 5 (LSD Reference > ELIM)
- Correlation order (HSQC before HMBC): stated ONCE in section 5 (LSD Reference > Correlation order)
- Score thresholds (0.85/0.65/0.50): stated ONCE in section 4 (Dereplication > Score interpretation)
- MAE thresholds (<2.0/2-3.5/3.5-5/>5): stated ONCE in section 6 (Ranking > MAE quality)
- Database stats (928K/7.9M): do NOT include in SKILL.md at all (stays in CLAUDE.md setup section)
- Pitfall solutions that reference LSD rules should say "See LSD Reference section" rather than restating the rule

**DO NOT include in SKILL.md:**
- End-User Setup (stays in CLAUDE.md)
- Developer Reference (stays in CLAUDE.md)
- Tool Output Reference table (stays in CLAUDE.md)
- Blind CASE Protocol (stays in skill/sanitize/SKILL.md or separate blind-case skill)
- Available Subskills / Workflow Selection (moves to SUPERVISOR.md in Plan 02)
- Database statistics (stays in CLAUDE.md)
- Python project structure (stays in CLAUDE.md)

**Tone:** Direct, instructional. Written for an AI agent performing CASE. Not a tutorial -- a reference manual the AI consults while working.
  </action>
  <verify>
1. `wc -l skill/SKILL.md` shows 450-600 lines (target ~500)
2. Verify frontmatter: `head -5 skill/SKILL.md` shows YAML with name: lucy-ng
3. Verify 8 major sections exist: `grep -c "^## " skill/SKILL.md` returns 8
4. Verify deduplication -- each rule appears once:
   - `grep -c "sp2.*even\|even.*sp2" skill/SKILL.md` returns 1
   - `grep -c "ELIM" skill/SKILL.md` returns <= 3 (one canonical statement, possibly a couple references)
   - `grep -c "0\.85" skill/SKILL.md` returns 1
   - `grep -c "MAE.*2\.0\|2\.0.*MAE" skill/SKILL.md` returns 1
5. Verify no setup content: `grep -c "pip install\|database download" skill/SKILL.md` returns 0
6. Verify key sections present: grep for "NMR Background", "Peak Picking", "Symmetry", "Dereplication", "LSD Reference", "Ranking", "CASE Workflow", "Quick Reference"
  </verify>
  <done>
skill/SKILL.md exists at ~500 lines with 8 sections of deduplicated CASE domain knowledge. No rule stated more than once. No project-level content (setup, dev reference) present. An AI agent reading this document has complete CASE knowledge.
  </done>
</task>

</tasks>

<verification>
- skill/SKILL.md is 450-600 lines
- Contains 8 major sections matching AUDIT-REPORT.md Section 5.3 outline
- Zero internal duplication of key rules (sp2, ELIM, correlation order, scores, MAE)
- No project-level content (setup, developer reference, database stats)
- YAML frontmatter present and valid
</verification>

<success_criteria>
- skill/SKILL.md exists with ~500 lines of deduplicated CASE domain knowledge
- All domain knowledge from CLAUDE.md has a canonical home in the new SKILL.md
- No rule stated more than once within the document
- Document is self-contained for AI-driven CASE workflow
</success_criteria>

<output>
After completion, create `.planning/phases/21-skill-restructure/21-01-SUMMARY.md`
</output>
