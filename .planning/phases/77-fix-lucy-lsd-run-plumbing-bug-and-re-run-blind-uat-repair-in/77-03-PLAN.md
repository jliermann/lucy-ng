---
phase: 77
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/lucy-case-agent.md
  - ~/.claude/agents/lucy-lsd-engineer.md
  - ~/.claude/commands/lucy-ng/case.md
  - ~/.claude/agents/lucy-devils-advocate.md
autonomous: true
requirements:
  - FIX-03
must_haves:
  truths:
    - "lucy-case-agent.md frontmatter name field is neutralized so Claude Code will not spawn it"
    - "lucy-lsd-engineer.md lines 124-130 no longer claim Phase 73 fix is complete/working"
    - "lucy-lsd-engineer.md references lucy detect aromatic-cosy CLI command"
    - "lucy-lsd-engineer.md D-04 escalation section specifies N=3 as ring-BOND escalation threshold with CASE-PROGRESS logging requirement"
    - "case.md carries D-77-06 UAT criteria: emergent=pass, documented-BOND=conditional, silent-BOND or SKEL=fail"
    - "No active workflow file under ~/.claude/commands/lucy-ng/ or ~/.claude/agents/lucy-*.md (except case-agent itself) spawns or references lucy-case-agent"
  artifacts:
    - path: "~/.claude/agents/lucy-case-agent.md"
      provides: "Neutralized deprecated agent"
      contains: "DEPRECATED-lucy-case-agent"
    - path: "~/.claude/agents/lucy-lsd-engineer.md"
      provides: "Corrected Phase-73 claim + aromatic-cosy reference + N=3 escalation"
  key_links:
    - from: "case.md skill"
      to: "D-77-06 criteria"
      via: "Phase 78 handoff note in case.md"
      pattern: "emergent ring = clean pass"
    - from: "lucy-devils-advocate.md G-gate"
      to: "lucy detect aromatic-cosy"
      via: "COSY equivalence-pair check updated to reference tool output"
      pattern: "aromatic-cosy"
---

<objective>
Perform FIX-03 skill hygiene: retire the deprecated `lucy-case-agent.md` by neutralizing its frontmatter, correct the false Phase-73 claim in `lucy-lsd-engineer.md`, add the `lucy detect aromatic-cosy` CLI reference to the lsd-engineer skill, update the D-04 escalation wording to match D-77-01/D-77-06 (threshold N=3, CASE-PROGRESS logging), and add D-77-06 UAT criteria to case.md for Phase 78 handoff. Run a targeted audit of the 4 active skill files.

This plan runs in Wave 1 (parallel with Plan 01) because it only touches Markdown skill files — no Python code dependency.

Purpose: Eliminate the stale/contradictory agent that could be accidentally spawned; correct documentation that currently gives agents false information about the tooling state; encode the emergent-ring vs ring-BOND escalation rule so Phase 78 inherits it correctly.

Output: Neutralized lucy-case-agent.md + corrected/updated lucy-lsd-engineer.md, case.md, lucy-devils-advocate.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-RESEARCH.md
@.planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-PATTERNS.md

<interfaces>
<!-- Key locations in skill files. Extracted from research. -->

~/.claude/agents/lucy-case-agent.md — current frontmatter:
  name: lucy-case-agent
  (body line 12: "> DEPRECATED -- DO NOT USE" blockquote already present)
  (body contains stale: outlsd 5 < compound.sol > solutions.smi manual invocation)
  Target change: name: DEPRECATED-lucy-case-agent

~/.claude/agents/lucy-lsd-engineer.md — key locations:
  Lines 124-130: false "Phase 73 fix works" claim — replace with correction note
  Near line 583: D-04 escalation section — update with N=3 threshold + CASE-PROGRESS logging + SKEL forbidden
  Add after the escalation section or the COSY section: reference to `lucy detect aromatic-cosy` CLI

~/.claude/commands/lucy-ng/case.md — key changes:
  Add D-77-06 UAT criterion section (Phase 78 handoff note):
    "emergent ring = clean pass; ring-BONDs only as CASE-PROGRESS-documented escalation after N non-aromatic iterations = conditional pass; silent ring-BONDs or any SKEL = fail"
  Verify emergent-ring guidance (COSY 4 7 + COSY 5 6 cross-ring) is visible and not buried

~/.claude/agents/lucy-devils-advocate.md — targeted check:
  Verify G5-G8 gates (added Phase 75) still cover FIX-01/02 failure modes
  Update the COSY equivalence-pair check (whichever G-gate detects within-group COSY) to reference
  `lucy detect aromatic-cosy` as the authoritative source of cross-ring pairs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire lucy-case-agent.md (neutralize frontmatter)</name>
  <files>~/.claude/agents/lucy-case-agent.md</files>
  <read_first>
    ~/.claude/agents/lucy-case-agent.md (full file — confirm frontmatter, blockquote header location, stale outlsd workflow presence)
    .planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-PATTERNS.md (section "lucy-case-agent.md retirement")
    .planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-RESEARCH.md (section "FIX-03 Skill Hygiene Scope")
  </read_first>
  <action>
    1. Read the full file to confirm: (a) frontmatter has `name: lucy-case-agent`; (b) `> DEPRECATED -- DO NOT USE` blockquote exists in the body; (c) stale `outlsd 5 < compound.sol > solutions.smi` workflow is present somewhere in the body.

    2. Change only the `name:` line in the frontmatter:
       FROM:  name: lucy-case-agent
       TO:    name: DEPRECATED-lucy-case-agent
       No other changes — do not remove the blockquote, do not rewrite the body, do not delete the file. Historical record must be preserved.

    3. Verify no active workflow spawns this agent:
       `grep -r "lucy-case-agent" ~/.claude/commands/lucy-ng/ ~/.claude/agents/lucy-nmr-chemist.md ~/.claude/agents/lucy-lsd-engineer.md ~/.claude/agents/lucy-devils-advocate.md ~/.claude/agents/lucy-solution-analyst.md 2>/dev/null`
       Expected: no matches (or only self-referential matches in lucy-case-agent.md itself).
       If matches found in active skill files: note them in the SUMMARY — do not automatically remove them (requires judgment).
  </action>
  <verify>
    <automated>grep -c "name: DEPRECATED-lucy-case-agent" ~/.claude/agents/lucy-case-agent.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "name: DEPRECATED-lucy-case-agent" ~/.claude/agents/lucy-case-agent.md` returns 1
    - `grep -c "name: lucy-case-agent$" ~/.claude/agents/lucy-case-agent.md` returns 0 (old name gone; the DEPRECATED- prefix version is fine to also match but the bare name must not exist)
    - `grep "DEPRECATED -- DO NOT USE" ~/.claude/agents/lucy-case-agent.md` still returns the blockquote line (blockquote not removed)
  </acceptance_criteria>
  <done>lucy-case-agent.md frontmatter name changed to DEPRECATED-lucy-case-agent. Active skill files have no references spawning it.</done>
</task>

<task type="auto">
  <name>Task 2: Correct lucy-lsd-engineer.md (Phase-73 claim + aromatic-cosy reference + D-04 escalation wording)</name>
  <files>~/.claude/agents/lucy-lsd-engineer.md</files>
  <read_first>
    ~/.claude/agents/lucy-lsd-engineer.md (full file — identify exact location of lines 124-130, the D-04 escalation section near line 583, and the COSY native-constraint section)
    .planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-PATTERNS.md (section "lucy-lsd-engineer.md targeted edit")
    .planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-RESEARCH.md (section "FIX-03 Skill Hygiene Scope" table row for lucy-lsd-engineer.md)
    .planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-CONTEXT.md (D-77-01 escalation rule, D-77-06 UAT criterion)
  </read_first>
  <action>
    Read the full file first to locate exact line numbers (they may differ from the ~124 and ~583 estimates given in RESEARCH.md — find the actual content).

    Change 1 — False Phase-73 claim (look for text around "Phase 73" and claims that lucy lsd run is fixed or working):
      Replace the false "Phase 73 fix works" claim with:
      "Phase 73 unified the _invoke_outlsd helper but did not fix the filter-file deployment gap in _execute_lsd. Phase 77 (FIX-01) corrects this: _execute_lsd now copies ring3/ring4 filter files to output_dir unconditionally (matching write_file() behavior), and _invoke_outlsd now raises RuntimeError on known-bad output instead of writing garbage to solutions.smi."
      Keep surrounding context intact; replace only the false claim sentences.

    Change 2 — Add lucy detect aromatic-cosy CLI reference:
      Find the section discussing COSY aromatic equivalence or D-04 (cross-ring COSY emission). After the existing COSY 4 7 / COSY 5 6 example or near the D-04 escalation rule, add:
      "Use `lucy detect aromatic-cosy \"<shifts>\" --multiplicities \"<mults>\"` to derive cross-ring COSY pairs deterministically. This outputs one `COSY a b` line per pair using the reversed-sort algorithm. Example: `lucy detect aromatic-cosy \"129.38,129.38,127.26,127.26\" --multiplicities \"CH,CH,CH,CH\"` → `COSY 4 7` + `COSY 5 6`. Do NOT hand-derive atom indices — use this tool to prevent the within-group COSY error (LSD error 283)."

    Change 3 — D-04 escalation wording (find the section about ring-BOND escalation or aromatic ring forcing):
      Update/replace the escalation threshold text to:
      "Ring-BOND forcing (explicit BOND constraints defining the aromatic ring) is allowed ONLY as a documented escalation after **3 consecutive non-aromatic iterations**. Escalation requirements: (1) log the decision in CASE-PROGRESS.md with the iteration number; (2) note that emergent ring was attempted with cross-ring COSY pairs and did not produce aromatic solutions. SKEL benzene fragment forcing (`SKEL \"c1ccccc1\"`) is forbidden in all cases — ring-BOND escalation is the only legitimate forced-ring path."

    Do NOT make changes beyond these three targeted edits. Do not rewrite sections, reorder content, or modify the skill's overall structure.
  </action>
  <verify>
    <automated>grep -c "aromatic-cosy" ~/.claude/agents/lucy-lsd-engineer.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aromatic-cosy" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 1 (reference added)
    - `grep -c "3 consecutive non-aromatic" ~/.claude/agents/lucy-lsd-engineer.md` returns 1 (N=3 threshold present)
    - `grep -c "Phase 73 fix works\|lucy lsd run.*fixed\|Phase 73.*complete" ~/.claude/agents/lucy-lsd-engineer.md` returns 0 (false claim removed — adjust the grep pattern to match the actual false claim text found in the file)
    - `grep -c "FIX-01\|Phase 77" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 1 (correction note references Phase 77)
    - `grep -c "SKEL.*forbidden\|forbidden.*SKEL" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 1
  </acceptance_criteria>
  <done>
    lucy-lsd-engineer.md has: false Phase-73 claim replaced with FIX-01 correction; lucy detect aromatic-cosy CLI reference added; D-04 escalation updated with N=3 threshold + CASE-PROGRESS logging + SKEL-forbidden clause.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 and Task 2 complete:
    - lucy-case-agent.md frontmatter neutralized (name: DEPRECATED-lucy-case-agent)
    - lucy-lsd-engineer.md: false Phase-73 claim corrected, lucy detect aromatic-cosy added, D-04 escalation wording updated (N=3, CASE-PROGRESS logging, SKEL forbidden)
    - Quick automated checks passed (grep gates)
  </what-built>
  <how-to-verify>
    1. Open ~/.claude/agents/lucy-case-agent.md — confirm frontmatter `name:` is `DEPRECATED-lucy-case-agent`, blockquote `> DEPRECATED -- DO NOT USE` is still present.

    2. Open ~/.claude/agents/lucy-lsd-engineer.md — confirm:
       a. The old false Phase-73 claim is gone and replaced with a correction mentioning Phase 77 / FIX-01.
       b. `lucy detect aromatic-cosy` CLI reference is present with example syntax.
       c. Ring-BOND escalation section mentions "3 consecutive non-aromatic iterations" and CASE-PROGRESS logging requirement.
       d. "SKEL" benzene forcing is explicitly marked as forbidden.

    3. Proceed to Task 4 for case.md + devils-advocate updates.
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Task 4, or describe issues to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update case.md (D-77-06 criteria) + devils-advocate (aromatic-cosy gate reference)</name>
  <files>~/.claude/commands/lucy-ng/case.md, ~/.claude/agents/lucy-devils-advocate.md</files>
  <read_first>
    ~/.claude/commands/lucy-ng/case.md (full file — find the UAT criteria section or Phase 78 handoff note, and the emergent-ring guidance section)
    ~/.claude/agents/lucy-devils-advocate.md (full file — find G5-G8 gates added in Phase 75, specifically the COSY equivalence-pair check)
    .planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-PATTERNS.md (section "case.md and lucy-devils-advocate.md targeted audit")
    .planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-CONTEXT.md (D-77-06 full text)
  </read_first>
  <action>
    case.md changes (targeted edits only):

    1. Find the UAT success criteria section or any section discussing what counts as a passing solution run. Add or update the mechanistic UAT pass criteria for Phase 78:
       "Mechanistic UAT criteria (Phase 78): EMERGENT RING = clean pass (cross-ring COSY constraints caused LSD to find the aromatic structure without explicit ring bonds). RING-BONDS AS ESCALATION = conditional pass (ring-BOND forcing is acceptable ONLY if CASE-PROGRESS.md documents the escalation decision and records that emergent ring was attempted for at least 3 iterations first; the ring-BOND must be the only forcing mechanism — no SKEL). SILENT RING-BONDS OR SKEL = fail (any forced benzene ring via SKEL or undocumented ring-BONDs fails the gate regardless of whether the correct compound is found)."

    2. Verify the emergent-ring guidance is not buried. Search for "COSY 4 7" or "cross-ring" or "aromatic equivalence" — if found, confirm it appears prominently (not at the bottom of a long appendix). If buried, add a brief cross-reference near the top of the relevant section pointing to the COSY pairs rule. Do not move or rewrite the existing guidance.

    lucy-devils-advocate.md changes (targeted edits only):

    3. Find the G-gate that checks COSY equivalence pairs (added in Phase 75 — look for gates G5-G8 or any check mentioning "within-group" or "COSY" or "aromatic equivalence"). Update its description to:
       "G-XX [COSY Aromatic Equivalence]: Verify any COSY constraints added for aromatic CH equivalence are cross-ring pairs (from different chemical-shift groups), NOT within-group pairs. Use `lucy detect aromatic-cosy \"<shifts>\" --multiplicities \"<mults>\"` to derive the correct pairs. Reject any LSD file where COSY pairs have both atoms at the same chemical shift (within-group — causes LSD error 283 when ring BONDs are present)."

    4. Verify G5-G8 gates added in Phase 75 are still present and unmodified (they cover: permutation constraint completeness, empty-merge detection, post-validation edit detection, reversion). If any were accidentally modified, restore them.

    Both files: do not rewrite sections, do not change unrelated content, do not alter the overall structure or ordering.
  </action>
  <verify>
    <automated>grep -c "aromatic-cosy\|aromatic_cosy" ~/.claude/agents/lucy-devils-advocate.md && grep -c "emergent ring = clean pass\|EMERGENT RING\|emergent.*clean pass" ~/.claude/commands/lucy-ng/case.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ic "emergent ring\|emergent.*pass\|EMERGENT RING" ~/.claude/commands/lucy-ng/case.md` returns >= 1 (D-77-06 criteria present)
    - `grep -ic "silent ring.*fail\|SKEL.*fail\|fail.*SKEL" ~/.claude/commands/lucy-ng/case.md` returns >= 1 (silent ring-BOND or SKEL = fail criterion present)
    - `grep -c "aromatic-cosy" ~/.claude/agents/lucy-devils-advocate.md` returns >= 1 (aromatic-cosy tool reference in DA gate)
    - `grep -c "G5\|G6\|G7\|G8" ~/.claude/agents/lucy-devils-advocate.md` returns >= 4 (Phase-75 gates still present, per gate listed)
  </acceptance_criteria>
  <done>
    case.md: D-77-06 mechanistic UAT criteria added (emergent=pass / documented-BOND=conditional / silent-BOND or SKEL=fail). Emergent-ring guidance confirmed not buried.
    lucy-devils-advocate.md: COSY equivalence G-gate updated to reference lucy detect aromatic-cosy. G5-G8 gates verified intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill .md files → Claude Code agent spawning | Frontmatter name: field determines whether Claude Code spawns a file as an agent; neutralizing the name prevents accidental spawning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03-01 | Spoofing | lucy-case-agent.md — active-looking frontmatter could cause Claude Code to spawn the deprecated agent | mitigate | Change name: lucy-case-agent → name: DEPRECATED-lucy-case-agent; prevents name-based agent matching |
| T-77-03-02 | Tampering | lucy-lsd-engineer.md — false Phase-73 claim causes agent to believe tooling is fixed when it is not | mitigate | Replace false claim with accurate FIX-01 correction note referencing Phase 77 |
| T-77-03-03 | Spoofing | skill files claiming COSY 4 5 is correct for ibuprofen | mitigate | Replace compound-specific COSY index guidance with reference to lucy detect aromatic-cosy CLI tool |
| T-77-03-04 | Tampering | case.md lacking D-77-06 criteria — Phase 78 agent applies Phase-76 SKEL=0 loophole | mitigate | Add D-77-06 mechanistic UAT criteria explicitly: emergent/conditional/fail categories |
| T-77-03-SC | Tampering | No npm/pip/cargo installs in this plan | accept | Skill file edits only — not applicable |
</threat_model>

<verification>
Full FIX-03 gate:
1. `grep -c "name: DEPRECATED-lucy-case-agent" ~/.claude/agents/lucy-case-agent.md` returns 1
2. `grep -c "name: lucy-case-agent$" ~/.claude/agents/lucy-case-agent.md` returns 0
3. `grep -c "aromatic-cosy" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 1
4. `grep -c "3 consecutive non-aromatic\|after 3\|after.*3.*iterations" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 1
5. `grep -ic "emergent ring\|EMERGENT RING\|emergent.*pass" ~/.claude/commands/lucy-ng/case.md` returns >= 1
6. `grep -ic "silent.*fail\|SKEL.*fail" ~/.claude/commands/lucy-ng/case.md` returns >= 1
7. `grep -c "aromatic-cosy" ~/.claude/agents/lucy-devils-advocate.md` returns >= 1
8. `grep -r "lucy-case-agent[^.]" ~/.claude/commands/lucy-ng/ ~/.claude/agents/lucy-nmr-chemist.md ~/.claude/agents/lucy-lsd-engineer.md ~/.claude/agents/lucy-devils-advocate.md 2>/dev/null | grep -v "DEPRECATED"` returns empty (no active references to the non-deprecated name)
</verification>

<success_criteria>
- lucy-case-agent.md: `name:` neutralized to DEPRECATED-lucy-case-agent; blockquote DEPRECATED notice preserved
- lucy-lsd-engineer.md: Phase-73 false claim replaced; lucy detect aromatic-cosy CLI reference present; D-04 escalation says N=3 + CASE-PROGRESS logging + SKEL forbidden
- case.md: D-77-06 UAT criterion present (emergent=pass / documented-BOND=conditional / silent-BOND or SKEL=fail)
- lucy-devils-advocate.md: COSY equivalence G-gate references lucy detect aromatic-cosy; G5-G8 gates intact
- No active skill or command file spawns or calls lucy-case-agent (without DEPRECATED prefix)
</success_criteria>

<output>
Create `.planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-03-SUMMARY.md` when done
</output>
