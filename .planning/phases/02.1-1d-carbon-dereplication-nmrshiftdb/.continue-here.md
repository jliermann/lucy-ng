---
phase: 02.1-1d-carbon-dereplication-nmrshiftdb
task: complete
total_tasks: complete
status: complete_with_extension
last_updated: 2026-01-09
---

<current_state>
Phase 2.1 is COMPLETE. This session extended the phase with Sherlock database extraction.

The 928K compound database (nmrshiftdb + COCONUT) has been extracted from Sherlock Docker
and a new SherlockLoader class was created. Ibuprofen dereplication test passed (94.9% match).

Ready to proceed to Phase 3: 2D NMR Reading.
</current_state>

<completed_work>
**This session:**
- Extracted Sherlock database from Docker image (michaelwenk/sherlock-db-instance-dataset)
- Created `data/reference/sherlock_13c.json` (338MB, 928K compounds, 107K formulas)
- Created SherlockLoader class in `src/lucy_ng/dereplication/sherlock.py`
- Updated dereplication module exports
- Tested Ibuprofen dereplication against 928K database (94.9% match, 9/10 peaks, 0.23ppm avg deviation)

**Previously completed (Phase 2.1):**
- NMRShiftDBLoader for SD file parsing
- SimplePeakPicker with threshold detection
- SpectrumMatcher with multi-mode matching
- DereplicationService with three entry points
- 18 comprehensive tests
</completed_work>

<remaining_work>
Phase 2.1 is complete. No remaining work.

**Next phase**: Phase 3 - 2D NMR Reading (HSQC, HMBC)
</remaining_work>

<decisions_made>
- Used SherlockLoader instead of full Sherlock stack (VM disk too small for full 7.4GB Docker images)
- JSON format indexed by molecular formula for O(1) lookup
- SherlockEntry dataclass compatible with existing SpectrumMatcher interface
- Data file (338MB) not committed to git - must be regenerated from Docker
</decisions_made>

<blockers>
None. Phase complete.
</blockers>

<context>
The Sherlock database extraction was originally interrupted (previous session) due to VM
disk space. This session:
1. Pulled only the dataset image (1.29GB) instead of full stack
2. Ran MongoDB container
3. Used mongoexport to extract 928K documents
4. Converted to compact JSON format with Python script
5. Created SherlockLoader to match existing interface

The dereplication system now has access to ~23x more compounds than the original nmrshiftdb
SD file approach (~40K vs 928K).
</context>

<uncommitted_changes>
Files staged but not committed (git identity not configured on VM):
- D  .planning/SESSION-HANDOFF-2026-01-09.md (deleted)
- M  .planning/STATE.md (updated)
- M  src/lucy_ng/dereplication/__init__.py (exports SherlockLoader)
- A  src/lucy_ng/dereplication/sherlock.py (new SherlockLoader class)
</uncommitted_changes>

<next_action>
1. Configure git identity and commit staged changes
2. Run `/gsd:plan-phase` for Phase 3 (2D NMR Reading)
3. Or run `/gsd:progress` to see overall status
</next_action>
