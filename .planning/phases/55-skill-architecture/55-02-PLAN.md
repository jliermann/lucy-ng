---
phase: 55-skill-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/lucy-case-agent.md
  - ~/.claude/commands/lucy-ng/references/nmr-basics.md
  - ~/.claude/agents/lucy-nmr-chemist.md
autonomous: true
requirements: [ARCH-02, ARCH-03]

must_haves:
  truths:
    - "lucy-case-agent.md has a deprecation header stating it is retired and not spawned by any active workflow"
    - "A references/nmr-basics.md file exists with shared experiment types and chemical shift regions"
    - "lucy-nmr-chemist.md references nmr-basics.md instead of inlining the NMR experiment and shift region tables"
  artifacts:
    - path: "~/.claude/agents/lucy-case-agent.md"
      provides: "Archived legacy agent with deprecation header"
      contains: "DEPRECATED"
    - path: "~/.claude/commands/lucy-ng/references/nmr-basics.md"
      provides: "Shared NMR experiment types and chemical shift regions reference"
      contains: "13C Chemical Shift Regions"
    - path: "~/.claude/agents/lucy-nmr-chemist.md"
      provides: "NMR chemist agent referencing shared NMR basics instead of duplicating tables"
      contains: "nmr-basics.md"
  key_links:
    - from: "~/.claude/agents/lucy-nmr-chemist.md"
      to: "~/.claude/commands/lucy-ng/references/nmr-basics.md"
      via: "Read file reference replacing inline NMR tables"
      pattern: "Read.*nmr-basics.md"
---

<objective>
Archive the legacy monolithic agent with a deprecation header, create a shared NMR reference file, and update the nmr-chemist agent to reference it instead of duplicating NMR tables.

Purpose: The legacy lucy-case-agent.md (666 lines) is no longer spawned by any active workflow but lacks a clear deprecation marker. NMR experiment types and chemical shift region tables are duplicated across agents (nmr-chemist inlines them, legacy agent has its own copy). Extracting to a shared reference reduces duplication and establishes a single source of truth.

Output: Deprecated legacy agent, shared nmr-basics.md, updated nmr-chemist agent
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@~/.claude/agents/lucy-case-agent.md
@~/.claude/agents/lucy-nmr-chemist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive legacy agent and create shared NMR reference</name>
  <files>~/.claude/agents/lucy-case-agent.md, ~/.claude/commands/lucy-ng/references/nmr-basics.md</files>
  <action>
**Part A -- Deprecate lucy-case-agent.md:**

Add a deprecation header block immediately after the frontmatter (after the closing `---`), before the existing `<role>` tag. Insert this block:

```markdown
> **DEPRECATED -- DO NOT USE**
>
> This monolithic agent is retired as of v4.0 (2026-02-18). It is NOT spawned by any active workflow.
> The /lucy-ng:case orchestrator now uses a 4-agent specialist team instead:
> - lucy-nmr-chemist.md (spectral analysis)
> - lucy-lsd-engineer.md (constraint building)
> - lucy-solution-analyst.md (ranking)
> - lucy-devils-advocate.md (validation)
>
> Preserved as historical reference only. For the active CASE workflow, see ~/.claude/commands/lucy-ng/case.md
```

Do NOT modify any other content in the file. The deprecation header is additive only.

**Part B -- Create references/nmr-basics.md:**

Create ~/.claude/commands/lucy-ng/references/nmr-basics.md (the references/ directory should already exist from Plan 01, but create it if not). Content should be the canonical shared NMR reference combining:

1. **NMR Experiment Types table** -- merge from nmr-chemist (lines 41-48) and legacy agent (lines 40-48). Use the most complete version. Include all experiments: 1H, 13C, DEPT-135, DEPT-90, HSQC, HMBC, COSY. Each row: Experiment, Information Provided, Key Insight.

2. **13C Chemical Shift Regions table** -- from nmr-chemist (lines 51-60) and legacy agent (lines 52-60). They are identical. Include all 7 regions from 0-220 ppm.

3. **DEPT-135 Sign Convention** -- add a brief note: "DEPT-135: positive peaks = CH and CH3, negative peaks = CH2. Sign is critical for multiplicity assignment."

Title the file: "# NMR Basics Reference". Add a note at the top: "Shared reference for all CASE team agents. Read by agents that need NMR experiment and shift region context."

Keep this file concise (~40-50 lines). It is reference data, not workflow instructions.
  </action>
  <verify>
    <automated>head -20 ~/.claude/agents/lucy-case-agent.md && echo "---" && grep -c "DEPRECATED" ~/.claude/agents/lucy-case-agent.md && echo "deprecation markers" && echo "---" && wc -l ~/.claude/commands/lucy-ng/references/nmr-basics.md && cat ~/.claude/commands/lucy-ng/references/nmr-basics.md</automated>
  </verify>
  <done>lucy-case-agent.md has deprecation header after frontmatter. nmr-basics.md exists with experiment types table, shift regions table, and DEPT sign convention (~40-50 lines).</done>
</task>

<task type="auto">
  <name>Task 2: Update nmr-chemist to reference shared NMR basics</name>
  <files>~/.claude/agents/lucy-nmr-chemist.md</files>
  <action>
In ~/.claude/agents/lucy-nmr-chemist.md, replace the inline NMR Experiment Reference table (Section 1, lines 39-48) and 13C Chemical Shift Regions table (Section 2, lines 49-60) with a Read-file reference:

Replace sections "## 1. NMR Experiment Reference" and "## 2. 13C Chemical Shift Regions" (lines 39-60) with:

```markdown
## 1. NMR Experiment and Shift Reference

Read the shared NMR basics reference for experiment types, 13C chemical shift regions, and DEPT sign conventions:

Read file: ~/.claude/commands/lucy-ng/references/nmr-basics.md
```

Renumber subsequent sections: what was "## 3. Critical Pitfalls" becomes "## 2. Critical Pitfalls", etc. Adjust all section numbers down by 1 (sections 3-8 become 2-7). Also update any cross-references within the file that mention section numbers (e.g., "Section 5" references should become "Section 4").

This removes ~22 lines of duplicated table content and replaces with ~4 lines of read reference, net saving of ~18 lines.
  </action>
  <verify>
    <automated>grep -n "nmr-basics.md" ~/.claude/agents/lucy-nmr-chemist.md && echo "---" && grep -c "Experiment.*Information\|Region.*ppm.*Typical" ~/.claude/agents/lucy-nmr-chemist.md && echo "inline tables remaining (should be 0)" && echo "---" && wc -l ~/.claude/agents/lucy-nmr-chemist.md</automated>
  </verify>
  <done>nmr-chemist.md references nmr-basics.md via Read file directive. No inline NMR experiment or shift region tables remain. Sections renumbered correctly. File is ~18 lines shorter than original 260 lines.</done>
</task>

</tasks>

<verification>
1. `grep "DEPRECATED" ~/.claude/agents/lucy-case-agent.md` shows deprecation header
2. `grep "NOT spawned" ~/.claude/agents/lucy-case-agent.md` confirms retirement language
3. `cat ~/.claude/commands/lucy-ng/references/nmr-basics.md` shows experiment types and shift regions
4. `grep "nmr-basics.md" ~/.claude/agents/lucy-nmr-chemist.md` shows Read-file reference
5. `grep -c "Experiment.*Information" ~/.claude/agents/lucy-nmr-chemist.md` returns 0 (no inline table)
6. Original content of lucy-case-agent.md below the deprecation header is unchanged
</verification>

<success_criteria>
- lucy-case-agent.md has clear deprecation header stating it is retired and not spawned by any active workflow
- nmr-basics.md exists at ~/.claude/commands/lucy-ng/references/ with experiment types, shift regions, and DEPT convention
- lucy-nmr-chemist.md references nmr-basics.md instead of inlining the two NMR tables
- No other agent files are modified (lsd-engineer, solution-analyst, devils-advocate, diagnostic do not have these tables to begin with)
</success_criteria>

<output>
After completion, create `.planning/phases/55-skill-architecture/55-02-SUMMARY.md`
</output>
