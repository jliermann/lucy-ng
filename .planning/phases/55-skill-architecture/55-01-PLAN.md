---
phase: 55-skill-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/lucy-ng/case.md
  - ~/.claude/commands/lucy-ng/references/progress-format.md
  - ~/.claude/commands/lucy-ng/references/loop-patterns.md
  - ~/.claude/commands/lucy-ng/references/advisory-templates.md
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "case.md is under 500 lines and still contains all orchestrator process steps"
    - "case.md has explicit read-file references to progress-format.md, loop-patterns.md, and advisory-templates.md"
    - "The three extracted reference files exist and contain the full content removed from case.md"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "Slim orchestrator skill with read-file references"
      max_lines: 500
    - path: "~/.claude/commands/lucy-ng/references/progress-format.md"
      provides: "CASE-PROGRESS.md format templates"
      contains: "CASE Progress Log"
    - path: "~/.claude/commands/lucy-ng/references/loop-patterns.md"
      provides: "Four loop pattern definitions"
      contains: "ELIM Thrashing"
    - path: "~/.claude/commands/lucy-ng/references/advisory-templates.md"
      provides: "Advisory delivery, diagnostic delegation, findings extraction, and result presentation templates"
      contains: "deliver_advisory"
  key_links:
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "~/.claude/commands/lucy-ng/references/progress-format.md"
      via: "Read tool file reference in write_progress step"
      pattern: "Read.*references/progress-format.md"
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "~/.claude/commands/lucy-ng/references/loop-patterns.md"
      via: "Read tool file reference in detect_loops step"
      pattern: "Read.*references/loop-patterns.md"
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "~/.claude/commands/lucy-ng/references/advisory-templates.md"
      via: "Read tool file reference in intervene/escalate steps"
      pattern: "Read.*references/advisory-templates.md"
---

<objective>
Factor case.md from ~1093 lines to under 500 lines by extracting three large reference sections into standalone files under references/, replacing them with Read-tool file references so the orchestrator loads them on demand.

Purpose: case.md currently exceeds comfortable context window size. Extracting rarely-varying reference material (progress format templates, loop pattern definitions, advisory/diagnostic/result templates) keeps the core orchestration flow lean while preserving full functionality via on-demand reads.

Output: Slim case.md (<500 lines) + 3 reference files in ~/.claude/commands/lucy-ng/references/
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@~/.claude/commands/lucy-ng/case.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract three reference files from case.md</name>
  <files>~/.claude/commands/lucy-ng/references/progress-format.md, ~/.claude/commands/lucy-ng/references/loop-patterns.md, ~/.claude/commands/lucy-ng/references/advisory-templates.md</files>
  <action>
Create directory ~/.claude/commands/lucy-ng/references/ and extract these three files from case.md:

**1. references/progress-format.md** -- Extract from case.md lines 196-360 (the `<step name="write_progress">` section). This contains the full CASE-PROGRESS.md template with all section headers, field definitions, and format examples. Include the step wrapper tags. Title the file: "# CASE Progress Format Reference"

**2. references/loop-patterns.md** -- Extract from case.md lines 906-963 (the `<loop_detection_reference>` section). This contains the four loop pattern definitions (ELIM Thrashing, Zero-Solution Loop, Solution Explosion, Constraint Churning) with their detection signatures and thresholds. Include the wrapper tags. Title the file: "# Loop Detection Patterns Reference"

**3. references/advisory-templates.md** -- Extract from case.md lines 719-870 and 965-1093 (the `deliver_advisory`, `present_results`, `delegate_specialist`, `extract_diagnostic_findings`, and `anti_patterns` sections). These are all advisory/intervention/result templates. Combine into one file maintaining section structure. Title the file: "# Advisory and Results Templates Reference". Preserve all step tags.

Each file must be self-contained: include all content verbatim from case.md (every template, every field, every example). Do NOT summarize or compress. The orchestrator will Read these files and needs the exact content.
  </action>
  <verify>
    <automated>wc -l ~/.claude/commands/lucy-ng/references/progress-format.md ~/.claude/commands/lucy-ng/references/loop-patterns.md ~/.claude/commands/lucy-ng/references/advisory-templates.md && echo "---" && ls ~/.claude/commands/lucy-ng/references/</automated>
  </verify>
  <done>Three reference files exist under references/ with combined line count approximately matching the ~500 lines extracted from case.md</done>
</task>

<task type="auto">
  <name>Task 2: Slim case.md with read-file references</name>
  <files>~/.claude/commands/lucy-ng/case.md</files>
  <action>
Rewrite case.md by:

1. **Replace the `<step name="write_progress">` section (lines 196-360)** with a compact stub that tells the orchestrator to read the reference file:

```
<step name="write_progress">
Read the full CASE-PROGRESS.md format template before writing any progress entries:

Read file: ~/.claude/commands/lucy-ng/references/progress-format.md

The orchestrator is the SOLE AUTHOR of CASE-PROGRESS.md. Create it at `<compound_path>/analysis/CASE-PROGRESS.md` after receiving [SETUP-COMPLETE] from nmr-chemist. Update after every [ITERATION-COMPLETE], [VALIDATION-PASSED/BLOCKED], and [RANKING-COMPLETE] message.
</step>
```

2. **Replace the `<loop_detection_reference>` section (lines 906-963)** with:

```
<loop_detection_reference>
Read the full loop pattern definitions before analyzing iteration patterns:

Read file: ~/.claude/commands/lucy-ng/references/loop-patterns.md

Four patterns to detect: ELIM Thrashing, Zero-Solution Loop, Solution Explosion, Constraint Churning.
</loop_detection_reference>
```

3. **Replace `deliver_advisory` (lines 719-757), `present_results` (lines 759-870), `delegate_specialist` (lines 965-1011), `extract_diagnostic_findings` (lines 1013-1050), and `anti_patterns` (lines 1052-1093)** with:

```
<step name="deliver_advisory_and_results">
Read advisory delivery, result presentation, diagnostic delegation, and anti-pattern templates:

Read file: ~/.claude/commands/lucy-ng/references/advisory-templates.md

This reference covers: advisory message formatting, result presentation (success/incomplete/failed), diagnostic specialist delegation, diagnostic findings extraction, and anti-patterns to avoid.
</step>
```

4. **Keep ALL other sections intact**: objective, parse_arguments, validate_prerequisites, spawn_case_team, monitor_progress, detect_loops, diagnose, intervene, track_and_decide, escalate, terminate_team. These are the core orchestration flow.

5. **Verify the final line count is under 500.** If slightly over, look for opportunities to tighten whitespace (remove consecutive blank lines, compress obvious redundancies in the remaining sections) -- but do NOT remove any process logic or step content.

The frontmatter (lines 1-18) must remain unchanged. The Read tool references must use exact file paths starting with ~/.claude/commands/lucy-ng/references/.
  </action>
  <verify>
    <automated>wc -l ~/.claude/commands/lucy-ng/case.md && echo "---" && grep -c "Read file:.*references/" ~/.claude/commands/lucy-ng/case.md && echo "references found" && grep "references/" ~/.claude/commands/lucy-ng/case.md</automated>
  </verify>
  <done>case.md is under 500 lines AND contains exactly 3 "Read file:" references pointing to progress-format.md, loop-patterns.md, and advisory-templates.md</done>
</task>

</tasks>

<verification>
1. `wc -l ~/.claude/commands/lucy-ng/case.md` shows under 500
2. `grep -c "Read file:" ~/.claude/commands/lucy-ng/case.md` shows 3
3. All three reference files exist and are non-empty
4. Core orchestration steps still present: `grep -c "step name=" ~/.claude/commands/lucy-ng/case.md` shows the main process steps (parse_arguments, validate_prerequisites, spawn_case_team, monitor_progress, detect_loops, diagnose, intervene, track_and_decide, escalate, terminate_team)
5. No content lost: key terms from extracted sections appear in reference files: `grep "ELIM Thrashing" ~/.claude/commands/lucy-ng/references/loop-patterns.md`, `grep "CASE Progress Log" ~/.claude/commands/lucy-ng/references/progress-format.md`, `grep "deliver_advisory\|present_results\|delegate_specialist" ~/.claude/commands/lucy-ng/references/advisory-templates.md`
</verification>

<success_criteria>
- case.md under 500 lines with all orchestrator process steps preserved
- 3 Read-file references in case.md pointing to references/ directory
- 3 reference files contain verbatim extracted content
- No orchestration logic removed (only templates/reference material extracted)
</success_criteria>

<output>
After completion, create `.planning/phases/55-skill-architecture/55-01-SUMMARY.md`
</output>
