---
phase: 44-case-progress-format
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/lucy-lsd-engineer.md
  - ~/.claude/agents/lucy-nmr-chemist.md
  - ~/.claude/agents/lucy-solution-analyst.md
  - ~/.claude/agents/lucy-devils-advocate.md
autonomous: true

must_haves:
  truths:
    - "lsd-engineer sends [ITERATION-COMPLETE] message instead of writing CASE-PROGRESS.md directly"
    - "nmr-chemist sends [SETUP-COMPLETE] / [DETECTION-COMPLETE] message instead of writing to CASE-PROGRESS.md"
    - "solution-analyst sends [RANKING-COMPLETE] message to coordinator"
    - "devils-advocate sends [VALIDATION-PASSED] / [VALIDATION-BLOCKED] message to coordinator"
    - "No agent definition references writing to CASE-PROGRESS.md (coordinator-only model)"
  artifacts:
    - path: "~/.claude/agents/lucy-lsd-engineer.md"
      provides: "SendMessage template replacing Section 4 CASE-PROGRESS.md write"
      contains: "ITERATION-COMPLETE"
    - path: "~/.claude/agents/lucy-nmr-chemist.md"
      provides: "SendMessage template for setup and detection results"
      contains: "SETUP-COMPLETE"
    - path: "~/.claude/agents/lucy-solution-analyst.md"
      provides: "SendMessage template for ranking results"
      contains: "RANKING-COMPLETE"
    - path: "~/.claude/agents/lucy-devils-advocate.md"
      provides: "SendMessage template for validation decisions"
      contains: "VALIDATION-PASSED"
  key_links:
    - from: "~/.claude/agents/lucy-lsd-engineer.md"
      to: "coordinator (case.md)"
      via: "SendMessage with [ITERATION-COMPLETE] containing solution count, constraints, inventory delta"
      pattern: "ITERATION-COMPLETE"
    - from: "~/.claude/agents/lucy-nmr-chemist.md"
      to: "coordinator (case.md)"
      via: "SendMessage with [SETUP-COMPLETE] containing spectra, peaks, multiplicities, detection"
      pattern: "SETUP-COMPLETE"
    - from: "~/.claude/agents/lucy-devils-advocate.md"
      to: "coordinator (case.md)"
      via: "SendMessage with [VALIDATION-PASSED] or [VALIDATION-BLOCKED]"
      pattern: "VALIDATION-PASSED|VALIDATION-BLOCKED"
---

<objective>
Update all 4 specialist agent definitions to use structured SendMessage templates for contributing to CASE-PROGRESS.md instead of writing the file directly. This implements the "coordinator-as-sole-writer" protocol (SC2) — agents post structured results to the coordinator, who is the exclusive writer of CASE-PROGRESS.md.

Purpose: Prevent file corruption from concurrent writes. Establish machine-parseable message templates with labeled fields matching the orchestrator's detect_loops expectations (solution count, sp2 count, H budget, HMBC correlations used).

Output: 4 updated agent definitions with structured message templates and no direct CASE-PROGRESS.md writing references.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-case-progress-format/44-RESEARCH.md
@~/.claude/agents/lucy-lsd-engineer.md
@~/.claude/agents/lucy-nmr-chemist.md
@~/.claude/agents/lucy-solution-analyst.md
@~/.claude/agents/lucy-devils-advocate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace lsd-engineer Section 4 and workflow CASE-PROGRESS.md references with SendMessage protocol</name>
  <files>~/.claude/agents/lucy-lsd-engineer.md</files>
  <action>
**Replace Section 4 entirely.** Currently Section 4 ("CASE-PROGRESS.md Format (Authoritative Specification)") defines the full format and mandates the lsd-engineer as the writer. Replace with a new Section 4 titled "CASE-PROGRESS.md Contribution Protocol" that:

1. States clearly: "You do NOT write CASE-PROGRESS.md. The coordinator (orchestrator) is the sole writer. You send structured messages and the coordinator writes your contribution."

2. Defines the [ITERATION-COMPLETE] message template that lsd-engineer sends to coordinator after EVERY LSD run via SendMessage:

```
[ITERATION-COMPLETE] Iteration N
LSD file: analysis/iteration_NN/compound.lsd
Solution count: <N>
Constraints added:
- <constraint with reasoning>
Constraints removed:
- <constraint with reasoning> (or "None")
Constraint inventory delta: MULT=N, HSQC=N, HMBC=+N (total N), DEFF NOT=N, SYME=N, BOND=N
sp2 count: N (even/odd)
H budget: N (matches/mismatch)
HMBC correlations used: X/Y
Why: <natural language reasoning>
Constraint effectiveness: <% reduction | "baseline" | "over-constrained">
Confidence: <too many / converging / stuck>
```

3. States that ALL labeled fields are MANDATORY (they match the orchestrator's loop detection parsing). Missing a field causes the orchestrator to fail to detect loop patterns.

4. Specifies terminal message rule: [ITERATION-COMPLETE] is the terminal message for the lsd-engineer within one iteration. Once sent, no follow-up about the same iteration. If the lsd-engineer needs to revise (e.g., after devils-advocate blocks), send a new [ITERATION-COMPLETE] for the same iteration number with "(revised)" suffix.

**Update workflow steps:**

- Step 3 (iteration 1): Remove "Create analysis/CASE-PROGRESS.md with header + Setup section." Replace with: "Send [ITERATION-COMPLETE] message to coordinator via SendMessage after LSD run."

- Step 11: Remove "IMMEDIATELY append iteration entry to CASE-PROGRESS.md". Replace with: "IMMEDIATELY send [ITERATION-COMPLETE] message to coordinator via SendMessage (before anything else)."

**Update the role description:** Remove "write CASE-PROGRESS.md entries after every LSD run" from the Job line. Replace with "send structured iteration results to coordinator via SendMessage after every LSD run."

**Update ABSOLUTE PROHIBITIONS:** Replace "NEVER skip writing CASE-PROGRESS.md after an LSD run" with "NEVER skip sending [ITERATION-COMPLETE] message to coordinator after an LSD run."

**Preserve everything else:** Sections 1, 2, 3, 5, shared_context, message_interface, and the rest of the workflow are NOT modified by this task. Only Section 4, the Job description, the ABSOLUTE PROHIBITIONS, and workflow steps 3 and 11 change.
  </action>
  <verify>
Read the updated file. Verify:
1. Section 4 title contains "Contribution Protocol" (not "Authoritative Specification")
2. [ITERATION-COMPLETE] template is present with all labeled fields (Solution count, sp2 count, H budget, HMBC correlations used)
3. No occurrence of "write CASE-PROGRESS.md" or "append iteration entry to CASE-PROGRESS.md" or "Create analysis/CASE-PROGRESS.md" in the file
4. grep -c "CASE-PROGRESS.md" should find references ONLY in the file organization section (Section 3, which lists analysis/CASE-PROGRESS.md as a file path) and the new Section 4 (which says "You do NOT write CASE-PROGRESS.md")
5. Workflow step 11 mentions SendMessage, not Write
6. Role/Job line says "send structured iteration results to coordinator" not "write CASE-PROGRESS.md"
  </verify>
  <done>lsd-engineer sends [ITERATION-COMPLETE] via SendMessage instead of writing CASE-PROGRESS.md directly. All mandatory labeled fields present. Section 4 is "Contribution Protocol" not "Authoritative Specification".</done>
</task>

<task type="auto">
  <name>Task 2: Add structured SendMessage templates to nmr-chemist, solution-analyst, and devils-advocate</name>
  <files>~/.claude/agents/lucy-nmr-chemist.md, ~/.claude/agents/lucy-solution-analyst.md, ~/.claude/agents/lucy-devils-advocate.md</files>
  <action>
**nmr-chemist (lucy-nmr-chemist.md):**

1. In Section 5 (Statistical Detection Protocol), remove the line: "Document every detection override in CASE-PROGRESS.md under 'Conflicts with NMR evidence'". Replace with: "Report detection overrides in your [SETUP-COMPLETE] or [DETECTION-COMPLETE] message under a 'Conflicts with NMR evidence' section. The coordinator writes these to CASE-PROGRESS.md."

2. Add a new section AFTER the `</domain_knowledge>` closing tag but BEFORE the `<message_interface>` tag (or within message_interface OUTPUTS). Title: "## CASE-PROGRESS.md Contribution Protocol". Content:

```
You do NOT write CASE-PROGRESS.md. Send structured messages to the coordinator, who writes your contribution.

### [SETUP-COMPLETE] — sent once, after initial peak picking and detection

[SETUP-COMPLETE]
DBE: <value> (calculation: (2C + 2 + N - H) / 2)
Spectra found: <list with experiment types and paths>
Peak counts: 13C: N, DEPT-135: N (N pos, N neg), HSQC: N, HMBC: N (raw)
Symmetry: expected N (formula), observed N (13C), equivalent N
Multiplicities: N CH3, N CH2, N CH, N Cq (from DEPT-135 sign and HSQC)
Quality assessment: 13C SNR=N (tier), HSQC SNR=N (tier), HMBC SNR=N (tier)
Statistical detection:
  - <shift> ppm: <hybridisation result>, <neighbours result>
  - ...
Grouping: <grouped signal pairs or "none detected">
HHB: <hetero-hetero bond result or "N/A">
Conflicts with NMR evidence:
  - <override descriptions or "None">
Key observations: <1-2 sentences summarizing notable chemistry>

### [DETECTION-COMPLETE] — sent for follow-up detection in later iterations (if requested)

[DETECTION-COMPLETE] Iteration N
Additional detection:
  - <shift> ppm: <new detection results>
Revised assignments:
  - <any changes to multiplicities or confidence>
```

3. Update message_interface OUTPUTS to reference the new template names: "[SETUP-COMPLETE] message with peak assignments, detection results, quality assessment" and "[DETECTION-COMPLETE] message with follow-up detection results".

4. In the workflow section, update step 8: change "Post peak assignments + detection results + quality assessment to team via SendMessage" to "Send [SETUP-COMPLETE] message to coordinator via SendMessage with all labeled fields."

**solution-analyst (lucy-solution-analyst.md):**

1. Add a new section within `<message_interface>` (or between domain_knowledge and message_interface). Title: "## CASE-PROGRESS.md Contribution Protocol". Content:

```
You do NOT write CASE-PROGRESS.md. Send structured messages to the coordinator, who writes your contribution. You DO still write analysis/final_results.md (that file is yours).

### [RANKING-COMPLETE] — sent after ranking solutions for an iteration

[RANKING-COMPLETE] Iteration N
Solutions: N total converted and ranked
Top solution: Rank #1: <name if known> — SMILES: <smiles>, MAE: N ppm, Matched: N/N
Strained rings: None / found in solutions <list>
Chemical plausibility: <PLAUSIBLE/QUESTIONABLE/IMPLAUSIBLE summary>
Quality: <Excellent/Good/Fair/Marginal/Poor>
Recommendation: <converge (stop) / continue (more HMBC) / escalate (problem detected)>
```

2. Update message_interface OUTPUTS item 4 to reference "[RANKING-COMPLETE] message to coordinator with ranking summary".

3. In the workflow section, after step 6 (confidence scores), add between steps 8 and 9: "Send [RANKING-COMPLETE] message to coordinator via SendMessage with all labeled fields."

**devils-advocate (lucy-devils-advocate.md):**

1. Add a new section within `<message_interface>` (or between domain_knowledge and message_interface). Title: "## CASE-PROGRESS.md Contribution Protocol". Content:

```
You do NOT write CASE-PROGRESS.md (you have no Write tool). Send structured validation messages to the coordinator, who writes your contribution.

### [VALIDATION-PASSED] — sent when LSD file passes all checks

[VALIDATION-PASSED] Iteration N
sp2 count: N (even)
H budget: N (matches <formula>)
DEFF NOT: N patterns (preserved from iteration N-1 / initialized)
SYME: N constraints (preserved / N/A)
Grouped notation: N entries (preserved / N/A)
Correlation order: HSQC before HMBC (correct)
Inventory accuracy: all counts match actual file
Concerns: <list or "None">

### [VALIDATION-BLOCKED] — sent when LSD file has CRITICAL issues

[VALIDATION-BLOCKED] Iteration N
CRITICAL issues:
  - <issue description with severity>
  - ...
WARNING issues:
  - <issue description or "None">
sp2 count: N (even/ODD)
H budget: N (matches/MISMATCH)
DEFF NOT: N patterns (DROPPED from N-1 count of M / preserved)
Action required: lsd-engineer must fix CRITICAL issues before solver run

### Terminal message rule
[VALIDATION-PASSED] and [VALIDATION-BLOCKED] are terminal — once sent, no follow-up for the same iteration. If validation is redone after fix, send [VALIDATION-PASSED] with "(revised)" suffix.
```

2. Update message_interface OUTPUTS to reference template names: "[VALIDATION-PASSED] or [VALIDATION-BLOCKED] message to coordinator".

3. In the workflow section, update step 10/11: replace "Send 'BLOCKED'" / "Send 'APPROVED'" with "Send [VALIDATION-BLOCKED]" / "Send [VALIDATION-PASSED]" respectively. Also update step 12: change "Post validation summary to team via SendMessage" to "The [VALIDATION-PASSED] or [VALIDATION-BLOCKED] message IS the validation summary — no separate post needed."
  </action>
  <verify>
Read all 3 updated files. For each, verify:
1. "CASE-PROGRESS.md Contribution Protocol" section exists
2. nmr-chemist: [SETUP-COMPLETE] and [DETECTION-COMPLETE] templates present
3. solution-analyst: [RANKING-COMPLETE] template present. "analysis/final_results.md" still referenced as solution-analyst's own file.
4. devils-advocate: [VALIDATION-PASSED] and [VALIDATION-BLOCKED] templates present. NO Write tool in frontmatter.
5. No agent says "write to CASE-PROGRESS.md" or "append to CASE-PROGRESS.md" (grep across all 3 files)
6. nmr-chemist Section 5 no longer says "Document every detection override in CASE-PROGRESS.md"
  </verify>
  <done>All 3 specialist agents have structured SendMessage templates with labeled fields. No agent writes CASE-PROGRESS.md directly. Devils-advocate retains no Write tool. All message types are terminal (one per iteration per agent).</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Grep all 4 agent files for direct CASE-PROGRESS.md writing instructions:
   ```bash
   grep -n "write.*CASE-PROGRESS\|append.*CASE-PROGRESS\|Create.*CASE-PROGRESS" ~/.claude/agents/lucy-*.md
   ```
   Expected: Zero matches for write/append/create. The only CASE-PROGRESS.md references should be:
   - File paths in organization sections (Section 3 of lsd-engineer)
   - "You do NOT write CASE-PROGRESS.md" statements
   - "coordinator writes" references

2. Verify all 4 message templates exist:
   ```bash
   grep -l "ITERATION-COMPLETE" ~/.claude/agents/lucy-lsd-engineer.md
   grep -l "SETUP-COMPLETE" ~/.claude/agents/lucy-nmr-chemist.md
   grep -l "RANKING-COMPLETE" ~/.claude/agents/lucy-solution-analyst.md
   grep -l "VALIDATION-PASSED" ~/.claude/agents/lucy-devils-advocate.md
   ```
   Expected: All 4 files match.

3. Verify backward-compatible field names preserved in templates:
   ```bash
   grep -c "Solution count:" ~/.claude/agents/lucy-lsd-engineer.md  # >= 1
   grep -c "sp2 count:" ~/.claude/agents/lucy-devils-advocate.md    # >= 1
   grep -c "H budget:" ~/.claude/agents/lucy-devils-advocate.md     # >= 1
   grep -c "HMBC correlations used:" ~/.claude/agents/lucy-lsd-engineer.md  # >= 1
   ```

4. Verify devils-advocate has no Write tool:
   ```bash
   grep "Write" ~/.claude/agents/lucy-devils-advocate.md | head -3
   ```
   Expected: No "Write" in tools list.
</verification>

<success_criteria>
All 4 agent definitions updated. Each agent sends structured messages via SendMessage instead of writing CASE-PROGRESS.md. Message templates contain all labeled fields the orchestrator needs for loop detection (solution count, constraints, sp2 count, H budget, HMBC correlations used). No agent references writing CASE-PROGRESS.md directly.
</success_criteria>

<output>
After completion, create `.planning/phases/44-case-progress-format/44-01-SUMMARY.md`
</output>
