---
phase: 39-agent-integration
plan: 03
type: execute
wave: 3
depends_on: ["39-01", "39-02"]
files_modified:
  - ~/.claude/agents/lucy-case-agent.md
  - CLAUDE.md
autonomous: false

must_haves:
  truths:
    - "Agent file passes structural validation (section ordering, no broken markdown, all sections referenced in workflow)"
    - "CLAUDE.md CLI Output Reference documents detection commands"
    - "CLAUDE.md CLI Syntax Reference shows detection command examples"
    - "Agent file workflow <workflow> section step 6 references statistical detection"
    - "User verifies agent knowledge by reviewing the complete updated file"
  artifacts:
    - path: "~/.claude/agents/lucy-case-agent.md"
      provides: "Validated agent with complete statistical detection knowledge"
      min_lines: 1000
    - path: "CLAUDE.md"
      provides: "Updated project documentation with detection CLI commands"
      contains: "lucy detect hybridisation"
  key_links:
    - from: "~/.claude/agents/lucy-case-agent.md (workflow)"
      to: "~/.claude/agents/lucy-case-agent.md (Section 3.5)"
      via: "Step 4 references 'see Section 3.5'"
    - from: "CLAUDE.md (CLI Output Reference)"
      to: "lucy detect CLI commands"
      via: "Table rows for detection commands"
---

<objective>
Validate the complete agent update, ensure structural integrity, update CLAUDE.md with detection CLI documentation, and have the user verify the agent knowledge.

Purpose: Plans 39-01 and 39-02 added ~300 lines of detection knowledge to the agent. This plan validates internal consistency (section references, workflow steps), updates CLAUDE.md to reflect new capabilities, and provides a human verification checkpoint.

Output: Structurally validated agent file and updated CLAUDE.md.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-agent-integration/39-01-SUMMARY.md
@.planning/phases/39-agent-integration/39-02-SUMMARY.md
@~/.claude/agents/lucy-case-agent.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate agent file structural integrity and update CLAUDE.md</name>
  <files>~/.claude/agents/lucy-case-agent.md, CLAUDE.md</files>
  <action>
**Part A: Validate agent file structure**

Read the complete agent file and verify these structural requirements:

1. **Section ordering** in `<inlined_critical_knowledge>`:
   - 1. NMR Background
   - 2. Spectral Quality Assessment
   - 3. LSD Command Reference
   - 3.5 Statistical Detection Protocol (NEW from Plan 01)
   - 3.6 Chemistry-First Hierarchy (NEW from Plan 02)
   - 4. Incremental HMBC Strategy
   - 5. CASE Workflow
   - 6. Error Tolerance
   - 7. Confidence Scoring
   - 8. CASE-PROGRESS.md Format

2. **Cross-references** are valid:
   - Workflow Step 4 references "Section 3.5" for detection
   - Pitfall 8 references "Chemistry-First Hierarchy in Section 3.6"
   - Section 3.6 references back to Section 3.5 for detection commands

3. **Workflow section** (`<workflow>`) step numbering is consistent:
   - Step 6 should reference both detection and LSD file building
   - Steps 7-10 (or whatever the final numbering) should be sequentially valid

4. **Pitfall numbering** is sequential: 1, 2, 3, 4, 5, 6, 7, 8, 9

5. **No broken markdown**: Verify no unclosed code blocks, no missing headers, no duplicate section numbers

If any issues found, fix them in the agent file.

**Part B: Update CLAUDE.md with detection commands**

Add detection CLI commands to the two reference tables in CLAUDE.md:

1. **CLI Output Reference table** — add these rows:

| Command | Key Output Fields (JSON) |
|---------|--------------------------|
| `lucy detect hybridisation` | shift_ppm, distributions (state, frequency, count) |
| `lucy detect neighbours` | shift_ppm, constraints (element, type, frequency) |
| `lucy detect hhb` | formula, bond_pairs (pair, frequency, status) |
| `lucy analyze grouping` | groups (shifts, lsd_atom_list), ungrouped |

2. **CLI Syntax Reference** — add a new "### Statistical Detection" subsection after the "### 13C Shift Prediction" subsection, showing:

```markdown
### Statistical Detection

**Hybridisation**
```bash
lucy detect hybridisation 132.5
lucy detect hybridisation 132.5 --format json
```

**Neighbours**
```bash
lucy detect neighbours 170.5
lucy detect neighbours 170.5 --mode relaxed --format json
```

**Hetero-Hetero Bonds**
```bash
lucy detect hhb C13H18O2
lucy detect hhb C13H18O2 --format json
```

**Signal Grouping**
```bash
lucy analyze grouping "130.2,130.4,155.1"
lucy analyze grouping "130.2,130.4,155.1" --format json
```
```

3. **Also add to the "### LSD Integration" section** — after the "Rank solutions" entry, add a note:

```markdown
**Statistical detection (run before writing LSD file)**
```bash
lucy detect hybridisation 132.5 --format json
lucy detect neighbours 170.5 --format json
lucy detect hhb C13H18O2 --format json
lucy analyze grouping "130.2,130.4" --format json
```
```
  </action>
  <verify>
1. Read ~/.claude/agents/lucy-case-agent.md — verify section ordering (1, 2, 3, 3.5, 3.6, 4, 5, 6, 7, 8)
2. Read ~/.claude/agents/lucy-case-agent.md — verify pitfall numbering (1-9 sequential)
3. `grep "Section 3.5" ~/.claude/agents/lucy-case-agent.md` returns at least 1 match (workflow cross-reference)
4. `grep "lucy detect hybridisation" CLAUDE.md` returns >= 2 matches (reference table + syntax)
5. `grep "lucy detect neighbours" CLAUDE.md` returns >= 2 matches
6. `grep "lucy detect hhb" CLAUDE.md` returns >= 2 matches
7. `grep "lucy analyze grouping" CLAUDE.md` returns >= 2 matches
8. `wc -l ~/.claude/agents/lucy-case-agent.md` shows >= 1000 lines
  </verify>
  <done>Agent file passes structural validation with correct section ordering, cross-references, and pitfall numbering. CLAUDE.md documents all detection commands in CLI reference tables.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete statistical detection knowledge update to the CASE agent:

1. **Section 3.5 - Statistical Detection Protocol** (~150 lines): Teaches when to call each of 4 detection commands (selective by shift range), how to interpret results (threshold meanings), how to translate to LSD constraints, and documentation requirements.

2. **Section 3.6 - Chemistry-First Hierarchy** (~100 lines): Evidence priority table (DEPT > HSQC > HMBC > shifts > statistics), conflict resolution decision tree, 3 worked examples (allylic CH2, formula mismatch, peroxide override), threshold override guidelines.

3. **Updated Workflow Step 4**: Now includes statistical detection as explicit sub-step before LSD file writing.

4. **Updated Pitfalls**: Pitfall 6 merged with detection data, new Pitfalls 8 (over-trusting) and 9 (under-using).

5. **Updated CLAUDE.md**: Detection commands in CLI Output Reference and CLI Syntax Reference.
  </what-built>
  <how-to-verify>
1. Review `~/.claude/agents/lucy-case-agent.md`:
   - Skim Section 3.5 (Statistical Detection Protocol) — does the CLI syntax match what you expect?
   - Skim Section 3.6 (Chemistry-First Hierarchy) — does the priority table make chemical sense?
   - Check Workflow Step 4 — does detection come at the right time in the workflow?
   - Check Pitfalls 8-9 — do they cover the right failure modes?

2. Review `CLAUDE.md`:
   - Check CLI Output Reference table — are detection commands listed?
   - Check CLI Syntax Reference — are detection examples correct?

3. Quick validation:
   ```bash
   wc -l ~/.claude/agents/lucy-case-agent.md
   # Should be ~1050-1100 lines

   grep -c "lucy detect" ~/.claude/agents/lucy-case-agent.md
   # Should be 10+ occurrences

   grep -c "Chemistry-First" ~/.claude/agents/lucy-case-agent.md
   # Should be 2+ occurrences (section title + cross-references)
   ```

4. CRITICAL: Does the agent knowledge correctly teach that DEPT always wins over statistics? This is the most important behavioral property.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 39, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Phase 39 is complete when:
1. Agent file contains Section 3.5 (Statistical Detection Protocol) with all 4 CLI commands
2. Agent file contains Section 3.6 (Chemistry-First Hierarchy) with priority table and examples
3. Agent file Workflow Step 4 includes detection sub-step
4. Agent file Pitfalls 6, 8, 9 cover detection usage patterns
5. CLAUDE.md documents all detection CLI commands
6. Agent file line count >= 1000
7. User has approved the knowledge update
</verification>

<success_criteria>
The complete agent knowledge update is structurally valid, documented in CLAUDE.md, and approved by the user. The CASE agent now has full knowledge of statistical detection commands, chemistry-first hierarchy, conflict resolution, and threshold override guidelines.
</success_criteria>

<output>
After completion, create `.planning/phases/39-agent-integration/39-03-SUMMARY.md`
</output>
