---
phase: 63-full-generation-run
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [VAL-04]
user_setup:
  - service: figshare
    why: "Database upload for public distribution"
    env_vars: []
    dashboard_config:
      - task: "Prepare new version of dataset 10.6084/m9.figshare.31073554"
        location: "Figshare Dashboard -> My Data -> lucy-ng-derep.db"

must_haves:
  truths:
    - "coupling_path_stats table is populated with statistics from all 928K compounds"
    - "Generation completed without errors or was successfully resumed to completion"
    - "lucy database info reports millions of coupling_path_stats rows"
  artifacts:
    - path: "data/reference/lucy-ng-derep.db"
      provides: "Populated coupling_path_stats table"
      contains: "coupling_path_stats rows > 1M"
  key_links:
    - from: "lucy database generate-coupling-stats"
      to: "data/reference/lucy-ng-derep.db"
      via: "CLI command populates database"
      pattern: "generate-coupling-stats"
---

<objective>
Run full coupling path statistics generation on all 928K compounds and verify completion.

Purpose: The coupling_path_stats table must be populated with real data from the full compound database before thresholds can be calibrated and ibuprofen 4J cases can be validated.
Output: Populated database ready for analysis.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start full generation run</name>
  <files></files>
  <action>
Run the full coupling path statistics generation on the production database:

```bash
lucy database generate-coupling-stats --db data/reference/lucy-ng-derep.db --fresh
```

If the database already has partial data from a previous attempt, use `--resume` instead of `--fresh`.

This command processes all 928K compounds and is estimated to take 13-26 hours. It checkpoints every 10K compounds, so it can be safely interrupted and resumed.

After starting the command, immediately verify it is making progress by watching the first few checkpoint messages. Then let it run to completion.

Note: This is a long-running process. The executor should start it and then the plan pauses at the checkpoint task below.
  </action>
  <verify>
    <automated>lucy database generate-coupling-stats --db data/reference/lucy-ng-derep.db --resume 2>&1 | head -5</automated>
  </verify>
  <done>Generation command started and producing checkpoint output</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Wait for generation to complete</name>
  <what-built>The generation command has been started. It will process all 928K compounds and populate the coupling_path_stats table. Estimated runtime: 13-26 hours.</what-built>
  <how-to-verify>
    1. Monitor the generation progress (it prints progress every 10K compounds)
    2. If interrupted, resume with: `lucy database generate-coupling-stats --db data/reference/lucy-ng-derep.db --resume`
    3. When complete, verify with: `lucy database info data/reference/lucy-ng-derep.db`
    4. The coupling_path_stats row count should be in the millions
  </how-to-verify>
  <resume-signal>Type "generation complete" with the coupling_path_stats row count from `lucy database info`</resume-signal>
</task>

</tasks>

<verification>
- `lucy database info data/reference/lucy-ng-derep.db` shows coupling_path_stats with millions of rows
- Generation log shows all compounds processed without fatal errors
</verification>

<success_criteria>
- coupling_path_stats table fully populated from all 928K compounds
- Row count is in the millions (expected: 5-15M based on HOSE R2 pair combinations)
- Database file is intact and queryable
</success_criteria>

<output>
After completion, create `.planning/phases/63-full-generation-run/63-01-SUMMARY.md`
</output>
