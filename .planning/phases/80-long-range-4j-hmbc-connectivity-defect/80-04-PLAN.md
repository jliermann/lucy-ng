---
phase: 80-long-range-4j-hmbc-connectivity-defect
plan: "04"
type: execute
wave: 3
depends_on:
  - "80-03"
files_modified: []
autonomous: false
requirements:
  - FIX-07

user_setup:
  - service: CASE blind UAT
    why: "Must be run by a fresh blind Claude instance with no knowledge of CASE9 or CASE1 identity — per feedback_blind_uat. The orchestrating instance is tainted and MUST NOT run this plan."
    env_vars: []
    dashboard_config:
      - task: "Spawn a fresh Claude Code session (new conversation, no memory of this phase)"
        location: "New Claude.app window or 'claude' CLI in a fresh terminal"
      - task: "Provide the CASE9 dataset path (NOT the compound name or expected answer)"
        location: "~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/"
      - task: "After the blind run completes, verify solutions.smi independently via scripts/verify_case_solution.py — DO NOT trust agent self-report"
        location: "Terminal in /Users/steinbeck/Dropbox/develop/lucy-ng"

must_haves:
  truths:
    - "CASE9 blind run: the correct para-benzoate CC(C)OC(=O)c1ccc(C(C)O)cc1 is in the top-3 ranked solutions, verified by scripts/verify_case_solution.py (not agent self-report)"
    - "CASE1 blind run: ibuprofen still found (no regression)"
    - "Both blind runs used lucy lsd run as the primary path (no direct LSD binary bypass)"
    - "Phase-78 AND-gate is re-applied and recorded in 80-UAT-VERDICT.md"
    - "v9.0 milestone ships if and only if both gates pass"
    - "80-ELIM-REGRESSION.md (SC-3 v4.0 regression guard) exists and records PASS before UAT proceeds"
  artifacts:
    - path: ".planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-UAT-VERDICT.md"
      provides: "AND-gate verdict for CASE9 + CASE1 blind runs with RDKit evidence"
      contains: "AND-gate"
    - path: ".planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md"
      provides: "SC-3 regression guard for ibuprofen v4.0 4J case — must be PASS before UAT"
      contains: "PASS"
    - path: "CASE9/analysis/final_results.md"
      provides: "CASE9 blind run results (from fresh instance)"
      min_lines: 10
    - path: "CASE1/analysis/final_results.md"
      provides: "CASE1 blind run results (from fresh instance)"
      min_lines: 10
  key_links:
    - from: "scripts/verify_case_solution.py"
      to: "CASE9/analysis/solutions.smi"
      via: "python scripts/verify_case_solution.py solutions.smi C12H16O3 exits 0"
      pattern: "C12H16O3"
    - from: "scripts/verify_case_solution.py"
      to: "CASE1/analysis/solutions.smi"
      via: "python scripts/verify_case_solution.py solutions.smi C13H18O2 exits 0"
      pattern: "C13H18O2"
    - from: "80-ELIM-REGRESSION.md"
      to: ".planning/phases/72-design-re-validation/experiment/arm_a.lsd"
      via: "ELIM 1 0 applied to arm_a.lsd; ibuprofen aromatic ring in top-3 confirmed"
      pattern: "PASS"
---

<objective>
Wave 3 — Blind UAT gate: the v9.0 milestone-ship gate. A fresh blind Claude instance runs the full CASE workflow on CASE9 (and separately on CASE1) using the Phase-80 mechanism (ELIM iterative escalation), then the orchestrating instance independently verifies the on-disk artifacts via RDKit.

Purpose: This is FIX-07-F and FIX-07-G — the integration gate that proves the entire stack (Python mechanism + agent skill surgery) works end-to-end on real NMR data. Must be run blind per feedback_blind_uat.
Output: 80-UAT-VERDICT.md recording the AND-gate result; v9.0 ships if both CASE9 and CASE1 pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-CONTEXT.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-03-SUMMARY.md

Note: Prior plan SUMMARYs from 80-01 and 80-02 are relevant for verifying the Python mechanism is in place before triggering the blind run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight gate — verify full suite green, mechanism in place, and SC-3 regression guard passed</name>
  <files></files>
  <read_first>
    - .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-01-SUMMARY.md
    - .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-02-SUMMARY.md
    - .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-03-SUMMARY.md
    - .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md
  </read_first>
  <action>
    Run ALL checks before spawning the blind instance. This is a verification-only task — no code is written.

    Automated checks:
    1. pytest (full suite) — must exit 0
    2. python -c "from lucy_ng.lsd.models import LSDProblem; assert LSDProblem().elim_budget == 0; print('elim_budget OK')"
    3. python -c "from lucy_ng.ranking.models import RankedSolution; assert RankedSolution(solution_index=0, smiles='C', mae=0.0, matched_count=1, total_carbons=1, prediction_rate=1.0, assignments=[], has_aromatic_ring=False).is_plausible is True; print('is_plausible OK')"
    4. grep -c "4J Deferral Rule" ~/.claude/agents/lucy-lsd-engineer.md (must return 0)
    5. grep -c "ELIM Escalation" ~/.claude/agents/lucy-lsd-engineer.md (must return >= 1)
    6. grep -E "COSY.*3 3|explicit.*range" ~/.claude/agents/lucy-lsd-engineer.md (must return >= 1 — COSY explicit-range protection present)

    SC-3 regression guard check (v4.0 ibuprofen 4J case):
    7. ls .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md (must exist)
    8. grep -c "PASS" .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md (must return >= 1 — the Arm A + ELIM 1 0 experiment passed)

    If check 7 or 8 fails: STOP. The ibuprofen v4.0 regression guard (ROADMAP SC-3) has not been run or failed. Do NOT spawn the blind UAT instance. Return to 80-03 Task 3 and complete the regression experiment first.

    If checks 1-6 fail: STOP. Fix the failing Wave 1/2 check before proceeding.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest 2>&1 | tail -3 && python -c "from lucy_ng.lsd.models import LSDProblem; assert LSDProblem().elim_budget == 0; print('elim_budget OK')" && ls .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md && grep -c "PASS" .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md</automated>
  </verify>
  <done>pytest exits 0; elim_budget field confirmed; is_plausible field confirmed; 4J Deferral Rule absent; ELIM Escalation section present; COSY explicit-range protection present; 80-ELIM-REGRESSION.md exists and records PASS</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: CASE9 blind UAT — spawn fresh blind instance</name>
  <what-built>The Phase-80 mechanism (elim_budget + plausibility pre-filter + skill surgery + SC-3 regression guard) is complete and all automated checks pass. Now the v9.0 milestone gate must be run blind.</what-built>
  <how-to-verify>
    IMPORTANT: The orchestrating instance that built this plan is TAINTED — it knows CASE9 is 4-(1-hydroxyethyl)benzoic acid isopropyl ester. You MUST spawn a fresh blind instance (new Claude.app window or new `claude` CLI session) with NO knowledge of the compound identity.

    Steps for the fresh blind instance:
    1. Open a new Claude session (new conversation, no memory of Phase 80 or CASE9 identity)
    2. Provide ONLY: the dataset path ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/ and the molecular formula C12H16O3
    3. Run: /lucy-ng:case
    4. Let the agent run to completion (no path-changing interventions)
    5. When the agent reports final results, NOTE the CASE-PROGRESS.md path and solutions.smi path

    After the blind run completes, return to THIS session (the orchestrating tainted instance):
    6. Run independently: python scripts/verify_case_solution.py CASE9/analysis/solutions.smi C12H16O3
    7. Check the exit code (0 = pass, 1 = fail) and the top-3 SMILES reported
    8. Verify independently with RDKit: python -c "from rdkit import Chem; mol = Chem.MolFromSmiles('CC(C)OC(=O)c1ccc(C(C)O)cc1'); print(mol is not None, Chem.rdMolDescriptors.CalcMolFormula(mol))"
    9. Check CASE-PROGRESS.md: confirm lucy lsd run was used as the primary path (no direct LSD binary bypass noted); note what ELIM N was used (if any escalation occurred); confirm COSY equivalence pairs were written as COSY X Y 3 3 (not plain COSY X Y)
    10. Check: was the aromatic ring emergent (via COSY) or forced (via ring-BOND/SKEL)? Emergent = PASS (clean); forced as documented escalation = PASS (conditional); silent forced = FAIL

    Blind run PASS criteria (all must be true):
    - scripts/verify_case_solution.py exits 0 for C12H16O3
    - The correct SMILES CC(C)OC(=O)c1ccc(C(C)O)cc1 (or an RDKit-verified aromatic C12H16O3 isomer) is in top-3
    - No path-changing bypass interventions documented in CASE-PROGRESS.md
    - Aromatic ring: emergent via COSY pairs OR documented BOND escalation (NOT silent ring-BOND/SKEL)
  </how-to-verify>
  <resume-signal>Type "CASE9-PASS" if all criteria met, or "CASE9-FAIL: [describe what failed]" to document the failure and trigger investigation</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: CASE1 regression run — verify ibuprofen still found</name>
  <what-built>CASE9 UAT result recorded. Now verify CASE1 (ibuprofen) does NOT regress under Phase-80 changes.</what-built>
  <how-to-verify>
    Spawn ANOTHER fresh blind instance (or reuse a clean session that has NOT seen the CASE9 run results).

    Steps:
    1. Fresh session with ONLY: dataset path ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/ and formula C13H18O2
    2. Run: /lucy-ng:case
    3. Let agent run to completion

    After the run:
    4. python scripts/verify_case_solution.py CASE1/analysis/solutions.smi C13H18O2
    5. Check CASE-PROGRESS.md: was ELIM escalated above N=0? If ibuprofen is found WITHOUT ELIM (N=0 throughout): IDEAL PASS. If found WITH ELIM N=1: CONDITIONAL PASS (acceptable — the SC-3 Arm A regression experiment (80-ELIM-REGRESSION.md) already confirmed ibuprofen survives ELIM 1 0; the emergent-ring mechanism handles it). If NOT found: REGRESSION FAIL.

    CASE1 PASS criteria:
    - scripts/verify_case_solution.py exits 0 for C13H18O2
    - Ibuprofen or a RDKit-verified aromatic C13H18O2 isomer in top-3
    - No path-changing bypass interventions
    - Aromatic ring emergent (via COSY) — CASE1 was already validated as clean in Phase 77+78
    - If ELIM escalated: consistent with 80-ELIM-REGRESSION.md Arm A result
  </how-to-verify>
  <resume-signal>Type "CASE1-PASS" or "CASE1-FAIL: [description]"</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: AND-gate verdict + write 80-UAT-VERDICT.md</name>
  <what-built>Both blind UAT runs are complete. The orchestrating (tainted) instance writes the verdict document and records the final milestone-ship decision.</what-built>
  <how-to-verify>
    The orchestrating instance (this session) writes .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-UAT-VERDICT.md containing:

    ## Phase 80 Blind UAT Verdict

    **SC-3 regression guard (Arm A + ELIM 1 0):** [see 80-ELIM-REGRESSION.md — PASS/FAIL]

    **CASE9 result:** [PASS/FAIL]
    - scripts/verify_case_solution.py exit code: [0/1]
    - Top-3 SMILES: [list]
    - Correct para-benzoate in top-3: [yes/no]
    - ELIM N used: [0/1/2/3]
    - COSY equivalence pairs: [COSY X Y 3 3 / plain COSY X Y]
    - Ring emergence: [emergent-COSY / documented-BOND / silent-SKEL]
    - Path-changing interventions: [count]

    **CASE1 result:** [PASS/FAIL]
    - scripts/verify_case_solution.py exit code: [0/1]
    - Top-3 SMILES: [list]
    - Ibuprofen found: [yes/no]
    - ELIM N used: [0/1]
    - Ring emergence: [emergent-COSY / documented-BOND]

    **AND-gate: [PASS / FAIL]**
    - Both pass → v9.0 SHIPS. Update ROADMAP.md Phase 80 status to Complete. Update STATE.md.
    - Either fails → v9.0 does NOT ship. Document root cause. Propose follow-up phase.

    **RDKit verification (independent):**
    - CASE9 correct SMILES RDKit parse: [yes/no]
    - CASE9 formula check: [C12H16O3 / other]
    - CASE1 correct SMILES RDKit parse: [yes/no]
    - CASE1 formula check: [C13H18O2 / other]
  </how-to-verify>
  <resume-signal>Type "VERDICT-WRITTEN" after 80-UAT-VERDICT.md is created with the AND-gate result</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan runs existing CLI scripts against NMR data already on-disk. The blind UAT runs are agent-driven local processes with no network exposure.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-04-NA | N/A | Blind UAT execution | accept | Local NMR data processing by agent. No untrusted input, no auth, no network. The ONLY verification concern is trusting agent self-report — mitigated by requiring scripts/verify_case_solution.py to be run independently by the tainted orchestrating instance, not the blind agent. |
</threat_model>

<verification>
The plan is self-verifying through the checkpoint structure. The final automated verification:

```bash
# After AND-gate verdict is written:
cd /Users/steinbeck/Dropbox/develop/lucy-ng
# Verify verdict file exists
ls .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-UAT-VERDICT.md
# Verify regression guard was recorded
grep -c "PASS" .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md
# Verify CASE9 solutions.smi can be independently checked
python scripts/verify_case_solution.py CASE9/analysis/solutions.smi C12H16O3
# Verify CASE1 solutions.smi can be independently checked
python scripts/verify_case_solution.py CASE1/analysis/solutions.smi C13H18O2
```
</verification>

<success_criteria>
- Pre-flight gate: full pytest GREEN; all mechanism checks pass; COSY explicit-range check passes; 80-ELIM-REGRESSION.md exists with PASS verdict (SC-3 ibuprofen v4.0 regression guard cleared)
- CASE9 blind run: correct para-benzoate CC(C)OC(=O)c1ccc(C(C)O)cc1 in top-3; scripts/verify_case_solution.py exits 0; aromatic ring emergent or documented; 0 path-changing bypass interventions
- CASE1 regression run: ibuprofen in top-3; scripts/verify_case_solution.py exits 0; aromatic ring emergent; 0 path-changing bypass interventions
- 80-UAT-VERDICT.md written with RDKit-verified evidence (not agent self-report); SC-3 regression guard result referenced
- AND-gate: PASS = v9.0 ships; FAIL = follow-up phase documented with root cause
- FIX-07-F and FIX-07-G validation rows updated in 80-VALIDATION.md
</success_criteria>

<output>
Create `.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-04-SUMMARY.md` when done.

If AND-gate = PASS: also update ROADMAP.md Phase 80 status to Complete and STATE.md milestone status.
If AND-gate = FAIL: record root cause in 80-04-SUMMARY.md and propose Phase 81.
</output>
