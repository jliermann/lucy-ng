---
phase: 80-long-range-4j-hmbc-connectivity-defect
plan: "02"
type: execute
wave: 1
depends_on:
  - "80-00"
files_modified:
  - src/lucy_ng/ranking/models.py
  - src/lucy_ng/ranking/ranker.py
autonomous: true
requirements:
  - FIX-07

must_haves:
  truths:
    - "Non-aromatic solutions are ranked AFTER plausible solutions when >= 4 experimental shifts are in 110-160 ppm"
    - "Plausible solutions retain the original matched_count desc / MAE asc ordering among themselves"
    - "RankedSolution.is_plausible flag is False for implausible solutions, True otherwise"
    - "Implausible solutions are NOT discarded — they appear in the ranked list after plausible ones"
  artifacts:
    - path: "src/lucy_ng/ranking/models.py"
      provides: "RankedSolution.is_plausible field (bool, default True)"
      contains: "is_plausible"
    - path: "src/lucy_ng/ranking/ranker.py"
      provides: "_is_chemically_plausible() helper + pre-filter in rank() + _calc_dbe helpers"
      contains: "_is_chemically_plausible"
  key_links:
    - from: "src/lucy_ng/ranking/ranker.py::rank()"
      to: "src/lucy_ng/ranking/ranker.py::_is_chemically_plausible()"
      via: "partition before MAE sort"
      pattern: "_is_chemically_plausible"
    - from: "src/lucy_ng/ranking/ranker.py::_calc_dbe_from_formula"
      to: "src/lucy_ng/lsd/generator.py::parse_molecular_formula"
      via: "local import"
      pattern: "parse_molecular_formula"
    - from: "tests/test_ranking.py::TestPlausibilityFilter"
      to: "src/lucy_ng/ranking/models.py::RankedSolution.is_plausible"
      via: "result.solutions[0].is_plausible"
      pattern: "is_plausible"
---

<objective>
Python mechanism Layer 1b: add chemical plausibility pre-filter to the SolutionRanker (D-09).

This plan is parallel to 80-01 — no shared files. It adds:
1. is_plausible field to RankedSolution (Pydantic v2 BaseModel)
2. _is_chemically_plausible() module-level helper + DBE helpers in ranker.py
3. Pre-filter step in rank() that partitions solutions into plausible/implausible before the MAE sort, preserving ranking contract for survivors

Purpose: FIX-07-C and FIX-07-D go GREEN. The ranking contract (signal-match-count desc, MAE asc) is preserved for plausible survivors; implausible solutions trail.
Output: Green TestPlausibilityFilter + TestPlausibilityFilterOrdering; existing ranking tests unaffected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-CONTEXT.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-RESEARCH.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-PATTERNS.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-00-SUMMARY.md

<interfaces>
<!-- Current state of files being modified. Read before editing. -->

From src/lucy_ng/ranking/models.py (lines 59-61, existing bool Field pattern):
```python
has_aromatic_ring: bool = Field(
    default=False, description="Whether the structure contains an aromatic ring"
)
```
RankedSolution inherits from Pydantic v2 BaseModel. Use Field(default=..., description="...").

From src/lucy_ng/ranking/ranker.py (key sections):
- Line 6: from rdkit import Chem  (already imported)
- Lines 85-100: aromatic ring detection block builds RankedSolution with has_aromatic_ring
- Line 104: ranked.sort(key=lambda r: (-r.matched_count, r.mae))  — THIS is the ranking contract
- Lines 110-126: aromatic sanity warning (promote logic to filter)
- rank() signature (line ~70): def rank(self, solutions, experimental_shifts, top_n=None)

From src/lucy_ng/lsd/generator.py (lines 13-29):
- parse_molecular_formula() is a module-level function returning dict[str, int]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_plausible field to RankedSolution</name>
  <files>src/lucy_ng/ranking/models.py</files>
  <read_first>
    - src/lucy_ng/ranking/models.py (full file — it is small; read the RankedSolution class, all existing fields, and confirm Pydantic v2 Field usage)
    - 80-PATTERNS.md Section "src/lucy_ng/ranking/models.py" (exact field definition)
  </read_first>
  <action>
    In the RankedSolution class in src/lucy_ng/ranking/models.py, add a new field immediately after `has_aromatic_ring`:

    ```
    is_plausible: bool = Field(
        default=True,
        description=(
            "False when chemical plausibility pre-filter (D-09) rejected this solution. "
            "Implausible solutions are appended after plausible ones in the ranked list."
        ),
    )
    ```

    Use the Pydantic v2 Field() pattern — same as has_aromatic_ring. Default must be True so existing code that constructs RankedSolution without this field continues to work.
    No other changes to models.py.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && python -c "from lucy_ng.ranking.models import RankedSolution; r = RankedSolution(solution_index=0, smiles='C', mae=0.1, matched_count=1, total_carbons=1, prediction_rate=1.0, assignments=[], has_aromatic_ring=False); print('is_plausible default:', r.is_plausible); assert r.is_plausible is True"</automated>
  </verify>
  <done>RankedSolution.is_plausible field present with default=True; existing RankedSolution instantiation in tests continues to work; no existing tests broken</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _is_chemically_plausible() helper + pre-filter in rank()</name>
  <files>src/lucy_ng/ranking/ranker.py</files>
  <read_first>
    - src/lucy_ng/ranking/ranker.py (full file — read the rank() method, aromatic detection block, existing sort, warning block, and import section)
    - 80-PATTERNS.md Section "src/lucy_ng/ranking/ranker.py" (complete helper functions and pre-filter insertion pattern)
    - 80-RESEARCH.md Q5 (pre-filter implementation and rank() signature change)
  </read_first>
  <behavior>
    - Non-aromatic solution is marked is_plausible=False when >= 4 experimental shifts in 110-160 ppm
    - Aromatic solution is marked is_plausible=True regardless of shift range
    - Non-aromatic solution is PLAUSIBLE (is_plausible=True) when < 4 shifts in 110-160 ppm
    - Plausible solution ranks BEFORE implausible solution even when implausible has lower MAE
    - Among plausible solutions: matched_count desc, MAE asc ordering is preserved
    - Implausible solutions appear at the END of result.solutions (not removed)
    - When formula is provided: DBE deviation > 1 marks solution IMPLAUSIBLE
    - rank() accepts optional formula: str | None = None kwarg (backward compatible)
  </behavior>
  <action>
    Three surgical changes to src/lucy_ng/ranking/ranker.py, in this order:

    1. Add module-level private helpers BEFORE the SolutionRanker class definition:
       - _calc_dbe_from_formula(formula: str) -> float: uses local import of parse_molecular_formula from lucy_ng.lsd.generator; computes DBE = (2C + 2 + N - H - X) / 2 where X = F + Cl + Br + I counts from parse_molecular_formula() result.
       - _calc_dbe_from_mol(mol: "Chem.Mol") -> float: uses local import of rdMolDescriptors from rdkit.Chem; calls rdMolDescriptors.CalcMolFormula(mol) -> formula string -> _calc_dbe_from_formula()
       - _is_chemically_plausible(solution: "RankedSolution", experimental_shifts: list[float], formula: str | None = None) -> bool:
         Check 1: count shifts in 110.0 <= s <= 160.0; if count >= 4 and not solution.has_aromatic_ring: return False
         Check 2: if formula is not None, compute expected DBE via _calc_dbe_from_formula and actual DBE from solution.smiles via _calc_dbe_from_mol; if abs(actual - expected) > 1: return False
         Return True otherwise

    2. Add optional formula parameter to rank() signature:
       def rank(self, solutions, experimental_shifts, top_n=None, formula: str | None = None) -> RankingResult:
       (insert formula as last parameter, default None — backward compatible)

    3. Replace the existing sort line (line ~104: ranked.sort(...)) with the pre-filter + partition:
       - Partition ranked list: plausible = [r for r in ranked if _is_chemically_plausible(r, experimental_shifts, formula)]
       - Partition: implausible = [r for r in ranked if not _is_chemically_plausible(r, experimental_shifts, formula)]
       - Sort plausible by contract: plausible.sort(key=lambda r: (-r.matched_count, r.mae))
       - Mark implausible solutions: use r.model_copy(update={"is_plausible": False}) for each implausible solution (Pydantic v2 immutable update pattern)
       - Combine: ranked = plausible + [r.model_copy(update={"is_plausible": False}) for r in implausible]
       - Update the aromatic sanity warning text to reflect that the pre-filter has acted (change "consider removing HMBC correlations" to neutral observation about ELIM escalation per D-04)

    Critical contract: ELIM handles correlation dropping — do NOT advise removing HMBC correlations in any warning text (D-03/D-04).
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest tests/test_ranking.py::TestPlausibilityFilter tests/test_ranking.py::TestPlausibilityFilterOrdering -x && pytest tests/test_ranking.py -x 2>&1 | tail -5</automated>
  </verify>
  <done>TestPlausibilityFilter: 3/3 GREEN; TestPlausibilityFilterOrdering: 2/2 GREEN; all pre-existing test_ranking.py tests still GREEN; _is_chemically_plausible() function defined at module level in ranker.py; rank() accepts formula kwarg</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This plan modifies pure Python ranking logic that operates on RDKit molecule objects derived from SMILES strings already validated upstream.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-02-NA | N/A | ranking/ranker.py, ranking/models.py | accept | Pure library computation on already-parsed RDKit mol objects and float arrays. No untrusted input crosses a trust boundary. No applicable STRIDE threats. |
</threat_model>

<verification>
Full plan verification:

```bash
cd /Users/steinbeck/Dropbox/develop/lucy-ng
# FIX-07-C: non-aromatic rejected when aromatic expected
pytest tests/test_ranking.py::TestPlausibilityFilter -x
# FIX-07-D: plausible ranks above implausible; survivor ordering preserved
pytest tests/test_ranking.py::TestPlausibilityFilterOrdering -x
# Regression: all pre-existing ranking tests still green
pytest tests/test_ranking.py -x
# Quick combined suite (all three modified test files)
pytest tests/test_lsd_generator.py tests/test_ranking.py tests/test_inventory_schema.py -x 2>&1 | tail -5
```
</verification>

<success_criteria>
- RankedSolution.is_plausible field (bool, default=True) present in ranking/models.py
- _is_chemically_plausible() module-level function in ranker.py checking aromatic ring (check 1) and DBE (check 2)
- rank() signature includes optional formula: str | None = None parameter
- Pre-filter partitions ranked list: plausible first (sorted by contract), implausible appended with is_plausible=False
- Ranking contract for survivors: (-matched_count, mae) sort — UNCHANGED for plausible survivors
- TestPlausibilityFilter: 3/3 GREEN
- TestPlausibilityFilterOrdering: 2/2 GREEN
- All pre-existing test_ranking.py tests GREEN (no regression)
- Full pytest GREEN
</success_criteria>

<output>
Create `.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-02-SUMMARY.md` when done
</output>
