---
phase: 80-long-range-4j-hmbc-connectivity-defect
plan: "03"
type: execute
wave: 2
depends_on:
  - "80-01"
  - "80-02"
files_modified:
  - ~/.claude/agents/lucy-lsd-engineer.md
  - ~/.claude/agents/lucy-devils-advocate.md
  - ~/.claude/agents/lucy-solution-analyst.md
  - ~/.claude/agents/lucy-diagnostic.md
  - ~/.claude/commands/lucy-ng/case.md
  - .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md
autonomous: true
requirements:
  - FIX-07

must_haves:
  truths:
    - "lsd-engineer no longer contains '4J Deferral Rule' section (D-03 retired)"
    - "lsd-engineer no longer routes to lucy pylsd run (D-05)"
    - "lsd-engineer no longer contains '>10 solutions' minimisation stopping condition (D-07)"
    - "lsd-engineer describes ELIM iterative escalation loop (D-02) with stopping rule (D-08)"
    - "lsd-engineer requires aromatic COSY equivalence pairs written as COSY X Y 3 3 (explicit range, ELIM-immune per manual line 521)"
    - "devils-advocate G1/G2/G3/G4/G8 gates removed; new ELIM-budget sanity gate present"
    - "solution-analyst describes post-hoc 4J identification via lucy lsd analyze as explanation only (D-04)"
    - "diagnostic ELIM section updated from 'last resort' to '4J mechanism'"
    - "case.md ELIM-Thrashing pattern updated to detect runaway N escalation (not ELIM as last resort)"
    - "80-ELIM-REGRESSION.md documents ibuprofen Arm A + ELIM 1 0 result: aromatic ring in top-3, solution count and SMILES recorded (SC-3 regression guard for historical v4.0 4J case)"
  artifacts:
    - path: "~/.claude/agents/lucy-lsd-engineer.md"
      provides: "ELIM escalation loop per D-01/D-02/D-07/D-08; no 4J Deferral Rule; no pyLSD routing; elim_budget in inventory; COSY 3 3 explicit-range protection documented"
      contains: "ELIM Escalation"
    - path: "~/.claude/agents/lucy-devils-advocate.md"
      provides: "Removed G1-G4/G8; new ELIM-budget gate (warn on N > 3 without documentation)"
      contains: "ELIM budget"
    - path: "~/.claude/agents/lucy-solution-analyst.md"
      provides: "Post-hoc 4J identification via lucy lsd analyze as explanation only"
      contains: "lucy lsd analyze"
    - path: "~/.claude/agents/lucy-diagnostic.md"
      provides: "ELIM section reframed as 4J mechanism, not last resort"
      contains: "4J tolerance mechanism"
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "ELIM-Thrashing updated to runaway-N detection; nmr-chemist 4J classification item removed"
      contains: "ELIM escalation"
    - path: ".planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md"
      provides: "SC-3 regression guard: ibuprofen Arm A + ELIM 1 0 run results"
      contains: "aromatic"
  key_links:
    - from: "~/.claude/agents/lucy-lsd-engineer.md::ELIM Escalation section"
      to: "src/lucy_ng/lsd/models.py::LSDProblem.elim_budget"
      via: "agent writes ELIM N 0 to LSD file; elim_budget tracks current N in inventory"
      pattern: "elim_budget"
    - from: "~/.claude/agents/lucy-solution-analyst.md"
      to: "src/lucy_ng/cli/lsd.py::lsd_analyze"
      via: "lucy lsd analyze compound.sol compound.lsd --format json for post-hoc path lengths"
      pattern: "lucy lsd analyze"
    - from: ".planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md"
      to: ".planning/phases/72-design-re-validation/experiment/arm_a.lsd"
      via: "arm_a.lsd run with ELIM 1 0 appended; outlsd + lucy lsd rank; result recorded"
      pattern: "arm_a"
---

<objective>
Wave 2 — Three tasks: (1) skill surgery on lsd-engineer.md (with COSY explicit-range protection added), (2) skill surgery on the other four files, (3) the Arm A + ELIM 1 0 ibuprofen regression experiment documenting SC-3.

Purpose: The Python mechanism (Wave 1) alone is not enough — the agents must know how to USE it. This plan surgically edits each skill file and produces the locked SC-3 regression guard artifact that the blind UAT pre-flight requires.
Output: Five skill files updated, grep-verifiable sentinel strings absent/present; 80-ELIM-REGRESSION.md written with live LSD run evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-CONTEXT.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-RESEARCH.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-01-SUMMARY.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgery on lucy-lsd-engineer.md — remove 4J Deferral Rule, pyLSD routing, minimisation reflex; add ELIM escalation loop + COSY explicit-range protection</name>
  <files>~/.claude/agents/lucy-lsd-engineer.md</files>
  <read_first>
    - ~/.claude/agents/lucy-lsd-engineer.md (full file — it is ~615 lines; read once for all anchor texts before editing)
    - 80-RESEARCH.md Q3 Section "lucy-lsd-engineer.md" (exact lines 261-299, 276-277, 285-290, 578-605, 504-525, 347-390, 119-127, 60 — each anchor text to remove/rewrite)
    - 80-RESEARCH.md Pitfall 2 ("COSY equivalence pairs being dropped by ELIM") and Open Question 1 — the explicit justification for COSY X Y 3 3
    - 80-CONTEXT.md Decisions D-01/D-02/D-03/D-05/D-07/D-08 (the replacement content derives directly from these)
  </read_first>
  <action>
    Make the following targeted section edits in order. Read the full file ONCE first; then edit:

    REMOVE — "Section 4J Deferral Rule" (lines ~261-299, heading "### 4J Deferral Rule"):
    Remove the entire section from the heading through its last line. This section contains: aromatic↔aliphatic heuristic, "Never include flagged 4J correlations", "Add to deferred_4j", "Only add as final batch if solutions > 10", and the 4J batch logic. Do not replace with anything at this location — the ELIM escalation goes in a different section below.

    REMOVE — pyLSD routing fallback block (lines ~578-605): The block starting "FALLBACK PATH when K≤3 deferred_4j + pylsd_mode=true" including the pylsd_mode=true inventory check, lucy pylsd run invocation, and pylsd routing documentation. Replace with: "Primary path (single path per D-05): lucy lsd run. Use ELIM escalation if 0 plausible solutions after all HMBC added (see ELIM Escalation below)."

    REMOVE — from "Constraint inventory update procedure" (lines ~504-525): Remove steps 5b ("Set pylsd_mode"), 5c ("Set elim_annotated"), 5d ("If nmr-chemist flagged potential 4J correlations: add to deferred_4j..."). Keep steps 5a and other non-pyLSD steps.
    ADD to inventory procedure: new step "5b. Record elim_budget: integer field (current ELIM N value; 0 when no ELIM command written). Update this value when ELIM is escalated."
    Also remove from the inventory block COMMENT EXAMPLE any lines showing pylsd_mode: true, elim_annotated: true, deferred_4j array. Replace with: elim_budget: 0 (or current N if escalated).

    REMOVE — [ITERATION-COMPLETE] template pyLSD appendix (lines ~368-390): Remove the block beginning "When lucy pylsd run was invoked (pylsd_mode=true)..." including per-permutation table and pylsd_output.json reference. Replace the "4J status:" line with: "ELIM budget: N (N=0 = no ELIM; N>0 = solver dropped correlations from accepted solutions)"

    REWRITE — "Section ELIM Command (LAST RESORT)" (lines ~119-127):
    Remove the "LAST RESORT" framing. Rewrite as:
    "### ELIM Escalation (Phase 80 D-01/D-02)
    ELIM is the native 4J tolerance mechanism. Use iterative escalation:
    0. First: add all MULT + HSQC + heteroatom + DEFF F/FEXP + HMBC (all correlations as standard HMBC X Y, no bond-range extension). Run without ELIM.
    1. If 0 plausible solutions AND all other checks pass (sp2 count, H budget, formula): add ELIM 1 0; re-run.
    2. If still 0: replace with ELIM 2 0; re-run.
    3. If still 0: replace with ELIM 3 0; re-run.
    4. If still 0 at N=3: escalate to diagnostic specialist (constraint conflict, not 4J).
    P2=0 always (no bond-distance limit). Do NOT use explicit bond range HMBC X Y 2 4 (immune to ELIM).
    Stopping rule (D-08): stop at the SMALLEST N that yields >= 1 plausible solution. Do NOT continue escalating. 'Plausible' = has aromatic ring (if >= 4 shifts 110-160 ppm) AND DBE consistent with formula."

    REWRITE — Adaptive Loop stopping conditions (lines ~276-277 and ~285-290):
    Remove: "solution_count <= 10 -> STOP, proceed to ranking"
    Replace: ">= 1 plausible solution (aromatic if >= 4 shifts in 110-160 ppm; DBE consistent) -> STOP at current ELIM N, rank ALL solutions via lucy lsd rank"

    CHANGE — Section 1 HMBC example (line ~60): Remove any HMBC X Y 2 4 example line. Replace with standard HMBC X Y (no bond range), with comment: "; standard 2-3J (ELIM handles 4J drops globally per D-01)"

    ADD — COSY explicit-range protection rule (insert in the ELIM Escalation section OR immediately after the aromatic COSY equivalence pair guidance, whichever appears first in the file):
    "CRITICAL — COSY equivalence pairs MUST use explicit 3-bond range to prevent ELIM from dropping them:
    Write: COSY X Y 3 3   (not plain COSY X Y)
    Rationale: With ELIM N 0 (P2=0, no bond-distance limit), a plain COSY X Y is a candidate for elimination. LSD manual line 521: 'This correlation will never be eliminated if an explicit bond range is given.' Writing COSY 4 7 3 3 locks the pair as ELIM-immune and preserves the aromatic ring emergence mechanism. Plain COSY X Y without an explicit range MAY be dropped by ELIM."
  </action>
  <verify>
    <automated>grep -c "4J Deferral Rule" ~/.claude/agents/lucy-lsd-engineer.md && echo "FAIL - section still present" || echo "PASS - 4J Deferral Rule removed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "4J Deferral Rule" ~/.claude/agents/lucy-lsd-engineer.md returns 0
    - grep -c "pylsd_mode" ~/.claude/agents/lucy-lsd-engineer.md returns 0 (or only in backward-compat historical note if needed)
    - grep -c "deferred_4j" ~/.claude/agents/lucy-lsd-engineer.md returns 0
    - grep -c "lucy pylsd run" ~/.claude/agents/lucy-lsd-engineer.md returns 0
    - grep -c "ELIM Escalation" ~/.claude/agents/lucy-lsd-engineer.md returns >= 1
    - grep -c "elim_budget" ~/.claude/agents/lucy-lsd-engineer.md returns >= 1
    - grep -c "stopping rule\|Stopping rule\|D-08" ~/.claude/agents/lucy-lsd-engineer.md returns >= 1
    - grep -E "COSY.*3 3|explicit.*range|3 3.*ELIM" ~/.claude/agents/lucy-lsd-engineer.md returns >= 1 (COSY explicit-range protection documented)
  </acceptance_criteria>
  <done>All 8 grep acceptance criteria satisfied; file remains syntactically valid markdown</done>
</task>

<task type="auto">
  <name>Task 2: Surgery on devils-advocate, solution-analyst, diagnostic, case.md</name>
  <files>~/.claude/agents/lucy-devils-advocate.md, ~/.claude/agents/lucy-solution-analyst.md, ~/.claude/agents/lucy-diagnostic.md, ~/.claude/commands/lucy-ng/case.md</files>
  <read_first>
    - ~/.claude/agents/lucy-devils-advocate.md (lines 314-490 for Check 4 and G1-G8 gates — read this section carefully before editing)
    - ~/.claude/agents/lucy-solution-analyst.md (lines 121-123 hardcoded 4J removal advisory; line 206 aromatic warning template)
    - ~/.claude/agents/lucy-diagnostic.md (lines 268-301 ELIM section; lines 575-609 Check 1 ELIM Presence)
    - ~/.claude/commands/lucy-ng/case.md (lines 192, 348, 352, 378, 394-395, 411-429, 451, 488 — all 4J/ELIM-related passages)
    - 80-RESEARCH.md Q3 (exact anchor texts and replacement content for each file)
  </read_first>
  <action>
    Four parallel targeted edits — read each file in full, then make the surgical changes:

    DEVILS-ADVOCATE (~/.claude/agents/lucy-devils-advocate.md):

    REMOVE — "Check 4: pyLSD Mode Consistency" section (lines ~314-364): Remove the entire Check 4 block including G1 (FORM consistency), G2 (no bare ELIM in pylsd_mode), G3 (annotation consistency), G4 (K<=3 permutation cap). These gates enforce the retired pyLSD path.

    REMOVE — G5 (perm constraint completeness) and G6 (empty-merge-vs-solncounter) gates from wherever they appear in the post-run checks section (lines ~368+). G5 and G6 are pyLSD permutation-specific.

    REMOVE — G8 (agent reversion detection, lines ~460-478): pylsd_mode=true in inventory triggers this — now dead code.

    ADD — new gate replacing the pyLSD Check 4 block:
    "Check 4: ELIM Budget Sanity (Phase 80)
    Extract elim_budget from the constraint inventory (default 0 if absent).
    G-ELIM-1: If elim_budget > 3 AND no prior [ITERATION-COMPLETE] documents N=1/2/3 attempts: WARNING — 'Excessive ELIM N without documented escalation history. Confirm N=1, N=2, N=3 were tried first.'
    G-ELIM-2: If any HMBC line contains '2 4' (extended bond range) AND ELIM is present: WARNING — 'HMBC with explicit bond range is immune to ELIM. Use plain HMBC X Y (no range) so ELIM can act on it per D-01 Pitfall 1.'
    G-ELIM-3: If ELIM present in first iteration (iteration == 1) AND elim_budget > 0: WARNING — 'ELIM should only be added after all HMBC are included and 0 plausible solutions remain. Escalate per D-02.'
    G-ELIM-4: If any aromatic COSY equivalence pair is written as plain COSY X Y (no explicit range) AND ELIM is present: WARNING — 'Plain COSY X Y is ELIM-droppable. Write COSY X Y 3 3 to protect aromatic equivalence pairs from elimination.'"

    UPDATE — Section 3 ELIM Usage lines (~183-186): Change framing from "ELIM as last resort" to "ELIM is the primary 4J mechanism (D-01). N=1 is expected when 4J is suspected after all HMBC added. Normal to see elim_budget > 0 in later iterations."

    SOLUTION-ANALYST (~/.claude/agents/lucy-solution-analyst.md):

    REPLACE lines ~121-123 (hardcoded 4J removal advisory starting "Identify HMBC correlations between aromatic ring carbon positions...Remove the most suspect ones and re-run LSD"):
    Replace with: "Use 'lucy lsd analyze compound.sol compound.lsd --format json' to identify which correlations have path_length >= 4 in the accepted solution — these are 4J couplings. Report as a plausibility annotation: 'ELIM dropped correlation [atom1-atom2 at shift1-shift2]: consistent with 4J in [inferred geometry].' Do NOT advise removing them — ELIM handles this automatically (D-04). Post-hoc explanation only."

    UPDATE line ~206 (aromatic warning template): Remove any "consider removing HMBC correlations between aromatic and benzylic positions" advisory. Replace with: "Aromatic ring expected based on shifts but absent in plausible solutions. Agent should escalate ELIM N by +1 per D-02 if all HMBC are already included."

    DIAGNOSTIC (~/.claude/agents/lucy-diagnostic.md):

    REWRITE "Section ELIM - Correlation Elimination" (~lines 268-301):
    Remove: "ELIM increases solution space (opposite of normal constraints). ELIM added prematurely → 1000+ solutions. NEVER use ELIM on first LSD run."
    Replace with: "ELIM is the native 4J tolerance mechanism (Phase 80 D-01). 'ELIM N 0' allows the solver to drop up to N HMBC/COSY correlations internally. Expected to produce larger solution sets — discrimination via chemical plausibility pre-filter (D-09) and HOSE-based ranking. Diagnostic use: 'lucy lsd analyze compound.sol compound.lsd --format json' shows path lengths in each accepted solution; correlations with j_coupling >= 4 are the ones ELIM dropped. Escalation ceiling: N=3 (document each escalation in [ITERATION-COMPLETE])."

    REWRITE Check 1 ELIM Presence (~lines 575-609):
    Remove: "FOUND → FAIL" logic (treating ELIM presence as a diagnostic failure).
    Replace: "ELIM expected when zero-solution recovery reached ELIM escalation (all HMBC added, 0 plausible solutions → ELIM 1 0 → ELIM 2 0 → ELIM 3 0). If ELIM N > 3 without documented prior escalation in [ITERATION-COMPLETE]: WARNING. If ELIM present in iteration 1 before all HMBC added: WARNING. If ELIM present and any HMBC line has explicit bond range '2 4': WARNING (immune to ELIM, per Pitfall 1). If COSY equivalence pairs lack explicit 3 3 range and ELIM is present: WARNING (may drop aromatic ring pairs)."

    CASE.MD (~/.claude/commands/lucy-ng/case.md):

    REMOVE line ~192: Remove the peak-picking checklist item "Potential 4J correlations" (nmr-chemist no longer classifies 4J suspects per D-03).

    REWRITE ELIM-Thrashing loop pattern (lines ~411-429 and detection at ~394-395):
    Old: ELIM Thrashing = ELIM being added/removed/re-added as last resort; detection = ELIM command present.
    New: ELIM Thrashing = ELIM being escalated beyond N=3 without plausible solutions (runaway escalation).
    Detection: "elim_budget > 3 in consecutive [ITERATION-COMPLETE] without a plausible solution".
    Advisory: "Runaway ELIM escalation detected. At N=3 with 0 plausible solutions, escalate to diagnostic specialist — this is a constraint conflict, not a 4J issue."

    UPDATE lines ~451, ~488 (zero-solution recovery: "Remove ELIM if present"):
    ELIM is now the final tool (after all HMBC added, escalate). Do NOT remove ELIM from zero-solution recovery. Change to: "ELIM is the final zero-solution recovery mechanism. If ELIM N=3 yields 0 solutions, escalate to diagnostic specialist."
  </action>
  <verify>
    <automated>grep -c "4J Deferral Rule\|pylsd_mode\|deferred_4j\|G1.*FORM\|G2.*bare ELIM\|G4.*Permutation" ~/.claude/agents/lucy-devils-advocate.md && echo "FAIL - retired gates still present" || echo "PASS - retired gates removed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "G1.*FORM\|pyLSD Mode Consistency" ~/.claude/agents/lucy-devils-advocate.md returns 0
    - grep -c "G-ELIM" ~/.claude/agents/lucy-devils-advocate.md returns >= 3 (new gates present)
    - grep -c "4J removal\|Remove the most suspect" ~/.claude/agents/lucy-solution-analyst.md returns 0
    - grep -c "lucy lsd analyze" ~/.claude/agents/lucy-solution-analyst.md returns >= 1
    - grep -c "NEVER use ELIM on first LSD run\|ELIM added prematurely" ~/.claude/agents/lucy-diagnostic.md returns 0
    - grep -c "4J tolerance mechanism" ~/.claude/agents/lucy-diagnostic.md returns >= 1
    - grep -c "Potential 4J correlations" ~/.claude/commands/lucy-ng/case.md returns 0
    - grep -c "runaway escalation\|elim_budget > 3\|ELIM Escalation" ~/.claude/commands/lucy-ng/case.md returns >= 1
  </acceptance_criteria>
  <done>All 8 grep acceptance criteria satisfied; all four files remain syntactically valid markdown</done>
</task>

<task type="auto">
  <name>Task 3: Run Arm A + ELIM 1 0 ibuprofen regression experiment; write 80-ELIM-REGRESSION.md (SC-3)</name>
  <files>.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md</files>
  <read_first>
    - .planning/phases/72-design-re-validation/experiment/arm_a.lsd (read the full file to understand existing constraints — 57 lines, no ELIM present, ibuprofen with full COSY/HMBC but WITHOUT the 3 known 4J correlations; this is the controlled ibuprofen reference that Phase 72 used to prove the emergent-ring hypothesis)
    - 80-RESEARCH.md Q7 (regression guard mechanics — exact experiment prescription)
    - 80-CONTEXT.md D-01/D-02 (ELIM N 0 semantics)
  </read_first>
  <action>
    Run the Arm A ibuprofen regression experiment and record results. This requires the live LSD binary on PATH (confirmed available from Phase-73 verification). Steps:

    1. Create a temporary copy of arm_a.lsd with ELIM 1 0 appended. Do NOT modify the committed arm_a.lsd file. Write the modified copy to a temp location (e.g., /tmp/arm_a_elim1.lsd).

       The modified file is arm_a.lsd content with ONE additional line appended before any FEXP/DEFF lines (i.e., after the last HMBC line and before the DEFF block):
       ELIM 1 0

       Verify COSY lines in arm_a.lsd use plain COSY X Y (no explicit range). If so, document this as a known limitation of the pre-Phase-80 file — the experiment tests whether ELIM 1 drops a COSY; it is acceptable here since the Arm A file is a controlled reference, not a live agent-written file.

    2. Run the experiment:
       cd /tmp && lucy lsd run arm_a_elim1.lsd --format json
       (or directly: lsd arm_a_elim1.lsd && outlsd 5 < arm_a_elim1.sol > arm_a_elim1_solutions.smi)

       If lucy lsd run is not available, use the binary directly:
       lsd /tmp/arm_a_elim1.lsd 2>/tmp/arm_a_stderr.txt
       outlsd 5 < /tmp/arm_a_elim1.sol > /tmp/arm_a_elim1_solutions.smi

    3. Rank the solutions:
       cd /Users/steinbeck/Dropbox/develop/lucy-ng && lucy lsd rank /tmp/arm_a_elim1_solutions.smi --shifts "180.5,156.0,141.9,137.9,129.5,127.3,45.1,45.0,30.5,22.7,22.7,18.3" --format json 2>/tmp/arm_a_rank_stderr.txt

       (Use the ibuprofen 13C shifts from Phase-65 or arm_a_solutions.smi header if available; the exact shifts from the Phase-72 experiment are the authoritative source.)

    4. Verify aromatic ring in top-3 solutions: check using RDKit that at least one top-3 SMILES has an aromatic ring:
       python -c "from rdkit import Chem; smiles = '<top-1 SMILES>'; mol = Chem.MolFromSmiles(smiles); print('aromatic:', any(a.GetIsAromatic() for a in mol.GetAtoms()))"

    5. Write .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md with the following content (fill in actual results):

       # Phase 80 — ELIM 1 0 Regression Guard: Ibuprofen Arm A (SC-3)

       **Run date:** [today]
       **Source file:** .planning/phases/72-design-re-validation/experiment/arm_a.lsd (unchanged; Phase-72 emergent-ring reference)
       **Modification:** ELIM 1 0 appended (solver may drop 1 HMBC/COSY; P2=0 = no bond-distance limit)
       **Purpose:** Confirm that adding ELIM 1 0 to the Phase-72 Arm A ibuprofen LSD file does NOT prevent the correct aromatic ibuprofen structure from appearing in top-3. This locks SC-3: ELIM 1 0 must not regress the v4.0 4J win.

       ## Results

       **LSD run:** [solutions found: N]
       **outlsd conversion:** [SMILES count: N]
       **Top-3 SMILES:**
       1. [SMILES] — aromatic ring: [yes/no] — formula: [C13H18O2/other]
       2. [SMILES] — aromatic ring: [yes/no]
       3. [SMILES] — aromatic ring: [yes/no]

       **Aromatic ring in top-3:** [yes/no]
       **RDKit formula check (top-1):** [C13H18O2/other]

       ## Verdict

       [PASS: ELIM 1 0 does not prevent ibuprofen from appearing; aromatic ring present in top-3]
       OR
       [FAIL: [reason — escalate to investigation before blind UAT]]

       ## Notes on COSY explicit-range gap

       The Phase-72 arm_a.lsd was written before Phase 80's COSY 3 3 requirement. Plain COSY X Y lines are present. If ELIM dropped a COSY pair and the aromatic ring still appears in top-3, this confirms the ring is robust to 1 dropped COSY at N=1. Agent-written LSD files in Phase 80 onwards MUST use COSY X Y 3 3 to fully protect equivalence pairs (per lsd-engineer skill update in 80-03 Task 1).
  </action>
  <verify>
    <automated>ls /Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md && grep -c "aromatic" /Users/steinbeck/Dropbox/develop/lucy-ng/.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - 80-ELIM-REGRESSION.md exists in .planning/phases/80-long-range-4j-hmbc-connectivity-defect/
    - grep -c "aromatic" 80-ELIM-REGRESSION.md returns >= 1
    - grep -c "PASS\|FAIL" 80-ELIM-REGRESSION.md returns >= 1 (verdict recorded)
    - grep -c "Top-3 SMILES" 80-ELIM-REGRESSION.md returns >= 1 (actual results present, not template placeholders)
    - File contains actual SMILES strings (not "[SMILES]" placeholder text)
  </acceptance_criteria>
  <done>80-ELIM-REGRESSION.md written with actual LSD run results; aromatic ring result recorded; PASS/FAIL verdict stated; SC-3 regression guard locked before blind UAT proceeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This plan edits agent skill markdown files and runs the local LSD binary against a committed fixture file.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-03-NA | N/A | Agent skill markdown files + local LSD binary execution | accept | Pure markdown prose edits and local binary execution against a committed fixture. No auth, network, or untrusted input involved. The only risk is incorrect skill guidance — mitigated by explicit grep acceptance_criteria. LSD binary runs on a committed fixture file, not user-supplied input. No applicable STRIDE threats. |
</threat_model>

<verification>
Full plan verification — run all acceptance criteria:

```bash
# lsd-engineer checks
grep -c "4J Deferral Rule" ~/.claude/agents/lucy-lsd-engineer.md
grep -c "pylsd_mode" ~/.claude/agents/lucy-lsd-engineer.md
grep -c "deferred_4j" ~/.claude/agents/lucy-lsd-engineer.md
grep -c "lucy pylsd run" ~/.claude/agents/lucy-lsd-engineer.md
grep -c "ELIM Escalation" ~/.claude/agents/lucy-lsd-engineer.md
grep -c "elim_budget" ~/.claude/agents/lucy-lsd-engineer.md
grep -E "COSY.*3 3|explicit.*range" ~/.claude/agents/lucy-lsd-engineer.md

# devils-advocate checks
grep -c "pyLSD Mode Consistency" ~/.claude/agents/lucy-devils-advocate.md
grep -c "G-ELIM" ~/.claude/agents/lucy-devils-advocate.md

# solution-analyst checks
grep -c "Remove the most suspect" ~/.claude/agents/lucy-solution-analyst.md
grep -c "lucy lsd analyze" ~/.claude/agents/lucy-solution-analyst.md

# diagnostic checks
grep -c "NEVER use ELIM on first LSD run" ~/.claude/agents/lucy-diagnostic.md
grep -c "4J tolerance mechanism" ~/.claude/agents/lucy-diagnostic.md

# case.md checks
grep -c "Potential 4J correlations" ~/.claude/commands/lucy-ng/case.md
grep -c "runaway escalation" ~/.claude/commands/lucy-ng/case.md

# SC-3 regression guard
ls .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md
grep -c "PASS\|FAIL" .planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-ELIM-REGRESSION.md

# Full Python suite still green after Wave 1+2
cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest 2>&1 | tail -5
```

Expected: all retired strings return 0; all new strings return >= 1; regression file exists with verdict; pytest GREEN.
</verification>

<success_criteria>
- lsd-engineer.md: 4J Deferral Rule removed; pylsd_mode/deferred_4j/lucy pylsd run removed; ELIM escalation loop present; elim_budget in inventory template; D-08 stopping rule present; COSY X Y 3 3 explicit-range protection documented
- devils-advocate.md: G1-G4/G8 (pyLSD mode gates) removed; new G-ELIM-1/G-ELIM-2/G-ELIM-3/G-ELIM-4 gates present; ELIM section reframed as 4J mechanism
- solution-analyst.md: 4J removal advisory removed; post-hoc lucy lsd analyze framing present (D-04)
- diagnostic.md: "NEVER use ELIM on first LSD run" removed; "4J tolerance mechanism" description present; Check 1 updated with COSY 3 3 warning
- case.md: "Potential 4J correlations" nmr-chemist item removed; ELIM-Thrashing updated to runaway-N detection
- 80-ELIM-REGRESSION.md: written with actual LSD results; aromatic ring in top-3 confirmed; SC-3 verdict recorded
- Full pytest GREEN (no Python regression from skill edits)
</success_criteria>

<output>
Create `.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-03-SUMMARY.md` when done
</output>
