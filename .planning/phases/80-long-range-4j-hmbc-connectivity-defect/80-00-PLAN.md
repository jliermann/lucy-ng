---
phase: 80-long-range-4j-hmbc-connectivity-defect
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_lsd_generator.py
  - tests/test_ranking.py
  - tests/test_inventory_schema.py
autonomous: true
requirements:
  - FIX-07

must_haves:
  truths:
    - "Three new test classes exist and FAIL before any implementation: TestElimBudget, TestPlausibilityFilter, TestPlausibilityFilterOrdering, TestSchemaV2Phase80"
    - "Running the quick test suite returns RED on all four new classes and GREEN on all existing tests"
  artifacts:
    - path: "tests/test_lsd_generator.py"
      provides: "TestElimBudget class (6 failing test methods)"
      contains: "class TestElimBudget"
    - path: "tests/test_ranking.py"
      provides: "TestPlausibilityFilter and TestPlausibilityFilterOrdering classes (5 failing test methods)"
      contains: "class TestPlausibilityFilter"
    - path: "tests/test_inventory_schema.py"
      provides: "TestSchemaV2Phase80 class (6 failing test methods)"
      contains: "class TestSchemaV2Phase80"
  key_links:
    - from: "tests/test_lsd_generator.py::TestElimBudget"
      to: "src/lucy_ng/lsd/models.py::LSDProblem.elim_budget"
      via: "field attribute access"
      pattern: "elim_budget"
    - from: "tests/test_ranking.py::TestPlausibilityFilter"
      to: "src/lucy_ng/ranking/ranker.py::_is_chemically_plausible"
      via: "ranker.rank() returning is_plausible flag"
      pattern: "is_plausible"
    - from: "tests/test_inventory_schema.py::TestSchemaV2Phase80"
      to: "src/lucy_ng/data/schemas/constraint_inventory_v2.json"
      via: "Draft202012Validator schema"
      pattern: "pylsd_mode.*required"
---

<objective>
Wave 0 foundation: write all failing test stubs before any production code changes.

Purpose: Establish the RED baseline (Nyquist rule — every verify must have an automated command; new test classes must fail before implementation). These four test classes cover FIX-07-A through FIX-07-E from 80-VALIDATION.md.
Output: Failing tests committed; Wave 1 plans (80-01 and 80-02) can now implement against a known test contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-CONTEXT.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-RESEARCH.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-PATTERNS.md
@.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-VALIDATION.md

<interfaces>
<!-- Key patterns extracted from existing test files. Read analogs before writing stubs. -->

From tests/test_lsd_generator.py (analog: TestLSDInputGeneratorEmit + test_generate_pylsd_elim_commands):
- Import block (lines 1-13): imports LSDProblem, LSDAtom, Hybridization, LSDInputGenerator
- test_generate_pylsd_elim_commands (lines 463-477): construct LSDProblem(...), call LSDInputGenerator.generate(problem), assert string in content
- Pattern: LSDProblem(name="test", elim_budget=N) — this field does NOT exist yet; test will fail with AttributeError

From tests/test_ranking.py (analog: TestSolutionRankerRank + TestTwoTierRanking):
- Import block (lines 1-13): MagicMock, C13Predictor, LSDSolution, SolutionRanker, RankedSolution
- Fixture: MagicMock(spec=C13Predictor); mock_predictor.predict_from_smiles.side_effect = fn
- Pattern: LSDSolution(index=N, smiles="..."); ranker.rank(solutions, experimental)
- result.solutions[0].is_plausible — this field does NOT exist yet; test will fail with AttributeError

From tests/test_inventory_schema.py (analog: TestSchemaValidation lines 123-199):
- Helpers: _load_schema(), _minimal_valid_v2(), Draft202012Validator
- Pattern: del instance["field"]; list(validator.iter_errors(instance)); assert errors == []
- Current "required" list includes "pylsd_mode", "elim_annotated", "deferred_4j" — tests must FAIL until schema is updated in plan 80-01
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestElimBudget class to test_lsd_generator.py</name>
  <files>tests/test_lsd_generator.py</files>
  <read_first>
    - tests/test_lsd_generator.py (read lines 1-20 for imports, lines 455-490 for test_generate_pylsd_elim_commands analog shape)
    - 80-PATTERNS.md Section "tests/test_lsd_generator.py::TestElimBudget" (complete class definition with 6 methods)
  </read_first>
  <action>
    Append class TestElimBudget after the last class in tests/test_lsd_generator.py. Copy the exact 6-method class from 80-PATTERNS.md Section "tests/test_lsd_generator.py::TestElimBudget":

    Methods (all must be present):
    - test_elim_budget_zero_emits_no_elim: constructs LSDProblem(name="test", elim_budget=0), generates content, asserts "ELIM" not in content
    - test_elim_budget_one_emits_elim_1_0: asserts "ELIM 1 0" in content for elim_budget=1
    - test_elim_budget_three_emits_elim_3_0: asserts "ELIM 3 0" in content for elim_budget=3
    - test_elim_budget_appears_before_mult: asserts content.index("ELIM 1 0") < content.index("MULT")
    - test_elim_budget_independent_of_pylsd_mode: LSDProblem(name="test", elim_budget=2, pylsd_mode=False) emits "ELIM 2 0"
    - test_elim_budget_default_is_zero: LSDProblem() default elim_budget == 0

    These tests MUST fail now because LSDProblem has no elim_budget field yet.
    Do NOT add any implementation. Do NOT import anything new — existing imports cover LSDProblem and LSDInputGenerator.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest tests/test_lsd_generator.py::TestElimBudget -x 2>&1 | tail -5</automated>
  </verify>
  <done>TestElimBudget class appended to test_lsd_generator.py; pytest reports ERROR or FAILED (not "no tests ran"); all existing tests in that file still pass when run without ::TestElimBudget filter</done>
</task>

<task type="auto">
  <name>Task 2: Add TestPlausibilityFilter and TestPlausibilityFilterOrdering to test_ranking.py</name>
  <files>tests/test_ranking.py</files>
  <read_first>
    - tests/test_ranking.py (read lines 1-40 for imports and make_prediction_result helper; lines 346-430 for TestSolutionRankerRank fixture pattern; lines 475-509 for TestTwoTierRanking ordering pattern)
    - 80-PATTERNS.md Sections "TestPlausibilityFilter" and "TestPlausibilityFilterOrdering" (complete class definitions)
  </read_first>
  <action>
    Append two classes after the last class in tests/test_ranking.py:

    Class 1 — TestPlausibilityFilter (3 methods):
    - test_non_aromatic_rejected_when_aromatic_expected: experimental has 5 shifts in 110-160 ppm; solution smiles="C1CCCCC1C(=O)O" (no aromatic ring); asserts result.solutions[0].is_plausible is False
    - test_aromatic_retained_when_aromatic_expected: solution smiles="CC(C)Cc1ccc(cc1)C(C)C(=O)O" (ibuprofen, has aromatic ring); asserts is_plausible is True
    - test_non_aromatic_ok_when_no_aromatic_shifts: experimental = [45.0, 30.0, 22.0] (no aromatic range); asserts is_plausible is True

    Class 2 — TestPlausibilityFilterOrdering (2 methods):
    - test_plausible_ranks_above_implausible: two solutions where non-aromatic has lower MAE but aromatic expected (5 shifts in 110-160 ppm); assert result.solutions[0].smiles == "AROMATIC" (note: need a valid aromatic SMILES for RDKit — use "c1ccccc1" for "AROMATIC" side-effect routing, but the actual SMILES passed to ranker must be valid; use dispatch by index NOT by SMILES string for mock side_effect since mock is called with the actual SMILES)
    - test_survivor_ordering_preserved: two aromatic solutions; higher matched_count ranks first

    IMPORTANT for mock dispatch: use smiles strings that RDKit can parse as aromatic/non-aromatic:
    - Use "c1ccccc1" (benzene) for the aromatic solution
    - Use "C1CCCCC1" (cyclohexane) for non-aromatic
    Dispatch in mock side_effect: check if "c1ccc" in smiles (aromatic) or smiles == "C1CCCCC1" (non-aromatic).

    Use make_prediction_result() helper already in the file. Existing imports cover everything.
    is_plausible field does NOT exist on RankedSolution yet — tests will fail with AttributeError.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest tests/test_ranking.py::TestPlausibilityFilter tests/test_ranking.py::TestPlausibilityFilterOrdering -x 2>&1 | tail -5</automated>
  </verify>
  <done>Both classes appended; pytest reports ERROR or FAILED on the new classes; all existing test_ranking.py tests still pass</done>
</task>

<task type="auto">
  <name>Task 3: Add TestSchemaV2Phase80 to test_inventory_schema.py</name>
  <files>tests/test_inventory_schema.py</files>
  <read_first>
    - tests/test_inventory_schema.py (read lines 1-60 for imports, _load_schema(), _minimal_valid_v2(), Draft202012Validator; lines 123-199 for TestSchemaValidation class structure)
    - 80-PATTERNS.md Section "tests/test_inventory_schema.py::TestSchemaV2Phase80" (complete 6-method class)
  </read_first>
  <action>
    Append class TestSchemaV2Phase80 after the last class in tests/test_inventory_schema.py.

    6 methods (exact names from 80-PATTERNS.md):
    - test_accepts_inventory_without_pylsd_mode: del instance["pylsd_mode"]; assert errors == []
    - test_accepts_inventory_without_elim_annotated: del instance["elim_annotated"]; assert errors == []
    - test_accepts_inventory_without_deferred_4j: del instance["deferred_4j"]; assert errors == []
    - test_accepts_inventory_with_elim_budget: remove all three retired fields; add instance["elim_budget"] = 1; assert errors == []
    - test_rejects_negative_elim_budget: instance["elim_budget"] = -1; assert len(errors) >= 1
    - test_schema_required_excludes_retired_pyLSD_fields: assert none of ("pylsd_mode", "elim_annotated", "deferred_4j") in schema["required"]

    These tests MUST fail now because the schema still has pylsd_mode/elim_annotated/deferred_4j in "required".
    test_rejects_negative_elim_budget will also fail (field not yet in schema, so negative value is not validated).
    No new imports needed — reuse _load_schema(), _minimal_valid_v2(), Draft202012Validator from existing file.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && pytest tests/test_inventory_schema.py::TestSchemaV2Phase80 -x 2>&1 | tail -5</automated>
  </verify>
  <done>TestSchemaV2Phase80 appended; pytest reports FAILED on new class; existing TestSchemaValidation tests still pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries relevant to this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-00-NA | N/A | test files only | accept | This plan writes test stubs to test files. No auth, network, crypto, or untrusted input is involved. No threats applicable. |
</threat_model>

<verification>
Full Wave 0 verification:

```bash
cd /Users/steinbeck/Dropbox/develop/lucy-ng
# All three new test classes must be present and RED (fail before implementation):
pytest tests/test_lsd_generator.py::TestElimBudget -x 2>&1 | tail -3
pytest tests/test_ranking.py::TestPlausibilityFilter -x 2>&1 | tail -3
pytest tests/test_ranking.py::TestPlausibilityFilterOrdering -x 2>&1 | tail -3
pytest tests/test_inventory_schema.py::TestSchemaV2Phase80 -x 2>&1 | tail -3
# All existing tests must still pass:
pytest tests/test_lsd_generator.py tests/test_ranking.py tests/test_inventory_schema.py -x 2>&1 | tail -5
```

Expected: new classes FAIL/ERROR; existing tests GREEN.
</verification>

<success_criteria>
- TestElimBudget (6 methods) appended to tests/test_lsd_generator.py
- TestPlausibilityFilter (3 methods) + TestPlausibilityFilterOrdering (2 methods) appended to tests/test_ranking.py
- TestSchemaV2Phase80 (6 methods) appended to tests/test_inventory_schema.py
- All new classes FAIL before implementation (RED baseline confirmed)
- All previously passing tests still pass (no regression from stub addition)
- Wave 0 complete: FIX-07-A/B/C/D/E validation rows are now testable
</success_criteria>

<output>
Create `.planning/phases/80-long-range-4j-hmbc-connectivity-defect/80-00-SUMMARY.md` when done
</output>
