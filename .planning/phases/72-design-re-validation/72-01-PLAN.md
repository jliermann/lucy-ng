---
phase: 72-design-re-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/72-design-re-validation/experiment/arm_a.lsd
  - .planning/phases/72-design-re-validation/experiment/arm_b.lsd
  - .planning/phases/72-design-re-validation/experiment/arm_c.lsd
  - .planning/phases/72-design-re-validation/experiment/run_experiment.py
  - .planning/phases/72-design-re-validation/experiment/results.json
autonomous: true
requirements:
  - DESIGN-01

must_haves:
  truths:
    - "D-01: Arm C (HMBC X Y 2 4 for 3 known 4J suspects) runs end-to-end and its aromatic/ibuprofen result is recorded"
    - "D-04: Arm A (no SKEL, full native constraints) runs end-to-end and the aromatic-ring presence is definitively determined by RDKit"
    - "D-05: Three arm files are built from the known-working iter3 baseline with only the specified modifications"
    - "The experiment script uses the CORRECT outlsd invocation (outlsd 5 < compound.sol, NOT the runner.py anti-pattern)"
    - "results.json exists and contains per-arm solution_count, any_aromatic, and ibuprofen_found fields"
  artifacts:
    - path: ".planning/phases/72-design-re-validation/experiment/arm_a.lsd"
      provides: "Emergent-test LSD file: iter3 baseline minus SKEL/PATH/F3"
      contains: "FEXP \"NOT F1 AND NOT F2\""
    - path: ".planning/phases/72-design-re-validation/experiment/arm_b.lsd"
      provides: "Forced-comparison LSD file: verbatim iter3 baseline"
      contains: "SKEL F3"
    - path: ".planning/phases/72-design-re-validation/experiment/arm_c.lsd"
      provides: "Bond-range 4J test: arm_a + HMBC 3 8 2 4 / HMBC 3 13 2 4 / HMBC 3 9 2 4"
      contains: "HMBC 3 8 2 4"
    - path: ".planning/phases/72-design-re-validation/experiment/run_experiment.py"
      provides: "Experiment runner: LSD + outlsd + RDKit aromatic/InChI check per arm"
      exports: ["main"]
    - path: ".planning/phases/72-design-re-validation/experiment/results.json"
      provides: "Machine-readable experiment results per arm"
      contains: "arm_a"
  key_links:
    - from: "run_experiment.py"
      to: "outlsd binary"
      via: "subprocess([outlsd_path, '5'], stdin=sol_file.open())"
      pattern: "\"5\""
    - from: "arm_a.lsd"
      to: "/Users/steinbeck/Dropbox/develop/LSD/Filters/ring3"
      via: "DEFF F1 absolute path"
      pattern: "DEFF F1"
    - from: "run_experiment.py"
      to: "rdkit Chem"
      via: "GetRingInfo().AtomRings()"
      pattern: "GetRingInfo"
---

<objective>
Build three controlled LSD arm files from the known-working iteration_03 baseline, run them via the correct direct LSD binary + outlsd invocation, and record per-arm results (solution count, aromatic ring presence, ibuprofen InChI key match) in a JSON results file. This is the empirical evidence that settles D-01 (bond-range mechanism) and D-04 (emergent vs forced aromatic ring).

Purpose: The v8.0 UAT evidence was confounded — iteration_02 ran without SYME/DEFF NOT (constraint loss bug), so we could not tell whether the ring failed to emerge because of missing constraints or because it genuinely needs forcing. This experiment controls for that confound by using a hand-crafted native-only file.

Output: Three arm files + run_experiment.py + results.json under .planning/phases/72-design-re-validation/experiment/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-design-re-validation/72-CONTEXT.md
@.planning/phases/72-design-re-validation/72-RESEARCH.md
@.planning/phases/72-design-re-validation/72-PATTERNS.md

<interfaces>
<!-- Locked decisions from CONTEXT.md that this plan implements -->
<!-- D-01: HMBC X Y 2 4 is PRIMARY 4J mechanism (Arm C tests this) -->
<!-- D-04: Test first, force only if needed (Arm A is the test) -->
<!-- D-05: The 3-arm experiment specification -->

<!-- The starting file (verified working, 2 aromatic solutions):
/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/iteration_03/compound_native.lsd -->

<!-- Correct outlsd invocation (from orchestrator.py lines 281-291, NOT runner.py):
subprocess.run([str(outlsd_path), "5"], stdin=sol_file.open(), capture_output=True, text=True, cwd=experiment_dir)
The "5" argument is SMILES output mode. Runner.py omits "5" AND passes the wrong file — do not copy runner.py. -->

<!-- LSD binary locations:
LSD:    /Users/steinbeck/Dropbox/develop/LSD/lsd
outlsd: /Users/steinbeck/Dropbox/develop/LSD/outlsd -->

<!-- Filter file absolute paths (must be used verbatim in DEFF commands):
ring3: /Users/steinbeck/Dropbox/develop/LSD/Filters/ring3
ring4: /Users/steinbeck/Dropbox/develop/LSD/Filters/ring4 -->

<!-- Benzene mol file (Arm B SKEL reference):
/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/iteration_03/filters/benzene.mol
Arm B PATH directive: "/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/iteration_03/filters" -->

<!-- Ibuprofen InChI key: HEFNNWSXXWATRW-UHFFFAOYSA-N -->

<!-- RDKit aromatic check (from PATTERNS.md):
rings = mol.GetRingInfo().AtomRings()
any(len(ring)==6 and all(mol.GetAtomWithIdx(a).GetIsAromatic() for a in ring) for ring in rings) -->

<!-- LSD writes .sol to CWD — must set cwd=experiment_dir in subprocess.run -->
<!-- LSD writes solution count to "solncounter" file in CWD -->
<!-- After LSD runs, the default output file is named after the input, e.g. arm_a.sol
     (BUT: LSD stdin mode writes "compound.sol" — rename to arm-specific name after run) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build three arm LSD files from the iter3 baseline</name>
  <files>
    .planning/phases/72-design-re-validation/experiment/arm_a.lsd
    .planning/phases/72-design-re-validation/experiment/arm_b.lsd
    .planning/phases/72-design-re-validation/experiment/arm_c.lsd
  </files>

  <read_first>
    Read the FULL content of the iter3 baseline before writing any arm file:
    /Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/iteration_03/compound_native.lsd
    (This is the 61-line native file that produced 2 aromatic solutions including ibuprofen. Verified in RESEARCH.md.)
  </read_first>

  <action>
    Create three LSD files in .planning/phases/72-design-re-validation/experiment/:

    ARM A — arm_a.lsd (per D-04: emergent test, no SKEL):
    Start from verbatim iter3 content. Replace the comment header with:
      ; arm_a.lsd -- Emergent test: no SKEL benzene, full constraints preserved
      ; D-04 question: does aromatic ring emerge from COSY/HMBC constraints alone?
      ; Derived from iteration_03/compound_native.lsd by removing SKEL/PATH/F3 from FEXP
    Remove these two lines entirely:
      PATH "/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/iteration_03/filters"
      SKEL F3 "benzene"
    Change FEXP from:
      FEXP "NOT F1 AND NOT F2 AND F3"
    To:
      FEXP "NOT F1 AND NOT F2"
    Everything else is verbatim from the baseline (MULT, HSQC, BOND, COSY, HMBC, DEFF F1/F2 lines — unchanged).

    ARM B — arm_b.lsd (per D-05 optional comparison: working baseline):
    Verbatim copy of iteration_03/compound_native.lsd. Replace only the comment header:
      ; arm_b.lsd -- Forced comparison: SKEL benzene present (baseline, 2-solution reference)
      ; Expected: 2 solutions (para-ibuprofen + ortho-isomer). If count != 2, environment issue.
      ; Verbatim copy of iteration_03/compound_native.lsd
    All constraint lines, DEFF, PATH, SKEL, FEXP lines unchanged.

    ARM C — arm_c.lsd (per D-01: bond-range 4J test):
    Start from arm_a.lsd content. Replace the comment header with:
      ; arm_c.lsd -- Bond-range 4J test: arm_a base + HMBC X Y 2 4 for 3 known 4J suspects
      ; D-01 validation: does extended bond range keep solutions? Does aromatic ring still emerge?
      ; Derived from arm_a.lsd + 3 HMBC X Y 2 4 lines for 4J W-path correlations
    After the last HMBC line of arm_a (the block ends with "HMBC 2 4"), append exactly:
      HMBC 3 8 2 4    ; Cq(136.96) -- CH2(45.03), 4J W-path through aromatic ring (D-01 test)
      HMBC 3 13 2 4   ; Cq(136.96) -- CH3(18.09), 4J W-path
      HMBC 3 9 2 4    ; Cq(136.96) -- CH(44.90), 4J W-path (removed in iter3; re-added as extended range)
    Remainder (DEFF/FEXP) unchanged from arm_a.

    Verification of arm files before proceeding:
    - arm_a.lsd must NOT contain "SKEL" or "PATH" → grep -c "SKEL" arm_a.lsd returns 0
    - arm_a.lsd must NOT contain "F3" in the FEXP line → the FEXP line is exactly: FEXP "NOT F1 AND NOT F2"
    - arm_a.lsd MUST contain DEFF F1 and DEFF F2 lines → grep -c "DEFF" arm_a.lsd returns 2
    - arm_b.lsd must contain "SKEL F3" and PATH line
    - arm_c.lsd must contain "HMBC 3 8 2 4"
    - arm_c.lsd must NOT contain SKEL (inherits arm_a base, not arm_b)
  </action>

  <verify>
    <automated>
      grep -c "SKEL" .planning/phases/72-design-re-validation/experiment/arm_a.lsd
      # expected: 0
      grep "FEXP" .planning/phases/72-design-re-validation/experiment/arm_a.lsd
      # expected: FEXP "NOT F1 AND NOT F2"
      grep -c "DEFF" .planning/phases/72-design-re-validation/experiment/arm_a.lsd
      # expected: 2
      grep -c "SKEL" .planning/phases/72-design-re-validation/experiment/arm_b.lsd
      # expected: 1
      grep -c "HMBC 3 8 2 4" .planning/phases/72-design-re-validation/experiment/arm_c.lsd
      # expected: 1
      grep -c "SKEL" .planning/phases/72-design-re-validation/experiment/arm_c.lsd
      # expected: 0
    </automated>
  </verify>

  <done>
    Three arm files exist under .planning/phases/72-design-re-validation/experiment/.
    arm_a.lsd: contains FEXP "NOT F1 AND NOT F2", 0 SKEL lines, 2 DEFF lines.
    arm_b.lsd: verbatim iter3 content (contains SKEL F3, PATH, FEXP with F3).
    arm_c.lsd: arm_a base + 3 HMBC X Y 2 4 lines appended, 0 SKEL lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write run_experiment.py, execute all arms, record results.json</name>
  <files>
    .planning/phases/72-design-re-validation/experiment/run_experiment.py
    .planning/phases/72-design-re-validation/experiment/results.json
  </files>

  <read_first>
    Read these two files to extract the outlsd and RDKit patterns to use:
    - src/lucy_ng/lsd/orchestrator.py (lines 255-295: the correct _run_outlsd method)
    - tests/test_lsd_regression.py (lines 52-78: SMILES file parsing + InChI key extraction)
    Also read the PATTERNS.md section "run_experiment.py" for the confirmed code structure.
  </read_first>

  <action>
    Write .planning/phases/72-design-re-validation/experiment/run_experiment.py as a self-contained
    throwaway Python script. The script must:

    1. SKIP if LSD binary is missing (skipif guard at top):
       LSD_BIN = Path("/Users/steinbeck/Dropbox/develop/LSD/lsd")
       OUTLSD_BIN = Path("/Users/steinbeck/Dropbox/develop/LSD/outlsd")
       if not LSD_BIN.exists() or not OUTLSD_BIN.exists():
           print("SKIP: LSD binary not found at expected location")
           sys.exit(0)

    2. Define EXPERIMENT_DIR = Path(__file__).parent (absolute, so .sol files land correctly).

    3. For each arm in ["arm_a", "arm_b", "arm_c"], implement run_arm(arm_name):
       a. LSD invocation — MUST set cwd=EXPERIMENT_DIR so .sol lands there:
          subprocess.run([str(LSD_BIN)], stdin=open(lsd_file), capture_output=True, text=True, cwd=EXPERIMENT_DIR)
       b. Read solncounter from EXPERIMENT_DIR/solncounter (LSD writes it there).
       c. Rename EXPERIMENT_DIR/"compound.sol" to EXPERIMENT_DIR/f"{arm_name}.sol" (LSD stdin mode uses "compound" as stem).
          If compound.sol does not exist, solution_count is 0.
       d. outlsd invocation — THE CORRECT PATTERN (per orchestrator.py, NOT runner.py):
          subprocess.run([str(OUTLSD_BIN), "5"], stdin=open(sol_file), capture_output=True, text=True, cwd=EXPERIMENT_DIR)
          Write stdout to f"{arm_name}_solutions.smi".
          The "5" argument is mandatory (SMILES mode). DO NOT omit it. DO NOT feed the .lsd file as stdin.

    4. RDKit aromatic check — use exactly this implementation (from RESEARCH.md and PATTERNS.md):
       from rdkit import Chem
       from rdkit.Chem.inchi import MolToInchi, InchiToInchiKey
       IBUPROFEN_INCHI_KEY = "HEFNNWSXXWATRW-UHFFFAOYSA-N"

       def has_aromatic_ring(smiles):
           mol = Chem.MolFromSmiles(smiles)
           if mol is None: return False
           rings = mol.GetRingInfo().AtomRings()
           return any(len(r)==6 and all(mol.GetAtomWithIdx(a).GetIsAromatic() for a in r) for r in rings)

       def smiles_to_inchi_key(smiles):
           mol = Chem.MolFromSmiles(smiles)
           if mol is None: return None
           inchi = MolToInchi(mol)
           return InchiToInchiKey(inchi) if inchi else None

    5. Parse SMILES file: take first whitespace-delimited field per non-empty line (pattern from
       test_lsd_regression.py). Skip lines where Chem.MolFromSmiles returns None.

    6. Build per-arm result dict:
       {"arm": arm_name, "solution_count": N, "any_aromatic": bool, "ibuprofen_found": bool,
        "aromatic_count": int, "solutions": [{"smiles":..., "aromatic_ring":..., "inchi_key":...}]}

    7. After all three arms, write results as JSON to EXPERIMENT_DIR/results.json.
       Also print a human-readable summary table to stdout:
         Arm | Solutions | Aromatic? | Ibuprofen?
         A   | N         | Y/N       | Y/N
         B   | N         | Y/N       | Y/N
         C   | N         | Y/N       | Y/N

    8. Run the script immediately after writing it:
       cd .planning/phases/72-design-re-validation/experiment && python3 run_experiment.py
       (Run from the experiment directory so relative imports work and .sol files land correctly.)

    CRITICAL — antipatterns to avoid in this script:
    - Never pass a .lsd file to outlsd stdin (that is the runner.py bug)
    - Never omit the "5" argument to outlsd
    - Never run LSD from a different cwd than EXPERIMENT_DIR (sol file lands in cwd)
    - Never use subprocess.run(..., input=file.read_text()) for the .sol → .smi step (use stdin=open(sol_file))
  </action>

  <verify>
    <automated>
      # Verify outlsd is called with "5" and stdin from .sol file (not .lsd)
      grep -n '"5"' .planning/phases/72-design-re-validation/experiment/run_experiment.py
      # expected: at least one match containing OUTLSD_BIN and "5" together

      # Verify the antipattern is absent: outlsd should not receive .lsd file
      python3 -c "
import ast, sys
src = open('.planning/phases/72-design-re-validation/experiment/run_experiment.py').read()
# Must contain '\"5\"' near OUTLSD_BIN usage
assert '\"5\"' in src or \"'5'\" in src, 'FAIL: outlsd missing mode argument 5'
# Must not feed .lsd as stdin to outlsd (the exact runner.py bug pattern)
assert 'lsd_file' not in src.split('OUTLSD_BIN')[1].split('capture_output')[0], 'FAIL: lsd_file as stdin to outlsd'
print('PASS: outlsd invocation pattern correct')
"

      # Verify results.json was written
      python3 -c "
import json
r = json.load(open('.planning/phases/72-design-re-validation/experiment/results.json'))
assert len(r) == 3, f'Expected 3 arm results, got {len(r)}'
for arm in r:
    assert 'solution_count' in arm
    assert 'any_aromatic' in arm
    assert 'ibuprofen_found' in arm
print('PASS: results.json has 3 arms with required fields')
for arm in r:
    print(f\"  {arm['arm']}: {arm['solution_count']} solutions, aromatic={arm['any_aromatic']}, ibuprofen={arm['ibuprofen_found']}\")
"
    </automated>
  </verify>

  <done>
    run_experiment.py exists and has been executed. results.json exists with 3 arm entries, each
    containing solution_count, any_aromatic, and ibuprofen_found. The stdout table (printed during
    execution) shows the raw numbers that Plan 02 will use to write the decision document.
    The script used the correct outlsd invocation: OUTLSD_BIN + "5" + stdin from .sol file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → LSD binary | LSD file content is hand-crafted from a known-working baseline; no untrusted input |
| LSD stdout/stderr → Python | subprocess capture_output; no shell injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | arm files | accept | Files are throwaway planning artifacts; no production path reads them |
| T-72-02 | Spoofing | outlsd result | accept | Running locally; RDKit independently re-verifies SMILES |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; RDKit already a project dep |
</threat_model>

<verification>
After Task 2 completes, the overall Plan 01 is verified by:
1. grep -c "SKEL" .planning/phases/72-design-re-validation/experiment/arm_a.lsd returns 0
2. cat .planning/phases/72-design-re-validation/experiment/results.json shows 3 entries
3. The SMILES output files (arm_a_solutions.smi, arm_b_solutions.smi, arm_c_solutions.smi) exist in the experiment directory
4. Arm B result shows solution_count == 2 and ibuprofen_found == true (baseline sanity check)
5. pytest -q passes (no regression in production code — this plan writes no src/ files)
</verification>

<success_criteria>
- Three arm LSD files exist with the correct derivation from the iter3 baseline (arm_a: no SKEL; arm_b: verbatim; arm_c: arm_a + 3 HMBC X Y 2 4)
- run_experiment.py uses the correct outlsd invocation (argument "5" + sol file as stdin)
- results.json contains empirical per-arm counts consumed by Plan 02 to write the decision document
- Arm B confirms the baseline: 2 solutions, both aromatic, ibuprofen present (environment sanity check)
- No production source files modified
</success_criteria>

<output>
Create .planning/phases/72-design-re-validation/72-01-SUMMARY.md when done.
</output>
