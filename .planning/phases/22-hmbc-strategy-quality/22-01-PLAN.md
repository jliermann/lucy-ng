---
phase: 22-hmbc-strategy-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skill/SKILL.md]
autonomous: true

must_haves:
  truths:
    - "SKILL.md contains a Spectral Quality Assessment section that teaches S/N evaluation, digital resolution impact, and artifact recognition"
    - "SKILL.md contains an Incremental HMBC Constraint Strategy section with adaptive 3-5 batch iteration, NOT a fixed 3-phase recipe"
    - "SKILL.md explicitly states to start with 3-5 high-confidence HMBC correlations per iteration"
    - "SKILL.md explicitly prohibits dumping all correlations at once"
    - "SKILL.md contains a failure decision tree for zero-solution and stalled-convergence scenarios"
    - "SKILL.md CASE Workflow references incremental HMBC approach instead of one-shot constraint building"
    - "SKILL.md Quick Reference includes spectral quality thresholds and iteration limits"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Complete CASE domain knowledge with spectral quality and incremental HMBC strategy"
      contains: "Spectral Quality Assessment"
    - path: "skill/SKILL.md"
      provides: "Incremental HMBC strategy"
      contains: "Incremental HMBC Constraint Strategy"
  key_links:
    - from: "SKILL.md Section: Spectral Quality Assessment"
      to: "SKILL.md Section: Peak Picking Strategy"
      via: "Quality assessment precedes and modifies peak picking"
      pattern: "assess.*quality.*BEFORE.*pick"
    - from: "SKILL.md Section: Incremental HMBC Constraint Strategy"
      to: "SKILL.md Section: CASE Workflow"
      via: "Workflow Step 5 references incremental approach"
      pattern: "incremental|batch|iteration"
---

<objective>
Add two new sections to skill/SKILL.md -- Spectral Quality Assessment and Incremental HMBC Constraint Strategy -- then update the CASE Workflow and Quick Reference sections to integrate this new knowledge.

Purpose: Teach the AI agent to assess spectral quality BEFORE peak picking and to build LSD files iteratively with small batches of high-confidence HMBC correlations, preventing the "dump all correlations at once" failure pattern.

Output: Updated skill/SKILL.md with 2 new sections and 2 updated sections, covering all 7 requirements (HMBC-01 through HMBC-04, QUAL-01 through QUAL-03).
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-hmbc-strategy-quality/22-CONTEXT.md
@.planning/phases/22-hmbc-strategy-quality/22-RESEARCH.md
@skill/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Spectral Quality Assessment and Incremental HMBC Strategy sections to SKILL.md</name>
  <files>skill/SKILL.md</files>
  <action>
Add two new sections to skill/SKILL.md. The current structure has 8 sections. After this task, it will have 10 sections. Insert and renumber as follows:

**Current structure (8 sections):**
1. NMR Background
2. Peak Picking Strategy
3. Symmetry Detection
4. Dereplication
5. LSD Reference
6. Ranking and Prediction
7. CASE Workflow
8. Quick Reference

**New structure (10 sections):**
1. NMR Background (unchanged)
2. Spectral Quality Assessment (NEW -- insert before Peak Picking)
3. Peak Picking Strategy (renumbered from 2)
4. Symmetry Detection (renumbered from 3)
5. Dereplication (renumbered from 4)
6. LSD Reference (renumbered from 5)
7. Incremental HMBC Constraint Strategy (NEW -- insert after LSD Reference)
8. Ranking and Prediction (renumbered from 6)
9. CASE Workflow (renumbered from 7)
10. Quick Reference (renumbered from 8)

**Section 2: Spectral Quality Assessment (~80-100 lines)**

This section teaches the AI agent to assess spectrum quality BEFORE picking peaks. Quality findings actively modify the agent's strategy. Include these subsections:

### When to Assess
- Assess quality of EVERY spectrum before peak picking
- Start with 1D spectra (13C, DEPT), then 2D (HSQC, HMBC)
- Quality assessment comes BEFORE any peak picking or analysis

### S/N Ratio Evaluation (QUAL-01)
- Compute relative to spectrum's own noise floor (NOT fixed absolute values)
- Noise floor: median of absolute data values in a quiet region (e.g., -5 to -2 ppm for 13C, or any region clearly free of peaks)
- SNR = tallest peak / noise floor
- Quality tiers with strategy adjustments:
  - SNR > 100: Excellent -- use default threshold (0.05), trust all validated peaks
  - SNR 30-100: Good -- use default threshold (0.05-0.08), standard tolerances
  - SNR 10-30: Moderate -- raise threshold to 0.10, widen tolerances, trust only top 50% of HMBC correlations by intensity, use batch size 3 (not 5) for HMBC iteration
  - SNR < 10: Poor -- raise threshold further, expect missed peaks, reduce trusted HMBC to top 25%, document significant quality caveats in results, consider requesting re-acquisition
- These thresholds are pragmatic defaults subject to refinement based on real-world usage

### Digital Resolution Impact (QUAL-02)
- Digital resolution = number of data points per ppm in the 13C dimension
- Low resolution causes peaks to merge and increases positional uncertainty
- Resolution tiers:
  - > 10 pts/ppm: Excellent -- standard ±1.5 ppm tolerance
  - 5-10 pts/ppm: Good -- standard tolerance acceptable
  - 2-5 pts/ppm: Moderate -- increase tolerance to ±2.0 ppm, expect aliasing, close carbons (< 2 ppm apart) may be unresolvable
  - < 2 pts/ppm: Poor -- increase tolerance to ±3.0 ppm, warn user about severe limitations
- Critical for HMBC: if 13C dimension has < 5 pts/ppm, two carbons within 2 ppm cannot be reliably distinguished -- mark all correlations involving close carbons as AMBIGUOUS

### Artifact Recognition (QUAL-03)
Three artifacts most relevant for automated CASE:

**1J Leakage (HMBC)**
- HMBC experiments suppress but do not fully eliminate direct 1JCH couplings
- Strong peaks in HMBC that appear at the same (C, H) position as an HSQC peak are likely 1J artifacts, NOT long-range correlations
- Detection: If an HMBC peak is within ±1.5 ppm (carbon) of any HSQC correlation AND the proton shifts match within ±0.3 ppm, flag as potential 1J artifact
- Impact: Including 1J artifacts as HMBC constraints tells LSD that "C is 2-3 bonds from H" when in fact C is directly bonded to H -- this creates impossible constraints and zero solutions
- Action: Exclude flagged peaks from HMBC constraint list; document exclusions

**t1 Noise (2D spectra)**
- Manifests as horizontal streaks in the F1 (indirect) dimension of 2D spectra
- More common in non-gradient-selected experiments
- Impact: Creates false peaks at correct 1H positions but incorrect 13C positions
- Action: If > 20% of "validated" HMBC peaks cluster at identical proton positions across different carbon positions, suspect t1 noise; reduce trusted correlation count; increase validation threshold

**Baseline Roll (1D spectra)**
- Broad undulation in 1D 13C baseline, can shift apparent peak positions by 0.5-1 ppm
- Impact: Shifts 13C peak positions, causing mismatches between 1D 13C and 2D carbon dimensions
- Action: If observed 13C peaks differ by > 1.0 ppm from HSQC carbon positions for the same carbon, baseline roll is likely present; increase all carbon tolerances by 0.5 ppm

### Strategy Adjustments Summary
Provide a compact decision table:
- If SNR < 30 AND digital resolution < 5 pts/ppm: trust only top 50% of HMBC correlations, increase 13C tolerance to ±2.5 ppm, use batch size 3, document quality caveats
- If SNR < 10 OR digital resolution < 2 pts/ppm: warn user that automated elucidation may not produce reliable results, consider requesting better data
- If 1J artifacts detected: exclude affected peaks, note in analysis
- Quality assessment findings MUST be documented in the analysis folder before proceeding to peak picking

**Section 7: Incremental HMBC Constraint Strategy (~80-120 lines)**

This section teaches the AI agent to build LSD files iteratively rather than dumping all HMBC correlations at once. Include these subsections:

### Core Principle
- NEVER add all HMBC correlations to an LSD file at once -- this is the most common cause of zero-solution or thousands-of-solutions failures
- Instead, add correlations in small batches (3-5 per iteration), observing how the solution count changes
- This adaptive iteration approach lets you build a solid structural core from high-confidence signals before adding more constrained relationships
- Maximum ~10 LSD iterations before stopping and presenting whatever results exist (prevents runaway loops)

### High-Confidence Correlation Selection
Teach how to select the best 3-5 correlations for each batch:
1. **Unique carbon assignment**: The HMBC carbon shift has no other carbon within 2x tolerance (±3.0 ppm). Isolated carbons have unambiguous assignment.
2. **Unique proton assignment**: The HMBC proton shift has no other proton within 2x tolerance (±0.2 ppm).
3. **Strong peak intensity**: Prefer peaks in the top quartile of validated HMBC peak intensities.
4. **Quaternary carbon involvement**: Correlations to quaternary carbons (visible in 13C but not HSQC/DEPT) are especially valuable because they are the ONLY way to connect these atoms to the structure.

Document reasoning for each selected correlation: "Starting with C-155.2/H-7.8 because carbon shift is isolated (nearest carbon 4.3 ppm away), proton shift is unique, and peak is strong."

### The Adaptive Iteration Loop
Write this as a clear algorithmic procedure (pseudocode-style with prose explanations):

```
1. Start with MULT definitions, HSQC correlations, and heteroatom constraints (NO HMBC yet)
2. Run LSD -- this gives the unconstrained solution count (baseline)
3. Select first batch of 3-5 high-confidence HMBC correlations
4. Add batch to LSD file, run LSD
5. Observe solution count:

   IF solution_count <= 10:
       STOP -- proceed to ranking (Section 8)

   IF solution_count == 0:
       STOP iteration -- go to Zero-Solution Recovery below

   IF solution_count decreased significantly (>30% reduction):
       CONTINUE -- these correlations are productive, select next batch

   IF solution_count barely changed (<10% reduction for 2+ consecutive iterations):
       STALLED -- go to Convergence Stall below

   IF solution_count INCREASED:
       CONFLICT -- remove last batch, diagnose why it caused more solutions

6. Repeat from step 3 until:
   - solution_count <= 10 (success -- rank)
   - iterations >= 10 (safety cap -- rank anyway with caveats)
   - all HMBC correlations exhausted (rank with caveats)
```

### Stopping Conditions
- **Success**: solution_count <= 10 -- proceed to ranking with standard confidence
- **Iteration cap (~10)**: safety limit, NOT a normal outcome. If hit, treat as diagnostic failure: review correlation selection, check for systematic issues (wrong formula, missing heteroatom constraints), document "structure elucidation incomplete, requires manual review"
- **Correlations exhausted**: all HMBC correlations added, solution_count still > 10 -- rank anyway, present top results with caveat that structure is under-determined
- **Target**: successful cases typically converge in 3-5 iterations. If taking > 7, something is likely wrong.

### Zero-Solution Recovery
When LSD returns 0 solutions after adding a batch, diagnose in this order:
1. **Remove last batch** and verify solutions return (confirms the batch caused the conflict)
2. **Check sp2 atom count** -- must be EVEN (most common cause of zero solutions)
3. **Verify hydrogen count** -- sum of all hydrogen counts from MULT must match molecular formula
4. **Review the conflicting batch** -- are any correlations potential 1J artifacts? Are carbon assignments ambiguous (close shifts)?
5. **Try individual correlations** from the batch one at a time to find the specific conflict
6. **Check molecular formula** -- if all correlations seem valid, formula may be wrong
7. **ONLY AFTER all above**: consider ELIM 1 0 to eliminate one correlation. ELIM is a LAST RESORT, not a first response.

### Convergence Stall Detection
If 3 consecutive iterations each show < 10% relative reduction in solution count AND solution_count > 50:
- Stop adding correlations -- further constraints are not productive
- Diagnose: Are remaining correlations low-confidence? Is the molecule under-determined by available data?
- Rank current solutions with caveat: "Structure is under-determined; additional NMR data may be needed"
- If solution_count is 10-50, rank and present (may still contain the correct structure)

### What NOT to Do (HMBC-04)
- **NEVER dump all HMBC correlations into LSD at once** -- this eliminates diagnostic feedback and either over-constrains (0 solutions) or under-constrains (thousands of solutions) without telling you why
- **NEVER add ELIM before diagnosing** -- ELIM tells LSD to ignore correlations, which masks real problems
- **NEVER ignore solution count trends** -- if 3 iterations show 500->450->420 (slow decline), continuing is futile; stop and diagnose
- **NEVER treat the iteration cap as a strategy** -- hitting 10 iterations means something went wrong, not that you tried hard enough

**Renumbering:** Update ALL section numbers in the file. Old section 2 becomes 3, old 3 becomes 4, old 4 becomes 5, old 5 becomes 6, old 6 becomes 8, old 7 becomes 9, old 8 becomes 10. Update all cross-references within the document (e.g., "see Section 5" references to LSD Reference should become "see Section 6").
  </action>
  <verify>
Verify the file structure:
1. grep for "## 1\." through "## 10\." headers to confirm 10 sections exist
2. grep for "Spectral Quality Assessment" to confirm new section 2 exists
3. grep for "Incremental HMBC Constraint Strategy" to confirm new section 7 exists
4. grep for "NEVER.*all.*HMBC.*correlations" or similar prohibition language (HMBC-04)
5. grep for "3-5" to confirm batch size is documented (HMBC-02)
6. grep for "S/N" or "SNR" to confirm quality assessment content (QUAL-01)
7. grep for "digital resolution" or "pts/ppm" to confirm resolution content (QUAL-02)
8. grep for "1J" or "t1 noise" or "baseline roll" to confirm artifact content (QUAL-03)
9. Verify no section numbers are duplicated or skipped
  </verify>
  <done>
SKILL.md has 10 numbered sections. Section 2 (Spectral Quality Assessment) covers QUAL-01, QUAL-02, QUAL-03. Section 7 (Incremental HMBC Constraint Strategy) covers HMBC-01, HMBC-02, HMBC-03, HMBC-04. All existing section numbers are correctly renumbered. Cross-references within the document are updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CASE Workflow and Quick Reference to integrate new sections</name>
  <files>skill/SKILL.md</files>
  <action>
Update the CASE Workflow (now Section 9) and Quick Reference (now Section 10) to reference the new incremental HMBC approach and spectral quality assessment.

**Section 9: CASE Workflow updates:**

In the Step-by-Step Workflow, make these changes:

1. **Insert new Step 2.5 "Quality Assessment"** between current Step 2 (Symmetry) and Step 3 (Peak Picking):
   - "Assess spectral quality (S/N, digital resolution, artifacts) for ALL spectra before peak picking. See Section 2 for quality tiers and strategy adjustments. Document quality findings in analysis folder. If quality is poor (SNR < 10 or resolution < 2 pts/ppm), warn user before proceeding."
   - Renumber subsequent steps (old Step 3 becomes Step 4, etc.)

2. **Update the LSD Generation + Solve step** (was Step 4-5, now Step 5-6):
   - Replace the current one-shot approach with a reference to the incremental HMBC strategy
   - Change Step 5 (LSD Generation) to: "Generate initial LSD file with MULT definitions, HSQC correlations, and heteroatom constraints. Do NOT add HMBC correlations yet. See Section 7 for the incremental approach."
   - Change Step 6 (Solve) to reference the adaptive iteration loop: "Follow the Incremental HMBC Constraint Strategy (Section 7). Add 3-5 high-confidence HMBC correlations per iteration. Stop when solution_count ≤ 10 or after ~10 iterations."
   - Remove the old language about adding all HMBC correlations to the LSD file

3. **Update solution count guidance**:
   - ">10 solutions" guidance should say: "Under-constrained. Continue incremental HMBC iteration (Section 7). Do NOT proceed to ranking until ≤10 solutions or all correlations/iterations exhausted."

4. Add a brief note at the top of the workflow: "This workflow assumes you have assessed spectral quality (Section 2) and will use the incremental HMBC strategy (Section 7) for constraint building."

**Section 10: Quick Reference updates:**

Add to the Key Tolerances subsection:
- Spectral quality (SNR): > 100 excellent, 30-100 good, 10-30 moderate, < 10 poor
- Digital resolution: > 10 pts/ppm excellent, 5-10 good, 2-5 moderate, < 2 poor
- HMBC batch size: 3-5 correlations per iteration
- HMBC iteration cap: ~10 iterations maximum
- High-confidence threshold: no other carbon within ±3.0 ppm, no other proton within ±0.2 ppm

Add to the Red Flags subsection:
- Solution count stalled for 3+ iterations: under-determined structure (see Section 7)
- Hitting 10-iteration cap: systematic issue, not normal convergence
- > 200 "validated" HMBC correlations: likely noise leakage from poor quality spectrum
- 1J artifact peaks in HMBC: exclude from constraints (see Section 2)

Ensure all section cross-references in the updated workflow and quick reference point to the correct new section numbers (e.g., "Section 6" for LSD Reference, "Section 7" for Incremental HMBC, "Section 2" for Spectral Quality).
  </action>
  <verify>
1. Read Section 9 (CASE Workflow) and confirm it references "Section 2" (quality) and "Section 7" (incremental HMBC)
2. Confirm the workflow no longer says to add all HMBC correlations at once
3. Confirm a quality assessment step exists before peak picking in the workflow
4. Read Section 10 (Quick Reference) and confirm SNR tiers, digital resolution tiers, batch size, and iteration cap are listed
5. Confirm new red flags are listed (stalled convergence, iteration cap, >200 correlations, 1J artifacts)
6. Verify no cross-references point to old section numbers
  </verify>
  <done>
CASE Workflow (Section 9) includes quality assessment as an early step and references the incremental HMBC strategy for constraint building. Quick Reference (Section 10) includes spectral quality thresholds, HMBC iteration parameters, and new red flags. The workflow explicitly prohibits the "add all correlations at once" approach.
  </done>
</task>

</tasks>

<verification>
After both tasks, verify all 7 requirements are met:

**HMBC-01 (Adaptive iteration strategy):** grep for "Adaptive Iteration Loop" or "adaptive" in Section 7 -- the approach is iterative with 3-5 batches, NOT a fixed 3-phase recipe
**HMBC-02 (Start with high-confidence):** grep for "3-5 high-confidence" or "High-Confidence Correlation Selection" in Section 7
**HMBC-03 (Decision tree for failures):** grep for "Zero-Solution Recovery" and "Convergence Stall" in Section 7
**HMBC-04 (Prohibit dump-all):** grep for "NEVER.*all.*HMBC" or "What NOT to Do" in Section 7
**QUAL-01 (S/N assessment):** grep for "S/N Ratio" or "SNR" in Section 2
**QUAL-02 (Digital resolution):** grep for "Digital Resolution" or "pts/ppm" in Section 2
**QUAL-03 (Artifact recognition):** grep for "1J Leakage" and "t1 Noise" and "Baseline Roll" in Section 2

Additionally verify:
- SKILL.md has exactly 10 numbered sections (## 1. through ## 10.)
- Section 9 (CASE Workflow) references Section 2 and Section 7
- Section 10 (Quick Reference) includes SNR/resolution tiers and HMBC iteration parameters
- No duplication of content from CLAUDE.md or other skill files
- No Virgiline/CASE7 case study reference (deferred)
- The approach is described as adaptive iteration, NOT a fixed 3-phase recipe (CONTEXT.md decision)
</verification>

<success_criteria>
1. SKILL.md contains 10 numbered sections (was 8)
2. Section 2 (Spectral Quality Assessment) teaches S/N evaluation, digital resolution impact, and artifact recognition with strategy adjustments
3. Section 7 (Incremental HMBC Constraint Strategy) teaches adaptive 3-5 batch iteration with high-confidence selection, convergence detection, and failure recovery
4. Section 9 (CASE Workflow) references quality assessment before peak picking and incremental HMBC for constraint building
5. Section 10 (Quick Reference) includes quality thresholds, iteration parameters, and new red flags
6. All 7 requirements (HMBC-01 through HMBC-04, QUAL-01 through QUAL-03) are covered
7. No "fixed 3-phase recipe" language -- the approach is explicitly adaptive/iterative
8. "Dump all correlations" approach is explicitly prohibited
9. Estimated total SKILL.md growth: ~160-200 lines (from 418 to ~580-620 lines)
</success_criteria>

<output>
After completion, create `.planning/phases/22-hmbc-strategy-quality/22-01-SUMMARY.md`
</output>
