---
phase: 85-hmbc-peak-picking-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_hmbc_peak_picking_integrity.py
  - .claude/agents/lucy-nmr-chemist.md
autonomous: true
requirements: [FIX-12]

must_haves:
  truths:
    - "A regression test proves that on CASE1 (ibuprofen, HMBC expno 7) the two ring-diagnostic 3J-meta cross-peaks (C≈141/H≈7.11 and C≈137/H≈7.23) are RETAINED above the SNR floor at the new default, while the ~0.5-1%-of-max noise is excluded and the total peak count is far below the ~165-peak fraction-of-max flood."
    - "The external CASE1 HMBC regression uses a skipif-on-missing-dataset guard so the suite still passes on machines without the data; the in-repo data/Ibuprofen/7 HMBC is used where possible so the test actually runs in CI."
    - "A self-contained synthetic 2D test asserts that a sub-1%-of-max high-SNR cross-peak is retained AND a ~1%-of-max low-SNR noise blob is rejected by the SNR floor — capturing the FIX-12 separation property without external data."
    - "The nmr-chemist agent gains a compound-agnostic note that the HMBC pick now has an SNR floor and that weak long-range correlations (especially aromatic 3J-meta) are real signal to keep, not noise to dismiss."
    - "The nmr-chemist agent edits contain NO blind-UAT contaminants (FIX-09 guard): no 'ibuprofen'/'CASE1'/'CASE9', no specific test ppm/expno, no answer-key compound/formula — phrased generically."
  artifacts:
    - path: "tests/test_hmbc_peak_picking_integrity.py"
      provides: "FIX-12 HMBC SNR-floor regression: CASE1 3J-meta retained + noise excluded + count ceiling, plus a synthetic separation test"
      contains: "snr_floor"
    - path: ".claude/agents/lucy-nmr-chemist.md"
      provides: "compound-agnostic HMBC SNR-floor / weak-long-range-is-signal note"
      contains: "SNR"
  key_links:
    - from: "tests/test_hmbc_peak_picking_integrity.py"
      to: "PeakPicker2D SNR-floor pick / `lucy pick hmbc` SNR mode"
      via: "asserts 3J-meta retention + noise exclusion at the new default"
      pattern: "164|141|137|snr_floor"
---

<objective>
Make the FIX-12 fix verifiable and teach the nmr-chemist agent (compound-agnostically) that weak long-range HMBC correlations are real signal. The CASE1 expno-7 regression test IS the Dimension-8 validation for this phase (no separate VALIDATION.md exists): it asserts, against real data, that the two ring-diagnostic 3J-meta cross-peaks survive the new SNR-floor default while the noise flood is excluded.

Purpose: Without a concrete regression test, the SNR-floor change could silently regress (back to fraction-of-max behavior, or over-permissive noise). This plan locks the FIX-12 behavior with both a real-data test (the actual emergent-ring evidence) and a synthetic test (always runs). The skill note ensures the blind CASE agent treats recovered weak long-range correlations as signal — without leaking any blind-UAT answer-key (FIX-09).

Output: tests/test_hmbc_peak_picking_integrity.py; compound-agnostic nmr-chemist HMBC SNR note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<read_first>
- .planning/REQUIREMENTS.md (FIX-12 bullet + FIX-09 bullet ~line 38 for the compound-agnostic guard rules)
- .planning/research/D04-emergent-ring/ibuprofen-correlation-analysis.md (the intensity table giving the exact 3J-meta positions: C140.76×H7.11 ≈ 0.64% of max; C≈137×H7.23 ≈ 0.6%; 2J-ortho at ~22%; global max 7.48e7; ~165-peak flood at -t 0.005 — the concrete acceptance numbers for the regression test)
- tests/test_peak_picker_snr.py (FIX-04 test patterns: the skipif-on-missing-dataset guard, the CASE-path constants, the synthetic-spectrum + threshold-helper unit tests to mirror)
- tests/test_peak_picking_integrity.py (FIX-08 patterns: external-vs-synthetic split into separate classes, the skipif guard, the CASE1 in-repo path constant `data/Ibuprofen/2`)
- tests/test_hmbc_guided_picker.py (how 2D/HMBC spectra are constructed and read in existing tests; the BrukerReader.read_2d usage)
- src/lucy_ng/processing/peak_picker_2d.py (the SNR-floor API from plan 85-01 the test will call: PeakPicker2D.pick_peaks(spectrum, snr_floor=..., use_snr=...))
- .claude/agents/lucy-nmr-chemist.md (the runtime skill; note existing "Pitfall 3 — HMBC noise", "Pitfall 10 — SNR floor", "Pitfall 11 — Carbonyl never discard", and the SNR-quality table — add the HMBC-specific note in the same compound-agnostic voice; this file is symlinked from ~/.claude/agents/, so editing the repo copy updates the runtime)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-12 HMBC SNR-floor regression test (CASE1 real-data + synthetic)</name>
  <files>tests/test_hmbc_peak_picking_integrity.py</files>
  <action>Create tests/test_hmbc_peak_picking_integrity.py with two test classes mirroring tests/test_peak_picking_integrity.py structure. Class TestFIX12CASE1HMBC (real data, in-repo HMBC at `data/Ibuprofen/7` — read via BrukerReader.read_2d; ALSO define the external CASE1 path constant with a `pytest.mark.skipif(not PATH.exists())` guard exactly like the FIX-08 tests, so the external variant skips cleanly when absent): (1) test that picking the HMBC at the new default SNR floor RETAINS a cross-peak near C≈141 ppm / H≈7.11 ppm and one near C≈137 ppm / H≈7.23 ppm (the two 3J-meta ring-diagnostic correlations) — assert at least one picked peak matches each within a tolerance window (e.g. C ±2 ppm, H ±0.15 ppm); (2) test that the total picked peak count at the new default is well below the fraction-of-max flood — assert the count is a sane number (e.g. <= 60, contrasting the ~165-peak `-t 0.005` flood) AND that the legacy `-t 0.05` pick does NOT contain the 3J-meta peaks (proving the floor is what recovers them). Class TestFIX12SyntheticSeparation (always runs, no external data): build a small synthetic Spectrum2D with (a) an intense aliphatic-region cross-peak setting the global max, (b) a weak aromatic-region cross-peak at <1% of that max but high SNR over an injected low-level noise floor, and (c) a noise blob at ~1% of max but low SNR; assert the SNR-floor pick (use_snr=True, snr_floor default) RETAINS the weak high-SNR peak and EXCLUDES the noise blob, while the legacy fraction-of-max pick at threshold=0.05 drops the weak peak — capturing the FIX-12 "separable in SNR space, not in fraction-of-max space" property. Ground all magic numbers and tolerances in the ibuprofen-correlation-analysis.md intensity table. Use the existing BrukerReader/Spectrum2D/PeakPicker2D APIs; do not introduce new helpers. The TEST file MAY reference CASE1/ibuprofen/expno-7 (it is not a runtime skill — FIX-09 applies only to runtime skill/agent files).</action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && python -m pytest tests/test_hmbc_peak_picking_integrity.py -v</automated>
  </verify>
  <done>Both test classes exist; the synthetic separation test passes unconditionally; the real-data CASE1 tests pass (in-repo data/Ibuprofen/7) and the external variant skips cleanly when the dataset is absent; the 3J-meta retention + count-ceiling + legacy-drops-them assertions hold.</done>
</task>

<task type="auto">
  <name>Task 2: Compound-agnostic nmr-chemist HMBC SNR-floor note (FIX-09 guard)</name>
  <files>.claude/agents/lucy-nmr-chemist.md</files>
  <action>Add a concise note to the nmr-chemist agent (near the existing "Pitfall 3 — HMBC noise" and the SNR section) stating, in fully compound-agnostic language, that: (1) the HMBC pick now applies an SNR floor (signal/noise separation) rather than fraction-of-max only, so an intense aliphatic or 2J cross-peak no longer suppresses weak-but-real long-range correlations; (2) weak long-range HMBC correlations — especially aromatic 3J correlations (e.g. meta H→Cq across a ring) — are real signal when they clear the SNR floor and must NOT be dismissed as noise just because they sit at a tiny fraction of the global max; (3) prefer the SNR-floor HMBC pick (default) and only fall back to a fraction-of-max threshold deliberately. Phrase as a generic NMR rule. Do NOT mention any specific compound, test case, formula, expno, or specific ppm values tied to a test (no "ibuprofen", "CASE1", "CASE9", "C13H18O2", "C12H16O3", "expno 7", "7.11", "141", "166.08", "para-benzoate", etc.). Generic chemical-shift ranges (e.g. "aromatic 110-160 ppm") are fine — they are domain knowledge, not answer-keys. Keep the edit small and in the file's existing voice.</action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && grep -qi "SNR" .claude/agents/lucy-nmr-chemist.md && echo "snr-note-present" && test "$(grep -Eic 'ibuprofen|CASE1|CASE9|expno|C13H18O2|C12H16O3|para-benzoate|166\.08|7\.11|140\.|136\.9' .claude/agents/lucy-nmr-chemist.md)" -eq 0 && echo "FIX-09-clean"</automated>
  </verify>
  <done>The agent has a compound-agnostic HMBC SNR-floor / weak-long-range-is-signal note; the FIX-09 grep gate returns 0 contaminant hits (no test compound name, formula, expno, or test-specific ppm in the agent file).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local test data → pytest | Read-only access to local Bruker datasets; no network/auth |
| repo skill file → blind CASE runtime | The agent .md is loaded verbatim by a blind CASE instance; leaked dev/answer-key content is the relevant "information disclosure" risk (FIX-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02-01 | Information Disclosure | nmr-chemist.md leaking blind-UAT answer-key to a fresh CASE instance | mitigate | FIX-09 grep gate as an acceptance criterion (compound names/formulas/expno/test-ppm count == 0); generic phrasing only |
| T-85-02-02 | Denial of Service | external dataset path hardcoded in test | accept | skipif-on-missing guard prevents hard failure; in-repo data/Ibuprofen/7 keeps the test live in CI |
| T-85-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies; pytest/click already vetted. No package-install task in this plan |
</threat_model>

<verification>
- pytest tests/test_hmbc_peak_picking_integrity.py -v passes (synthetic always; CASE1 in-repo runs; external skips cleanly)
- ruff check src tests clean; mypy src/lucy_ng clean (test file follows existing lint conventions)
- FIX-09 grep gate on .claude/agents/lucy-nmr-chemist.md returns 0 contaminant hits
- Full suite (~1071+ tests) still green; 1D FIX-08 path not regressed
</verification>

<success_criteria>
- CASE1 expno-7 regression proves the two 3J-meta cross-peaks are retained at the new default and the noise flood is excluded (count ceiling) — the FIX-12 Dimension-8 validation.
- Synthetic separation test captures the SNR-vs-fraction-of-max property without external data.
- nmr-chemist agent has a compound-agnostic HMBC SNR-floor note; FIX-09 grep gate clean.
</success_criteria>

<output>
Create `.planning/phases/85-hmbc-peak-picking-integrity/85-02-SUMMARY.md` when done. Record the actual picked positions/counts observed for the CASE1 HMBC at the new default vs legacy (the empirical retention evidence).
</output>
