---
phase: 78-blind-re-uat-gate
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - .planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md
autonomous: false
requirements: [UAT-03]

must_haves:
  truths:
    - "A fresh blind instance (not the bookkeeper) ran /lucy-ng:case on CASE1 (C13H18O2) in a /clear'd session"
    - "solutions.smi written to disk by the blind run is independently verified by verify_case_solution.py"
    - "The final emitted LSD file(s) contain BOND/COSY/DEFF F native constraints and zero SYME, DEFF NOT, SKEL"
    - "Command history / CASE-PROGRESS.md shows lucy lsd run as the solver path — no direct lsd/outlsd binary"
    - "Every bookkeeper-to-instance message is logged and classified advisory vs path-changing"
    - "78-UAT-CASE1.md records the full evidence chain and a UAT-03 pass/fail verdict"
  artifacts:
    - path: ".planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md"
      provides: "CASE1 evidence record and UAT-03 verdict"
      contains: "UAT-03.*PASS|UAT-03.*FAIL"
  key_links:
    - from: "~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/solutions.smi"
      to: "scripts/verify_case_solution.py"
      via: "python scripts/verify_case_solution.py <path>/solutions.smi C13H18O2"
      pattern: "\"pass\": true"
    - from: "CASE1 analysis/ iteration LSD files"
      to: "78-UAT-CASE1.md mechanism section"
      via: "grep SYME / DEFF NOT / SKEL / BOND / COSY / DEFF F / lucy lsd run"
      pattern: "SYME.*0|DEFF NOT.*0|SKEL.*0"
---

<objective>
Run the CASE1 blind UAT (ibuprofen, C13H18O2) in a fresh session isolated from the tainted bookkeeper, then collect and record the full evidence chain for the four gate criteria against on-disk artifacts — never from agent self-report (per D-78-01 and D-78-03).

Purpose: UAT-03 is the first half of the AND-gate that decides whether v9.0 ships. The run tests whether the Phase 77 fixes (working lucy lsd run + deterministic aromatic COSY emission) make the intended mechanism work end-to-end on the original failure compound.

Output: `.planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md` — complete evidence record with UAT-03 pass/fail verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-blind-re-uat-gate/78-CONTEXT.md
@.planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-CONTEXT.md
@.planning/phases/76-milestone-uat-gate/VERIFICATION.md
@.planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run CASE1 blind session (human-action)</name>
  <read_first>
    - .planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md — confirm Overall gate = READY before proceeding
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/molecular-formula.txt — formula is C13H18O2
  </read_first>
  <what-to-do>
    Open a SEPARATE terminal (do NOT reuse this session). Run /clear in the new terminal to ensure a completely fresh context. In the new terminal, run exactly:

      /lucy-ng:case ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1 C13H18O2

    Bookkeeper (this session) advisory-intervention rules (D-78-04):
    - ALLOWED (non-disqualifying): generic process nudges that reveal neither the answer nor the path. Examples: "your constraint inventory was lost across iterations", "re-read the skill guidance", "you have not yet run lucy lsd run".
    - DISQUALIFYING (path-changing = fail): any path switch instruction ("use the direct lsd binary", "try lucy pylsd run instead"), structural hints (the compound identity, atom indices, COSY pairs, "it's aromatic", "it has a benzene ring"), forced-ring instructions ("add BOND ring constraints").
    - Borderline cases: score as DISQUALIFYING (conservative, per D-78-04).

    Keep a running log of every message you send to the blind instance during the run. Record: message text, timestamp, and your classification (ADVISORY / PATH-CHANGING). This log feeds Task 2.

    When the blind run completes (success or failure or the agent declares it stuck), signal back here with: "CASE1 run complete. solutions.smi at [path]."
  </what-to-do>
  <acceptance_criteria>
    - The blind run was performed in a separate /clear'd terminal session
    - solutions.smi (or an explicit failure report) exists under ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/
    - You have the intervention log (even if empty) ready for Task 2
  </acceptance_criteria>
  <how-to-verify>
    Confirm the separate terminal session was /clear'd. Confirm solutions.smi file exists (or confirm the blind agent reported zero solutions + declared the run complete). Resume when done.
  </how-to-verify>
  <resume-signal>Type "CASE1 run complete" and the absolute path to solutions.smi, or "CASE1 run complete — no solutions found" with the path to CASE-PROGRESS.md.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Collect CASE1 evidence and write 78-UAT-CASE1.md</name>
  <read_first>
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/solutions.smi — the primary output
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/CASE-PROGRESS.md — intervention log + solver path used
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/ — final iteration LSD file (highest iteration_NN dir)
    - scripts/verify_case_solution.py — CLI contract: python scripts/verify_case_solution.py <smi_path> <formula>
    - .planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-CONTEXT.md §D-77-06 — three-tier mechanistic criterion
    - .planning/phases/78-blind-re-uat-gate/78-CONTEXT.md §D-78-03, §D-78-04 — evidence checklist and intervention classification
  </read_first>
  <action>
    Run all four evidence checks mechanically from on-disk artifacts. Do NOT rely on the blind agent's self-report for any criterion.

    CRITERION 1 — Solve correctness:
    Run: python scripts/verify_case_solution.py ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/analysis/solutions.smi C13H18O2
    Capture full JSON output. Record: exit code, pass field, aromatic_atoms count, computed_formula, formula_match for each of the top-3 entries.
    Additionally re-parse solutions.smi with a quick inline RDKit check: python -c "from rdkit import Chem; from rdkit.Chem import rdMolDescriptors; smi=open('solutions.smi').readline().split()[0]; m=Chem.MolFromSmiles(smi); print(rdMolDescriptors.CalcMolFormula(m), sum(1 for a in m.GetAtoms() if a.GetIsAromatic()))"
    Verdict: PASS if exit code 0 AND "pass": true. FAIL otherwise.

    CRITERION 2 — Native-only emission / mechanism:
    Locate the FINAL emitted LSD file: highest-numbered iteration_NN/compound.lsd (or equivalent) in the analysis directory. Run these greps on that file:
      grep -v "^;" [final_lsd_file] | grep -c "SYME"          # must be 0
      grep -v "^;" [final_lsd_file] | grep -c "DEFF NOT"      # must be 0
      grep -v "^;" [final_lsd_file] | grep -cE "^SKEL"        # must be 0
      grep -v "^;" [final_lsd_file] | grep -cE "^BOND|^COSY"  # must be >= 1
      grep -v "^;" [final_lsd_file] | grep -cE "^DEFF F|^FEXP" # must be >= 1

    For the aromatic-ring tier (D-77-06):
      grep -v "^;" [final_lsd_file] | grep -E "^BOND [0-9]+ [0-9]+"  # list all BOND lines for aromatic ring atoms (ring atoms = indices 2-7 in standard ibuprofen numbering)
    - If no ring-BOND lines for ring atoms: emergent ring — CLEAN PASS on this sub-criterion.
    - If ring-BOND lines exist for ring atoms AND CASE-PROGRESS.md contains an explicit escalation entry (e.g. "ring-BOND escalation after N non-aromatic iterations"): CONDITIONAL PASS.
    - If ring-BOND lines exist for ring atoms AND no such CASE-PROGRESS entry: SILENT ring-BOND — FAIL on this sub-criterion.
    - Any SKEL line (SKEL count > 0): FAIL on this sub-criterion regardless.
    Record which tier applies. Criterion 2 PASS requires: SYME=0, DEFF NOT=0, SKEL=0, BOND/COSY>=1, DEFF F/FEXP>=1, AND (emergent OR conditional-pass ring tier).

    CRITERION 3 — Solver path:
    In CASE-PROGRESS.md, grep for "lucy lsd run" (command used) and for "lsd " or "outlsd " or "/usr/local/bin/lsd" (direct binary bypass). Also check the terminal command history if visible in CASE-PROGRESS.
    Verdict: PASS if "lucy lsd run" present AND no direct binary bypass pattern found. FAIL if direct binary bypass found.

    CRITERION 4 — Interventions:
    Take the log of every message sent from the bookkeeper to the blind instance during Task 1. For each message, apply D-78-04 classification: ADVISORY (non-disqualifying) or PATH-CHANGING (disqualifying). Count of PATH-CHANGING messages.
    Verdict: PASS if PATH-CHANGING count = 0. FAIL if PATH-CHANGING count > 0.

    Write .planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md with:
    - Date/time of run
    - Brief narrative of how the run progressed (iterations, pivots)
    - Criterion table: C1/C2/C3/C4 each with command run, raw output, verdict (PASS/FAIL)
    - For C2: explicit SYME/DEFF NOT/SKEL counts + aromatic-ring tier (emergent/conditional/silent/SKEL-fail)
    - Intervention log (full text of each message + classification)
    - Overall UAT-03 verdict: PASS (all 4 PASS or C2 conditional-pass with documented escalation) or FAIL (any criterion fails)
    - If FAIL: root-cause paragraph identifying which criterion failed and what on-disk evidence confirms it
  </action>
  <verify>
    <automated>grep -E "UAT-03.*PASS|UAT-03.*FAIL" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md</automated>
  </verify>
  <acceptance_criteria>
    - 78-UAT-CASE1.md exists in .planning/phases/78-blind-re-uat-gate/
    - File contains the output of python scripts/verify_case_solution.py (exit code + JSON)
    - File contains grep counts for SYME (=0), DEFF NOT (=0), SKEL (=0), BOND/COSY (>=1), DEFF F/FEXP (>=1)
    - File identifies the aromatic-ring tier (emergent / conditional / silent / SKEL-fail)
    - File contains the solver-path check (lucy lsd run present / bypass absent)
    - File contains the intervention log with D-78-04 classification per message
    - File ends with a UAT-03 PASS or UAT-03 FAIL verdict line
    - grep -E "UAT-03.*PASS|UAT-03.*FAIL" 78-UAT-CASE1.md returns exactly 1 match
  </acceptance_criteria>
  <done>78-UAT-CASE1.md is written with all four criteria checked from on-disk artifacts. UAT-03 verdict is unambiguous (PASS or FAIL). Forensics included if FAIL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bookkeeper → blind instance | The tainted bookkeeper must not transmit compound identity, expected structure, or path hints |
| Agent self-report → evidence | All verdicts derive from on-disk artifacts and independent tool runs — never from what the blind agent says about its own result |
| solutions.smi → verify_case_solution.py | The harness is the authoritative correctness gate; re-parse confirms the harness result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-05 | Information Disclosure | Bookkeeper intervention messages | mitigate | D-78-04 classification applied to every message; PATH-CHANGING messages = criterion 4 FAIL; documented in 78-UAT-CASE1.md |
| T-78-06 | Information Disclosure | This plan's context section | mitigate | Plan references .planning/ files (bookkeeper-only); blind instance is instructed NOT to read .planning/ per D-78-01; the blind prompt (Task 1) contains only compound path + formula |
| T-78-07 | Repudiation | Agent self-report of solutions | mitigate | Evidence collected from on-disk artifacts + independent verify_case_solution.py run; agent self-report explicitly excluded per D-78-03 |
| T-78-08 | Tampering | verify_case_solution.py invocation | accept | Script is read-only; plan invokes it without modification; any defect is documented for follow-up, not fixed here |
| T-78-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
Checks that confirm this plan is complete:

1. `test -f .planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md` exits 0
2. `grep -E "UAT-03.*PASS|UAT-03.*FAIL" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md` exits 0 and returns exactly 1 line
3. `grep -c "verify_case_solution" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md` returns >= 1 (harness was invoked and output recorded)
4. `grep -cE "SYME.*0|DEFF NOT.*0|SKEL.*0" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md` returns >= 3 (mechanism greps recorded)
5. `grep -c "PATH-CHANGING\|ADVISORY" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE1.md` returns >= 1 (intervention log present, even if empty)
</verification>

<success_criteria>
78-UAT-CASE1.md records all four criterion checks from on-disk artifacts with an unambiguous UAT-03 PASS or FAIL verdict. If PASS: all criteria green, aromatic ring emergent or conditionally escalated per D-77-06, zero path-changing interventions. If FAIL: root-cause clearly identified for Phase 78 forensics.
</success_criteria>

<output>
Create `.planning/phases/78-blind-re-uat-gate/78-02-SUMMARY.md` when done.
</output>
