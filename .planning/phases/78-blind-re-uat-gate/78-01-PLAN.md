---
phase: 78-blind-re-uat-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md
autonomous: true
requirements: [UAT-03, UAT-04]

must_haves:
  truths:
    - "CASE9 dataset contains no compound-name or structure leakage (formula C12H16O3 in titles is acceptable — it is provided to the agent)"
    - "CASE1 dataset sanitisation is re-confirmed (already sanitised ibuprofen)"
    - "scripts/verify_case_solution.py is present and executable with the expected CLI contract"
    - "A written pre-check record exists before any blind run begins"
  artifacts:
    - path: ".planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md"
      provides: "Pre-run blindness confirmation for both datasets"
      contains: "CASE1 PASS, CASE9 PASS (or SANITISED)"
  key_links:
    - from: "78-SANITISATION-CHECK.md"
      to: "78-02-PLAN.md and 78-03-PLAN.md"
      via: "Gate — blind runs may not proceed until this file records PASS"
      pattern: "CASE1.*PASS|CASE9.*PASS"
---

<objective>
Verify that both datasets are free of compound-identity leakage and that the verification tooling is operational before any blind run begins. Per D-78-02, this is the mandatory gate for the entire UAT.

Purpose: A blind run is only as blind as the data it reads. This plan audits both datasets and the harness script, then records a signed pre-check artifact that the blind-run plans depend on.

Output: `.planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md` — a written record of each check performed, its result, and a final PASS/FAIL/SANITISED verdict per dataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-blind-re-uat-gate/78-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit CASE1 and CASE9 datasets for identity leakage</name>
  <read_first>
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1/ — existing sanitised dataset
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/ — to-be-verified dataset
    - .planning/phases/76-milestone-uat-gate/VERIFICATION.md — § "UAT-04 — CASE9: NOT RUN. Dataset confirmed blind" (76-SANITISATION-VERIFIED.md ref)
    - scripts/verify_case_solution.py — the harness to be confirmed executable
  </read_first>
  <action>
    For CASE1: the dataset was sanitised prior to Phase 76 and confirmed by 76-SANITISATION-VERIFIED.md. Re-verify by checking all pdata title files for compound name (not just formula). Run: find ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1 -name title -exec sh -c 'echo "$1: $(cat "$1")"' _ {} \; and confirm no title contains "ibuprofen", "ibuprofen" variant, or the SMILES/InChI of ibuprofen. Also grep -ri "ibuprofen" over the entire CASE1 directory tree to confirm the word is absent.

    For CASE9: audit all pdata title files the same way. Also run grep -ri "isopropyl\|hydroxy.*benzoic\|benzoic.*ester\|4-.*hydroxy\|C12H16O3.*ester\|ibuprofen" over the CASE9 directory (formula C12H16O3 in titles is acceptable — the agent is given the formula). Confirm acqus/procs/audita.txt files do not contain the compound name (check already performed: no leakage found beyond formula). If any leakage is found, invoke /lucy-ng:sanitise on that file/field and re-check before proceeding.

    For verify_case_solution.py: confirm the file exists at scripts/verify_case_solution.py, run python scripts/verify_case_solution.py --help to confirm it is executable, and capture the help text showing the expected positional arguments (merged_smi, formula).

    Record all findings in a new file at .planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md with the following structure:
    - Date/time of check
    - CASE1: list of title file checks performed, grep command + result, verdict: PASS (already sanitised, no leakage found) or FAIL + remediation taken
    - CASE9: same structure, verdict: PASS (no name leakage; formula only) or SANITISED (leakage found + remediated) or FAIL (leakage found, could not remediate)
    - verify_case_solution.py: file exists (yes/no), --help exits 0 (yes/no), verdict: PASS
    - Overall gate: READY (all three PASS/SANITISED) or BLOCKED (any FAIL)

    If Overall gate = BLOCKED, note the blocking reason clearly. The blind runs MUST NOT proceed if overall gate = BLOCKED.
  </action>
  <verify>
    <automated>grep -E "Overall gate.*READY|CASE1.*PASS|CASE9.*PASS|CASE9.*SANITISED" .planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md</automated>
  </verify>
  <acceptance_criteria>
    - 78-SANITISATION-CHECK.md exists in .planning/phases/78-blind-re-uat-gate/
    - File contains per-dataset verdict (PASS or SANITISED) for CASE1 and CASE9
    - File contains verify_case_solution.py PASS
    - Overall gate = READY
    - grep -v "^#\|^$" .planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md | grep -c "PASS\|SANITISED" returns >= 3
    - If any leakage was found and remediated, the remediation steps and post-fix re-check are documented
  </acceptance_criteria>
  <done>78-SANITISATION-CHECK.md records READY overall gate. Both datasets confirmed identity-clean. verify_case_solution.py confirmed executable. Blind runs may proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dataset files → blind instance | Title/acqus metadata visible to any Claude reading the compound directory — the primary leakage surface for this plan |
| Bookkeeper → blind instance | The bookkeeper must not transmit compound identity; this plan's output (sanitisation record) is bookkeeper-internal only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Information Disclosure | CASE9 pdata/title files | mitigate | Audit title files for compound name; if found, invoke /lucy-ng:sanitise and re-check; document remediation in 78-SANITISATION-CHECK.md |
| T-78-02 | Information Disclosure | CASE9 acqus/audita.txt metadata | mitigate | grep for compound name across all metadata files; formula-only presence is acceptable |
| T-78-03 | Information Disclosure | 78-SANITISATION-CHECK.md content | accept | File is bookkeeper-internal (.planning/ dir); blind instance is instructed not to read .planning/; low residual risk |
| T-78-04 | Tampering | scripts/verify_case_solution.py | accept | Phase 78 explicitly must not modify this file; executor reads only — confirms existence and CLI contract |
| T-78-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing; no package manager invocations |
</threat_model>

<verification>
Checks that confirm this plan is complete:

1. `test -f .planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md` exits 0
2. `grep "Overall gate.*READY" .planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md` exits 0
3. `python scripts/verify_case_solution.py --help` exits 0 and prints positional args: merged_smi, formula
4. No compound name found in CASE1 or CASE9 dataset trees (grep -ri "ibuprofen" and grep -ri "isopropyl.*benzoic\|hydroxy.*benzoic" return 0 matches or the match is in a remediated/sanitised file)
</verification>

<success_criteria>
Both datasets pass the identity-leakage audit. The 78-SANITISATION-CHECK.md artifact records the full audit trail with READY gate. The independent verification harness (verify_case_solution.py) is confirmed executable. Blind runs (Plan 02 and 03) may proceed.
</success_criteria>

<output>
Create `.planning/phases/78-blind-re-uat-gate/78-01-SUMMARY.md` when done.
</output>
