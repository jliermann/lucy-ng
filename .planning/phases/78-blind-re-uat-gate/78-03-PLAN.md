---
phase: 78-blind-re-uat-gate
plan: 03
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - .planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md
autonomous: false
requirements: [UAT-04]

must_haves:
  truths:
    - "A fresh blind instance (not the bookkeeper) ran /lucy-ng:case on CASE9 (C12H16O3) in a /clear'd session"
    - "solutions.smi written to disk by the blind run is independently verified by verify_case_solution.py"
    - "The final emitted LSD file(s) contain BOND/COSY/DEFF F native constraints and zero SYME, DEFF NOT, SKEL"
    - "Command history / CASE-PROGRESS.md shows lucy lsd run as the solver path — no direct lsd/outlsd binary"
    - "Every bookkeeper-to-instance message is logged and classified advisory vs path-changing"
    - "78-UAT-CASE9.md records the full evidence chain and a UAT-04 pass/fail verdict"
  artifacts:
    - path: ".planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md"
      provides: "CASE9 evidence record and UAT-04 verdict"
      contains: "UAT-04.*PASS|UAT-04.*FAIL"
  key_links:
    - from: "~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/analysis/solutions.smi"
      to: "scripts/verify_case_solution.py"
      via: "python scripts/verify_case_solution.py <path>/solutions.smi C12H16O3"
      pattern: "\"pass\": true"
    - from: "CASE9 analysis/ iteration LSD files"
      to: "78-UAT-CASE9.md mechanism section"
      via: "grep SYME / DEFF NOT / SKEL / BOND / COSY / DEFF F / lucy lsd run"
      pattern: "SYME.*0|DEFF NOT.*0|SKEL.*0"
---

<objective>
Run the CASE9 blind UAT (C12H16O3, para-aromatic compound with the same 4J failure mode as ibuprofen) in a fresh session isolated from the tainted bookkeeper, then collect and record the full evidence chain for the four gate criteria against on-disk artifacts — never from agent self-report (per D-78-01 and D-78-03).

Purpose: UAT-04 is the second half of the AND-gate. CASE9 is a different molecule that tests whether the Phase 77 fixes generalise beyond ibuprofen. The run proceeds regardless of CASE1's outcome (D-78-05: both compounds run independently and fully, maximise diagnostic data).

Output: `.planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md` — complete evidence record with UAT-04 pass/fail verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-blind-re-uat-gate/78-CONTEXT.md
@.planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-CONTEXT.md
@.planning/phases/76-milestone-uat-gate/VERIFICATION.md
@.planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run CASE9 blind session (human-action)</name>
  <read_first>
    - .planning/phases/78-blind-re-uat-gate/78-SANITISATION-CHECK.md — confirm Overall gate = READY before proceeding
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/molecular-formula.txt — formula is C12H16O3
    - .planning/phases/78-blind-re-uat-gate/78-CONTEXT.md §D-78-05 — run CASE9 even if CASE1 fails; one-shot only
  </read_first>
  <what-to-do>
    IMPORTANT: This run is independent of CASE1's result. Run it fully even if Plan 02 (CASE1) failed.

    Open a SEPARATE terminal (do NOT reuse this session or the CASE1 session). Run /clear in the new terminal to ensure a completely fresh context. The blind instance for CASE9 must have no knowledge of CASE1's outcome. In the new terminal, run exactly:

      /lucy-ng:case ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9 C12H16O3

    Bookkeeper (this session) advisory-intervention rules (D-78-04 — identical to CASE1):
    - ALLOWED (non-disqualifying): generic process nudges that reveal neither the answer nor the path. Examples: "your constraint inventory was lost across iterations", "re-read the skill guidance", "you have not yet run lucy lsd run".
    - DISQUALIFYING (path-changing = fail): any path switch instruction, structural hints (compound identity, expected structure, atom indices, COSY pairs, "it's aromatic", any hint about the compound), forced-ring instructions.
    - Borderline: score as DISQUALIFYING (conservative, per D-78-04).

    Note: CASE9 is a different compound with 12 carbons. Its para-aromatic 4J failure mode is structurally analogous to ibuprofen's, but the constraint set, atom indices, and solution space differ. The blind agent should derive these independently — do NOT reference the ibuprofen run in any advisory message.

    Keep a running log of every message sent to the blind instance. Record: message text, timestamp, classification (ADVISORY / PATH-CHANGING).

    When the blind run completes, signal back here with: "CASE9 run complete. solutions.smi at [path]."
  </what-to-do>
  <acceptance_criteria>
    - The blind run was performed in a separate /clear'd terminal session (not the same as CASE1's session)
    - solutions.smi (or an explicit failure report) exists under ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/analysis/
    - Intervention log (even if empty) is ready for Task 2
  </acceptance_criteria>
  <how-to-verify>
    Confirm the separate terminal session was /clear'd and is different from the CASE1 session. Confirm solutions.smi exists or the blind agent declared zero solutions. Resume when done.
  </how-to-verify>
  <resume-signal>Type "CASE9 run complete" and the absolute path to solutions.smi, or "CASE9 run complete — no solutions found" with the path to CASE-PROGRESS.md.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Collect CASE9 evidence and write 78-UAT-CASE9.md</name>
  <read_first>
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/analysis/solutions.smi — the primary output
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/analysis/CASE-PROGRESS.md — intervention log + solver path used
    - ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/analysis/ — final iteration LSD file (highest iteration_NN dir)
    - scripts/verify_case_solution.py — CLI contract: python scripts/verify_case_solution.py <smi_path> <formula>
    - .planning/phases/77-fix-lucy-lsd-run-plumbing-bug-and-re-run-blind-uat-repair-in/77-CONTEXT.md §D-77-06 — three-tier mechanistic criterion
    - .planning/phases/78-blind-re-uat-gate/78-CONTEXT.md §D-78-03, §D-78-04 — evidence checklist and intervention classification
  </read_first>
  <action>
    Run all four evidence checks mechanically from on-disk artifacts. Do NOT rely on the blind agent's self-report for any criterion.

    CRITERION 1 — Solve correctness:
    Run: python scripts/verify_case_solution.py ~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE9/analysis/solutions.smi C12H16O3
    Capture full JSON output. Record: exit code, pass field, aromatic_atoms count, computed_formula, formula_match for each top-3 entry.
    Additionally re-parse solutions.smi with an inline RDKit check: python -c "from rdkit import Chem; from rdkit.Chem import rdMolDescriptors; smi=open('solutions.smi').readline().split()[0]; m=Chem.MolFromSmiles(smi); print(rdMolDescriptors.CalcMolFormula(m), sum(1 for a in m.GetAtoms() if a.GetIsAromatic()))"
    Verdict: PASS if exit code 0 AND "pass": true. FAIL otherwise.

    CRITERION 2 — Native-only emission / mechanism:
    Locate the FINAL emitted LSD file: highest-numbered iteration_NN/compound.lsd in the CASE9 analysis directory. Run these greps on that file:
      grep -v "^;" [final_lsd_file] | grep -c "SYME"           # must be 0
      grep -v "^;" [final_lsd_file] | grep -c "DEFF NOT"       # must be 0
      grep -v "^;" [final_lsd_file] | grep -cE "^SKEL"         # must be 0
      grep -v "^;" [final_lsd_file] | grep -cE "^BOND|^COSY"   # must be >= 1
      grep -v "^;" [final_lsd_file] | grep -cE "^DEFF F|^FEXP" # must be >= 1

    For the aromatic-ring tier (D-77-06):
      grep -v "^;" [final_lsd_file] | grep -E "^BOND [0-9]+ [0-9]+"  # list all BOND lines — inspect whether any connect ring atoms
    - If no ring-BOND lines for ring atoms: emergent ring — CLEAN PASS on this sub-criterion.
    - If ring-BOND lines for ring atoms AND CASE-PROGRESS.md has an explicit escalation entry: CONDITIONAL PASS.
    - If ring-BOND lines for ring atoms AND no CASE-PROGRESS escalation entry: SILENT ring-BOND — FAIL.
    - Any SKEL line (count > 0): FAIL regardless.
    Record which tier applies. Criterion 2 PASS requires: SYME=0, DEFF NOT=0, SKEL=0, BOND/COSY>=1, DEFF F/FEXP>=1, AND (emergent OR conditional-pass ring tier).

    CRITERION 3 — Solver path:
    In CASE-PROGRESS.md, grep for "lucy lsd run" and for direct binary patterns ("lsd " invocation outside lucy, "outlsd " standalone, absolute binary path). Verdict: PASS if lucy lsd run present AND no bypass. FAIL if bypass found.

    CRITERION 4 — Interventions:
    Apply D-78-04 classification to every message from the bookkeeper to the blind instance during Task 1. Count PATH-CHANGING messages. Verdict: PASS if 0. FAIL if > 0.

    Write .planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md with:
    - Date/time of run
    - Brief narrative (iterations, pivots)
    - Criterion table: C1/C2/C3/C4 each with command run, raw output, verdict
    - For C2: explicit SYME/DEFF NOT/SKEL counts + aromatic-ring tier
    - Intervention log (full text + classification per message)
    - Overall UAT-04 verdict: PASS or FAIL
    - If FAIL: root-cause paragraph for Phase 78 forensics
  </action>
  <verify>
    <automated>grep -E "UAT-04.*PASS|UAT-04.*FAIL" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md</automated>
  </verify>
  <acceptance_criteria>
    - 78-UAT-CASE9.md exists in .planning/phases/78-blind-re-uat-gate/
    - File contains verify_case_solution.py output (exit code + JSON)
    - File contains grep counts for SYME (=0), DEFF NOT (=0), SKEL (=0), BOND/COSY (>=1), DEFF F/FEXP (>=1)
    - File identifies the aromatic-ring tier
    - File contains the solver-path check
    - File contains the intervention log with D-78-04 classification
    - File ends with UAT-04 PASS or UAT-04 FAIL verdict
    - grep -E "UAT-04.*PASS|UAT-04.*FAIL" 78-UAT-CASE9.md returns exactly 1 match
  </acceptance_criteria>
  <done>78-UAT-CASE9.md is written with all four criteria checked from on-disk artifacts. UAT-04 verdict is unambiguous (PASS or FAIL). Forensics included if FAIL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bookkeeper → blind instance | Must not transmit compound identity, expected structure, or path hints for CASE9 |
| CASE1 run → CASE9 run | The CASE9 blind instance must not receive any information from the CASE1 run outcome — separate session required |
| Agent self-report → evidence | All verdicts derive from on-disk artifacts + independent tooling, never agent self-report |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-09 | Information Disclosure | CASE1 outcome leaking to CASE9 blind instance | mitigate | Separate /clear'd terminal for CASE9; bookkeeper must not reference CASE1 result in any advisory message to the CASE9 blind instance |
| T-78-10 | Information Disclosure | Bookkeeper intervention messages to CASE9 instance | mitigate | D-78-04 classification; PATH-CHANGING = criterion 4 FAIL; documented in 78-UAT-CASE9.md |
| T-78-11 | Repudiation | Agent self-report of CASE9 solutions | mitigate | Evidence from on-disk artifacts + verify_case_solution.py + inline RDKit check |
| T-78-12 | Information Disclosure | CASE9 compound identity in title files | accept | Pre-check (Plan 01) confirmed formula-only in titles; formula is given to the blind agent; compound name absent |
| T-78-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
Checks that confirm this plan is complete:

1. `test -f .planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md` exits 0
2. `grep -E "UAT-04.*PASS|UAT-04.*FAIL" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md` exits 0 and returns exactly 1 line
3. `grep -c "verify_case_solution" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md` returns >= 1
4. `grep -cE "SYME.*0|DEFF NOT.*0|SKEL.*0" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md` returns >= 3
5. `grep -c "PATH-CHANGING\|ADVISORY" .planning/phases/78-blind-re-uat-gate/78-UAT-CASE9.md` returns >= 1
</verification>

<success_criteria>
78-UAT-CASE9.md records all four criterion checks from on-disk artifacts with an unambiguous UAT-04 PASS or FAIL verdict. Run was performed in a separate session from CASE1 with no cross-contamination. If PASS: all criteria green on a compound the mechanism had not previously seen. If FAIL: root-cause clearly identified for Phase 78 forensics.
</success_criteria>

<output>
Create `.planning/phases/78-blind-re-uat-gate/78-03-SUMMARY.md` when done.
</output>
