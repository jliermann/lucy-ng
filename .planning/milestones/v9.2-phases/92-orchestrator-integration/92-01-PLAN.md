---
phase: 92-orchestrator-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_case_md_wv07.py
  - tests/test_cli_webview.py
autonomous: true
requirements: [WV-07]

must_haves:
  truths:
    - "A pytest file grep-asserts case.md launches `lucy webview serve` before the first [BEGIN] peak-picking push"
    - "A pytest test asserts the terminate_team section does NOT contain the literal `lucy webview stop` command"
    - "An integration test proves `lucy webview serve` returns promptly and the spawned server outlives its caller process"
  artifacts:
    - path: "tests/test_case_md_wv07.py"
      provides: "Grep-based contract tests for the case.md WV-07 edit"
      min_lines: 40
    - path: "tests/test_cli_webview.py"
      provides: "test_serve_outlives_caller lifecycle test added to TestWebviewLifecycle"
      contains: "test_serve_outlives_caller"
  key_links:
    - from: "tests/test_case_md_wv07.py"
      to: ".claude/commands/lucy-ng/case.md"
      via: "Path(...).read_text() string search"
      pattern: "lucy webview serve"
    - from: "tests/test_cli_webview.py::test_serve_outlives_caller"
      to: "lucy webview serve subprocess"
      via: "subprocess.run returns, os.kill(pid, 0) still alive"
      pattern: "os\\.kill"
---

<objective>
Create the Wave 0 test scaffold that defines the WV-07 contract before any case.md edit
lands (Nyquist: tests first). Two artifacts:

1. `tests/test_case_md_wv07.py` — grep-based assertions over the `case.md` skill file (and
   `progress-format.md`) that pin the exact behavioral contract the Plan 92-02 edit must satisfy.
2. `tests/test_cli_webview.py::TestWebviewLifecycle::test_serve_outlives_caller` — an integration
   test proving `lucy webview serve` returns promptly (non-blocking) AND the spawned server
   survives its launching process (the `start_new_session=True` detachment that underpins SC #2).

Purpose: The case.md grep tests are RED until Plan 92-02 edits case.md — that is the intended
Wave 0 state. The outlives-caller test is GREEN immediately (survival already works in Phase 90).
Output: Two test files that make the phase's success criteria machine-checkable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-orchestrator-integration/92-RESEARCH.md
@.planning/phases/92-orchestrator-integration/92-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_case_md_wv07.py grep-contract tests</name>
  <files>tests/test_case_md_wv07.py</files>
  <read_first>
    - .claude/commands/lucy-ng/case.md — target skill file; spawn_case_team Step 5 (~lines 214-229) and terminate_team (~lines 876-908). These are RED-until-92-02 targets.
    - .claude/commands/lucy-ng/references/progress-format.md — header block (~lines 30-40) where the OD-5 `**Dashboard:**` field will be added by 92-02.
    - .planning/phases/92-orchestrator-integration/92-RESEARCH.md — "Verification Strategy for case.md" table (the three grep tests) + OD-5.
    - tests/test_cli_webview.py — existing test conventions (Path-based reads, try/except import guards).
  </read_first>
  <behavior>
    - test_webview_launch_present: case.md text contains the substring `lucy webview serve`, AND its first occurrence index is LESS than the first occurrence index of `[BEGIN] peak-picking` (launch happens before the first team push). Both substrings must be present.
    - test_stop_hint_present: the run-start / spawn region of case.md (everything BEFORE the `<step name="terminate_team">` marker) contains the literal `lucy webview stop` (the stop-hint documentation line in the launch block).
    - test_terminate_team_no_stop: the terminate_team section (from `<step name="terminate_team">` to its closing `</step>`) does NOT contain the literal `lucy webview stop` — proving the orchestrator never stops the server on shutdown (SC #2).
    - test_progress_format_dashboard_field: progress-format.md contains a `**Dashboard:**` header field (OD-5).
  </behavior>
  <action>
    Create `tests/test_case_md_wv07.py`. Read the skill files with `Path(".claude/commands/lucy-ng/case.md").read_text()` and `Path(".claude/commands/lucy-ng/references/progress-format.md").read_text()` (repo-root-relative; pytest runs from repo root). Implement the four tests from the `<behavior>` block as plain `def test_*` functions (no class needed).
    For test_webview_launch_present use `case.index("lucy webview serve") < case.index("[BEGIN] peak-picking")` after asserting both substrings are present, with descriptive assertion messages.
    For test_stop_hint_present and test_terminate_team_no_stop, split the text on the marker `"<step name=\"terminate_team\">"`: the pre-marker part is the run-start region (must contain `lucy webview stop`), the post-marker part is the terminate_team region (must NOT contain `lucy webview stop`). If the marker is not found, fail with a clear message so a renamed step surfaces immediately.
    These tests assert BEHAVIOR that Plan 92-02 delivers — they are expected RED (AssertionError, not ImportError/collection error) until 92-02 lands. Do NOT weaken them to pass now. Do NOT add fenced code blocks to case.md; these are read-only assertions over it.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('tests/test_case_md_wv07.py').read())" && pytest tests/test_case_md_wv07.py -q --no-header || true</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_case_md_wv07.py` parses (ast.parse exits 0) and pytest COLLECTS all four tests (no collection/import errors).
    - The three case.md tests + the progress-format test FAIL with an AssertionError (RED contract), NOT an error — confirmed by `pytest tests/test_case_md_wv07.py -q` showing 4 failed / 0 errors while case.md is unedited.
    - No test reads `~/.claude/...`; all reads use the repo-relative `.claude/commands/lucy-ng/` paths.
  </acceptance_criteria>
  <done>Four grep tests exist, collectable, RED via AssertionError against the current (unedited) case.md and progress-format.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add test_serve_outlives_caller to TestWebviewLifecycle</name>
  <files>tests/test_cli_webview.py</files>
  <read_first>
    - tests/test_cli_webview.py — TestWebviewLifecycle class (~lines 286-381) and top-of-file imports (subprocess, sys, os, json, time already available; add `import time` if absent).
    - tests/conftest.py — webview_analysis_dir + webview_server fixtures (~lines 157-210); the fastapi-absent skip pattern and start_new_session launch.
    - src/lucy_ng/webview/server.py — start() lines ~120-160: `subprocess.Popen(..., start_new_session=True)` + the 0.5 s startup probe (the survival + promptness basis under test).
    - src/lucy_ng/cli/webview.py — serve command `--format json` output shape `{"url","pid","port"}` and the stop command.
  </read_first>
  <action>
    Add a `test_serve_outlives_caller` method to the existing `TestWebviewLifecycle` class in `tests/test_cli_webview.py`. It must: (1) skip if fastapi is not importable (mirror the fixture guard); (2) launch the server as a caller process that RETURNS via `subprocess.run([sys.executable, "-m", "lucy_ng.cli", "webview", "serve", str(webview_analysis_dir), "--format", "json"], capture_output=True, text=True, timeout=30)`, timing it with `time.monotonic()`; (3) assert `returncode == 0`, assert elapsed < 10 s (non-blocking return), and parse the JSON stdout for `pid`; (4) after the caller `subprocess.run` has already exited, assert the server is STILL alive with `os.kill(pid, 0)` (must not raise `ProcessLookupError`). Wrap steps 3-4 in try/finally and ALWAYS clean up by invoking `lucy webview stop` via `subprocess.run([sys.executable, "-m", "lucy_ng.cli", "webview", "stop", str(webview_analysis_dir)])` in the finally block so no orphan server survives the test. Use the `webview_analysis_dir` fixture (not `webview_server`, which would pre-start a server). This is not a case.md test — no `tdd="true"`; it verifies an already-shipped Phase 90 mechanism.
  </action>
  <verify>
    <automated>pytest tests/test_cli_webview.py::TestWebviewLifecycle::test_serve_outlives_caller -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_cli_webview.py::TestWebviewLifecycle::test_serve_outlives_caller -q` exits 0 (PASS) in a dev env with the webview extra, or SKIPS cleanly where fastapi is absent — never ERRORS.
    - The test asserts `elapsed < 10` (promptness) AND `os.kill(pid, 0)` after the caller returned (survival), and its finally block calls `lucy webview stop`.
    - Running the full `tests/test_cli_webview.py` leaves no orphan uvicorn process (state file removed by the cleanup stop).
  </acceptance_criteria>
  <done>test_serve_outlives_caller is GREEN (or SKIP without the extra), proves promptness + survival, and self-cleans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → lucy CLI subprocess | Test spawns `lucy webview serve` with a tmp_path analysis dir (orchestrator-controlled path, not web input) |
| uvicorn server → 127.0.0.1 | Server binds loopback only (Phase 90 control, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Denial of Service | orphan test server process | mitigate | test finally-block calls `lucy webview stop`; tmp_path teardown |
| T-92-02 | Tampering | analysis_dir arg to serve | accept | path is a pytest `tmp_path`, not user-web input; passed as argv (no shell=True) |
| T-92-SC | Tampering | package installs | accept | this phase installs NO packages (no new deps); nothing to audit |
</threat_model>

<verification>
- `pytest tests/test_case_md_wv07.py tests/test_cli_webview.py -q` collects with 0 errors.
- The four case.md/progress grep tests are RED (AssertionError) — the intended Wave 0 contract before 92-02.
- test_serve_outlives_caller is GREEN (or SKIP without the extra).
</verification>

<success_criteria>
Two test files exist; the grep contract is defined and RED-by-assertion; the survival/promptness
integration test is GREEN. No orphan processes left behind.
</success_criteria>

<output>
Create `.planning/phases/92-orchestrator-integration/92-01-SUMMARY.md` when done.
</output>
