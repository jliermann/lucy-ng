---
phase: 87-final-identity-verification-gate
plan: 04
type: execute
wave: 2
depends_on: ["87-03"]
files_modified:
  - .claude/agents/lucy-solution-analyst.md
  - .claude/agents/lucy-devils-advocate.md
autonomous: true
gap_closure: true
requirements: [IDENT-01, IDENT-02, IDENT-03]
must_haves:
  truths:
    - "The solution-analyst invokes the installed `lucy identify` command (reachable from an NMR data dir) instead of the repo-relative `scripts/verify_case_solution.py` path to derive identity (IDENT-01, D-01, GAP-87-A)"
    - "The analyst's verdict-keyed Identity Rendering Rule (IDENT-03, D-07) and the `## Identity` report block are unchanged — only HOW the tool is invoked changes"
    - "The devils-advocate G-IDENT post-solution advisory gate (IDENT-02 advisory half, D-04) is preserved and still does NOT call the deterministic tool (independence, D-05)"
    - "Both agent files carry a fresh-Claude-Code-session reload note (these are prompt edits; runtime behavior is validated by the next blind UAT, not unit tests)"
  artifacts:
    - path: ".claude/agents/lucy-solution-analyst.md"
      provides: "Identity-derivation step repointed to `lucy identify --smiles ... --format json`"
      contains: "lucy identify"
    - path: ".claude/agents/lucy-devils-advocate.md"
      provides: "G-IDENT gate prose referencing `lucy identify` as the analyst's binding tool (not scripts/...)"
      contains: "lucy identify"
  key_links:
    - from: ".claude/agents/lucy-solution-analyst.md"
      to: "lucy identify (installed CLI from 87-03)"
      via: "the Section 6.0 identity-derivation bash invocation"
      pattern: "lucy identify"
    - from: ".claude/agents/lucy-devils-advocate.md"
      to: "lucy identify (installed CLI from 87-03)"
      via: "G-IDENT prose naming the analyst's binding tool"
      pattern: "lucy identify"
---

<objective>
Close the runtime half of GAP-87-A: re-point the two CASE team agents from the repo-relative
`python scripts/verify_case_solution.py check-identity ...` invocation (unreachable when a CASE
run executes in an NMR data dir outside the repo) to the installed `lucy identify` command
delivered by plan 87-03. The deterministic verdict contract is identical; only the invocation
path changes, so the existing IDENT-03 tentative-rendering rule and the G-IDENT advisory gate
stay exactly as 87-02 left them.

Purpose: With `lucy identify` installed and on PATH, the analyst (IDENT-01) and the binding-tool
reference in the devils-advocate prose (IDENT-02) finally resolve at CASE runtime. The next blind
CASE5 UAT will show `lucy identify` actually firing instead of the "verify_case_solution.py not
present in repo" fallback.
Output: edited `lucy-solution-analyst.md` and `lucy-devils-advocate.md` prompts.

NOTE: These are Markdown CASE-agent prompt edits. A FRESH Claude Code session is required to
reload edited agents; runtime behavior is validated by the next blind CASE5 UAT, not by unit
tests in this session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-final-identity-verification-gate/87-CONTEXT.md
@.planning/phases/87-final-identity-verification-gate/87-02-SUMMARY.md
@.planning/phases/87-final-identity-verification-gate/87-03-PLAN.md

<interfaces>
<!-- The installed command delivered by 87-03 (wave 1). The agents must invoke THIS, not the
     repo-relative script. JSON contract is identical to the old check-identity output. -->

  lucy identify --smiles '<top solution SMILES>' [--reported-name '<name>'] --format json

  emits JSON:
    { "mode": "identity",
      "reported_name": <str|null>,
      "derived": { "matched": bool, "name"?: str, "source"?: "coconut"|"nmrshiftdb",
                   "inchi_key": str, "canonical_smiles": str,
                   "confidence": "confirmed"|"confirmed-structure"|"novel",
                   "trivial_name_confirmed"?: bool },
      "name_match": true|false|null,
      "verdict": "confirmed" | "confirmed-structure" | "tentative" | "novel",
      "warning": <str|null> }

<!-- Exact current strings to replace in lucy-solution-analyst.md -->
  line ~172-175 (Section 6.0 fenced bash):
    python scripts/verify_case_solution.py check-identity \
      --smiles '<top solution canonical SMILES>' \
      --reported-name '<any trivial name you are inclined to report — OR omit this flag entirely>'
  line ~177 (relative-path caveat): "(If the script is not on the relative path from the CASE
    working dir, use its absolute repo path; it always exits 0.)"
  line ~302 (workflow step 6a):
    run `python scripts/verify_case_solution.py check-identity --smiles '<top solution SMILES>' [--reported-name '<name>']`

<!-- Exact current strings to update in lucy-devils-advocate.md -->
  line ~422 (G-IDENT purpose): "The deterministic `scripts/verify_case_solution.py check-identity`
    tool is the BINDING name↔structure agreement check"
  line ~569 (workflow step 13): "do NOT call the deterministic `check-identity` tool — that is the
    analyst's binding layer"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the solution-analyst identity derivation to `lucy identify`</name>
  <read_first>
    - .claude/agents/lucy-solution-analyst.md (the file being edited; Section 6.0 ~lines 165-189, [RANKING-COMPLETE] template ~line 265, workflow step 6a ~line 302)
    - .planning/phases/87-final-identity-verification-gate/87-03-PLAN.md (the lucy identify command + JSON contract this agent now calls)
    - .planning/phases/87-final-identity-verification-gate/87-02-SUMMARY.md (what Section 6.0/6.1 already provide — do NOT rebuild the rendering rule)
  </read_first>
  <action>
    In `.claude/agents/lucy-solution-analyst.md`, replace every invocation of
    `python scripts/verify_case_solution.py check-identity` with
    `lucy identify` (the installed command from 87-03), preserving the exact same flags
    (`--smiles '<top solution canonical SMILES>'`, optional `--reported-name '<name>'`) and add
    `--format json` so the JSON contract is explicit. Update the three sites: the Section 6.0
    fenced bash block (~line 172), the workflow step 6a one-liner (~line 302), and any other
    `scripts/verify_case_solution.py` mention used for the LIVE identity call. Replace the
    relative-path caveat at ~line 177 ("If the script is not on the relative path from the CASE
    working dir, use its absolute repo path...") with a note that `lucy identify` is an installed
    subcommand reachable from the CASE data directory (like `lucy lsd rank`), so no repo-relative
    path is needed; it always exits 0. Do NOT change the JSON-parsing prose, the Section 6.1
    verdict-keyed Identity Rendering Rule, the `## Identity` report block, the
    `Identity:` field in [RANKING-COMPLETE], or the IDENT-03 `(tentative, unverified)` behavior —
    those are correct (87-02) and only the invocation path is wrong. Ensure the existing
    fresh-session reload note remains present (re-add it near the top of the identity section if
    the edit displaced it). The reported identity must still be TOOL-DERIVED, never from recalled
    knowledge.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && test "$(grep -c 'lucy identify' .claude/agents/lucy-solution-analyst.md)" -ge 2 && test "$(grep -v '^#' .claude/agents/lucy-solution-analyst.md | grep -c 'scripts/verify_case_solution.py check-identity')" -eq 0 && grep -qi 'fresh.*session' .claude/agents/lucy-solution-analyst.md && grep -q '(tentative, unverified)' .claude/agents/lucy-solution-analyst.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/agents/lucy-solution-analyst.md` contains `lucy identify` (≥2 occurrences: Section 6.0 + step 6a) and `--format json` on the identity invocation.
    - It NO LONGER references `scripts/verify_case_solution.py check-identity` for the live identity call (grep, comment lines excluded, returns 0).
    - The Section 6.1 verdict-keyed rendering rule, the `## Identity` block, and the `(tentative, unverified)` IDENT-03 text are still present (unchanged).
    - A fresh-Claude-Code-session reload note is present.
  </acceptance_criteria>
  <done>The analyst derives identity by calling the installed `lucy identify --format json` (runtime-reachable), with IDENT-01 derivation and IDENT-03 tentative rendering preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the devils-advocate G-IDENT prose to name `lucy identify` as the binding tool</name>
  <read_first>
    - .claude/agents/lucy-devils-advocate.md (the file being edited; G-IDENT purpose ~line 422, workflow step 13 ~line 569)
    - .planning/phases/87-final-identity-verification-gate/87-02-SUMMARY.md (G-IDENT lifecycle + independence semantics — must stay intact)
    - .planning/phases/87-final-identity-verification-gate/87-03-PLAN.md (the lucy identify command the analyst now uses as its binding layer)
  </read_first>
  <action>
    In `.claude/agents/lucy-devils-advocate.md`, update the G-IDENT prose so the references to the
    analyst's BINDING deterministic tool name the installed `lucy identify` command instead of
    `scripts/verify_case_solution.py check-identity`. Two sites: the G-IDENT purpose paragraph
    (~line 422, "The deterministic `scripts/verify_case_solution.py check-identity` tool is the
    BINDING name↔structure agreement check") and workflow step 13 (~line 569, "do NOT call the
    deterministic `check-identity` tool"). CRITICAL — preserve the independence semantics (D-05):
    G-IDENT MUST still NOT call `lucy identify` / `derive_identity` itself; it reasons
    independently from the SMILES. So the edit only changes the NAME of the analyst's binding tool
    that G-IDENT is described as complementary to — the "do NOT call it" instruction stays, now
    phrased as "do NOT call `lucy identify` — that is the analyst's binding layer." Do NOT change
    the G-IDENT lifecycle (post-solution, on `final_results.md`), the WARNING/advisory/never-blocks
    severity (D-06), or the CASE4/CASE5 worked triggers. Ensure the existing fresh-session reload
    note remains present.
  </action>
  <verify>
    <automated>cd /Users/steinbeck/Dropbox/develop/lucy-ng && grep -q 'lucy identify' .claude/agents/lucy-devils-advocate.md && grep -q 'G-IDENT' .claude/agents/lucy-devils-advocate.md && grep -qi 'post-solution' .claude/agents/lucy-devils-advocate.md && grep -q 'final_results.md' .claude/agents/lucy-devils-advocate.md && grep -qi 'fresh.*session' .claude/agents/lucy-devils-advocate.md && test "$(grep -v '^#' .claude/agents/lucy-devils-advocate.md | grep -c 'scripts/verify_case_solution.py check-identity')" -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/agents/lucy-devils-advocate.md` contains `lucy identify` and no longer names `scripts/verify_case_solution.py check-identity` as the analyst's binding tool (grep, comments excluded, returns 0).
    - G-IDENT is still present, still POST-SOLUTION on `final_results.md`, still advisory/never-blocks, and the "do NOT call the tool" independence instruction (now referencing `lucy identify`) is intact.
    - CASE4 + CASE5 worked triggers (indigo/isoindigo) still present.
    - A fresh-Claude-Code-session reload note is present.
  </acceptance_criteria>
  <done>G-IDENT correctly names `lucy identify` as the analyst's binding layer while remaining the independent, tool-free advisory cross-check; lifecycle and severity unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CASE agent prompt → installed `lucy identify` | The agent constructs a shell invocation embedding the solution SMILES; SMILES is semi-trusted pipeline output. The hardening lives in the 87-03 CLI (graceful invalid-SMILES handling, parameterized SQL); this plan only changes the invocation string in prompt text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04-01 | Tampering | SMILES interpolated into the `lucy identify` shell command in agent prose | accept | SMILES is solver output (not external user input); the CLI handles malformed input gracefully (87-03 T-87-03-02). Prompt-level edit introduces no new execution surface beyond what 87-03 already mitigates. |
| T-87-04-02 | Repudiation | Agent prompt edits not reloaded | mitigate | Fresh-Claude-Code-session reload note added/preserved in both files; functional confirmation deferred to the next blind CASE5 UAT (these are prompt files, not unit-tested code). |
| T-87-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (Markdown edits only); no Package Legitimacy Gate needed. |
</threat_model>

<verification>
- Both agent files reference `lucy identify` (installed, runtime-reachable) and no longer reference `scripts/verify_case_solution.py check-identity` for the live call.
- Analyst: Section 6.0 derivation + step 6a use `lucy identify --format json`; Section 6.1 rendering rule, `## Identity` block, and IDENT-03 `(tentative, unverified)` preserved.
- Devils-advocate: G-IDENT preserved (post-solution, advisory, never-blocks, CASE4/CASE5 triggers) and still does NOT call the tool (independence, D-05).
- Both files carry a fresh-session reload note.
- These are prompt edits; runtime validation is the next blind CASE5 UAT, not unit tests.
</verification>

<success_criteria>
- The solution-analyst derives identity via the installed `lucy identify` command (IDENT-01, runtime-reachable — GAP-87-A closed end to end).
- IDENT-03 tentative rendering and the verdict-keyed rule are unchanged.
- The devils-advocate G-IDENT advisory gate (IDENT-02 advisory half) is preserved with its independence intact.
- Fresh-session reload note present in both files.
</success_criteria>

<output>
Create `.planning/phases/87-final-identity-verification-gate/87-04-SUMMARY.md` when done.
</output>
